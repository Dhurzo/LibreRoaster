---
phase: 52-performance-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/hardware/max31856.rs]
autonomous: true

must_haves:
  truths:
    - "MAX31856 temperature reads do not block the async executor"
    - "Temperature reads complete within expected timeframe"
    - "Failed reads are retried before returning error"
  artifacts:
    - path: "src/hardware/max31856.rs"
      provides: "Async temperature reading with retry"
      contains: "embassy_time::Timer"
      min_lines: 140
  key_links:
    - from: "src/hardware/max31856.rs"
      to: "embassy_time"
      via: "Timer::after_ms() async delay"
      pattern: "Timer::after_ms"
---

<objective>
Convert MAX31856 blocking temperature read to async using embassy-time::Timer, with retry logic.

Purpose: Eliminate 160ms busy-wait that blocks the async executor during temperature reading.
Output: Async-compatible MAX31856 driver with retry support.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-performance-fixes/52-CONTEXT.md
@src/hardware/max31856.rs

**Phase Context (from CONTEXT.md):**
- MAX31856 async API: Async token pattern (caller passes async context)
- Simple Result<f32, Error> return
- One-shot read (no cancellation support)
- Retry N times then fail (not retry forever)
- 2 retries before giving up (3 total attempts)
- Fixed duration between retries (not exponential backoff)
- Reuse existing Max31856Error type for failures
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add async read_temperature method with embassy-time Timer</name>
  <files>src/hardware/max31856.rs</files>
  <action>
Replace the blocking spin loop (lines 48-52) with embassy-time::Timer delay:

1. Add `use embassy_time::{Timer, Duration}` import (check Cargo.toml for exact embassy-time version)
2. Create new async method `async fn read_temperature_async(&mut self) -> Result<f32, Max31856Error>`
3. Replace spin loop with: `Timer::after_ms(160).await;`
4. Keep existing synchronous `read_temperature` method - it can call the async version in an async context at the call site

The async method should:
- Write one-shot bit (0x80)
- Use `Timer::after_ms(160).await` for conversion delay
- Read temperature registers and fault status
- Convert to Celsius (keep existing logic)
- Return Result<f32, Max31856Error>
  </action>
  <verify>
`cargo check` passes - no import errors, embassy-time available
</verify>
  <done>
MAX31856 has async read_temperature_async method using embassy_time::Timer for 160ms delay instead of spin loop
</done>
</task>

<task type="auto">
  <name>Task 2: Add retry logic with fixed delay</name>
  <files>src/hardware/max31856.rs</files>
  <action>
Add retry wrapper around the async read:

1. Create method `async fn read_with_retry(&mut self, max_retries: u8) -> Result<f32, Max31856Error>`
2. Loop up to max_retries times (CONTEXT.md specifies 2 retries = 3 total attempts)
3. On failure, wait fixed 10ms between retries using `Timer::after_ms(10).await`
4. After all retries exhausted, return the last error
5. Use existing Max31856Error variants (CommunicationError, FaultDetected, InvalidTemperature)

The retry logic should:
- Try read_temperature_async up to max_retries + 1 times
- On each failure, wait fixed 10ms before retry
- Return Ok(temperature) on success
- Return Err(last_error) after all retries fail
  </action>
  <verify>
`cargo check` passes - retry logic compiles correctly
</verify>
  <done>
MAX31856 has read_with_retry that attempts up to 3 reads with 10ms delay between attempts
</done>
</task>

</tasks>

<verification>
- [ ] MAX31856 driver uses embassy_time::Timer instead of spin loop
- [ ] Async temperature reading method exists and compiles
- [ ] Retry logic attempts 3 total reads with fixed delay between attempts
- [ ] Existing synchronous Thermometer trait impl still works (for compatibility)
- [ ] No new dependencies added (embassy-time already in use by project)
</verification>

<success_criteria>
- MAX31856::read_temperature_async uses Timer::after_ms(160).await instead of blocking spin loop
- MAX31856::read_with_retry provides 2 retries (3 total attempts) with 10ms fixed delay
- Both methods compile and integrate with existing error handling (Max31856Error)
</success_criteria>

<output>
After completion, create `.planning/phases/52-performance-fixes/52-01-SUMMARY.md`
</output>
