---
phase: 52-performance-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/main.rs, src/hardware/ledc_bus.rs, src/config/constants.rs]
autonomous: true

must_haves:
  truths:
    - "SSR uses Timer0 at ~1Hz for zero-crossing control"
    - "Fan uses Timer1 at 25kHz for silent operation"
    - "SSR and Fan have independent LEDC timers"
  artifacts:
    - path: "src/main.rs"
      provides: "LEDC timer configuration"
      contains: "Timer1"
      min_lines: 170
    - path: "src/config/constants.rs"
      provides: "Timer and channel constants"
      contains: "SSR_LEDC_TIMER"
  key_links:
    - from: "src/main.rs"
      to: "esp_hal::ledc"
      via: "timer::Number::Timer0 and timer::Number::Timer1"
      pattern: "timer::Number::Timer"
---

<objective>
Separate SSR and Fan LEDC timers - SSR on Timer0 (~1Hz), Fan on Timer1 (25kHz).

Purpose: SSR needs ~1Hz for zero-crossing control while Fan needs 25kHz for silent operation. Currently both share Timer0 causing frequency conflicts.
Output: Independent timer configuration for SSR and Fan channels.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-performance-fixes/52-CONTEXT.md
@src/main.rs (lines 85-150)
@src/config/constants.rs
@src/hardware/ledc_bus.rs

**Phase Context (from CONTEXT.md):**
- Timer0 for SSR (~1Hz zero-crossing)
- Timer1 for Fan (25kHz)
- Separate LEDC bus instances per timer
- Type-safe timer configuration at compile time
- Constants in src/config/constants.rs should define timer numbers
</context>

<tasks>

<task type="auto">
<name>Task 1: Add timer constants to config/constants.rs</name>
<lines>src/config/constants.rs</lines>
<action>
Add LEDC timer constants to src/config/constants.rs:

1. Add `pub const SSR_LEDC_TIMER: u8 = 0;` (Timer0 for SSR)
2. Add `pub const FAN_LEDC_TIMER: u8 = 1;` (Timer1 for Fan)
3. Group with existing LEDC channel constants (around line 16-22)

Keep existing:
- FAN_PWM_FREQUENCY_HZ = 25000
- SSR_PWM_FREQUENCY_HZ = 1
- FAN_LEDC_CHANNEL = 0  
- SSR_LEDC_CHANNEL = 1
</action>
<verify>
`cargo check` passes - constants compile
</verify>
<done>
src/config/constants.rs defines SSR_LEDC_TIMER=0 and FAN_LEDC_TIMER=1 constants
</done>
</task>

<task type="auto">
<name>Task 2: Configure separate LEDC timers in main.rs</name>
<files>src/main.rs</files>
<action>
Separate Timer0 and Timer1 in main.rs LEDC configuration (lines 91-145):

1. **Configure Timer0 for SSR (~1Hz):**
   - Create `let mut ssr_timer = ledc.timer(timer::Number::Timer0);`
   - Configure with SSR_PWM_FREQUENCY_HZ (1 Hz)
   - 8-bit duty resolution

2. **Configure Timer1 for Fan (25kHz):**
   - Create `let mut fan_timer = ledc.timer(timer::Number::Timer1);`
   - Configure with FAN_PWM_FREQUENCY_HZ (25000 Hz)
   - 8-bit duty resolution

3. **Wire channels to correct timers:**
   - Fan channel (Channel0) uses fan_timer (Timer1)
   - SSR channel (Channel1) uses ssr_timer (Timer0)
   - Remove the unsafe timer_ref pattern - each channel references its own timer

4. **Update LedcBus::new() to accept timer numbers:**
   - Pass timer numbers to LedcBus constructor
   - Update ledc_bus.rs to store and use separate timer references
</action>
<verify>
`cargo check` passes - builds with separate timers
</verify>
<done>
main.rs configures Timer0 for SSR (1Hz) and Timer1 for Fan (25kHz), each channel uses its dedicated timer
</done>
</task>

<task type="auto">
<name>Task 3: Update LedcBus to work with separate timers</name>
<files>src/hardware/ledc_bus.rs</files>
<action>
Update LedcBus to handle separate timers:

1. Modify LedcBus::new() signature to accept timer numbers (from constants)
2. Store timer references or numbers for both fan and SSR
3. Keep the serialization guard (LedcBus ensures SSR/Fan writes don't conflict)
4. Update any timer-related checks or assertions

The LedcBus serialization logic should remain - it still prevents concurrent LEDC writes, just now the timers are independent.
</action>
<verify>
`cargo check` passes - LedcBus works with separate timers
</verify>
<done>
LedcBus supports independent timers for fan and SSR channels
</done>
</task>

</tasks>

<verification>
- [ ] src/config/constants.rs defines SSR_LEDC_TIMER=0 and FAN_LEDC_TIMER=1
- [ ] main.rs creates Timer0 for SSR at 1Hz and Timer1 for Fan at 25kHz
- [ ] Fan channel uses Timer1, SSR channel uses Timer0
- [ ] LedcBus constructor accepts timer configuration
- [ ] Project compiles with separate timers
- [ ] SSR PWM runs at ~1Hz (appropriate for zero-crossing SSR)
- [ ] Fan PWM runs at 25kHz (inaudible)
</verification>

<success_criteria>
- Timer0 configured for SSR at 1Hz frequency
- Timer1 configured for Fan at 25kHz frequency  
- SSR channel bound to Timer0
- Fan channel bound to Timer1
- LedcBus supports separate timer configuration
- Code compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/52-performance-fixes/52-02-SUMMARY.md`
</output>
