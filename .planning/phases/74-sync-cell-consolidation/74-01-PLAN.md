---
phase: 74-sync-cell-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hardware/static_sync.rs
  - src/hardware/uart/tasks.rs
  - src/hardware/usb_cdc/tasks.rs
autonomous: true
user_setup: []

must_haves:
  truths:
    - "A single shared SyncCell module exists"
    - "Both uart/tasks.rs and usb_cdc/tasks.rs import from shared module"
    - "Build succeeds with no duplicate SyncCell definitions"
    - "Both UART and USB CDC communication paths function correctly"
  artifacts:
    - path: "src/hardware/static_sync.rs"
      provides: "Shared SyncCell wrapper using static_cell::StaticCell"
    - path: "src/hardware/uart/tasks.rs"
      provides: "UART tasks using shared SyncCell"
    - path: "src/hardware/usb_cdc/tasks.rs"
      provides: "USB CDC tasks using shared SyncCell"
  key_links:
    - from: "src/hardware/static_sync.rs"
      to: "uart/tasks.rs, usb_cdc/tasks.rs"
      via: "import statement"
      pattern: "use crate::hardware::static_sync::SyncCell"
---

<objective>
Consolidate duplicate SyncCell<T> wrappers from UART and USB CDC tasks into a shared module using static_cell::StaticCell.

Purpose: Remove code duplication and use the safer static_cell crate instead of manual UnsafeCell implementation.
Output: Shared module + updated imports in both task files.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Create shared SyncCell module using static_cell</name>
  <files>src/hardware/static_sync.rs</files>
  <action>
Create new file src/hardware/static_sync.rs that:
1. Imports static_cell::StaticCell
2. Creates a SyncCell<T> wrapper that wraps StaticCell<Option<T>>
3. Implements:
   - `const fn new() -> Self` - creates a new SyncCell with None inside
   - `fn get(&self) -> *mut T` - returns raw pointer to inner data (compatible with existing usage pattern)

The wrapper must maintain API compatibility with existing code that uses `*cell.get() = value` pattern.

Example implementation:
```rust
use static_cell::StaticCell;

pub struct SyncCell<T>(StaticCell<Option<T>>);

impl<T> SyncCell<T> {
    pub const fn new() -> Self {
        Self(StaticCell::new())
    }
    
    pub fn get(&self) -> *mut T {
        // Return pointer to the Option<T> - callers will use *ptr = Some(value)
        self.0.as_ptr() as *mut T
    }
}
```

Also update src/hardware/mod.rs to add:
```rust
pub mod static_sync;
```
  </action>
  <verify>
- File src/hardware/static_sync.rs exists
- File compiles: cargo check --features=esp32c6
- Module is public and exported from hardware mod
  </verify>
  <done>
Shared SyncCell module exists in src/hardware/static_sync.rs and is importable
</done>
</task>

<task type="auto">
  <name>Update UART tasks to use shared SyncCell</name>
  <files>src/hardware/uart/tasks.rs</files>
  <action>
Update src/hardware/uart/tasks.rs:
1. Remove the local SyncCell<T> struct definition (lines 20-31)
2. Add import: `use crate::hardware::static_sync::SyncCell;`
3. Keep all static declarations and initialization logic unchanged - they should work with the new SyncCell wrapper since get() returns the same pointer type

The existing code pattern:
```rust
static COMMAND_QUEUE: SyncCell<...> = SyncCell::new(None);
// Later:
*COMMAND_QUEUE.get() = Some(CommandQueue::new());
```

Should continue to work because our wrapper's get() returns `*mut T` (pointer to Option<T>).
  </action>
  <verify>
- cargo check --features=esp32c6 passes
- No duplicate SyncCell definition in this file
  </verify>
  <done>
uart/tasks.rs imports SyncCell from shared module, builds successfully
</done>
</task>

<task type="auto">
  <name>Update USB CDC tasks to use shared SyncCell</name>
  <files>src/hardware/usb_cdc/tasks.rs</files>
  <action>
Update src/hardware/usb_cdc/tasks.rs:
1. Remove the local SyncCell<T> struct definition (lines 19-31)
2. Add import: `use crate::hardware::static_sync::SyncCell;`
3. Keep all static declarations and initialization logic unchanged

Same pattern as UART tasks - the wrapper maintains API compatibility.
  </action>
  <verify>
- cargo check --features=esp32c6 passes
- No duplicate SyncCell definition in this file
  </verify>
  <done>
usb_cdc/tasks.rs imports SyncCell from shared module, builds successfully
</done>
</task>

</tasks>

<verification>
Run full build to verify both communication paths work:
```bash
cargo check --features=esp32c6
```

Verify:
1. No duplicate SyncCell definitions
2. Build succeeds
3. Both uart and usb_cdc modules compile correctly
</verification>

<success_criteria>
- Shared SyncCell module exists and is imported by both task files
- Build succeeds with cargo check --features=esp32c6
- No duplicate SyncCell<T> definitions in codebase
- REFR-01, REFR-02, REFR-03 requirements satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/74-sync-cell-consolidation/74-01-SUMMARY.md`
</output>
