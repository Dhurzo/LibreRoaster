---
phase: 69-regression-helper-alignment
plan: 01
type: execute
wave: 1
depends_on:
  - 65-watchdog-timer-safety
files_modified:
  - src/safety/regression.rs
autonomous: true
user_setup: []
must_haves:
  truths:
    - "The public `safety::regression` API exposes only `regression_task` and `request_regression`, so no dead exports remain."
    - "The helper that runs the over-temperature regression stays callable inside `target_impl` so the regression task can complete its work."
    - "No other module imports `run_overtemp_regression`, keeping the helper private to `regression.rs`."
  artifacts:
    - path: "src/safety/regression.rs"
      provides: "Regression task, caller queue, and helper with matching exports."
      contains: "target_impl::{regression_task, request_regression}"
    - path: "src/application/app_builder.rs"
      provides: "Bootstraps the regression task so the safety helper executes during setup."
      contains: "regression::regression_task"
    - path: "src/application/tasks.rs"
      provides: "Instrumentation pathways that request the regression helper when automation triggers safety flows."
      contains: "regression::request_regression"
  key_links:
    - from: "src/safety/regression.rs"
      to: "src/application/app_builder.rs"
      via: "regression_task is spawned during configuration"
      pattern: "regression::regression_task"
    - from: "src/safety/regression.rs"
      to: "src/application/tasks.rs"
      via: "regression::request_regression enqueues the regression workflow"
      pattern: "regression::request_regression"
---

<objective>
Reconcile the regression helper export so the public API matches what the audit observed: only `regression_task` and `request_regression` should be exposed while the helper stays private to `regression.rs`. Purpose: close the tech-debt gap from the milestone audit, reduce API surface area, and keep the regression workflow runnable from the safety task queue. Output: `src/safety/regression.rs` with a private helper, a trimmed re-export list, and verification steps that prove no other module imports the helper.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v4.1-MILESTONE-AUDIT.md
# Gap source: exported helper `safety::regression::run_overtemp_regression` has no additional consumers.
# Outcome: hide the helper while keeping regression_task and request_regression public.
</context>

<tasks>

<task type="auto">
  <name>Privatize the regression helper</name>
  <files>src/safety/regression.rs</files>
  <action>
Convert `pub async fn run_overtemp_regression` into a private `async fn` inside `mod target_impl` while keeping `regression_task` as the only caller. Maintain the `Spawner` parameter so the helper can still be invoked by the task loop.
  </action>
  <verify>rg -n "pub async fn run_overtemp_regression" src/safety/regression.rs</verify>
  <done>The helper definition no longer includes `pub`, but `regression_task` still calls it from inside the module.
</task>

<task type="auto">
  <name>Limit the module exports</name>
  <files>src/safety/regression.rs</files>
  <action>
Update `pub use target_impl::{...}` so only `regression_task` and `request_regression` are re-exported; remove `run_overtemp_regression` from that list and avoid introducing any alternative public helpers.
  </action>
  <verify>rg -n "pub use target_impl" src/safety/regression.rs</verify>
  <done>The re-export list contains exactly `regression_task` and `request_regression`.
</task>

<task type="auto">
  <name>Prove no external references remain</name>
  <files>src</files>
  <action>
Run `rg -n run_overtemp_regression src` to ensure the helper is still referenced only from `regression.rs`. If other matches exist, refactor those imports to use `request_regression` or `regression_task` instead.
  </action>
  <verify>rg -n "run_overtemp_regression" src</verify>
  <done>The only matches for `run_overtemp_regression` appear inside `src/safety/regression.rs`.
</task>

</tasks>

<verification>
rg -n "pub async fn run_overtemp_regression" src/safety/regression.rs
rg -n "pub use target_impl" src/safety/regression.rs
rg -n "run_overtemp_regression" src
</verification>

<success_criteria>
- `run_overtemp_regression` is private and only called inside `target_impl::regression_task`.
- `pub use target_impl` exports only `regression_task` and `request_regression`.
- `rg run_overtemp_regression src` finds matches only in `src/safety/regression.rs`.
</success_criteria>

<output>
After completion, create `.planning/phases/69-regression-helper-alignment/69-01-SUMMARY.md`
</output>
