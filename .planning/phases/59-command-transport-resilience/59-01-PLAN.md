---
phase: 59-command-transport-resilience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/command_multiplexer_concurrency.rs
  - src/hardware/uart/tasks.rs
  - src/hardware/usb_cdc/tasks.rs
  - src/application/tasks.rs
  - README.md
autonomous: true

must_haves:
  truths:
    - "USB CDC and UART command bursts that hit the shared queue never drop or reorder Artisan commands."
    - "Queue processor instrumentation detects backlog/backpressure and recovers without losing commands."
    - "The README describes how to rerun the host concurrency test and interpret queue metrics."
  artifacts:
    - path: "tests/command_multiplexer_concurrency.rs"
      provides: "Integration test exercising concurrent USB + UART Artisan commands and the queue processor."
      min_lines: 80
    - path: "src/application/tasks.rs"
      provides: "Queue processor metrics exposed to tests and back-pressure detectors."
      min_lines: 120
    - path: "src/hardware/uart/tasks.rs"
      provides: "UART task instrumentation that records queue depth and backlog events."
      min_lines: 120
  key_links:
    - from: "tests/command_multiplexer_concurrency.rs"
      to: "src/application/tasks.rs::queue_processor_task"
      via: "spawned ServiceContainer commands for USB + UART"
      pattern: "queue_processor_task"
    - from: "src/hardware/uart/tasks.rs"
      to: "src/hardware/usb_cdc/tasks.rs"
      via: "shared queue metrics"
      pattern: "queue_depth"
---

<objective>
Shield the shared command queue from concurrent load: add a host-side concurrency test, expose backlog instrumentation, and document how to rerun the test so operators know when USB CDC + UART bursts may saturate the pipeline.

Purpose: Reduce the window where command bursts from both channels could drop or reorder commands and give operators a reliable way to reproduce, measure, and monitor saturation
Output: Integration test + instrumentation + doc for diagnostic runbook.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/PROJECT.md
@.planning/phases/59-command-transport-resilience/59-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add host concurrency test for the command multiplexer</name>
  <files>tests/command_multiplexer_concurrency.rs</files>
  <action>
1. Create `tests/command_multiplexer_concurrency.rs` that boots `ServiceContainer`, populates the async `roaster` fields, and mimics both USB CDC and UART command producers sending Artisan commands and reads at the same time.
2. Drive the same queue_processor_task used by production via futures (e.g., a `ThreadPool` with `SpawnExt`) so that five concurrent futures call `roaster_async_sensor_read()` and send READ/OT1/IO3 commands concurrently.
3. Assert that every queued command completes without panicking or returning `ContainerError`, and capture queue depth metrics emitted by the instrumentation added in Task 2 so the test fails if backlog grows past the documented threshold.
4. Reuse the existing host test pattern from `tests/mock_uart_integration.rs` for ServiceContainer initialization so the harness matches previous plans.
  </action>
  <verify>cargo test --target x86_64-unknown-linux-gnu --test command_multiplexer_concurrency</verify>
  <done>Concurrent USB + UART command bursts execute through `queue_processor_task` without drops; the test surface queue depth metrics for later assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Instrument queue processors for backlog detection</name>
  <files>src/hardware/uart/tasks.rs, src/hardware/usb_cdc/tasks.rs, src/application/tasks.rs</files>
  <action>
1. Introduce a small `QueueProcessorMetrics` helper (or reuse a struct in `application/tasks.rs`) that tracks `queue_depth`, `backlog_events`, and `max_depth` without new dependencies.
2. Wrap the `queue_processor_task` loop so each iteration samples the queue mutex depth, records an event when depth exceeds the concurrency test threshold, and makes the metric visible to host tests (e.g., via a shared `static AtomicUsize` or `embassy_sync::Mutex`).
3. Update both USB and UART task loops to publish those metrics whenever they add commands to the queue so the instrumentation reflects both producers. Keep existing queue ordering/priority guarantees.
4. Ensure instrumentation is non-blocking and safely exposes metrics to `tests/command_multiplexer_concurrency.rs` for assertions.
5. Rerun the host concurrency test to confirm backlog events never trigger unless the queue is legitimately saturated.
  </action>
  <verify>cargo test --target x86_64-unknown-linux-gnu --test command_multiplexer_concurrency</verify>
  <done>The queue_processor_task publishes queue depth/backlog metrics used by host tests, and both USB + UART producers report their contributions so the test can assert no unexpected backlog events.</done>
</task>

<task type="auto">
  <name>Task 3: Document how to reproduce and read the concurrency test</name>
  <files>README.md</files>
  <action>
1. Add a subsection under "Development" describing the new concurrency test: how to build it, what it exercises (USB/UART command bursts), and the expected queue_metric output.
2. Explain what backlog_event counts mean and how to interpret `queue_depth` values (e.g., the test fails if depth stays above the threshold for more than one iteration).
3. Mention the instrumentation fields so operators know exactly where to look when debugging command transport regressions.
  </action>
  <verify>rg -n "concurrency test" README.md</verify>
  <done>README.md includes explicit instructions for running the concurrency test and interpreting the new queue depth/backlog metrics.</done>
</task>

</tasks>

<verification>
- `cargo test --target x86_64-unknown-linux-gnu --test command_multiplexer_concurrency` passes and reports queue depth < threshold
- `rg -n "concurrency test" README.md` finds the new documentation block
</verification>

<success_criteria>
1. Host concurrency integration test lives in `tests/command_multiplexer_concurrency.rs` and passes.
2. Queue processor metrics (depth/backlog) are exposed from `src/application/tasks.rs`, `uart/tasks.rs`, and `usb_cdc/tasks.rs` and remain non-blocking.
3. README describes how to run the test and how to read queue depth/backlog metrics.
4. CONCURRENCY truth: USB + UART bursts survive without dropping commands.
</success_criteria>

<output>
After completion, create `.planning/phases/59-command-transport-resilience/59-01-SUMMARY.md`
</output>
