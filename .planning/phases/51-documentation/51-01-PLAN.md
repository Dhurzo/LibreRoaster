---
phase: 51-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "README.md states READ returns 4 values"
    - "README.md examples show ET,BT,HEATER,FAN format"
    - "README.md matches PROTOCOL.md exactly"
  artifacts:
    - path: "README.md"
      provides: "Updated protocol documentation"
      contains: "ET,BT,HEATER,FAN"
  key_links: []
---

<objective>
Update README.md Protocol section to match PROTOCOL.md's 4-value format specification.
</objective>

<context>
@internalDoc/PROTOCOL.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Update README Protocol section to 4-value format</name>
  <files>README.md</files>
  <action>
Update the Protocol section (lines 99-115) in README.md to reflect the new 4-value READ format:

1. Replace the header from:
   ```
   ### READ Response Format
   
   ```
   ET,BT,ET2,BT2,ambient,fan,heater
   ```
   
   Example: `185.2,192.3,-1,-1,24.5,45,75`
   
   | Field | Description |
   |-------|-------------|
   | ET | Environment temperature (°C) |
   | BT | Bean temperature (°C) |
   | ET2 | Extra channel (-1 if unused) |
   | BT2 | Extra channel (-1 if unused) |
   | ambient | Ambient temperature |
   | fan | Fan output % |
   | heater | Heater output % |
   ```

2. With the new format matching PROTOCOL.md (exact specification at lines 191-225):
   ```
   ### READ Response Format
   
   4-value CSV: ET,BT,HEATER,FAN
   
   | Field | Type | Unit | Description |
   |-------|------|------|-------------|
   | ET | Decimal | °C | Exhaust Temperature |
   | BT | Decimal | °C | Bean Temperature |
   | HEATER | Decimal | % | Heater PWM percentage |
   | FAN | Decimal | % | Fan PWM percentage |
   
   Example: `185.3,201.4,45,80`
   ```

3. Ensure consistency:
   - The table format matches PROTOCOL.md (4 fields, includes Type and Unit columns)
   - Example values match PROTOCOL.md exactly: `185.3,201.4,45,80`
   - Field names match PROTOCOL.md exactly: ET, BT, HEATER, FAN

4. Remove any other references to the old 7-value format in README.md (check for "ambient", "ET2", "BT2" mentions in context of READ command).
  </action>
  <verify>
grep -n "ET,BT" README.md && grep -n "185.3,201.4,45,80" README.md</verify>
  <done>
README.md Protocol section shows 4-value format (ET,BT,HEATER,FAN) matching PROTOCOL.md exactly. Old 7-value format removed.</done>
</task>

</tasks>

<verification>
- Confirm README.md line shows "ET,BT,HEATER,FAN" format
- Confirm example matches PROTOCOL.md: "185.3,201.4,45,80"
- Confirm no old format references remain in Protocol section
</verification>

<success_criteria>
1. README.md states READ returns 4 values
2. README.md examples show ET,BT,HEATER,FAN format  
3. README.md matches PROTOCOL.md exactly
</success_criteria>

<output>
After completion, create `.planning/phases/51-documentation/51-01-SUMMARY.md`
</output>
