---
phase: 89-readme-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["README.md"]
autonomous: true
must_haves:
  truths:
    - "README contains 'Project Status' section with version v5.0 and recent changes"
    - "Pinout table lists GPIO 3,4,5,6,7,9,10,1,20,21 with correct functions and notes"
    - "Build instructions include references to quality-baseline.sh and run-regression-checks.sh"
    - "Command table includes STATUS/STAT and REG rows with link to INSTRUMENTATION_README.MD"
    - "Artisan connection section links to ARTISAN_CONNECTION.md"
  artifacts:
    - path: "README.md"
      provides: "Updated project documentation"
      min_lines: 400
  key_links:
    - from: "README.md#pinout"
      to: "src/config/constants.rs"
      via: "GPIO mapping consistency"
      pattern: "GPIO.*(5|6|7).*(MOSI|MISO|SCLK)"
    - from: "README.md#supported-artisan-commands"
      to: "internalDoc/INSTRUMENTATION_README.MD"
      via: "link"
      pattern: "\\[internalDoc/INSTRUMENTATION_README.MD\\]"
    - from: "README.md#artisan-connection"
      to: "internalDoc/ARTISAN_CONNECTION.md"
      via: "link"
      pattern: "\\[ARTISAN_CONNECTION.md\\]"
    - from: "README.md#development"
      to: "scripts/quality-baseline.sh"
      via: "reference"
      pattern: "quality-baseline.sh"
---

<objective>
Update README.md with current project status, accurate hardware pinout, up‑to‑date build/test instructions, and verified links to internal documentation.

Purpose: Ensure users and contributors have accurate, discoverable information about the project's current state, hardware setup, and development workflow.
Output: Updated README.md that reflects v5.0 completion and aligns with internal documentation.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-readme-update/89-RESEARCH.md
@README.md
@src/config/constants.rs
@internalDoc/INSTRUMENTATION_README.MD
@internalDoc/ARTISAN_CONNECTION.md
@scripts/quality-baseline.sh
@scripts/run-regression-checks.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Project Status section and refresh Pinout table</name>
  <files>README.md</files>
  <action>
1. **Project Status section**:
   - Insert a new heading "## Project Status" immediately after the main title (line 2).
   - Use the template from 89-RESEARCH.md "Project Status Section Example" (lines 138–152).
   - Ensure the version is "v5.0 (2026‑03‑10)", milestone "v5.0 complete – Artisan protocol compatibility, instrumentation, safety features", next "v5.1 – Documentation update and minor improvements".
   - Keep the "Recent Changes" bullet list exactly as shown (STATUS command, REG command, watchdog instrumentation, quality‑baseline.sh, unsafe code refactoring, internal documentation updates).
   - Verify the section flows naturally with the existing "## Project Philosophy" that follows.

2. **Pinout table**:
   - Locate the existing "## Pinout" table (lines 95–106).
   - Replace the current table with the detailed GPIO mapping from constants.rs, following the example in 89-RESEARCH.md "Pinout Table Example" (lines 154–168).
   - Each GPIO must be listed separately: 3, 4, 5, 6, 7, 9, 10, 1, 20, 21.
   - Add a "Note" column as shown, with entries:
     - GPIO 3,4: "SPI bus shared"
     - GPIO 5: "Master Out Slave In"
     - GPIO 6: "Master In Slave Out"
     - GPIO 7: "Serial Clock"
      - GPIO 9: "Strapping pin – safe with pull‑up (see internalDoc/hardware.md)"
     - GPIO 10: (empty)
     - GPIO 1: "Pull‑up enabled"
     - GPIO 20,21: (empty)
   - Ensure the table uses proper Markdown formatting (|‑separated columns, header separator).
   - After the table, add a brief note: "For detailed hardware wiring and strapping pin information, see [hardware.md](internalDoc/hardware.md)."

3. **Verify changes**:
   - After editing, run `head -30 README.md` to confirm the Project Status section appears.
   - Run `grep -n "Pinout" README.md` to locate the table and verify each GPIO line.
  </action>
  <verify>
- `grep -c "## Project Status" README.md` returns 1
- `grep -A 5 "Project Status" README.md` shows the version and milestone lines
- `grep -E "GPIO.*5.*MOSI" README.md` returns a match
- `grep -E "GPIO.*6.*MISO" README.md` returns a match
- `grep -E "GPIO.*7.*SCLK" README.md` returns a match
- `grep -E "Strapping pin" README.md` returns a match for GPIO9
  </verify>
  <done>
- README contains a "Project Status" section with v5.0 version and recent changes.
- Pinout table lists GPIO 3,4,5,6,7,9,10,1,20,21 individually with correct functions and notes.
- Table includes a note column and a reference to internalDoc/hardware.md.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Build/Test instructions and verify command table & links</name>
  <files>README.md</files>
  <action>
1. **Build instructions**:
   - Locate the "### Build Commands" section (around line 212).
   - After the existing code block, add a new subsection "### Quality Baseline and Regression Testing":
     ```markdown
     ### Quality Baseline and Regression Testing

     The project includes automated quality gates and regression checks:

     ```bash
     # Run the quality baseline (format, clippy, tests)
     scripts/quality-baseline.sh

     # Run the full regression suite
     scripts/run-regression-checks.sh
     ```

     These scripts are the authoritative source for quality policy and regression validation. They are used in CI and should be run before submitting changes.
     ```
   - Ensure the code block is properly nested (four backticks for outer, three for inner).

2. **Test instructions**:
   - In the "### Test Commands" section (around line 238), add a bullet point after the "Basic Test Commands" code block:
     ```markdown
     **Quality and Regression:** For a complete quality and regression check, run the scripts described in [Quality Baseline and Regression Testing](#quality-baseline-and-regression-testing).
     ```
   - Keep the existing host integration test table unchanged.

3. **Command table verification**:
   - Locate the "### Supported Artisan Commands" table (around line 44).
   - Verify the table already contains rows for `STATUS/STAT` and `REG`. If missing, add them using the example from 89-RESEARCH.md lines 170–187.
   - Ensure the note below the table includes a working link to `internalDoc/INSTRUMENTATION_README.MD`. Check that the target file exists (`ls internalDoc/INSTRUMENTATION_README.MD`).
   - If the link is broken (e.g., wrong path), correct it to `[internalDoc/INSTRUMENTATION_README.MD](internalDoc/INSTRUMENTATION_README.MD)`.

4. **Artisan connection verification**:
   - Locate the "## Artisan Connection" section (around line 108).
   - Confirm the link to `[ARTISAN_CONNECTION.md](internalDoc/ARTISAN_CONNECTION.md)` is present and correct.
   - If missing, add it following the example in 89-RESEARCH.md lines 222–236.

5. **Internal link validation**:
   - For each referenced internal document (`INSTRUMENTATION_README.MD`, `ARTISAN_CONNECTION.md`, `internalDoc/hardware.md`), verify the file exists with `ls`.
   - If any file is missing, note it in a comment but do not create new files (they should already exist).
  </action>
  <verify>
- `grep -c "Quality Baseline and Regression Testing" README.md` returns 1
- `grep -c "scripts/quality-baseline.sh" README.md` returns at least 1
- `grep -c "scripts/run-regression-checks.sh" README.md` returns at least 1
- `grep -E "STATUS/STAT|REG" README.md | head -5` shows both commands present in the table
- `ls internalDoc/INSTRUMENTATION_README.MD` succeeds
- `ls internalDoc/ARTISAN_CONNECTION.md` succeeds
- `ls internalDoc/hardware.md` succeeds
  </verify>
  <done>
- Build instructions include a dedicated subsection for quality‑baseline.sh and run‑regression‑checks.sh.
- Test instructions reference the quality/regression scripts.
- Command table contains STATUS/STAT and REG rows with a working link to INSTRUMENTATION_README.MD.
- Artisan connection section links to ARTISAN_CONNECTION.md.
- All referenced internal documents exist.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Run `head -50 README.md` to confirm Project Status appears near the top.
2. Search for the pinout table and verify each GPIO line matches constants.rs:
   ```bash
   grep -A 10 "## Pinout" README.md | grep -E "GPIO.*[5-7]"
   ```
3. Check that the quality‑baseline.sh reference appears in the Build/Test sections:
   ```bash
   grep -n "quality-baseline" README.md
   ```
4. Confirm internal links are valid (no broken references):
   ```bash
   grep -o '\[[^]]*\]([^)]*)' README.md | grep -v "http" | sed 's/.*(//;s/).*//' | while read f; do [ -f "$f" ] || echo "Missing: $f"; done
   ```
   (Should output nothing.)
</verification>

<success_criteria>
- README.md updated with all required changes per phase goal.
- All must‑have truths satisfied (project status, pinout, build instructions, command table, links).
- No regression in existing README content (all other sections unchanged).
- Ready for human review in Plan 02.
</success_criteria>

<output>
After completion, create `.planning/phases/89-readme-update/89-01-SUMMARY.md`
</output>
