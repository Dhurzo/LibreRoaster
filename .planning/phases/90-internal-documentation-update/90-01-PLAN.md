---
phase: 90-internal-documentation-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["internalDoc/ARCHITECTURE.md", "internalDoc/PROTOCOL.md"]
autonomous: true
must_haves:
  truths:
    - "ARCHITECTURE.md describes current system architecture, task structure, and async model"
    - "PROTOCOL.md includes complete Artisan command reference and message formats (including 18-field STATUS command)"
  artifacts:
    - path: "internalDoc/ARCHITECTURE.md"
      provides: "System architecture overview, task diagram, async model"
      min_lines: 300
    - path: "internalDoc/PROTOCOL.md"
      provides: "Artisan command reference with syntax, response format, examples"
      min_lines: 300
  key_links:
    - from: "internalDoc/PROTOCOL.md"
      to: "internalDoc/INSTRUMENTATION_README.MD"
      via: "link"
      pattern: "\\[INSTRUMENTATION_README.MD\\]"
    - from: "internalDoc/ARCHITECTURE.md"
      to: "internalDoc/PROTOCOL.md"
      via: "reference"
      pattern: "PROTOCOL.md"
---

<objective>
Update ARCHITECTURE.md and PROTOCOL.md to accurately reflect current system design and Artisan protocol implementation.

Purpose: Ensure internal design documentation matches the codebase after recent architectural changes (ManualCommandPolicy pattern, 18-field STATUS command, quality baseline integration).
Output: Updated ARCHITECTURE.md and PROTOCOL.md that are synchronized with the current firmware.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/90-internal-documentation-update/90-RESEARCH.md

# Reference source files for verification
@internalDoc/ARCHITECTURE.md
@internalDoc/PROTOCOL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ARCHITECTURE.md</name>
  <files>internalDoc/ARCHITECTURE.md</files>
  <action>
Review the current ARCHITECTURE.md (last updated 2026-03-10) against the codebase to ensure it reflects:
1. Current system architecture (ESP32-C3, Embassy async tasks, command flow)
2. Task structure (usb_reader_task, uart_reader_task, control_loop_task, etc.) and their responsibilities
3. Async model (Embassy executor, task scheduling, message passing)
4. Recent changes: ManualCommandPolicy pattern for UNITS command (via forward_artisan_manual_command), 18-field STATUS command integration, quality baseline policy.

Compare with key source files:
- `src/main.rs` for task setup
- `src/tasks/` for task implementations
- `src/hardware/` for hardware abstraction
- `src/control/` for control loop
- `src/artisan/` for command handling

Update any outdated sections, add missing details, ensure terminology matches current code. Keep the "Last Updated" date current.

Verify cross‑references to other internal docs (PROTOCOL.md, INSTRUMENTATION_README.MD) are correct and links work.
  </action>
  <verify>
1. Read the updated ARCHITECTURE.md and confirm it describes the current system.
2. Check that the task table matches the tasks defined in `src/main.rs`.
3. Search for "ManualCommandPolicy" or "forward_artisan_manual_command" – ensure the architecture mentions this pattern.
4. Ensure there is a mention of the 18‑field STATUS command and its purpose.
5. Run `grep -l "ARCHITECTURE.md" internalDoc/*.md` to see which files reference it; verify those references are still valid.
  </verify>
  <done>
ARCHITECTURE.md accurately reflects the current system architecture, task structure, and async model, including recent changes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update PROTOCOL.md</name>
  <files>internalDoc/PROTOCOL.md</files>
  <action>
Review the current PROTOCOL.md (last updated 2026-03-10) to ensure it includes:
1. Complete Artisan command reference (UNITS, READ, OT1, OT2, FAN, STATUS, REG, etc.)
2. Message formats (syntax, response format, examples)
3. 18‑field STATUS command description with all fields explained (ET, BT, Heater, Fan, WatchdogOK, WatchdogFailures, LastWatchdogReason, LEDCGuardTimeouts, RegressionActive, PV, MV, IntegratorValue, DerivativeValue, SaturationFlag, IntegratorClampFlag, DerivativeAvailableFlag, CommandLatency, MaxCommandLatency)
4. Reference to INSTRUMENTATION_README.MD for detailed telemetry explanation
5. Any recent protocol changes (e.g., REG command behavior, safety notes)

Compare with source files:
- `src/artisan/commands.rs` for command definitions
- `src/artisan/formatter.rs` for STATUS formatting
- `src/control/regression.rs` for REG command
- `src/hardware/watchdog.rs` for watchdog details

Update any outdated sections, ensure the STATUS command table lists all 18 fields. Ensure the "Last Updated" date is current.

Verify cross‑references to ARCHITECTURE.md and INSTRUMENTATION_README.MD are correct.
  </action>
  <verify>
1. Read the updated PROTOCOL.md and confirm it includes all Artisan commands.
2. Check that the STATUS command section lists exactly 18 fields with descriptions.
3. Verify that the REG command description matches current behavior (ramps heater/fan to 100% for regression testing).
4. Search for broken links (`grep -o '\[.*\](.*\.md)' internalDoc/PROTOCOL.md`) and ensure they point to existing files.
5. Confirm that the "Last Updated" date is 2026-03-11 or later.
  </verify>
  <done>
PROTOCOL.md includes complete Artisan command reference and message formats, with accurate STATUS and REG command descriptions.
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. Run `grep -r "ARCHITECTURE.md\|PROTOCOL.md" internalDoc/` to ensure cross‑references are consistent.
2. Quick visual check of both files for obvious outdated information.
</verification>

<success_criteria>
- ARCHITECTURE.md updated with current system architecture, task structure, async model.
- PROTOCOL.md updated with complete Artisan command reference, including 18‑field STATUS command.
- Both files have "Last Updated" date reflecting today.
- No broken internal links.
</success_criteria>

<output>
After completion, create `.planning/phases/90-internal-documentation-update/90-01-SUMMARY.md`
</output>