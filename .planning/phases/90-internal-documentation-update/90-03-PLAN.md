---
phase: 90-internal-documentation-update
plan: 03
type: execute
wave: 3
depends_on: ["90-01", "90-02"]
files_modified: ["internalDoc/INSTRUMENTATION_README.MD", "internalDoc/ARCHITECTURE.md", "internalDoc/PROTOCOL.md", "internalDoc/HARDWARE.md", "internalDoc/DEVELOPMENT.md", "internalDoc/ARTISAN_CONNECTION.md", "README.md"]
autonomous: true
must_haves:
  truths:
    - "INSTRUMENTATION_README.MD describes watchdog, guard, regression telemetry"
    - "All internal documentation cross‑references are correct and up‑to‑date"
  artifacts:
    - path: "internalDoc/INSTRUMENTATION_README.MD"
      provides: "Instrumentation details for watchdog, guard, regression telemetry"
      min_lines: 40
  key_links:
    - from: "internalDoc/INSTRUMENTATION_README.MD"
      to: "internalDoc/PROTOCOL.md"
      via: "reference"
      pattern: "PROTOCOL.md"
    - from: "internalDoc/PROTOCOL.md"
      to: "internalDoc/INSTRUMENTATION_README.MD"
      via: "link"
      pattern: "\\[INSTRUMENTATION_README.MD\\]"
    - from: "internalDoc/ARCHITECTURE.md"
      to: "internalDoc/HARDWARE.md"
      via: "reference"
      pattern: "HARDWARE.md"
---

<objective>
Update INSTRUMENTATION_README.MD to accurately describe watchdog, guard, and regression telemetry, and verify/fix cross‑references across all internal documentation.

Purpose: Ensure instrumentation documentation is complete and that the internal documentation web is consistent, with no broken links or outdated references.
Output: Updated INSTRUMENTATION_README.MD and a verified, coherent set of internal docs.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/90-internal-documentation-update/90-RESEARCH.md

# Reference files (post‑update from plans 01 and 02)
@internalDoc/INSTRUMENTATION_README.MD
@internalDoc/ARCHITECTURE.md
@internalDoc/PROTOCOL.md
@internalDoc/HARDWARE.md
@internalDoc/DEVELOPMENT.md
@internalDoc/ARTISAN_CONNECTION.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update INSTRUMENTATION_README.MD</name>
  <files>internalDoc/INSTRUMENTATION_README.MD</files>
  <action>
Review the current INSTRUMENTATION_README.MD (last updated 2026-03-10) to ensure it fully describes:
1. **Watchdog telemetry** (WatchdogOK, WatchdogFailures, LastWatchdogReason) – explain what each field means, how watchdog feeding works, and how failures are logged.
2. **Guard telemetry** (LEDCGuardTimeouts) – describe the LEDC guard’s role in safety and how timeouts are counted.
3. **Regression telemetry** (RegressionActive) – explain over‑temperature regression testing, how the flag is set, and safety implications.
4. **PID internal state fields** (PV, MV, IntegratorValue, DerivativeValue, SaturationFlag, IntegratorClampFlag, DerivativeAvailableFlag) – briefly describe each field’s purpose in the control loop.
5. **Command latency fields** (CommandLatency, MaxCommandLatency) – clarify that these measure command‑processing latency in microseconds.

Update any missing descriptions, improve clarity, ensure terminology matches the codebase (`src/hardware/watchdog.rs`, `src/control/regression.rs`, `src/control/pid.rs`). Add links to PROTOCOL.md for the STATUS command and to ARCHITECTURE.md for the control loop context.

Update the "Last Updated" date to today.
  </action>
  <verify>
1. Read the updated INSTRUMENTATION_README.MD and confirm all three telemetry categories are covered.
2. Check that each of the 18 STATUS fields is mentioned or explained.
3. Verify that links to PROTOCOL.md and ARCHITECTURE.md are present and correct.
4. Search for outdated phrases (e.g., "USB instrumentation wiring" – ensure the document still has a clear purpose).
  </verify>
  <done>
INSTRUMENTATION_README.MD accurately describes watchdog, guard, and regression telemetry, with clear explanations and proper internal links.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify and fix cross‑references across internal documentation</name>
  <files>internalDoc/ARCHITECTURE.md, internalDoc/PROTOCOL.md, internalDoc/HARDWARE.md, internalDoc/DEVELOPMENT.md, internalDoc/ARTISAN_CONNECTION.md, README.md</files>
  <action>
Perform a systematic link‑check across all internal documentation:

1. **Collect all markdown files** in `internalDoc/` and `README.md`.
2. **Extract all internal links** (patterns `[text](*.md)` and `[text](internalDoc/*.md)`).
3. **For each link**:
   - Check that the target file exists (after renames; e.g., HARDWARE.md not hardware.md).
   - If the target is a section anchor (`#section`), verify the section exists in the target file.
   - If broken, fix by updating the link path or removing the anchor.
4. **Update any outdated references**:
   - Ensure ARCHITECTURE.md references HARDWARE.md (not hardware.md).
   - Ensure PROTOCOL.md references INSTRUMENTATION_README.MD and HARDWARE.md correctly.
   - Ensure ARTISAN_CONNECTION.md links to other internal docs are valid.
   - Ensure README.md links to DEVELOPMENT.md, HARDWARE.md, and ARTISAN_CONNECTION.md.
5. **Check for consistency** of terminology (e.g., "ESP32‑C3" vs "ESP32 C3") across documents; unify where trivial.

Use tools: `grep -o '\[.*\]([^)]*)' *.md internalDoc/*.md` to find links, and a simple script to verify existence. Manual review for anchor links.

After fixes, run a quick visual scan of each document to ensure no obvious broken formatting.
  </action>
  <verify>
1. Run a link‑check script (or manual check) and confirm zero broken internal links.
2. Verify that all references to renamed files (HARDWARE.md, DEVELOPMENT.md) are correct.
3. Spot‑check a few cross‑references (e.g., from PROTOCOL.md to INSTRUMENTATION_README.MD) to ensure they navigate correctly.
  </verify>
  <done>
All internal documentation cross‑references are correct and up‑to‑date; no broken links remain.
  </done>
</task>

</tasks>

<verification>
1. Run `grep -r "\\[.*\\]\\(.*\\.md\\)" internalDoc/ README.md 2>/dev/null | wc -l` to count links; ensure no obvious broken patterns.
2. Quick visual review of each document's "Last Updated" date (should be 2026-03-11 or later).
</verification>

<success_criteria>
- INSTRUMENTATION_README.MD updated with clear descriptions of watchdog, guard, regression telemetry.
- All internal links are valid; no broken references.
- Terminology consistent across documents.
</success_criteria>

<output>
After completion, create `.planning/phases/90-internal-documentation-update/90-03-SUMMARY.md`
</output>