---
phase: 60-concurrent-sensor-test
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/concurrent_sensor_test.rs
  - src/application/service_container.rs
  - README.md
autonomous: true

must_haves:
  truths:
    - "Host-run harness executes several `roaster_async_sensor_read()` futures in parallel and every future completes without `ContainerError` while async lock instrumentation proves the mutex never reports more than one holder."
    - "ServiceContainer exposes test-only async lock metrics (current and max depth plus reset helpers) so the harness can detect dropped locks or unexpected parallel holders."
    - "README.md records how to run the new host test, interpret its async lock depth telemetry, and links the coverage back to ASYNC-06 for the milestone audit."
  artifacts:
    - path: "tests/concurrent_sensor_test.rs"
      provides: "Parallel integration harness that boots the ServiceContainer, spawns several sensor reads through a host executor, and asserts both success and instrumentation expectations."
      min_lines: 90
    - path: "src/application/service_container.rs"
      provides: "Async mutex instrumentation (atomic counters plus accessors) gated under `cfg(test)` so tests can monitor current and historical lock depth."
      min_lines: 170
    - path: "README.md"
      provides: "Developer guidance for running `cargo test --test concurrent_sensor_test`, reading async lock depth metrics, and referencing ASYNC-06 coverage."
      min_lines: 20
  key_links:
    - from: "tests/concurrent_sensor_test.rs"
      to: "src/application/service_container.rs"
      via: "`async_lock_depth_max_for_tests()` and related helpers that the harness calls before and after the batch."
      pattern: "async_lock_depth"
    - from: "README.md"
      to: "tests/concurrent_sensor_test.rs"
      via: "Docs pointing to the host test command and what it proves about the async mutex."
      pattern: "cargo test --test concurrent_sensor_test"
---

<objective>
Add measurable instrumentation and documentation around the host-side concurrent sensor read test so ASYNC-06 is satisfied: the harness proves `ServiceContainer::roaster_async_sensor_read()` survives parallel load and operators can rerun the same command to demonstrate coverage.
Purpose: Provide a reproducible experience plus observable metrics that prove the async mutex never admits multiple holders or drops locks under the stressed test harness.
Output: hardened `tests/concurrent_sensor_test.rs`, test-only instrumentation in `service_container.rs`, and README guidance linking the test back to ASYNC-06.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/phases/60-concurrent-sensor-test/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strengthen the host concurrent sensor read harness</name>
  <files>tests/concurrent_sensor_test.rs</files>
  <action>
1. Reuse the existing ServiceContainer initialization so both the async and sync RoasterControl targets are populated before the test begins, matching production setup.
2. Spawn a host `ThreadPool` and use `spawn_with_handle` to launch eight to ten concurrent `ServiceContainer::roaster_async_sensor_read()` futures, awaiting them with `join_all` so every `Result<(), ContainerError>` is collected.
3. After the batch completes, assert every future returned `Ok(())`, and call the new instrumentation helpers (from Task 2) to confirm `max_async_lock_depth_for_tests()` never exceeded `1` and that `reset_async_lock_metrics_for_tests()` returns the counters to zero so the test fails if multiple holders or dropped locks occurred.
  </action>
  <verify>cargo test --target x86_64-unknown-linux-gnu --test concurrent_sensor_test</verify>
  <done>The host harness runs multiple concurrent sensor reads and fails if any read errors or if the async lock depth metric reports more than one holder or fails to reset.</done>
</task>

<task type="auto">
  <name>Task 2: Expose async mutex depth metrics for the test</name>
  <files>src/application/service_container.rs</files>
  <action>
1. Introduce `#[cfg(test)]` `AtomicUsize` counters (current depth and max depth) alongside helpers that allow tests to sample and reset the values, keeping these symbols private to the module but `pub(crate)` accessible for `/tests`.
2. Within `roaster_async_sensor_read()`, increment the current counter before acquiring the embassy mutex and update the max counter when a new record is observed, then decrement the current counter after the guard is dropped so the metrics faithfully capture concurrent holders.
3. Add `pub(crate)` functions such as `async_lock_depth_max_for_tests()` and `reset_async_lock_metrics_for_tests()` that the harness can call before/after the concurrent batch, ensuring instrumentation only exists under `cfg(test)` so production builds remain untouched.
4. Keep the instrumentation code in the same module (no new files) and document the guards so future tests can rely on the same helpers.
  </action>
  <verify>cargo test --target x86_64-unknown-linux-gnu --test concurrent_sensor_test</verify>
  <done>The ServiceContainer records async lock depth only during tests and exposes helpers the harness uses to detect multiple holders or dropped locks.</done>
</task>

<task type="auto">
  <name>Task 3: Document the concurrent sensor read integration test</name>
  <files>README.md</files>
  <action>
1. Write a subsection documenting `tests/concurrent_sensor_test.rs`, how to execute `cargo test --test concurrent_sensor_test` (host target only), and what the harness proves about the async mutex.
2. Explain the test-only async lock depth instrumentation, including how to read `max_async_lock_depth` and why it must stay at `1` so ASYNC-06 is satisfied.
3. Mention that the milestone audit should cite this test when confirming concurrent sensor reads remain safe, giving operators and reviewers the command and expected telemetry.
  </action>
  <verify>rg -n "concurrent sensor read" README.md</verify>
  <done>README.md explains how to run the host harness, what metrics it reports, and how it ties back to ASYNC-06 auditing coverage.</done>
</task>

</tasks>

<verification>
- `cargo test --target x86_64-unknown-linux-gnu --test concurrent_sensor_test`
- `rg -n "concurrent sensor read" README.md`
</verification>

<success_criteria>
1. Host harness in `tests/concurrent_sensor_test.rs` executes multiple futures in parallel, asserts all reads succeed, and fails if instrumentation reports depth > 1 or fails to reset.
2. `src/application/service_container.rs` stores async lock depth metrics under `cfg(test)` and exposes helpers that tests can call before/after the concurrent batch.
3. README.md documents how to run the concurrent sensor read test, interpret async lock depth metrics, and references ASYNC-06 for milestone verification.
</success_criteria>

<output>
After completion, create `.planning/phases/60-concurrent-sensor-test/60-01-SUMMARY.md`
</output>
