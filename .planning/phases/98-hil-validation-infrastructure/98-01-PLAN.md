---
phase: 98-hil-validation-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/hardware/SCENARIO_MATRIX.md
  - tests/hardware/scenario_manifest.json
  - tests/hardware/METHODOLOGY.md
autonomous: true

must_haves:
  truths:
    - "HIL scenario contract catalogs every watchdog/guard/comms scenario plus the expected telemetry columns and golden output artifact location."
    - "A machine-readable manifest lists scenario IDs, command steps, expected telemetry, golden CSV paths, and retention guidance so automation can drive them."
    - "The methodology document tells operators how to use the manifest, where the captured CSVs land, and how long the evidence must be retained for audits."
  artifacts:
    - path: "tests/hardware/SCENARIO_MATRIX.md"
      provides: "Human-readable fault injection matrix linked to golden outputs."
      contains: "Golden output"  # ensures the new section is present
    - path: "tests/hardware/scenario_manifest.json"
      provides: "Machine-readable definition of each scenario, command sequence, telemetry expectation, golden artifact, and retention window."
      contains: "\"id\": \"WD-01\""
    - path: "tests/hardware/METHODOLOGY.md"
      provides: "Evidence retention policy referencing manifest-driven runs."
      contains: "evidence retention"
  key_links:
    - from: "tests/hardware/SCENARIO_MATRIX.md"
      to: "tests/hardware/scenario_manifest.json"
      via: "Each matrix entry must have a manifest record so tooling knows the scenario metadata."
      pattern: "WD-|GD-|CM-"
    - from: "tests/hardware/scenario_manifest.json"
      to: "tests/hardware/METHODOLOGY.md"
      via: "Methodology text points to the manifest for command sequences and expected artifacts."
      pattern: "scenario_manifest\.json"
---

<objective>
Capture the agreement between operators and automation: the fault matrix, the machine-readable manifest, and the retention policy for artifact-backed runs.

Purpose: Without a clear contract and instructions, we cannot prove artifact-backed HIL scenarios or keep the evidence auditors demand.

Output: An expanded scenario matrix doc, a JSON manifest that enumerates every scenario, and refreshed methodology prose that links them to evidence paths.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v5.2-ROADMAP.md
@.planning/phases/97-traceability-matrix-tooling/97-01-SUMMARY.md
@tests/hardware/SCENARIO_MATRIX.md
@tests/hardware/METHODOLOGY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand scenario matrix with golden-output contract</name>
  <files>tests/hardware/SCENARIO_MATRIX.md</files>
  <action>
    Extend the fault injection matrix to include:
    1. A summary table of the 18-column STATUS payload and which columns feed the golden CSV (latency, watchdog, guard, fault flags).
    2. A "Golden outputs" subsection that describes the per-scenario CSV naming convention, run directory (tests/hardware/runs/{scenario_id}/{timestamp}.csv), and references the manifest file for command sequences.
    3. Guidance on what qualifies as a golden run (stable sensor reads, guard counts below threshold, checksum present) plus a pointer to the manifest so auditors can correlate scenario IDs with records.
    Why: Auditors and automation need one place that explains what a golden run looks like, where it lives, and how it ties to scenario IDs.
  </action>
  <verify>rg -n "Golden output" tests/hardware/SCENARIO_MATRIX.md</verify>
  <done>SCENARIO_MATRIX.md includes the STATUS column summary, golden output section, and manifest cross-reference for every WD/GD/CM scenario.</done>
</task>

<task type="auto">
  <name>Task 2: Publish machine-readable scenario manifest</name>
  <files>tests/hardware/scenario_manifest.json</files>
  <action>
    Create tests/hardware/scenario_manifest.json with an array of scenario objects. Each object should contain:
    - id, category (Watchdog/Guard/Comms), and plaintext description matching SCENARIO_MATRIX.md.
    - command_sequence (e.g., "feed_watchdog; check_status"), expected_columns (list of STATUS fields that matter), and golden_output (relative CSV path under tests/hardware/goldens/{id}.csv).
    - metadata (expected_watchdog_feed_ok, guard_timeouts, fault_condition) plus retention_days (60) and evidence_owner ("libreroaster/audits").
    Document the schema in the manifest so future automation knows how to interpret it.
    Why: Automation needs structured metadata before it can drive runs, compare to thresholds, and upload golden outputs.
  </action>
  <verify>python3 - <<'PY'
import json
from pathlib import Path
path = Path('tests/hardware/scenario_manifest.json')
data = json.load(path.open())
print([entry['id'] for entry in data])
assert len(data) >= 12
assert any(entry['id'] == 'WD-01' for entry in data)
PY</verify>
  <done>scenario_manifest.json exists, parses as JSON, and covers the WD/GD/CM scenario IDs documented in SCENARIO_MATRIX.md.</done>
</task>

<task type="auto">
  <name>Task 3: Document evidence retention and manifest usage</name>
  <files>tests/hardware/METHODOLOGY.md</files>
  <action>
    Add sections describing:
    - How to locate/test each scenario via tests/hardware/scenario_manifest.json (schema, run steps, and command sequences).
    - Where validation_runner deposits CSV telemetry and metadata (tests/hardware/runs/{scenario}/{timestamp}.csv/json) plus how to name artifacts for release audits.
    - The retention policy (e.g., keep golden outputs and reports for 60 days) and the process for marking a run as golden.
    Why: Without written guidance, operators will capture telemetry but auditors will not know which files prove the run.
  </action>
  <verify>rg -n "evidence retention" tests/hardware/METHODOLOGY.md</verify>
  <done>METHODOLOGY.md references scenario_manifest.json, describes the run directory structure, and states the retention policy for golden outputs.</done>
</task>

</tasks>

<verification>
Confirm SCENARIO_MATRIX.md now describes golden outputs, scenario_manifest.json parses and covers every scenario, and METHODOLOGY.md explains how to use the manifest and keep artifacts for audits.
</verification>

<success_criteria>
- Scenario matrix includes the STATUS column summary, golden output contract, and manifest pointer.
- Scenario manifest JSON lists every WD/GD/CM scenario with metadata, command_sequence, and artifact path.
- Methodology doc outlines how to run manifest-driven scenarios, where to find CSVs/metadata, and how long to keep them.
</success_criteria>

<output>
After completion, create `.planning/phases/98-hil-validation-infrastructure/98-01-SUMMARY.md`
</output>
