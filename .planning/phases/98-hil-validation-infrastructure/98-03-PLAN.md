---
phase: 98-hil-validation-infrastructure
plan: 03
type: execute
wave: 3
depends_on:
  - 98-02
files_modified:
  - tests/hardware/report_template.md
  - tests/hardware/HIL-PLAYBOOK.md
  - README.md
autonomous: true

must_haves:
  truths:
    - "The validation report template surfaces scenario metadata, golden output links, and threshold verdicts so auditors can read the evidence trail."
    - "A new HIL playbook documents the manifest-driven workflow, CLI flags, artifact paths, and how to mark a run as golden."
    - "README.md highlights the HIL validation playbook and the requirement to keep artifact-backed golden outputs for HW-03 audits."
  artifacts:
    - path: "tests/hardware/report_template.md"
      provides: "Template that embeds scenario descriptions, golden output references, and verdict summaries."
      contains: "Scenario Metadata"
    - path: "tests/hardware/HIL-PLAYBOOK.md"
      provides: "Step-by-step instructions for running manifest-driven HIL scenarios and packaging evidence."
      contains: "HIL validation playbook"
    - path: "README.md"
      provides: "Project overview that links to the HIL playbook and emphasizes artifact-backed validation."
      contains: "HIL validation"
  key_links:
    - from: "tests/hardware/report_template.md"
      to: "tests/hardware/HIL-PLAYBOOK.md"
      via: "Reports cite the playbook for run steps and golden output placement."
      pattern: "HIL validation playbook"
    - from: "tests/hardware/HIL-PLAYBOOK.md"
      to: "README.md"
      via: "README links to the playbook so contributors know where to start."
      pattern: "tests/hardware/HIL-PLAYBOOK.md"
---

<objective>
Capture the workflow for turning manifest-driven HIL telemetry into auditor-friendly reports and documentation.

Purpose: Once the tooling exists, we still need to teach people how to run it, which reports to trust, and where to find golden outputs.

Output: An updated report template, a dedicated HIL playbook, and README guidance that points to the playbook and artifact requirements.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v5.2-ROADMAP.md
@tests/hardware/report_template.md
@tests/hardware/validation_runner.py
@tests/hardware/analysis.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add scenario metadata section to the report template</name>
  <files>tests/hardware/report_template.md</files>
  <action>
    Enrich the existing Markdown template with:
    1. A "Scenario Metadata" section that echoes the manifest entry (ID, category, description, command_sequence).
    2. A "Golden artifact" row that links to the expected CSV path from the manifest and states how long it is retained.
    3. Threshold verdicts written as PASS/FAIL badges and a link to the HIL playbook for run instructions.
    Why: Reports should self-describe the scenario that generated them and point to the golden output so auditors trust the artifact.
  </action>
  <verify>rg -n "Scenario Metadata" tests/hardware/report_template.md</verify>
  <done>Report template now documents scenario metadata, artifacts, and threshold verdicts referencing the manifest-driven run.</done>
</task>

<task type="auto">
  <name>Task 2: Write the HIL playbook</name>
  <files>tests/hardware/HIL-PLAYBOOK.md</files>
  <action>
    Create tests/hardware/HIL-PLAYBOOK.md describing the end-to-end workflow:
    - How to read tests/hardware/scenario_manifest.json and pick a scenario.
    - Commands for running validation_runner with --manifest/--scenario/--runs-dir.
    - Where telemetry/metadata land and how to run analysis.py to emit reports.
    - How to bundle CSV, metadata JSON, and report markdown into a golden artifact tarball for release audits and how long to keep it.
    - Safety notes (ensure firmware has gold status before recording, flush logs before copying files).
    Why: Contributors must be able to reproduce golden outputs and know what evidence to deliver for HW-03 signoff.
  </action>
  <verify>rg -n "HIL validation playbook" tests/hardware/HIL-PLAYBOOK.md</verify>
  <done>HIL-PLAYBOOK.md explains how to execute scenarios, capture telemetry, run analysis, and package artifacts for audits.</done>
</task>

<task type="auto">
  <name>Task 3: Highlight HIL validation in README</name>
  <files>README.md</files>
  <action>
    Add a short section or paragraph under Development or Hardware Requirements that:
    - Points to tests/hardware/HIL-PLAYBOOK.md.
    - Announces that HW-03 requires artifact-backed HIL scenarios with golden outputs.
    - Suggests the validation_runner + analysis workflow described in the playbook is the authoritative process for those artifacts.
    Why: The README is the first thing contributors read; it should surface the new HIL workflow and where to find golden evidence.
  </action>
  <verify>rg -n "HIL validation" README.md</verify>
  <done>README.md references the HIL playbook and reminds contributors to follow the artifact-backed workflow for HW-03.</done>
</task>

</tasks>

<verification>
Ensure the template includes scenario metadata/golden artifact references, the new playbook documents the workflow, and README links to the playbook with HW-03 context.
</verification>

<success_criteria>
- The report template contains a scenario metadata table and golden artifact link.
- HIL-PLAYBOOK.md documents the manifest-driven toolchain, artifact storage, and packaging instructions.
- README.md highlights the HIL validation flow and links to the playbook.
</success_criteria>

<output>
After completion, create `.planning/phases/98-hil-validation-infrastructure/98-03-SUMMARY.md`
</output>
