---
phase: 84-solid-seam-hardening-and-fault-injection
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [Cargo.toml, tests/hardware/fault_injection_scenarios.rs, tests/hardware/SCENARIO_MATRIX.md, tests/regression_status.rs]
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Handler decisions flow through explicit policy contracts before any heater/fan writes, so hardware authority stays centralized."
    - "Stage instrumentation records SensorRead->ControlUpdate->LedcWrite->WatchdogFeed->TelemetryEmit order even when faults are injected."
    - "Watchdog, guard, and comms fault-injection scenarios emit STATUS telemetry that captures safe responses plus failure metadata."
  artifacts:
    - path: "src/control/policies.rs"
      provides: "Ports-and-policies trait definitions that describe manual and safety decisions outside hardware writes."
      min_lines: 30
    - path: "src/control/handlers.rs"
      provides: "Handlers implement the policy contracts and mutate SystemStatus without directly calling heater/fan traits."
      min_lines: 40
    - path: "src/control/roaster_refactored.rs"
      provides: "RoasterControl centralizes heater/fan/watchdog writes and exposes policy results from the handler chain."
      min_lines: 50
    - path: "src/application/tasks.rs"
      provides: "Stage-tracked control loop instrumentation that emits stage events to telemetry."
      min_lines: 40
    - path: "tests/hardware/fault_injection_scenarios.rs"
      provides: "Fault injection harness that records scenario IDs plus STATUS telemetry."
      min_lines: 30
  key_links:
    - from: "src/control/handlers.rs"
      to: "src/control/roaster_refactored.rs"
      via: "ManualCommandPolicy outputs"
      pattern: "forward_artisan_manual_command|ManualCommandPolicy"
    - from: "src/application/tasks.rs"
      to: "src/control/roaster_refactored.rs"
      via: "control_loop_task stage transitions"
      pattern: "stage_tracker\\.set_stage"
    - from: "tests/hardware/fault_injection_scenarios.rs"
      to: "tests/regression_status.rs"
      via: "STATUS telemetry validation"
      pattern: "format_status_response"
---
<objective>
Formalize the watchdog/guard/comms fault-injection matrix so each scenario writes CSV evidence and STATUS telemetry that prove safe-system responses.

Purpose: Provide an auditable evidence pack and regression checks that satisfy SOLID-02 without inventing new tooling outside the approved host harness.
Output: `Cargo.toml` gains `serialport`/`csv` dev-dependencies, a host harness `tests/hardware/fault_injection_scenarios.rs`, the scenario matrix doc, and updated regression status assertions.
</objective>
<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-solid-seam-hardening-and-fault-injection/84-RESEARCH.md
</context>
<tasks>
<task type="auto">
  <name>Task 1: Build the fault-injection harness</name>
  <files>Cargo.toml, tests/hardware/fault_injection_scenarios.rs, tests/hardware/SCENARIO_MATRIX.md</files>
  <action>Add `serialport = "4.8.1"` and `csv = "1.4.0"` to the dev-dependencies so the host harness can compile. Create `tests/hardware/fault_injection_scenarios.rs` that iterates through guards/watchdog/comms scenario IDs, uses `embedded-hal-mock` to induce failures, captures `SystemStatus`, formats STATUS via `ArtisanFormatter`, and writes timestamped rows to a CSV evidence file described in `SCENARIO_MATRIX.md`.</action>
  <verify>Run `cargo test --test fault_injection_scenarios` once the harness exists to ensure each scenario builds and the CSV writer runs without panics.</verify>
  <done>The harness compiles, emits the expected CSV rows per scenario, and the scenario matrix doc describes the expected STATUS flags for watchdog/guard/comms failures.</done>
</task>
<task type="auto">
  <name>Task 2: Assert STATUS metadata for each scenario</name>
  <files>tests/regression_status.rs</files>
  <action>Extend `tests/regression_status.rs` to cover the fault-injection matrix by asserting that the `STATUS` tail includes the correct `watchdog_feed_ok`, `ledc_guard_timeouts`, and `fault_condition` bits for every scenario documented in `SCENARIO_MATRIX.md`. Keep the regression flag comparisons so the host evidence still matches.</action>
  <verify>Run `cargo test --test regression_status --features regression` to prove the STATUS lines continue to have deterministic columns for the new scenarios.</verify>
  <done>The regression status test now covers the documented scenarios and passes, confirming STATUS telemetry has the right watchdog/guard metadata.</done>
</task>
</tasks>
<verification>
`cargo test --test fault_injection_scenarios`
`cargo test --test regression_status --features regression`
</verification>
<success_criteria>
All documented fault-injection scenarios run on the host harness, emit CSV/STATUS evidence, and every STATUS regression test still passes with the added metadata.
</success_criteria>
<output>
After completion, create `.planning/phases/84-solid-seam-hardening-and-fault-injection/84-03-SUMMARY.md`
</output>
