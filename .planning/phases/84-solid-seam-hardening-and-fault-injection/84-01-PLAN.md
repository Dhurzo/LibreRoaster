---
phase: 84-solid-seam-hardening-and-fault-injection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/control/policies.rs, src/control/handlers.rs, src/control/roaster_refactored.rs]
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Handler decisions flow through explicit policy contracts before any heater/fan writes, so hardware authority stays centralized."
    - "Stage instrumentation records SensorRead->ControlUpdate->LedcWrite->WatchdogFeed->TelemetryEmit order even when faults are injected."
    - "Watchdog, guard, and comms fault-injection scenarios emit STATUS telemetry that captures safe responses plus failure metadata."
  artifacts:
    - path: "src/control/policies.rs"
      provides: "Ports-and-policies trait definitions that describe manual and safety decisions outside hardware writes."
      min_lines: 30
    - path: "src/control/handlers.rs"
      provides: "Handlers implement the policy contracts and mutate SystemStatus without directly calling heater/fan traits."
      min_lines: 40
    - path: "src/control/roaster_refactored.rs"
      provides: "RoasterControl centralizes heater/fan/watchdog writes and exposes policy results from the handler chain."
      min_lines: 50
    - path: "src/application/tasks.rs"
      provides: "Stage-tracked control loop instrumentation that emits stage events to telemetry."
      min_lines: 40
    - path: "tests/hardware/fault_injection_scenarios.rs"
      provides: "Fault injection harness that records scenario IDs plus STATUS telemetry."
      min_lines: 30
  key_links:
    - from: "src/control/handlers.rs"
      to: "src/control/roaster_refactored.rs"
      via: "ManualCommandPolicy outputs"
      pattern: "forward_artisan_manual_command|ManualCommandPolicy"
    - from: "src/application/tasks.rs"
      to: "src/control/roaster_refactored.rs"
      via: "control_loop_task stage transitions"
      pattern: "stage_tracker\\.set_stage"
    - from: "tests/hardware/fault_injection_scenarios.rs"
      to: "tests/regression_status.rs"
      via: "STATUS telemetry validation"
      pattern: "format_status_response"
---
<objective>
Harden the handler-to-hardware seam by codifying ports-and-policies contracts so RoasterControl remains the single writer for heater, fan, and safety outputs.

Purpose: Describe the SOLID seam so future refactors can move policy logic out of hardware code while keeping authority and timing intact.
Output: `src/control/policies.rs` plus the handler and RoasterControl updates that implement the new contracts.
</objective>
<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-solid-seam-hardening-and-fault-injection/84-RESEARCH.md
</context>
<tasks>
<task type="auto">
  <name>Task 1: Define manual and safety policy contracts</name>
  <files>src/control/policies.rs</files>
  <action>Create `ManualPolicyOutcome` plus `ManualCommandPolicy` and `SafetyPolicy` traits that describe heater/fan targets, failure signals, and guard flags without touching hardware ports. Export the traits from `control::policies` so handlers can return those outcomes before the single writer applies them.</action>
  <verify>Run `cargo test --test command_errors --test command_multiplexer_concurrency` to make sure the new module compiles with the handler chain.</verify>
  <done>The module defines the policy traits/outcomes, compiles cleanly, and documents the hand-off contract for handlers.</done>
</task>
<task type="auto">
  <name>Task 2: Refactor handlers to implement the policies</name>
  <files>src/control/handlers.rs</files>
  <action>Update each handler to implement the `ManualCommandPolicy`/`SafetyPolicy` traits, returning `ManualPolicyOutcome` instances that describe desired heater/fan adjustments and status flags. Keep only status mutations (PID enable, artisan_control, emergency flags) in handlers and avoid direct heater/fan writes.</action>
  <verify>Re-run `cargo test --test command_errors --test command_multiplexer_concurrency` so the handler implementations build against the new interfaces.</verify>
  <done>Handlers compile, only mutate SystemStatus, and return policy outcomes instead of operating hardware ports themselves.</done>
</task>
<task type="auto">
  <name>Task 3: Let RoasterControl be the sole hardware writer</name>
  <files>src/control/roaster_refactored.rs</files>
  <action>Change `process_command`/`forward_artisan_manual_command` to consume the policy outcomes and to apply heater/fan values via `apply_guarded_heater` and `fan.set_speed`, preserving the guard/watchdog ordering while logging the policy inputs for instrumentation.</action>
  <verify>Run `cargo test --test artisan_integration_test --test command_idempotence` so command behavior remains intact after the new hand-off.</verify>
  <done>RoasterControl applies all hardware writes after policy evaluation, manual commands keep their PID/safety ordering, and the policy hand-off logs stay in place.</done>
</task>
</tasks>
<verification>
`cargo test --test artisan_integration_test --test command_idempotence --test command_errors --test command_multiplexer_concurrency`
</verification>
<success_criteria>
Handlers rely on the policy contracts, the manual/safety authority stays centralized, and the command-focused tests pass with the new module in place.
</success_criteria>
<output>
After completion, create `.planning/phases/84-solid-seam-hardening-and-fault-injection/84-01-SUMMARY.md`
</output>
