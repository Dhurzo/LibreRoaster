---
phase: 84-solid-seam-hardening-and-fault-injection
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/application/stage_instrumentation.rs, src/application/tasks.rs, tests/control_loop_stage.rs]
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Handler decisions flow through explicit policy contracts before any heater/fan writes, so hardware authority stays centralized."
    - "Stage instrumentation records SensorRead->ControlUpdate->LedcWrite->WatchdogFeed->TelemetryEmit order even when faults are injected."
    - "Watchdog, guard, and comms fault-injection scenarios emit STATUS telemetry that captures safe responses plus failure metadata."
  artifacts:
    - path: "src/control/policies.rs"
      provides: "Ports-and-policies trait definitions that describe manual and safety decisions outside hardware writes."
      min_lines: 30
    - path: "src/control/handlers.rs"
      provides: "Handlers implement the policy contracts and mutate SystemStatus without directly calling heater/fan traits."
      min_lines: 40
    - path: "src/control/roaster_refactored.rs"
      provides: "RoasterControl centralizes heater/fan/watchdog writes and exposes policy results from the handler chain."
      min_lines: 50
    - path: "src/application/tasks.rs"
      provides: "Stage-tracked control loop instrumentation that emits stage events to telemetry."
      min_lines: 40
    - path: "tests/hardware/fault_injection_scenarios.rs"
      provides: "Fault injection harness that records scenario IDs plus STATUS telemetry."
      min_lines: 30
  key_links:
    - from: "src/control/handlers.rs"
      to: "src/control/roaster_refactored.rs"
      via: "ManualCommandPolicy outputs"
      pattern: "forward_artisan_manual_command|ManualCommandPolicy"
    - from: "src/application/tasks.rs"
      to: "src/control/roaster_refactored.rs"
      via: "control_loop_task stage transitions"
      pattern: "stage_tracker\\.set_stage"
    - from: "tests/hardware/fault_injection_scenarios.rs"
      to: "tests/regression_status.rs"
      via: "STATUS telemetry validation"
      pattern: "format_status_response"
---
<objective>
Add a lightweight stage instrumentation reporter so observers can see the deterministic SensorRead->ControlUpdate->LedcWrite->WatchdogFeed->TelemetryEmit sequence even when fault conditions occur.

Purpose: Surface stage names/durations without changing the 100 ms budget, enabling curlable evidence for requirement SOLID-02.
Output: `src/application/stage_instrumentation.rs`, updated `control_loop_task`, and a regression test that validates the emitted stage events.
</objective>
<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-solid-seam-hardening-and-fault-injection/84-RESEARCH.md
</context>
<tasks>
<task type="auto">
  <name>Task 1: Emit stage events from the control loop</name>
  <files>src/application/stage_instrumentation.rs, src/application/tasks.rs</files>
  <action>Introduce `StageReporter` that serializes `ControlLoopStage` names, elapsed time, guard/ watchdog state, and optional failure markers into heapless strings written to the Artisan output channel. Wire it into `control_loop_task` so it runs after each `stage_tracker.set_stage` call without allocating or delaying the 100 ms cadence.</action>
  <verify>Run `cargo test --test control_loop_stage` even before the test exists to ensure the module compiles and the reporter hook builds.</verify>
  <done>StageReporter writes deterministic `STAGE,<name>,elapsed=<ms>,guard=<flag>,watchdog=<state>` entries for every stage and is invoked by the control loop immediately after each stage change.</done>
</task>
<task type="auto">
  <name>Task 2: Verify stage instrumentation</name>
  <files>tests/control_loop_stage.rs</files>
  <action>Create a test harness that drives `StageReporter` with fake stage sequences and collects the serialised events to assert the correct order, durations, and guard/watchdog flags. Keep the harness pure Rust so it can run on the host with `#[cfg(test)]` only and match the expected output format.</action>
  <verify>Run `cargo test --test control_loop_stage` to validate the reporter and its formatting rules.</verify>
  <done>The new test suite passes, confirming that stage events contain the expected names and guard/watchdog indicators in deterministic order.</done>
</task>
</tasks>
<verification>
`cargo test --test control_loop_stage`
</verification>
<success_criteria>
StageReporter produces deterministic events for every control loop stage and the host test proves the order/format so observers can rely on the instrumentation.
</success_criteria>
<output>
After completion, create `.planning/phases/84-solid-seam-hardening-and-fault-injection/84-02-SUMMARY.md`
</output>
