---
phase: 64-documentation-consistency-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internalDoc/FLASH_GUIDE.md
  - README.md
autonomous: true
user_setup: []

must_haves:
  truths:
    - "FLASH_GUIDE.md binary paths reference correct target directory (riscv32imc-unknown-none-elf)"
    - "README.md target name references riscv32imc (matches rust-toolchain.toml)"
    - "README.md includes macOS port path reference for Artisan connection"
  artifacts:
    - path: "internalDoc/FLASH_GUIDE.md"
      contains: "target/riscv32imc-unknown-none-elf/release/libreroaster.bin"
      occurrences: 7
    - path: "README.md"
      contains: "riscv32imc-unknown-none-elf"
      occurrences: 4
    - path: "README.md"
      contains: "macOS"
      context: "port"
  key_links:
    - from: "rust-toolchain.toml"
      to: "FLASH_GUIDE.md, README.md"
      via: "binary path documentation consistency"
      pattern: "riscv32imc-unknown-none-elf"
---

<objective>
Fix documentation inconsistencies identified in v4.1 audit. Three gaps: binary path in FLASH_GUIDE.md, target name in README.md, and macOS port reference in README.md.

Purpose: Ensure documentation accurately reflects the correct target architecture (riscv32imc) as specified in rust-toolchain.toml.
Output: Updated FLASH_GUIDE.md and README.md with correct paths and references.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@rust-toolchain.toml

# Gap source: v4.1-MILESTONE-AUDIT.md
# - FLASH_GUIDE.md binary path fix (target/riscv32imc-unknown-none-elf/release)
# - README.md target name fix (riscv32imc)
# - README.md macOS port reference

# Current state verified:
# - rust-toolchain.toml: specifies riscv32imc-unknown-none-elf
# - FLASH_GUIDE.md: Contains wrong paths (target/release)
# - README.md: Contains wrong target (riscv32imac)
# - internalDoc/ARTISAN_CONNECTION.md: Has macOS port reference
</context>

<tasks>

<task type="auto">
  <name>Fix FLASH_GUIDE.md binary paths</name>
  <files>internalDoc/FLASH_GUIDE.md</files>
  <action>
Replace all occurrences of `target/release/libreroaster.bin` with `target/riscv32imc-unknown-none-elf/release/libreroaster.bin`.

Affected lines (from audit):
- Line 63: GUI browse path
- Line 72: espflash flash command
- Line 75: espflash flash with port
- Line 78: espflash flash with monitor
- Line 99: Building firmware output path
- Line 162: Custom partition table command
- Line 170: Dual-bank flashing command

Use replaceAll for `target/release/libreroaster.bin` → `target/riscv32imc-unknown-none-elf/release/libreroaster.bin`
  </action>
  <verify>grep -c "target/riscv32imc-unknown-none-elf/release/libreroaster.bin" internalDoc/FLASH_GUIDE.md
# Should return 7</verify>
  <done>All 7 binary path references in FLASH_GUIDE.md now point to correct target directory</done>
</task>

<task type="auto">
  <name>Fix README.md target name</name>
  <files>README.md</files>
  <action>
Replace all occurrences of `riscv32imac-unknown-none-elf` with `riscv32imc-unknown-none-elf` to match rust-toolchain.toml.

Affected lines (from audit):
- Line 200: rustup target add command
- Line 227: cargo build --target command
- Line 230: Output location (target/riscv32imac-unknown-none-elf/release/libreroaster.bin)
- Line 232: Note about --target flag

Use replaceAll for `riscv32imac-unknown-none-elf` → `riscv32imc-unknown-none-elf`
  </action>
  <verify>grep -c "riscv32imc-unknown-none-elf" README.md
# Should return 4 (was 4 instances of riscv32imac)
grep -c "riscv32imac-unknown-none-elf" README.md  
# Should return 0</verify>
  <done>All target references in README.md now use riscv32imc (matching rust-toolchain.toml)</done>
</task>

<task type="auto">
  <name>Add macOS port reference to README.md</name>
  <files>README.md</files>
  <action>
Add macOS port path reference to the "Connect to Artisan" section in README.md.

Current content (lines 71-78):
"1. Identify the USB port (ttyACM on Linux, COM on Windows)"

Should add macOS port reference similar to FLASH_GUIDE.md and ARTISAN_CONNECTION.md.

Find line 76 in README.md section "### 2. Connect to Artisan" and update to include macOS:
"1. Identify the USB port (ttyACM on Linux, /dev/cu.usbmodem-* on macOS, COM on Windows)"

Reference from FLASH_GUIDE.md line 39: "macOS: `/dev/cu.usbserial-*` or `/dev/cu.usbmodem-*`"
Reference from ARTISAN_CONNECTION.md lines 30-31: "macOS: `/dev/cu.usbmodem-*` or `/dev/cu.usbserial-*`"
  </action>
  <verify>grep -i "macOS" README.md | grep -i "cu\."
# Should find macOS port reference in Artisan connection section</verify>
  <done>README.md includes macOS port path (/dev/cu.usbmodem-*) in Artisan connection section</done>
</task>

</tasks>

<verification>
# Verify all three gaps are closed
grep -c "target/riscv32imc-unknown-none-elf/release/libreroaster.bin" internalDoc/FLASH_GUIDE.md
# Expected: 7

grep -c "riscv32imc-unknown-none-elf" README.md
# Expected: 4

grep -c "riscv32imac-unknown-none-elf" README.md  
# Expected: 0

grep -i "macOS" README.md | grep -i "cu\."
# Expected: 1+ (macOS port reference present)
</verification>

<success_criteria>
- FLASH_GUIDE.md: All 7 binary path references updated to target/riscv32imc-unknown-none-elf/release/libreroaster.bin
- README.md: All 4 target name references updated to riscv32imc-unknown-none-elf
- README.md: macOS port path reference added to Artisan connection section
- Documentation now consistent with rust-toolchain.toml
</success_criteria>

<output>
After completion, create `.planning/phases/64-documentation-consistency-fixes/64-01-SUMMARY.md`
</output>
