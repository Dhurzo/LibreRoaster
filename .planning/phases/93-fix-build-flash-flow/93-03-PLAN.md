---
phase: 93-fix-build-flash-flow
plan: 03
type: execute
wave: 3
depends_on: ["93-02"]
files_modified: []
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Flash command syntax is correct"
    - "Binary path references match actual build output"
    - "E2E build → flash flow is documented and validated"
  artifacts:
    - path: "README.md"
      provides: "Flash instructions with correct binary path"
      contains: "libreroaster.bin"
    - path: "internalDoc/DEVELOPMENT.md"
      provides: "Detailed flash commands with cargo espflash"
      contains: "cargo espflash flash"
  key_links:
    - from: "README.md flash instructions"
      to: "Build output directory"
      via: "Binary path reference"
      pattern: "target/riscv32imc-unknown-none-elf/release/libreroaster.bin"
    - from: "Flash commands"
      to: "Generated binary"
      via: "File path matching"
      pattern: "libreroaster.bin"
---

<objective>
Verify flash command syntax, confirm binary path references are accurate, and document the complete E2E build → flash workflow to ensure users can successfully flash firmware to ESP32-C3.

Purpose: Complete the build → flash flow verification by confirming flash commands are syntactically correct and reference the binary artifact produced by the corrected build command, ensuring documentation accuracy for the entire workflow.

Output: Verified flash documentation with accurate binary path references, documented E2E workflow, readiness for hardware flashing.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/93-fix-build-flash-flow/93-RESEARCH.md

# Prior context
@.planning/phases/93-fix-build-flash-flow/93-01-SUMMARY.md
@.planning/phases/93-fix-build-flash-flow/93-02-SUMMARY.md

# Key files
@README.md
@internalDoc/DEVELOPMENT.md
@internalDoc/FLASH_GUIDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify flash command syntax and binary path references</name>
  <files>README.md, internalDoc/DEVELOPMENT.md</files>
  <action>
    Verify that flash command syntax is correct and binary path references match the actual build output.

    Check README.md flash instructions:
    1. Line 88: References `libreroaster.bin` - verify this matches build output
    2. Lines 369-372: Verify `cargo espflash flash` commands are syntactically correct
    3. Confirm binary output path on line 269 matches actual build output: `target/riscv32imc-unknown-none-elf/release/libreroaster.bin`

    Check DEVELOPMENT.md flash instructions:
    1. Verify `cargo espflash flash` commands are syntactically correct
    2. Confirm binary path references match the corrected build output
    3. Ensure all flash commands reference the correct `.bin` file

    Verify command syntax by checking espflash usage:
    ```bash
    cargo espflash flash --help
    ```

    Expected correct flash commands (from documentation):
    ```bash
    # Flash firmware
    cargo espflash flash --release

    # Flash and monitor
    cargo espflash flash --release --monitor
    ```

    Note: These commands will work with the binary produced by the corrected build command (93-02) because `cargo espflash flash --release` automatically finds the built binary.

    This verification ensures:
    - Documentation doesn't reference incorrect binary paths
    - Flash commands are syntactically valid
    - E2E workflow is coherent: build produces .bin → flash command uses .bin
  </action>
  <verify>grep -n "libreroaster.bin" README.md && grep -n "cargo espflash flash" README.md</verify>
  <done>Flash command syntax verified correct; binary path references match actual build output</done>
</task>

<task type="auto">
  <name>Task 2: Document E2E build → flash flow</name>
  <files>README.md</files>
  <action>
    Update the README to clearly document the complete E2E build → flash workflow, ensuring users understand the corrected process.

    Add or update a section in README.md that documents:
    1. Build command with `--features embedded` flag produces flashable .bin
    2. Binary location: `target/riscv32imc-unknown-none-elf/release/libreroaster.bin`
    3. Flash command: `cargo espflash flash --release`
    4. Verification: `ls -lh target/.../release/libreroaster.bin` to confirm binary exists before flashing

    Example to add (if not already present):
    ```markdown
    ### Build and Flash Workflow

    1. Build firmware with embedded features:
       ```bash
       cargo build --release --target riscv32imc-unknown-none-elf --features embedded
       ```

    2. Verify binary was produced:
       ```bash
       ls -lh target/riscv32imc-unknown-none-elf/release/libreroaster.bin
       ```

    3. Flash to ESP32-C3:
       ```bash
       cargo espflash flash --release
       ```

    For detailed flashing instructions and troubleshooting, see [DEVELOPMENT.md](internalDoc/DEVELOPMENT.md).
    ```

    Place this in an appropriate location in the README, such as:
    - After the "Building for ESP32-C3" section
    - Or in the "Flash Instructions" section (if it exists)

    This documentation ensures users have a clear, complete workflow reference for building and flashing firmware.
  </action>
  <verify>grep -n "Build and Flash Workflow" README.md || grep -A 20 "Build for ESP32-C3" README.md | grep -q "cargo espflash flash"</verify>
  <done>README.md documents complete E2E build → flash workflow with corrected commands and verification step</done>
</task>

</tasks>

<verification>
After task completion, verify:
1. Flash command syntax is correct (verified via `cargo espflash flash --help`)
2. Binary path references in documentation match actual build output
3. README.md includes clear E2E build → flash workflow documentation
4. Documentation references DEVELOPMENT.md for detailed instructions
5. Workflow is coherent and complete
</verification>

<success_criteria>
1. Flash command syntax verified correct
2. Binary path references match actual build output
3. README.md documents complete E2E build → flash workflow
4. Documentation includes verification step to check binary exists
5. E2E flow is ready for users: build → verify binary → flash
</success_criteria>

<output>
After completion, create `.planning/phases/93-fix-build-flash-flow/93-03-SUMMARY.md`
</output>
