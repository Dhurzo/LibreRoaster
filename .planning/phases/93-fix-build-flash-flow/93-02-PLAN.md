---
phase: 93-fix-build-flash-flow
plan: 02
type: execute
wave: 2
depends_on: ["93-01"]
files_modified: []
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Build command produces flashable .bin binary"
    - "Binary file exists in target directory"
    - "Binary size is reasonable (typically 400-800KB)"
  artifacts:
    - path: "target/riscv32imc-unknown-none-elf/release/libreroaster.bin"
      provides: "Flashable firmware binary for ESP32-C3"
      min_size: 100KB
  key_links:
    - from: "Build command"
      to: "Cargo.toml [[bin]] section"
      via: "required-features = [\"embedded\"]"
      pattern: "cargo build.*--features embedded"
    - from: "Binary output"
      to: "README.md line 269"
      via: "Expected output path"
      pattern: "target/riscv32imc-unknown-none-elf/release/libreroaster.bin"
---

<objective>
Build the firmware using the corrected command with `--features embedded` flag and verify that a flashable .bin binary is produced, confirming the documentation fix resolves the build issue.

Purpose: Validate that adding `--features embedded` flag to build command actually produces the expected .bin binary artifact, proving the documentation fix enables the build → flash workflow.

Output: Flashable .bin binary in target directory, verification that fix produces expected artifact.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/93-fix-build-flash-flow/93-RESEARCH.md

# Prior context
@.planning/phases/93-fix-build-flash-flow/93-01-SUMMARY.md

# Key files
@Cargo.toml
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build with corrected command and verify .bin binary</name>
  <files>target/riscv32imc-unknown-none-elf/release/libreroaster.bin</files>
  <action>
    Build the firmware using the corrected build command with `--features embedded` flag.

    Run:
    ```bash
    cargo build --release --target riscv32imc-unknown-none-elf --features embedded
    ```

    After build completes, verify that:
    1. Binary file exists: `target/riscv32imc-unknown-none-elf/release/libreroaster.bin`
    2. Binary size is reasonable (typically 400-800KB for ESP32-C3)
    3. Both `.bin` and `.rlib` artifacts are present (.bin is the flashable firmware)

    Expected output directory contents:
    - `liblibreroaster.rlib` (Rust library, ~3.3M)
    - `libreroaster.bin` (Flashable binary, ~400-800KB)

    This verifies that:
    - The `--features embedded` flag enables the binary build
    - Cargo's `required-features = ["embedded"]` configuration is satisfied
    - Build produces the flashable artifact users need

    Note: This build may take 1-3 minutes depending on system resources.
  </action>
  <verify>ls -lh target/riscv32imc-unknown-none-elf/release/libreroaster.bin && echo "Binary exists" || echo "Binary missing"</verify>
  <done>Build produces libreroaster.bin binary in target directory; binary size is reasonable (400-800KB)</done>
</task>

</tasks>

<verification>
After task completion, verify:
1. `libreroaster.bin` file exists in target directory
2. Binary size is reasonable (400-800KB)
3. Build completed without errors
4. Both .bin and .rlib artifacts are present
</verification>

<success_criteria>
1. Flashable .bin binary produced by build command
2. Binary exists at target/riscv32imc-unknown-none-elf/release/libreroaster.bin
3. Binary size is reasonable (400-800KB)
4. Confirms that adding --features embedded flag resolves the build issue
</success_criteria>

<output>
After completion, create `.planning/phases/93-fix-build-flash-flow/93-02-SUMMARY.md`
</output>
