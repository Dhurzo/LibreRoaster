---
phase: 93-fix-build-flash-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "README.md build command includes --features embedded flag"
    - "DEVELOPMENT.md build command already has --features embedded flag"
    - "Build command syntax matches DEVELOPMENT.md pattern"
  artifacts:
    - path: "README.md"
      provides: "Main user-facing build documentation"
      contains: "cargo build --release --target riscv32imc-unknown-none-elf --features embedded"
    - path: "internalDoc/DEVELOPMENT.md"
      provides: "Development build instructions with correct flag"
      contains: "--features embedded"
  key_links:
    - from: "README.md line 266"
      to: "DEVELOPMENT.md line 44"
      via: "Same build command pattern with --features embedded flag"
      pattern: "cargo build.*--features embedded"
---

<objective>
Update README.md embedded build command to include `--features embedded` flag, matching the correct pattern in DEVELOPMENT.md, so builds produce flashable .bin binary instead of only .rlib library.

Purpose: Fix critical documentation gap where README.md build command omits required feature flag, causing users to get non-flashable artifacts and breaking the build → flash workflow.

Output: README.md with corrected build command, DEVELOPMENT.md verified as accurate baseline.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/93-fix-build-flash-flow/93-RESEARCH.md

# Prior context from documentation phases
@.planning/phases/90-internal-documentation-update/90-02-SUMMARY.md
@.planning/phases/91-documentation-verification/91-03-SUMMARY.md

# Key files
@README.md
@internalDoc/DEVELOPMENT.md
@Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md embedded build command</name>
  <files>README.md</files>
  <action>
    Add `--features embedded` flag to the embedded build command on line 266.

    Current command (line 266):
    ```bash
    cargo build --release --target riscv32imc-unknown-none-elf
    ```

    Update to:
    ```bash
    cargo build --release --target riscv32imc-unknown-none-elf --features embedded
    ```

    Also check and update any other embedded build command references in the README (e.g., in the Development Features table on line 338) to ensure consistency.

    Why this fix is needed:
    - Cargo.toml defines binary with `required-features = ["embedded"]`
    - Without the flag, Cargo skips building the binary target
    - Build produces only `.rlib` library, not `.bin` binary
    - Users cannot flash firmware to ESP32-C3

    Pattern to follow (from DEVELOPMENT.md line 44):
    ```bash
    cargo build --release --target riscv32imc-unknown-none-elf --features embedded
    ```
  </action>
  <verify>grep -n "cargo build --release --target riscv32imc-unknown-none-elf --features embedded" README.md</verify>
  <done>README.md line 266 build command includes --features embedded flag matching DEVELOPMENT.md pattern</done>
</task>

<task type="auto">
  <name>Task 2: Verify DEVELOPMENT.md baseline accuracy</name>
  <files>internalDoc/DEVELOPMENT.md</files>
  <action>
    Verify that DEVELOPMENT.md already has the correct `--features embedded` flag on line 44.

    Check line 44 of DEVELOPMENT.md:
    ```bash
    cargo build --release --target riscv32imc-unknown-none-elf --features embedded
    ```

    Confirm that the flag is present and the command is correct.
    Document that DEVELOPMENT.md is the accurate baseline and requires no changes.

    This verification is important because:
    - DEVELOPMENT.md was previously identified as having the correct flag (Phase 91-03)
    - We need to confirm it's still accurate before updating README.md to match it
    - Ensures we're aligning to a valid reference implementation
  </action>
  <verify>grep -n "cargo build --release --target riscv32imc-unknown-none-elf --features embedded" internalDoc/DEVELOPMENT.md</verify>
  <done>DEVELOPMENT.md line 44 confirmed to have --features embedded flag; baseline is accurate</done>
</task>

</tasks>

<verification>
After task completion, verify:
1. README.md line 266 includes `--features embedded` flag
2. Any other embedded build command references in README.md are updated
3. DEVELOPMENT.md line 44 has the flag (baseline confirmed)
4. Both files show consistent build command pattern
</verification>

<success_criteria>
1. README.md embedded build command includes `--features embedded` flag
2. Build command syntax matches DEVELOPMENT.md pattern
3. DEVELOPMENT.md verified as accurate baseline (no changes needed)
4. Documentation consistency established between README.md and DEVELOPMENT.md
</success_criteria>

<output>
After completion, create `.planning/phases/93-fix-build-flash-flow/93-01-SUMMARY.md`
</output>
