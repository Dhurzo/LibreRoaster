---
phase: 54-clean-up-tech-debt
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/application/tasks.rs
  - src/control/traits.rs
autonomous: true
user_setup: []

must_haves:
  truths:
    - "No unused variable warnings in application/tasks.rs"
    - "No async_fn_in_trait warnings in traits.rs"
  artifacts:
    - path: "src/application/tasks.rs"
      provides: "Application tasks without unused variable warning"
      contains: "update_result prefixed with underscore"
    - path: "src/control/traits.rs"
      provides: "Control traits with suppressed async_fn_in_trait warning"
      contains: "#[allow(async_fn_in_trait)] on AsyncThermometer trait"
  key_links: []
---

<objective>
Fix remaining compilation warnings: unused variable in application/tasks.rs and async_fn_in_trait in traits.rs.

Purpose: Achieve clean compilation with minimal warnings. Leave static_mut_refs warnings as-is per phase context (existing patterns from prior phases).

Output: Fixed files with warnings addressed.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 54: Clean Up Tech Debt)
@.planning/phases/54-clean-up-tech-debt/54-CONTEXT.md
@.planning/phases/54-clean-up-tech-debt/54-RESEARCH.md

# Source files referenced
src/application/tasks.rs (unused variable: update_result)
src/control/traits.rs (async_fn_in_trait warning on AsyncThermometer trait)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix unused variable warning in application/tasks.rs</name>
  <files>src/application/tasks.rs</files>
  <action>
Fix the unused variable warning at line 67 in src/application/tasks.rs.

Current code:
```rust
let update_result = ServiceContainer::with_roaster(...)
```

Change to:
```rust
let _update_result = ServiceContainer::with_roaster(...)
```

Prefix the variable with underscore to indicate it's intentionally unused.
  </action>
  <verify>
`cargo build 2>&1 | grep -q "unused variable.*update_result"` returns no match (warning fixed)
  </verify>
  <done>
application/tasks.rs builds without unused variable warning for update_result.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add async_fn_in_trait suppression to AsyncThermometer</name>
  <files>src/control/traits.rs</files>
  <action>
Add #[allow(async_fn_in_trait)] attribute to the AsyncThermometer trait in src/control/traits.rs.

Current code (around line 11-13):
```rust
pub trait AsyncThermometer: Send {
    async fn read_temperature_async(&mut self) -> Result<f32, RoasterError>;
}
```

Change to:
```rust
#[allow(async_fn_in_trait)]
pub trait AsyncThermometer: Send {
    async fn read_temperature_async(&mut self) -> Result<f32, RoasterError>;
}
```

This trait is internal-only and doesn't need the auto trait bounds. The warning is about public traits but this is acceptable for internal use.
  </action>
  <verify>
`cargo build 2>&1 | grep -q "async_fn_in_trait.*AsyncThermometer"` returns no match (warning suppressed)
  </verify>
  <done>
traits.rs builds without async_fn_in_trait warning for AsyncThermometer.
  </done>
</task>

</tasks>

<verification>
After both tasks: `cargo build 2>&1 | grep -c "warning"` shows reduced count (target: 9 static_mut_refs warnings only)

Note: Leave static_mut_refs warnings as-is. These are existing patterns from prior phases and per the phase context should not be fixed.
</verification>

<success_criteria>
1. application/tasks.rs builds without unused variable warning
2. traits.rs builds without async_fn_in_trait warning
3. Remaining warnings are only static_mut_refs (9 warnings - intentionally left as-is)
</success_criteria>

<output>
After completion, create `.planning/phases/54-clean-up-tech-debt/54-02-SUMMARY.md`
</output>
