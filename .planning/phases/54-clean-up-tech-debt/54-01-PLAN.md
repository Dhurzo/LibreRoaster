---
phase: 54-clean-up-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hardware/ledc_bus.rs
  - src/hardware/uart/tasks.rs
autonomous: true
user_setup: []

must_haves:
  truths:
    - "No dead code (unused fields) in ledc_bus.rs"
    - "No dead code (unused functions) in uart/tasks.rs"
  artifacts:
    - path: "src/hardware/ledc_bus.rs"
      provides: "LEDC bus driver without unused fields"
      contains: "LedcBus struct without fan_timer/ssr_timer"
    - path: "src/hardware/uart/tasks.rs"
      provides: "UART tasks without unused functions"
      contains: "No handle_complete_command or send_parse_error functions"
  key_links: []
---

<objective>
Remove dead code from the codebase: unused fields in ledc_bus.rs and unused functions in uart/tasks.rs.

Purpose: Clean up the codebase by removing code that is not used anywhere, reducing maintenance burden and improving code clarity.

Output: Cleaned files with dead code removed.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 54: Clean Up Tech Debt)
@.planning/phases/54-clean-up-tech-debt/54-CONTEXT.md
@.planning/phases/54-clean-up-tech-debt/54-RESEARCH.md

# Source files referenced
src/hardware/ledc_bus.rs (dead fields: fan_timer, ssr_timer)
src/hardware/uart/tasks.rs (dead functions: handle_complete_command, send_parse_error)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove unused fields from LedcBus</name>
  <files>src/hardware/ledc_bus.rs</files>
  <action>
Remove the `fan_timer` and `ssr_timer` fields from the `LedcBus` struct. These fields are marked as dead code by the compiler. The timer configuration is now handled internally by the Channel implementation.

Current struct (lines 67-74):
```rust
pub struct LedcBus<'a> {
    guard: LedcGuard,
    fan: ChannelEntry<'a>,
    ssr: ChannelEntry<'a>,
    /// Timer number used by Fan channel (Timer1 = 25kHz)
    fan_timer: u8,
    /// Timer number used by SSR channel (Timer0 = 1Hz)
    ssr_timer: u8,
}
```

Remove fan_timer and ssr_timer fields entirely. Keep the struct with guard, fan, and ssr fields only.
  </action>
  <verify>
`cargo build 2>&1 | grep -q "fan_timer\|ssr_timer"` returns no matches (fields removed)
  </verify>
  <done>
LedcBus struct no longer has fan_timer or ssr_timer fields. Build succeeds with reduced dead_code warnings.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove unused functions from uart/tasks.rs</name>
  <files>src/hardware/uart/tasks.rs</files>
  <action>
Remove two unused functions from src/hardware/uart/tasks.rs:

1. `handle_complete_command` (around line 261) - defined but never called
2. `send_parse_error` (around line 294) - defined but never called

These functions are dead code. Delete them entirely.
  </action>
  <verify>
`cargo build 2>&1 | grep -q "handle_complete_command\|send_parse_error"` returns no matches (functions removed)
  </verify>
  <done>
uart/tasks.rs no longer contains handle_complete_command or send_parse_error functions. Build succeeds.
  </done>
</task>

</tasks>

<verification>
After both tasks: `cargo build 2>&1 | grep -c "dead_code"` shows reduced warnings (ideally 0 for these files)
</verification>

<success_criteria>
1. ledc_bus.rs builds without fan_timer/ssr_timer dead_code warnings
2. uart/tasks.rs builds without handle_complete_command/send_parse_error dead_code warnings
</success_criteria>

<output>
After completion, create `.planning/phases/54-clean-up-tech-debt/54-01-SUMMARY.md`
</output>
