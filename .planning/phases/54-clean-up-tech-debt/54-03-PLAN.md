---
phase: 54-clean-up-tech-debt
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/ssr_monitor.rs
  - tests/fan_serialization.rs
  - tests/command_idempotence.rs
  - tests/command_errors.rs
  - tests/transport_flood_test.rs
  - tests/artisan_integration_test.rs
  - tests/mock_usb_driver.rs
  - tests/mock_uart.rs
  - tests/ssr_scheduler.rs
  - tests/mock_uart_integration.rs
  - tests/usb_cdc_tests.rs
  - tests/multiplexer_tests.rs
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Integration tests compile with --features std on host target"
    - "Integration tests run successfully on x86_64 target"
    - "No warnings in integration test code"
  artifacts:
    - path: "tests/*.rs"
      provides: "Integration tests that compile on host target"
      contains: "All integration tests compile cleanly"
  key_links: []
---

<objective>
Fix integration tests to compile and run with --features std on host target (x86_64-unknown-linux-gnu).

Purpose: Enable running integration tests on host for development and CI. The embedded target (riscv32imc-unknown-none-elf) doesn't support std, so tests must run on host.

Output: Working integration tests that compile and run on host target.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 54: Clean Up Tech Debt)
@.planning/phases/54-clean-up-tech-debt/54-CONTEXT.md
@.planning/phases/54-clean-up-tech-debt/54-RESEARCH.md
@.planning/STATE.md (blocker note: cargo test --test ssr_monitor cannot run on default target)

# Issue: Integration tests need host target
The default target riscv32imc-unknown-none-elf doesn't support std. Must run on x86_64-unknown-linux-gnu.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify integration tests compile on host target</name>
  <files>tests/*.rs</files>
  <action>
Run cargo build for tests on host target with std feature:

```bash
cargo build --tests --features std --target x86_64-unknown-linux-gnu 2>&1
```

If compilation fails, fix the issues. Common problems:
1. Missing imports (e.g., `use core::matches;` instead of `use std::matches;`)
2. ESP-specific dependencies not available on host
3. Missing mocks for hardware dependencies

Read the error messages and fix each issue. Use conditional compilation #[cfg(target_arch = "riscv32")] for ESP-specific code.
  </action>
  <verify>
`cargo build --tests --features std --target x86_64-unknown-linux-gnu 2>&1` completes without errors
  </verify>
  <done>
Integration tests compile successfully on host target with --features std.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run integration tests on host target</name>
  <files>tests/*.rs</files>
  <action>
Run the integration tests on host target:

```bash
cargo test --features std --target x86_64-unknown-linux-gnu 2>&1
```

Fix any test failures. Tests should work on host target with mock implementations.
  </action>
  <verify>
`cargo test --features std --target x86_64-unknown-linux-gnu 2>&1` shows tests passing
  </verify>
  <done>
Integration tests run successfully on host target.
  </done>
</task>

<task type="auto">
  <name>Task 3: Ensure clean compilation (no warnings)</name>
  <files>tests/*.rs</files>
  <action>
Verify the integration tests compile without warnings:

```bash
cargo build --tests --features std --target x86_64-unknown-linux-gnu 2>&1 | grep -c "warning"
```

Integration tests must meet same warning standards as main code (clean compilation).
  </verify>
  <done>
Integration tests compile with zero warnings on host target.
  </done>
</task>

</tasks>

<verification>
After all tasks:
1. `cargo build --tests --features std --target x86_64-unknown-linux-gnu` completes without errors
2. `cargo test --features std --target x86_64-unknown-linux-gnu` shows passing tests
3. No warnings in test compilation output
</verification>

<success_criteria>
1. Integration tests compile with --features std on x86_64-unknown-linux-gnu
2. Integration tests run successfully (or are known-failing with clear reason)
3. Zero warnings in test compilation
</success_criteria>

<output>
After completion, create `.planning/phases/54-clean-up-tech-debt/54-03-SUMMARY.md`
</output>
