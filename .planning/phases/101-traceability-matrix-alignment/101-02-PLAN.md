---
phase: 101-traceability-matrix-alignment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internalDoc/INSTRUMENTATION_README.MD
  - scripts/traceability_matrix.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - INSTRUMENTATION_README.MD TRACE stream documentation reflects actual runtime event names
    - Sample TRACE entries in documentation match real firmware output format
    - Parser docstring correctly describes event names it can handle
    - Regression triage workflow documented with corrected parser usage
  artifacts:
    - path: "internalDoc/INSTRUMENTATION_README.MD"
      provides: "TRACE stream documentation with correct event names and field formats"
      min_lines: 411
      contains: "queue_enqueue|actuation|telemetry|guard|queue_fallback"
    - path: "scripts/traceability_matrix.py"
      provides: "Updated docstring reflecting correct event names"
      min_lines: 156
  key_links:
    - from: "internalDoc/INSTRUMENTATION_README.MD"
      to: "src/logging/traceability.rs"
      via: "event name references"
      pattern: "queue_enqueue|actuation|telemetry|guard"
    - from: "internalDoc/INSTRUMENTATION_README.MD"
      to: "scripts/traceability_matrix.py"
      via: "parser usage instructions"
      pattern: "traceability_matrix\\.py"
---

<objective>
Update documentation to reflect the corrected TRACE event names and parser behavior.

Purpose: Documentation currently references planned event names from Phase 97 research, not the actual runtime implementation. After fixing the parser in 101-01, documentation must be updated to match the corrected parser behavior and actual TRACE stream format.

Output: Updated INSTRUMENTATION_README.MD and parser docstring with correct event names, field formats, and usage instructions.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@internalDoc/INSTRUMENTATION_README.MD
@scripts/traceability_matrix.py
@src/logging/traceability.rs
@.planning/phases/97-traceability-matrix-tooling/97-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Update INSTRUMENTATION_README.MD TRACE stream documentation</name>
  <files>internalDoc/INSTRUMENTATION_README.MD</files>
  <action>
    In internalDoc/INSTRUMENTATION_README.MD, find the TRACE stream documentation section and update:

    1. Event names:
       - Change "command_enqueue" → "queue_enqueue"
       - Change "queue_dequeue" → "queue_dequeue" (if correct, leave as-is)
       - Add "queue_fallback" description
       - Change "actuator_output" → "actuation"
       - Change "telemetry_emit" → "telemetry"
       - Change "guard_report" → "guard"

    2. Field name documentation:
       - Document that command is emitted as "cmd={:?}" (Debug format, e.g., "ArtisanCommand::STATUS")
       - Document all queue fields: cmd, channel, depth, fallback
       - Document actuation fields: cmd, channel, ssr, fan, latency_us, saturation_active
       - Document telemetry fields: guard_timeout, guard_timeouts, watchdog, error_category, error_source
       - Document guard fields: guard_timeout, guard_timeouts, watchdog, watchdog_failure, error_category, error_source

    3. Update example TRACE entries to match actual runtime format:
       ```
       TRACE,1,queue_enqueue,cmd=ReadStatus,channel=Uart,depth=5,fallback=0
       TRACE,1,actuation,cmd=ReadStatus,channel=Uart,ssr=33.5,fan=12,latency_us=150,saturation_active=1
       TRACE,1,telemetry,guard_timeout=0,guard_timeouts=0,watchdog=ok
       TRACE,1,guard,guard_timeout=0,guard_timeouts=0,watchdog=ok
       ```

    4. Update regression triage workflow section:
       - Reference corrected parser usage: `python scripts/traceability_matrix.py <trace.log>`
       - Note that parser now handles Debug-formatted cmd field
       - Document queue_fallback handling in matrix output
       - Reference updated sample-trace.log for examples

    5. Cross-reference updates:
       - Verify all references to TRACE events in other sections are updated
       - Ensure src/logging/traceability.rs examples match documentation
  </action>
  <verify>Search INSTRUMENTATION_README.MD for old event names - none found. grep for "command_enqueue|actuator_output|telemetry_emit|guard_report" returns zero matches</verify>
  <done>All TRACE event names match runtime output, field formats documented correctly, examples reflect actual firmware output</done>
</task>

<task type="auto">
  <name>Update traceability_matrix.py docstring</name>
  <files>scripts/traceability_matrix.py</files>
  <action>
    In scripts/traceability_matrix.py, update the module docstring (lines 1-5) to reflect corrected event names:

    Current docstring (if still incorrect):
    ```python
    """Parse TRACE logs into a command → queue → actuator → telemetry → guard matrix.

    Columns: TraceId, Command, QueueDepth, Actuator (SSR/fan outputs), Telemetry (ET/BT/PV/MV), Guard (watchdog/guard flags).
    """
    ```

    Update to:
    ```python
    """Parse TRACE logs into a command → queue → actuator → telemetry → guard matrix.

    Parses runtime TRACE events: queue_enqueue, queue_dequeue, queue_fallback, actuation, telemetry, guard.
    Command field uses Debug format (e.g., ArtisanCommand::STATUS).

    Columns: TraceId, Command, QueueDepth, Actuator (SSR/fan outputs), Telemetry (ET/BT/PV/MV), Guard (watchdog/guard flags).
    """
    ```

    Also update argparse epilog (line 134) if needed to reflect correct column descriptions.
  </action>
  <verify>Read lines 1-5 of traceability_matrix.py - docstring mentions correct event names (queue_enqueue, queue_fallback, actuation, telemetry, guard)</verify>
  <done>Docstring accurately describes parser capabilities and correct event names</done>
</task>

</tasks>

<verification>
- INSTRUMENTATION_README.MD TRACE section uses correct event names (queue_enqueue, queue_fallback, actuation, telemetry, guard)
- Example TRACE entries in documentation match actual runtime format with Debug-formatted cmd field
- Parser docstring accurately describes supported events
- All cross-references point to correct parser usage
</verification>

<success_criteria>
Documentation aligns with corrected parser and actual TRACE stream format, enabling SOLID-03 to consume live logs.
</success_criteria>

<output>
After completion, create `.planning/phases/101-traceability-matrix-alignment/101-02-SUMMARY.md`
</output>
