---
phase: 101-traceability-matrix-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/traceability_matrix.py
  - scripts/test_traceability_matrix.py
  - logs/traceability/sample-trace.log
autonomous: true
user_setup: []

must_haves:
  truths:
    - Parser correctly parses queue_enqueue, queue_dequeue, queue_fallback, actuation, telemetry, guard events
    - Parser handles Debug-formatted cmd field and extracts base command name (e.g., "ArtisanCommand::STATUS" → "STATUS")
    - Parser formats queue_depth as space-separated string with channel (e.g., "depth=5 channel=Uart")
    - Regression tests verify happy path, fallback path, partial traces, and mixed log lines
    - Sample log contains correctly formatted TRACE entries matching runtime output
  artifacts:
    - path: "scripts/traceability_matrix.py"
      provides: "Updated parser with correct step names and field mappings"
      min_lines: 156
    - path: "scripts/test_traceability_matrix.py"
      provides: "Regression tests for parser"
      exports: ["TestTraceabilityMatrix", "test_parse_queue_enqueue", "test_parse_actuation", "test_parse_queue_fallback", "test_complete_trace_flow", "test_mixed_log_lines"]
    - path: "logs/traceability/sample-trace.log"
      provides: "Correctly formatted sample TRACE log"
      contains: "TRACE,\\d+,queue_enqueue,"
  key_links:
    - from: "scripts/test_traceability_matrix.py"
      to: "scripts/traceability_matrix.py"
      via: "import of parse_trace_line and _update_summary functions"
      pattern: "from traceability_matrix import (parse_trace_line, _update_summary, TraceSummary)"
    - from: "scripts/traceability_matrix.py"
      to: "src/logging/traceability.rs"
      via: "step name matching"
      pattern: "queue_enqueue|actuation|telemetry|guard"
---

<objective>
Fix the traceability_matrix.py parser to match actual runtime TRACE event names and add comprehensive regression tests.

Purpose: The parser was written based on planned event names from Phase 97 research, but the actual firmware implementation uses different step names. This misalignment prevents SOLID-03 from consuming live logs and breaks TRACE flow restorability.

Output: Updated parser that correctly parses all runtime TRACE events, plus regression tests that validate parsing behavior.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@scripts/traceability_matrix.py
@src/logging/traceability.rs
</context>

<tasks>

<task type="auto">
  <name>Update parser step names and field mappings in traceability_matrix.py</name>
  <files>scripts/traceability_matrix.py</files>
  <action>
    In scripts/traceability_matrix.py, update the _update_summary() function (lines 96-111) to match runtime event names:

    1. Change step name comparisons:
       - "command_enqueue" → "queue_enqueue"
       - "actuator_output" → "actuation"
       - "telemetry_emit" → "telemetry"
       - "guard_report" → "guard"

    2. Add handler for "queue_fallback" (insert between dequeue and actuation):
       - Update queue_depth with fallback=True flag

    3. Update field name mapping and extract base command name:
        - Change "command" → "cmd" to match Debug-formatted output
        - Extract base command name from Debug format by stripping "ArtisanCommand::" prefix if present
          Example: `cmd_name = cmd_value.split("::")[-1] if "::" in cmd_value else cmd_value`
          Result: "ArtisanCommand::STATUS" → "STATUS"

    4. Update docstring (line 2-5) to reflect correct event names

    Example fix for _update_summary():
    ```python
    def _update_summary(summary: TraceSummary, step: str, data: Mapping[str, str]) -> None:
        if step == "queue_enqueue":
            cmd_value = data.get("cmd", summary.command)
            summary.command = cmd_value.split("::")[-1] if "::" in cmd_value else cmd_value
            queue_info = _format_queue_depth(data)
            if queue_info:
                summary.queue_depth = queue_info
        elif step == "queue_fallback":
            cmd_value = data.get("cmd", summary.command)
            summary.command = cmd_value.split("::")[-1] if "::" in cmd_value else cmd_value
            queue_info = _format_queue_depth(data)
            if queue_info:
                summary.queue_depth = f"{queue_info} fallback=true"
        elif step == "queue_dequeue":
            queue_info = _format_queue_depth(data)
            if queue_info:
                summary.queue_depth = queue_info
        elif step == "actuation":
            summary.actuator.update(data)
        elif step == "telemetry":
            summary.telemetry.update(data)
        elif step == "guard":
            summary.guard.update(data)
    ```

    Update _format_queue_depth() to handle depth and fallback fields correctly.
  </action>
  <verify>Run `python scripts/traceability_matrix.py logs/traceability/sample-trace.log` after updating sample log (task 3) - should output table without errors</verify>
  <done>All step names match runtime output (queue_enqueue, queue_fallback, actuation, telemetry, guard), cmd field is parsed correctly</done>
</task>

<task type="auto">
  <name>Create regression tests for traceability_matrix.py</name>
  <files>scripts/test_traceability_matrix.py</files>
  <action>
    Create scripts/test_traceability_matrix.py with comprehensive test cases using unittest:

    1. test_parse_queue_enqueue: Verify parsing of queue_enqueue event with cmd, depth, channel fields
    2. test_parse_queue_fallback: Verify parsing of queue_fallback event
    3. test_parse_actuation: Verify parsing of actuation event with ssr, fan, latency_us fields
    4. test_parse_telemetry: Verify parsing of telemetry event with guard_timeout, guard_timeouts, watchdog fields
    5. test_parse_guard: Verify parsing of guard event with AppError metadata (error_category, error_source)
    6. test_complete_trace_flow: Verify full trace from queue_enqueue through all steps produces correct matrix row
    7. test_mixed_log_lines: Verify parser handles interleaved TRACE, STATUS, DEBUG lines correctly
    8. test_debug_format_cmd: Verify cmd field with Debug format (e.g., "cmd=ArtisanCommand::STATUS") is parsed

    Use sample log data matching actual runtime format:
    - TRACE,1,queue_enqueue,cmd=ReadStatus,channel=Uart,depth=5,fallback=0
    - TRACE,1,queue_dequeue,cmd=ReadStatus,channel=Uart,depth=4
    - TRACE,1,actuation,cmd=ReadStatus,channel=Uart,ssr=33.5,fan=12,latency_us=150,saturation_active=1
    - TRACE,1,telemetry,guard_timeout=0,guard_timeouts=0,watchdog=ok
    - TRACE,1,guard,guard_timeout=0,guard_timeouts=0,watchdog=ok

    Expected _format_queue_depth output (space-separated with channel):
    - `depth=5 channel=Uart`
    - `depth=4 channel=Uart`
    - `depth=4 channel=Uart fallback=true` (for queue_fallback events)

    All tests should verify:
    - TraceEntry is parsed correctly
    - TraceSummary aggregates steps properly
    - Matrix row output is correct

    Run with: `python -m unittest scripts.test_traceability_matrix`
  </action>
  <verify>Run `python -m unittest scripts.test_traceability_matrix` - all tests pass</verify>
  <done>All 8 tests pass, covering happy path, fallback, partial traces, mixed logs, and Debug format cmd</done>
</task>

<task type="auto">
  <name>Regenerate sample-trace.log with correct runtime format</name>
  <files>logs/traceability/sample-trace.log</files>
  <action>
    Replace logs/traceability/sample-trace.log with correctly formatted TRACE entries matching runtime output:

    Current synthetic log uses incorrect event names (command_enqueue, actuator_output, etc.)
    Must use actual runtime format from src/logging/traceability.rs:

    Sample trace entries:
    ```
    TRACE,1,queue_enqueue,cmd=ReadStatus,channel=Uart,depth=5,fallback=0
    TRACE,1,queue_dequeue,cmd=ReadStatus,channel=Uart,depth=4
    TRACE,1,actuation,cmd=ReadStatus,channel=Uart,ssr=33.5,fan=12,latency_us=150,saturation_active=1
    TRACE,1,telemetry,guard_timeout=0,guard_timeouts=0,watchdog=ok
    TRACE,1,guard,guard_timeout=0,guard_timeouts=0,watchdog=ok
    TRACE,2,queue_enqueue,cmd=SetPoint,channel=Uart,depth=4,fallback=0
    TRACE,2,queue_fallback,cmd=SetPoint,channel=Uart,depth=4,fallback=1
    STATUS,12.3,18.7,200.0,33.5,12.0,0,0,0,1,0,150,0,0,0,0,0,0
    TRACE,3,queue_enqueue,cmd=ReadStatus,channel=Uart,depth=3,fallback=0
    TRACE,3,telemetry,guard_timeout=1,guard_timeouts=3,watchdog=fail,error_category=control,error_source=pid_error
    ```

    Include:
    - Complete trace flow (all 5 steps)
    - Fallback event (queue_fallback)
    - Partial trace (missing actuation)
    - AppError metadata in telemetry/guard events
    - Mixed non-TRACE lines (STATUS)
  </action>
  <verify>Run `python scripts/traceability_matrix.py logs/traceability/sample-trace.log` - outputs matrix with 3 trace rows, queue_fallback appears as "depth=4 channel=Uart fallback=true"</verify>
  <done>Sample log contains correct runtime format (queue_enqueue, actuation, telemetry, guard, queue_fallback) with all field names matching output from traceability.rs</done>
</task>

</tasks>

<verification>
- Parser correctly parses all runtime TRACE event names (queue_enqueue, queue_dequeue, queue_fallback, actuation, telemetry, guard)
- Regression tests (8 test cases) all pass
- Sample log regenerates with correct format and parser produces meaningful matrix
- No firmware changes required - only host-side parser fixes
</verification>

<success_criteria>
Parser aligns with runtime TRACE events, regression tests validate behavior, sample log demonstrates correct parsing.
</success_criteria>

<output>
After completion, create `.planning/phases/101-traceability-matrix-alignment/101-01-SUMMARY.md`
</output>
