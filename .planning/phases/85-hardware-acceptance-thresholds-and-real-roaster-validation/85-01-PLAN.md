---
phase: 85-hardware-acceptance-thresholds-and-real-roaster-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tests/hardware/thresholds.json, tests/hardware/METHODOLOGY.md, src/config/constants.rs, src/application/tasks.rs, src/output/artisan.rs]
autonomous: true
must_haves:
  truths:
    - "Numeric thresholds exist for latency and safety limits"
    - "SystemStatus includes command_latency_us and max_command_latency_us"
    - "STATUS command includes HIL metrics in its CSV response"
    - "Firmware calculates latency from command arrival to processing completion"
  artifacts:
    - path: "tests/hardware/thresholds.json"
      provides: "Acceptance criteria for HIL validation"
    - path: "src/config/constants.rs"
      provides: "Updated SystemStatus with latency tracking"
  key_links:
    - from: "src/application/tasks.rs"
      to: "SystemStatus.command_latency_us"
      via: "Local timestamping and update"
---

<objective>
Define numeric acceptance thresholds and instrument the firmware to measure and report command-to-actuator latency and other HIL metrics.

Purpose: Provide the "ground truth" data required for unambiguous hardware validation.
Output: Threshold definitions and an instrumented firmware reporting performance metrics via STATUS telemetry.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/85-hardware-acceptance-thresholds-and-real-roaster-validation/85-RESEARCH.md
@src/config/constants.rs
@src/application/tasks.rs
@src/output/artisan.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Thresholds and Methodology</name>
  <files>tests/hardware/thresholds.json, tests/hardware/METHODOLOGY.md</files>
  <action>
    1. Create `tests/hardware/thresholds.json` with the following initial limits:
       - `max_command_latency_ms`: 250
       - `avg_command_latency_ms`: 100
       - `thermal_envelope_deg`: 5.0
       - `max_watchdog_consecutive_fails`: 0
       - `max_ledc_guard_timeouts`: 5
    2. Create `tests/hardware/METHODOLOGY.md` explaining how latency is measured (µs delta between command dequeue and handler completion) and how the thermal envelope is calculated (BT actual vs BT target during stable heating).
  </action>
  <verify>Files exist and contain valid JSON/Markdown.</verify>
  <done>Thresholds are defined and methodology is documented.</done>
</task>

<task type="auto">
  <name>Task 2: Instrument SystemStatus for Latency Tracking</name>
  <files>src/config/constants.rs, src/output/artisan.rs</files>
  <action>
    1. Update `SystemStatus` struct in `src/config/constants.rs` to add:
       - `pub command_latency_us: u32`
       - `pub max_command_latency_us: u32`
    2. Update `SystemStatus::default()` implementation.
    3. Update `ArtisanFormatter::format_status_response` in `src/output/artisan.rs` to append these two fields to the CSV response (making it 18 fields total).
    4. Update tests in `src/output/artisan.rs` to account for the new fields.
  </action>
  <verify>`cargo check` passes and `ArtisanFormatter` tests pass.</verify>
  <done>Firmware state can now store and report command latency metrics.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Latency Measurement Logic in Application Layer</name>
  <files>src/application/tasks.rs</files>
  <action>
    1. In `src/application/tasks.rs`, inside `control_loop_task`, record a local `start_time = Instant::now()` immediately before calling `roaster.process_artisan_command(command)` inside the `with_roaster_async` closure.
    2. Immediately after `process_artisan_command` returns (still inside the closure), calculate latency: `let latency = start_time.elapsed().as_micros() as u32`.
    3. Update `roaster.status_mut().command_latency_us = latency`.
    4. Update `roaster.status_mut().max_command_latency_us` if `latency` is higher.
    5. This implementation avoids any dependency from the `control` module back to the `application` layer by performing the instrumentation in the task that orchestrates command processing.
  </action>
  <verify>`cargo check` passes. Latency should be non-zero when processing commands like `OT1`.</verify>
  <done>Firmware accurately measures and tracks processing latency for Artisan commands without circular dependencies.</done>
</task>

</tasks>

<verification>
1. Run `cargo check --lib` to ensure no syntax errors.
2. Run `cargo test` to verify `ArtisanFormatter` still generates valid CSV (now with 18 fields).
3. (Optional) Run `cargo test --test mock_uart_integration` to see if it still passes.
</verification>

<success_criteria>
- `STATUS` response from firmware contains 18 comma-separated values.
- The 17th and 18th values are non-negative integers representing latency in microseconds.
- `thresholds.json` is available for downstream tooling.
</success_criteria>

<output>
After completion, create `.planning/phases/85-hardware-acceptance-thresholds-and-real-roaster-validation/85-01-SUMMARY.md`
</output>
