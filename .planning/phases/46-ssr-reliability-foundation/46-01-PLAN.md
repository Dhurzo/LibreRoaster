---
phase: 46-ssr-reliability-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/constants.rs
  - src/hardware/ssr.rs
autonomous: true

must_haves:
  truths:
    - "SSR commands convert percentages to LEDC duties with saturating math so 0% writes 0, 100% writes 255, and no double-division artifact occurs."
    - "Hardware exposes the cycle guard duration and duty-tolerance constants so downstream schedulers and monitors can obey the datasheet limits."
    - "Unit tests inside `hardware::ssr` cover the new helper and guard constants so regressions in the duty math get caught immediately."
  artifacts:
    - path: "src/hardware/ssr.rs"
      provides: "Saturating `percentage_to_ledc_duty` helper plus guard-aware hooks for LEDC duty tracking and tests."
    - path: "src/config/constants.rs"
      provides: "`SSR_PWM_RESOLUTION`, `SSR_CYCLE_GUARD_MS`, and `SSR_DUTY_TOLERANCE_TICKS` constants shared across scheduler and monitor code."
  key_links:
    - from: "src/hardware/ssr.rs"
      to: "src/config/constants.rs"
      via: "`percentage_to_ledc_duty` reads resolution/guard/tolerance constants before clamping, so mis-matching values anywhere breaks the law."
      pattern: "SSR_PWM_RESOLUTION|SSR_CYCLE_GUARD_MS|SSR_DUTY_TOLERANCE_TICKS"
---

<objective>
Fix the SSR hardware layer so LEDC duties are computed with saturating math, guard constants live in config, and the helper is covered by unit tests.

Purpose: Give the scheduler and monitor a reliable duty conversion baseline (no more half-step artifacts) and safe guard/tolerance knobs.
Output: Updated constants plus helper and tests inside `src/hardware/ssr.rs`.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/46-ssr-reliability-foundation/46-RESEARCH.md
@src/config/constants.rs
@src/hardware/ssr.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add saturating LEDC duty math and guard constants</name>
  <files>src/config/constants.rs, src/hardware/ssr.rs</files>
  <action>
Rewrite `set_percentage` (and any shared helper) to compute `(clamped_percentage / 100.0) * max_duty` where `max_duty = (1 << SSR_PWM_RESOLUTION) - 1`, then saturate to `u8` before calling `pwm_channel.set_duty`. Remove the old `duty / 100` truncation so 100% always maps to 255 and 0% stays 0.

Add `SSR_CYCLE_GUARD_MS: u32 = 1000` and `SSR_DUTY_TOLERANCE_TICKS: u8 = 2` next to the existing PWM constants so the scheduler and monitor can read consistent guard/tolerance values. Expose a helper `percentage_to_ledc_duty` (pub(crate) or public) so tests can exercise it directly.
  </action>
  <verify>cargo test --lib hardware::ssr</verify>
  <done>All SSR percentage paths (simple and PWM helpers) use the saturating helper, and the guard/tolerance constants live in `constants.rs` for reuse.</done>
</task>

<task type="auto">
  <name>Task 2: Cover the new duty helper with unit tests</name>
  <files>src/hardware/ssr.rs</files>
  <action>
Extend `hardware::ssr` with `#[cfg(test)]` coverage that directly invokes `percentage_to_ledc_duty` (and any public guard helpers) to assert 0% → 0, 100% → max duty, and clamped behavior for -50%, 150%, and intermediate values.

Add a test that reads `SSR_CYCLE_GUARD_MS` and `SSR_DUTY_TOLERANCE_TICKS` so future schedulers break if those constants move unexpectedly.
  </action>
  <verify>cargo test --lib hardware::ssr</verify>
  <done>Tests assert the helper saturates correctly and the guard/tolerance constants stay within documented bounds.</done>
</task>

</tasks>

<verification>
- `cargo test --lib hardware::ssr`
</verification>

<success_criteria>
- `set_percentage` always writes between 0 and 255 with no double division artifacts.
- `SSR_CYCLE_GUARD_MS` and `SSR_DUTY_TOLERANCE_TICKS` are defined in `constants.rs` and referenced by the helper.
- Unit tests fail if any conversion or constant drifts outside the documented ranges.
</success_criteria>

<output>
After completion, create `.planning/phases/46-ssr-reliability-foundation/46-01-SUMMARY.md`
</output>
