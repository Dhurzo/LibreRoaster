---
phase: 71-anti-windup-stabilization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/control/pid.rs
  - src/control/handlers.rs
autonomous: true

must_haves:
  truths:
    - "The PID integrator term stops accumulating while the actuator reports saturation or LEDC guard busy, preventing further MV increases until the guard clears."
    - "Anti-windup feedback keeps the MV bounded by the actual applied output so telemetry never claims the actuator saw more power than it did."
    - "Integrator and derivative internals are exposed through the PID helper so `SystemStatus` can publish real-time values instead of mirroring the desired output."
  artifacts:
    - path: "src/control/pid.rs"
      provides: "The CoffeeRoasterPid struct with integral/derivative state, saturation-aware gating, and accessors used by the control loop."
      min_lines: 200
    - path: "src/control/handlers.rs"
      provides: "TemperatureCommandHandler wiring the PID to Artisan command handling so saturation feedback propagates into the controller."
      min_lines: 150
  key_links:
    - from: "src/control/pid.rs"
      to: "src/control/roaster_refactored.rs"
      via: "`TemperatureCommandHandler::get_pid_output` provides PID output for `update_control`."
      pattern: "get_pid_output"
    - from: "src/control/pid.rs"
      to: "src/config/constants.rs"
      via: "Telemetry exposes the integrator/derivative getters inside `SystemStatus`."
      pattern: "integrator_value"
---

<objective>
Replace the stubbed PID with a stateful controller that tracks proportional, integral, and derivative terms plus anti-windup gating so the integrator stops growing whenever LEDC saturation or the guard rejects cycles.
Purpose: Control the loop’s energy bookkeeping at the source so instrumentation never claims more actuator output than the hardware can deliver.
Output: A CoffeeRoasterPid API that exposes integrator/derivative state, accepts actuator feedback, and keeps MV bounded when the actuator is blocked.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-anti-windup-stabilization/71-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build a saturation-aware CoffeeRoasterPid</name>
  <files>src/control/pid.rs</files>
  <action>
1. Replace the placeholder struct with `kp`, `ki`, `kd`, `integrator`, `last_error`, and `last_update_ms` so the PID can accumulate and release energy with time-aware deltas.
2. Introduce a `PidFeedback`/`ActuatorState` argument (desired vs. applied, guard busy flag) and clamp the integrator update whenever saturation is detected so the controller never keeps integrating while the hardware cannot honor the request.
3. Keep the MV bounded by the actual `applied_output` (or 100% clamp) and expose methods such as `integrator_value()` so `SystemStatus` can publish the true integrator term instead of equating it to the desired SSR duty.
4. Preserve (or extend) the existing `compute_output` signature so the rest of the loop can swap in the richer controller without rewriting the call sites, but document the new feedback hook for later wiring.
  </action>
  <verify>cargo test --lib</verify>
  <done>The new PID stores time-aware integral/derivative state, clamps integrator updates while guard/saturation is active, and provides telemetry accessors without regressing existing call sites.</done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests that prove anti-windup gating</name>
  <files>src/control/pid.rs</files>
  <action>
1. Inside `src/control/pid.rs` add `#[cfg(test)]` scenarios that drive the controller across multiple ticks so the integrator grows while motor commands are accepted.
2. Simulate saturation feedback (applied output < desired or guard busy) and assert the integrator term no longer increases and the output stays pinned to the guard-limited value.
3. Add a regression that re-enables integration once saturation clears and verify the integrator resumes accumulating in subsequent ticks.
4. Keep the tests deterministic so they run under `cargo test --lib` without hardware, mocking times via manual `dt` inputs.
  </action>
  <verify>cargo test --lib</verify>
  <done>Tests fail when the integrator still grows during saturation and pass once the new gating logic exists, proving the PID honors actuator feedback.</done>
</task>

</tasks>

<verification>
- `cargo test --lib`
</verification>

<success_criteria>
1. CoffeeRoasterPid now tracks each term explicitly and skips integral buildup whenever actuator feedback marks saturation/guard busy.
2. The MV never exceeds the last successfully applied output, even if the previous command would have requested more energy.
3. Unit tests capture the anti-windup behavior and keep failing until the gating code exists.
</success_criteria>

<output>
After completion, create `.planning/phases/71-anti-windup-stabilization/71-01-SUMMARY.md`
</output>
