---
phase: 71-anti-windup-stabilization
plan: 03
type: execute
wave: 2
depends_on:
  - 71-01
  - 71-02
files_modified:
  - src/application/tasks.rs
  - src/output/artisan.rs
autonomous: true

must_haves:
  truths:
    - "Every 100 ms tick logs whether saturation/guard busy blocked the SSR and correlates that with `SystemStatus::integrator_clamped`/`saturation_active` so telemetry consumers can link stage duration to anti-windup events."
    - "`ArtisanFormatter::format_status_response` keeps the agreed-upon CSV order while populating PV, MV, integrator, derivative, and saturation flags with the filtered values produced by plan 1/2."
    - "Telemetry tests fail if the new `SystemStatus` fields leak incorrect values, ensuring the formatter still emits 16 columns even after the new instrumentation is wired up."
  artifacts:
    - path: "src/application/tasks.rs"
      provides: "`control_loop_task` instrumentation logs that now mention saturation/integrator/derivative wiring each tick so stage durations can be traced in automation logs."
      min_lines: 360
    - path: "src/output/artisan.rs"
      provides: "Status formatter tests that assert the anti-windup indicator flags and filtered derivative value appear in the CSV tail."
      min_lines: 320
  key_links:
    - from: "src/application/tasks.rs"
      to: "src/output/artisan.rs"
      via: "The `control_loop_task` publishes the status that `ArtisanFormatter` formats for telemetry."
      pattern: "format_status_response"
    - from: "src/output/artisan.rs"
      to: "src/config/constants.rs"
      via: "Telemetry consumes `SystemStatus` fields introduced for anti-windup/derivatives."
      pattern: "SystemStatus"
---

<objective>
Tie the new PID/derivative work into telemetry by logging stage-specific saturation events and verifying the STATUS formatter still emits the filtered PV/MV/integrator/derivative/saturation tail in its deterministic column order.
Purpose: So automation and verification scripts can see exactly when the integrator was clamped, the derivative became available, and the actuator was limited.
Output: `control_loop_task` logs that mention the anti-windup state each tick and `ArtisanFormatter` tests proving the STATUS CSV reflects the filtered data from plans 1 & 2.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-anti-windup-stabilization/71-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Surface anti-windup state in the heartbeat logs</name>
  <files>src/application/tasks.rs</files>
  <action>
1. After `control_snapshot` completes, read `status` to capture `saturation_active`, `integrator_clamped`, and `derivative_available` plus the latest `derivative_rate` so you can log the combined state in the `ControlUpdate` stage.
2. Include those flags in the per-stage debug lines (e.g., `stage=ControlUpdate` and `stage=TelemetryEmit`) so automation logs can correlate guard timeouts with the anti-windup state.
3. When the guard rejects cycles or the watchdog reports a failure, append the current saturation/integrator flags to the `guard_timeout` and `watchdog_failure` debug lines so the instrumentation shows both the failure and the anti-windup response.
  </action>
  <verify>cargo test --lib</verify>
  <done>Each tick’s logs mention `saturation_active`/`integrator_clamped`/`derivative_available` alongside guard/watchdog info so automation traces the anti-windup behavior without searching source code.</done>
</task>

<task type="auto">
  <name>Task 2: Extend Artisan STATUS tests for the anti-windup tail</name>
  <files>src/output/artisan.rs</files>
  <action>
1. Update the formatter tests so `SystemStatus` is seeded with the filtered derivative and integrator values that the new PID/loop now provides, and assert the generated CSV still has 16 entries.
2. Add assertions that the saturation, integrator clamp, and derivative availability flags appear in the expected positions, and that their numeric values (`0`/`1`) reflect the current `SystemStatus` booleans.
3. Keep the existing status-output tests but extend coverage to verify the `derivative_rate` field reflects the filtered derivative (not the raw delta) and that the integrator value matches the PID accessor from plan 1.
  </action>
  <verify>cargo test --lib</verify>
  <done>The STATUS formatter tests fail if any anti-windup flags or filtered derivative values are missing, ensuring instrumentation keeps the deterministic CSV layout intact.</done>
</task>

</tasks>

<verification>
- `cargo test --lib`
</verification>

<success_criteria>
1. `control_loop_task` logs saturations, integrator clamps, and derivative availability every tick so automation can trace anti-windup events by reading the stage logs.
2. `ArtisanFormatter::format_status_response` still produces the same column order and now reflects the filtered derivative and true integrator values.
3. Formatter tests break if any anti-windup flags or filtered values are removed, protecting the instrumentation contract.
</success_criteria>

<output>
After completion, create `.planning/phases/71-anti-windup-stabilization/71-03-SUMMARY.md`
</output>
