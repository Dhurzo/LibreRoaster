---
phase: 102-diagnostics-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/logging/traceability.rs
  - src/main.rs
  - scripts/test_traceability_matrix.py
  - logs/traceability/sample-safe-shutdown.log
  - internalDoc/INSTRUMENTATION_README.MD
autonomous: true
must_haves:
  truths:
    - Safe-shutdown failures produce guard-level TRACE events with AppError metadata so TRACE logs capture initialization diagnostics.
    - The trace parser/tests consume guard_timeout/watchdog_failure entries and still generate the regression matrix when the failure log is replayed.
    - Documentation and sample logs describe how to capture and replay a safe-shutdown trace for auditors.
  artifacts:
    - path: "src/logging/traceability.rs"
      provides: "Formats guard events with guard_timeouts/watchdog flags plus AppError metadata and exposes a helper for safe-shutdown diagnostics."
    - path: "src/main.rs"
      provides: "Safe-shutdown path that converts InitError into AppError, generates a TraceId, and emits a guard TRACE event before the LED blink loop."
    - path: "scripts/test_traceability_matrix.py"
      provides: "Regression tests covering guard events that surface AppError metadata and watchdog_failure markers."
    - path: "logs/traceability/sample-safe-shutdown.log"
      provides: "Representative TRACE log with queue/actuation/guard entries emitted during an InitError failure so auditors can replay it."
    - path: "internalDoc/INSTRUMENTATION_README.MD"
      provides: "Guidance on capturing safe-shutdown traces and linking them to the sample log."
  key_links:
    - from: "src/main.rs"
      to: "src/logging/traceability.rs"
      via: "enter_safe_shutdown() calls the guard-emitting helper when init_hardware() fails."
      pattern: "trace_guard"
    - from: "logs/traceability/sample-safe-shutdown.log"
      to: "scripts/traceability_matrix.py"
      via: "Parser invocation against the safe-shutdown log produces a failure matrix."
      pattern: "python scripts/traceability_matrix.py logs/traceability/sample-safe-shutdown\.log"
    - from: "internalDoc/INSTRUMENTATION_README.MD"
      to: "logs/traceability/sample-safe-shutdown.log"
      via: "Documented reproduction steps reference the new sample log."
      pattern: "sample-safe-shutdown\.log"
---

<objective>
Make InitError failures traceable by emitting guard events with AppError metadata, extending the regression parser/tests, and documenting how to replay the failure log.
Purpose: Close the v5.2 diagnostics gap so auditors can tie startup failures to the TRACE regression matrix.
Output: New guard helper, safe-shutdown instrumentation, updated docs/logs/tests plus a reproducible sample log.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/v5.2-MILESTONE-AUDIT.md
@.planning/v5.2-v5.2-MILESTONE-AUDIT.md
@.planning/phases/101-traceability-matrix-alignment/101-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Format guard events for safe shutdown</name>
  <files>src/logging/traceability.rs</files>
  <action>Add a helper that builds a guard TRACE event for safe-shutdown failures (guard_timeout=1, guard_timeouts=1, watchdog=fail, watchdog_failure="init_error_failure"). The helper should reuse the AppError metadata logic so `error_category`/`error_source` stay in sync with existing telemetry events, and it should be testable through the module’s `#[cfg(test)]` helpers.</action>
  <verify>`cargo test --package libre-roaster traceability`</verify>
  <done>New helper formats the guard TRACE string with initialized AppError metadata, a deterministic watchdog failure marker, and a unit test that asserts the string contains the guard_timeout/watchdog_failure/error_category pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Emit the guard trace from enter_safe_shutdown()</name>
  <files>src/main.rs</files>
  <action>Import `AppError` and `TraceId`, convert the incoming `InitError` into `AppError::Initialization`, call `TraceId::next()` to get a unique ID, and invoke the new helper before entering the LED blink loop so every InitError run writes a guard event with error_category/source metadata.</action>
  <verify>`cargo check --release --features embedded`</verify>
  <done>Safe-shutdown path now emits a guard TRACE event with AppError diagnostics immediately after logging the failure and before the LED blink loop begins.</done>
</task>

<task type="auto">
  <name>Task 3: Document and test the safe-shutdown trace</name>
  <files>scripts/test_traceability_matrix.py, logs/traceability/sample-safe-shutdown.log, internalDoc/INSTRUMENTATION_README.MD</files>
  <action>Add a sample safe-shutdown TRACE log that strings together queue/actuation/telemetry/guard entries with the new watchdog_failure + AppError fields, update `scripts/test_traceability_matrix.py` with a regression that exercises the log, and expand `internalDoc/INSTRUMENTATION_README.MD` to describe how to capture and replay the failure trace using the sample log.</action>
  <verify>`python scripts/test_traceability_matrix.py` and `python scripts/traceability_matrix.py logs/traceability/sample-safe-shutdown.log`</verify>
  <done>The parser tests cover the safe-shutdown log, the sample log exists for auditors, and the instrumentation doc describes how to use the log with the trace parser.</done>
</task>

</tasks>

<verification>
- `cargo test --package libre-roaster`
- `cargo check --release --features embedded`
- `python scripts/test_traceability_matrix.py`
- `python scripts/traceability_matrix.py logs/traceability/sample-safe-shutdown.log`
</verification>

<success_criteria>
1. Guard-level TRACE events carry AppError metadata and appear before the safe-shutdown LED loop.
2. Parser/tests replay a safe-shutdown log with watchdog_failure + guard_timeout, so the regression matrix can prove the failure path.
3. Instrumentation docs + sample log describe how to capture/initiate this safe-shutdown trace.
</success_criteria>

<output>
After completion, create `.planning/phases/102-diagnostics-verification/102-01-SUMMARY.md`
</output>
