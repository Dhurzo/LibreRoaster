---
phase: 42-cross-reference-validation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: ["internalDoc/ARCHITECTURE.md", "internalDoc/PROTOCOL.md", "internalDoc/hardware.md", "CODE_QUALITY_ISSUES.md", "CODE_QUALITY_REMEDIATION.md"]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "All internalDoc files reference v2.2/v2.3 milestone versions"
    - "No stale references to pre-v2.2 features remain"
    - "All modified documentation includes Last updated timestamps"
  artifacts:
    - path: "internalDoc/ARCHITECTURE.md"
      provides: "Architecture documentation"
      contains: "v2.2" or "v2.3"
    - path: "internalDoc/PROTOCOL.md"
      provides: "Protocol documentation"
      contains: "v2.2" or "v2.3"
    - path: "internalDoc/hardware.md"
      provides: "Hardware documentation"
      contains: "v2.2" or "v2.3"
    - path: "CODE_QUALITY_ISSUES.md"
      provides: "Quality issues documentation"
      contains: "v2.2" or "v2.3"
  key_links:
    - from: "All internalDoc files"
      to: "v2.2/v2.3 milestone"
      via: "Version reference consistency"
      pattern: "v2\\.[0-9]+"
---

<objective>
Validate all documentation cross-references are current and consistent with v2.3 milestone.

**Purpose:** Ensure all internal documentation updated in Phases 38-41 properly references v2.2/v2.3 and has no stale pre-v2.2 references.

**Output:** Validated documentation with consistent version references and timestamps.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/juan/Repos/LibreRoaster/.planning/STATE.md
@/home/juan/Repos/LibreRoaster/.planning/ROADMAP.md
@/home/juan/Repos/LibreRoaster/.planning/REQUIREMENTS.md

**Phase 42 Requirements:**
- XREF-01: Verify all internalDoc files reference correct milestone versions
- XREF-02: Ensure no stale references to pre-v2.2 features
- XREF-03: Add "Last updated" timestamps to all modified docs

**Files to validate:**
- internalDoc/ARCHITECTURE.md (updated Phase 38)
- internalDoc/PROTOCOL.md (created Phase 39)
- internalDoc/hardware.md (updated Phase 41)
- CODE_QUALITY_ISSUES.md (reviewed Phase 40)
- CODE_QUALITY_REMEDIATION.md (may need update)
</context>

<tasks>

<task type="auto">
  <name>Audit version references in all internalDoc files</name>
  <files>internalDoc/ARCHITECTURE.md, internalDoc/PROTOCOL.md, internalDoc/hardware.md</files>
  <action>
    Check each file for version references:

    **Check for v2.2/v2.3 references:**
    ```bash
    # Find version references in each file
    grep -o "v2\.[0-9]" internalDoc/ARCHITECTURE.md | sort -u
    grep -o "v2\.[0-9]" internalDoc/PROTOCOL.md | sort -u
    grep -o "v2\.[0-9]" internalDoc/hardware.md | sort -u
    ```

    **Check for pre-v2.2 references (v1.x, etc.):**
    ```bash
    grep -n "v1\.[0-9]" internalDoc/*.md
    ```

    **Check for missing version references:**
    ```bash
    # Files with no version at all
    for f in internalDoc/*.md; do
      if ! grep -q "v2\.[0-9]" "$f"; then
        echo "MISSING: $f"
      fi
    done
    ```

    **Report findings:**
    - Which files have correct v2.2/v2.3 references
    - Which files have stale v1.x references
    - Which files have no version references
  </action>
  <verify>
    echo "=== Version Audit Results ==="
    echo "ARCHITECTURE.md references:"
    grep -o "v2\.[0-9]" internalDoc/ARCHITECTURE.md 2>/dev/null | sort -u || echo "NONE"
    echo "PROTOCOL.md references:"
    grep -o "v2\.[0-9]" internalDoc/PROTOCOL.md 2>/dev/null | sort -u || echo "NONE"
    echo "hardware.md references:"
    grep -o "v2\.[0-9]" internalDoc/hardware.md 2>/dev/null | sort -u || echo "NONE"
    echo "Stale references (v1.x):"
    grep -l "v1\.[0-9]" internalDoc/*.md 2>/dev/null || echo "NONE FOUND"
  </verify>
  <done>
    - Audit complete with list of files needing version reference fixes
    - Stale v1.x references identified
  </done>
</task>

<task type="auto">
  <name>Fix version references and add timestamps</name>
  <files>internalDoc/ARCHITECTURE.md, internalDoc/PROTOCOL.md, internalDoc/hardware.md</files>
  <action>
    Fix version references based on audit findings:

    **For files missing version references:**
    - Add "v2.3" milestone reference where appropriate (header or first paragraph)

    **For files with stale v1.x references:**
    - Update to v2.2 or v2.3 as appropriate

    **Add/verify timestamps:**

    **ARCHITECTURE.md:** Check if timestamp exists
    - If missing: Add after title line: "> Last Updated: 2026-02-07 (v2.3)"

    **PROTOCOL.md:** Check if timestamp exists
    - If missing: Add after title line: "> Last Updated: 2026-02-08 (v2.3)"

    **hardware.md:** Already has timestamp from Phase 41
    - Verify it shows: "Last Updated: 2026-02-08 (v2.3)"

    **Format:** Use consistent timestamp format: "> Last Updated: YYYY-MM-DD (v2.3)"
  </action>
  <verify>
    echo "=== Timestamp Verification ==="
    grep "Last Updated" internalDoc/ARCHITECTURE.md 2>/dev/null || echo "ARCHITECTURE.md: MISSING"
    grep "Last Updated" internalDoc/PROTOCOL.md 2>/dev/null || echo "PROTOCOL.md: MISSING"
    grep "Last Updated" internalDoc/hardware.md 2>/dev/null || echo "hardware.md: MISSING"
  </verify>
  <done>
    - All internalDoc files have version references (v2.2 or v2.3)
    - No stale v1.x references remain
    - All files have consistent timestamp format
  </done>
</task>

<task type="auto">
  <name>Validate CODE_QUALITY files version references</name>
  <files>CODE_QUALITY_ISSUES.md, CODE_QUALITY_REMEDIATION.md</files>
  <action>
    Check CODE_QUALITY files for version references and timestamps:

    ```bash
    # Check for version references
    grep -o "v2\.[0-9]" CODE_QUALITY_ISSUES.md | sort -u
    grep -o "v2\.[0-9]" CODE_QUALITY_REMEDIATION.md | sort -u

    # Check for timestamps
    grep "Last Updated" CODE_QUALITY_ISSUES.md
    grep "Last Updated" CODE_QUALITY_REMEDIATION.md
    ```

    **Fix if needed:**
    - Add v2.3 reference if missing
    - Add timestamp: "> Last Updated: 2026-02-08 (v2.3)"
  </action>
  <verify>
    echo "=== CODE_QUALITY Files ==="
    grep "Last Updated" CODE_QUALITY_ISSUES.md 2>/dev/null || echo "CODE_QUALITY_ISSUES.md: MISSING"
    grep "Last Updated" CODE_QUALITY_REMEDIATION.md 2>/dev/null || echo "CODE_QUALITY_REMEDIATION.md: MISSING"
  </verify>
  <done>
    - CODE_QUALITY files have version references
    - CODE_QUALITY files have timestamps
  </done>
</task>

<task type="auto">
  <name>Update REQUIREMENTS.md and verify completion</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Mark XREF requirements as complete in REQUIREMENTS.md:

    Change:
    - [ ] **XREF-01**: Verify all internalDoc files reference correct milestone versions
    - [ ] **XREF-02**: Ensure no stale references to pre-v2.2 features
    - [ ] **XREF-03**: Add "Last updated" timestamps to all modified docs

    To:
    - [x] **XREF-01**: Verify all internalDoc files reference correct milestone versions
    - [x] **XREF-02**: Ensure no stale references to pre-v2.2 features
    - [x] **XREF-03**: Add "Last updated" timestamps to all modified docs

    Also update the Traceability table to mark XREF-01, XREF-02, XREF-03 as "Complete"
  </action>
  <verify>
    echo "=== REQUIREMENTS.md Update ==="
    grep -A1 "XREF-0" .planning/REQUIREMENTS.md | grep -E "^\s*\[x\]" | wc -l
    # Should show 3 completed XREF requirements
  </verify>
  <done>
    - All 3 XREF requirements marked complete in REQUIREMENTS.md
    - Traceability table updated
  </done>
</task>

</tasks>

<verification>
**Final cross-reference validation:**

```bash
# Verify all internalDoc files have version references
echo "=== Version Reference Check ==="
for f in internalDoc/*.md; do
  echo -n "$f: "
  grep -o "v2\.[0-9]" "$f" 2>/dev/null | head -1 || echo "MISSING"
done

# Verify no stale v1.x references
echo ""
echo "=== Stale Reference Check ==="
grep -l "v1\.[0-9]" internalDoc/*.md 2>/dev/null && echo "STALE FOUND" || echo "No stale v1.x references ✓"

# Verify all timestamps
echo ""
echo "=== Timestamp Check ==="
grep "Last Updated" internalDoc/ARCHITECTURE.md 2>/dev/null
grep "Last Updated" internalDoc/PROTOCOL.md 2>/dev/null
grep "Last Updated" internalDoc/hardware.md 2>/dev/null
grep "Last Updated" CODE_QUALITY_ISSUES.md 2>/dev/null
```

**Expected results:**
- All files show v2.2 or v2.3 reference
- No files listed in "Stale Reference Check"
- All 4 files show "Last Updated" timestamps
</verification>

<success_criteria>
- [x] All internalDoc files (ARCHITECTURE.md, PROTOCOL.md, hardware.md) reference v2.2/v2.3
- [x] No stale v1.x references found in documentation
- [x] All modified files have consistent "Last Updated: YYYY-MM-DD (v2.3)" timestamps
- [x] CODE_QUALITY files also have version references and timestamps
- [x] REQUIREMENTS.md updated with XREF-01, XREF-02, XREF-03 marked complete
</success_criteria>

<output>
After completion, create `.planning/phases/42-cross-reference-validation/42-01-SUMMARY.md`
</output>
