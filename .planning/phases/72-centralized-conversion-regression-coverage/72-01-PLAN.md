---
phase: 72-centralized-conversion-regression-coverage
plan: 72-01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hardware/mod.rs
  - src/hardware/sensors/conversion.rs
  - src/control/roaster_refactored.rs
  - src/application/app_builder.rs
  - src/main.rs
autonomous: true
must_haves:
  truths:
    - Control loop, telemetry, and any future consumer depend on `SensorConversionHub::sample()` before SystemStatus is updated so every 100 ms read shares the same math and fault semantics.
    - `RoasterControl` receives the shared hub from `AppBuilder`/`main` so the async lock and instrumentation see the same helper instance that caches the last good bean/environment pair.
    - The helper surface captures per-sensor fault flags and exposes them through `SensorSample` so logging or automation can detect faults without re-implementing the conversion.
  artifacts:
    - path: src/hardware/sensors/conversion.rs
      provides: Shared conversion helper (`SensorConversionHub`, `SensorSample`, fault mapping) with the 0.0078125 °C scaling, two's-complement math, and caching/timer logic.
      min_lines: 200
    - path: src/control/roaster_refactored.rs
      provides: `RoasterControl::read_sensors` wired through the hub, `SystemStatus` updates that consume `SensorSample`, and constructors that accept the hub.
  key_links:
    - from: src/control/roaster_refactored.rs
      to: src/hardware/sensors/conversion.rs
      via: calling `SensorConversionHub::sample()` before `update_temperatures`
      pattern: "SensorConversionHub::sample"
    - from: src/application/app_builder.rs
      to: src/main.rs
      via: passing the newly built `SensorConversionHub` into `RoasterControl::new`
      pattern: "with_sensor_conversion_hub"
---

<objective>
Define the centralized MAX31856 conversion helper and weave it into the control loop so every heartbeat, telemetry writer, and downstream flow shares the same 0.0078125 °C math, fault detection, and cached samples.
Purpose: keep the 100 ms gauge, statuses, and StageTracker instrumentation tied to a single helper instead of duplicated conversions.
Output: `src/hardware/sensors/conversion.rs` plus updated `RoasterControl`, `AppBuilder`, and `main.rs` wiring.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/72-centralized-conversion-regression-coverage/72-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement SensorConversionHub helper</name>
  <files>
src/hardware/sensors/conversion.rs
src/hardware/mod.rs
  </files>
  <action>
Create `hardware::sensors::conversion.rs` with:
- `SensorConversionHub` that owns the bean/env SPI drivers (or their mocks) and exposes an async `sample()` that awaits the MAX31856 conversion duration, reads the fault register, applies the 0.0078125 °C scale, handles two's-complement, and caches the last-good `SensorSample` so faults can fall back gracefully.
- `SensorSample`/`SensorFault` types carrying raw bean/env floats, per-sensor fault bits, and timestamps for instrumentation.
- A shared conversion helper (e.g., `convert_raw_temp(u32) -> f32`) so tests, regression harnesses, and the hub cannot diverge on rounding.
Update `hardware/mod.rs` to expose the new `sensors` module.
  </action>
  <verify>
Run `cargo check --lib` to make sure the new module compiles with the crate-level features.
  </verify>
  <done>
The conversion helper compiles, exports `SensorConversionHub`/`SensorSample`, documents the fault-paying API, and the module is reachable via `hardware::sensors`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire RoasterControl and builders through the hub</name>
  <files>
src/control/roaster_refactored.rs
src/application/app_builder.rs
src/main.rs
  </files>
  <action>
- Replace the `bean_sensor`/`env_sensor` fields with a single `SensorConversionHub` owned by `RoasterControl`.
- Update `RoasterControl::new` (and the `#[cfg(not(target_arch = "riscv32"))`] stub) so it receives the hub, keeps the previous `update_temperatures` logic, and `read_sensors`/`read_sensors_sync` call `hub.sample()`/`hub.sample_sync()` before feeding `update_temperatures`.
- Ensure `ServiceContainer` consumers continue to see the same behavior by retaining `RoasterControl::update_temperatures` offsets and exposing fault flags for later logging.
- Expand `AppBuilder`/`main.rs` so the builder now builds the hub (`SensorConversionHub::new(bean_sensor, env_sensor)`) and passes it into `RoasterControl::new`, keeping `Max31856` initialization responsibilities where they currently live.
  </action>
  <verify>
Run `cargo test --lib` to confirm the control loop path compiles with the hub in place.
  </verify>
  <done>
`RoasterControl` no longer touches `Max31856` directly; everyone gets the shared hub from `AppBuilder`/`main`, sensor reads return `SensorSample`, and the statuses/stage tracker still emit the instrumentation they expect.
  </done>
</task>

</tasks>

<verification>
Ensure `cargo check --lib` passes after both files exist and the `SensorConversionHub` API surfaces before any consumer compiles.
</verification>

<success_criteria>
1. `SensorConversionHub` lives in `src/hardware/sensors/conversion.rs` and exposes `sample()` + faults.
2. `RoasterControl` owns the hub and `read_sensors`/`read_sensors_sync` consume `SensorSample` before updating `SystemStatus`.
3. `AppBuilder` and `main.rs` still construct the sensors but now wrap them in the hub before calling `RoasterControl::new`.
</success_criteria>

<output>
After completion, create `.planning/phases/72-centralized-conversion-regression-coverage/72-01-SUMMARY.md`
</output>
