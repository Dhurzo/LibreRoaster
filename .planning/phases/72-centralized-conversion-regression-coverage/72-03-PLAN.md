---
phase: 72-centralized-conversion-regression-coverage
plan: 72-03
type: execute
wave: 3
depends_on:
  - 72-02
files_modified:
  - src/hardware/sensors/conversion.rs
  - tests/sensor_conversion.rs
  - tests/regression_status.rs
autonomous: true
must_haves:
  truths:
    - Unit tests exhaustively validate the 0.0078125 °C LSB, two's-complement conversion, rounding, and fault propagation so any change to `SensorConversionHub` math fails the suite.
    - Regression fixtures run through the helper emit `SystemStatus` snapshots that match `ArtisanFormatter::format_status_response` (16-column CSV) for every canonical ADC sequence.
    - Both conversion and regression tests rely on the same fixture catalog so there are no duplicated magic numbers.
  artifacts:
    - path: tests/sensor_conversion.rs
      provides: Conversion tests that instantiate `SensorConversionHub` with mock SPI transactions and assert float results + fault flags for positive, negative, and overflow inputs.
    - path: tests/regression_status.rs
      provides: Regression-status tests that replay the fixture catalog through the hub and compare the resulting string to `ArtisanFormatter::format_status_response` output.
  key_links:
    - from: tests/sensor_conversion.rs
      to: src/hardware/sensors/conversion.rs
      via: `SensorConversionHub::from_fixture` and `convert_raw_temp`
      pattern: "from_fixture"
    - from: tests/regression_status.rs
      to: src/output/artisan.rs
      via: comparing hub-derived `SystemStatus` to `ArtisanFormatter::format_status_response`
      pattern: "format_status_response"
---

<objective>
Add deterministic regression and conversion tests that cover the helper’s math, per-sensor faults, and the 16-column STATUS tail so instrumentation drift is caught before deployment.
Purpose: give confidence that the centralized helper keeps the same numbers across control, telemetry, and regression.
Output: two new test binaries (`tests/sensor_conversion.rs`, `tests/regression_status.rs`) plus any test helpers inside `src/hardware/sensors/conversion.rs`.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/72-centralized-conversion-regression-coverage/72-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert fixture math tests</name>
  <files>
tests/sensor_conversion.rs
src/hardware/sensors/conversion.rs
  </files>
  <action>
Write `tests/sensor_conversion.rs` that:
- Uses `embedded-hal-mock` SPI transactions for bean/env ADC reads (positive, negative, max/min, open/short) along with CJ/fault reads.
- Instantiates `SensorConversionHub` via a `from_fixture` helper so the same helper paths used by the regression harness are exercised.
- Asserts the hub returns the expected floats (with the 0.0078125 °C LSB and rounding rules) plus fault flags or caching when a fault occurs.
- Adds helper macros for reusing raw ADC bytes so the tests remain readable.
  </action>
  <verify>
Run `cargo test --test sensor_conversion --features regression` to catch any drift in conversion math and fault reporting.
  </verify>
  <done>
Conversion tests run through the hub, cover the canonical ADC cases, and the suite fails if the two's-complement/rounding logic or fault propagation deviates.
  </done>
</task>

<task type="auto">
  <name>Task 2: Regression status snapshot tests</name>
  <files>
tests/regression_status.rs
src/output/artisan.rs
  </files>
  <action>
Create `tests/regression_status.rs` that:
- Pulls each canonical fixture from `tests/fixtures/max31856_sequences.rs` and replays it through `SensorConversionHub::from_fixture` to produce a `SystemStatus`.
- Passes that status to `ArtisanFormatter::format_status_response` and compares the resulting string to the expected CSV stored alongside the fixture.
- Runs under `--features regression` so the same feature gate used by the harness is covered.
  </action>
  <verify>
Run `cargo test --test regression_status --features regression` to ensure the fixture-encoded STATUS tail matches the formatter output.
  </verify>
  <done>
Every regression snapshot yields the exact same 16-column string the production formatter would emit, so regression watches for deviations before automation sees them.
  </done>
</task>

</tasks>

<verification>
Success means `cargo test --features regression --test sensor_conversion --test regression_status` passes and flags any change to the shared helper math or STATUS ordering.
</verification>

<success_criteria>
1. Sensor conversion tests cover positive/negative/fault sequences and rely on the hub’s fixture helpers.
2. Regression-status tests compare hub snapshots to `ArtisanFormatter::format_status_response` and fail on any CSV drift.
3. Both test suites compile only when `feature = "regression"`, matching the gating in the harness.
</success_criteria>

<output>
After completion, create `.planning/phases/72-centralized-conversion-regression-coverage/72-03-SUMMARY.md`
</output>
