---
phase: 72-centralized-conversion-regression-coverage
plan: 72-02
type: execute
wave: 2
depends_on:
  - 72-01
files_modified:
  - Cargo.toml
  - src/safety/regression.rs
  - tests/fixtures/max31856_sequences.rs
autonomous: true
must_haves:
  truths:
    - Regression runs only compile/execute when `feature = "regression"` is enabled and every replayed ADC sequence flows through `SensorConversionHub` so the conversion math stays identical to the control loop.
    - The regression fixture catalog encodes canonical ADC + offset sequences plus expected `SystemStatus` snapshots so the harness can assert the 16-column STATUS tail matches production.
    - Fault conditions surfaced by the helper propagate into the regression log output so automation sees the same `SAFETY` signals that the real loop emits when a sensor misbehaves.
  artifacts:
    - path: src/safety/regression.rs
      provides: Feature-gated regression task that replays fixtures through `SensorConversionHub`, feeds watchdogs, and emits `SAFETY OT-REGRESSION` with the same `SystemStatus` snapshot as the live loop.
    - path: tests/fixtures/max31856_sequences.rs
      provides: Deterministic SPI transaction arrays (ADC, CJ, fault) paired with expected `SystemStatus` deltas used by the harness and future tests.
  key_links:
    - from: src/safety/regression.rs
      to: src/hardware/sensors/conversion.rs
      via: instantiating the hub with fixture transactions when replaying ADC/offset pairs
      pattern: "SensorConversionHub::from_fixture"
    - from: Cargo.toml
      to: tests/fixtures/max31856_sequences.rs
      via: enabling `embedded-hal-mock` under the `regression` feature so host fixtures compile
      pattern: "embedded-hal-mock"
---

<objective>
Gate the regression harness behind `feature = "regression"`, replay canonical ADC/offset sequences through the centralized helper, and document the fixtures that keep automation-status snapshots aligned with production.
Purpose: keep regression instrumentation lean when disabled while still proving the helper math and STATUS tail with deterministic fixtures.
Output: gated `regression_task` plus a fixture catalog in `tests/fixtures/max31856_sequences.rs`.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/72-centralized-conversion-regression-coverage/72-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Feature-gate the regression harness through the hub</name>
  <files>
Cargo.toml
src/safety/regression.rs
  </files>
  <action>
1. Add a `regression` feature in `Cargo.toml` that optionally pulls in `embedded-hal-mock` so host fixtures compile without bloating the default build.
2. Restrict the existing `regression_task`, `OverTempTestRunner`, and `request_regression` helper behind `#[cfg(all(target_arch = "riscv32", feature = "regression"))]` so they only ship when regression instrumentation is desired.
3. Within the gated code path, instantiate `SensorConversionHub` with fixture builders (e.g., `SensorConversionHub::from_fixture`) so the regression replays SPI sequences instead of hitting live ADCs, keeps feeding the watchdog, and emits `SAFETY OT-REGRESSION` after each snapshot.
4. Document in-code which `SystemStatus` fields come from the hub sample so the regression comments can point to the live instrumentation columns.
  </action>
  <verify>
Run `cargo test --features regression --lib` to ensure the gated harness compiles with the shared helper and feature flag.
  </verify>
  <done>
`regression_task` only exists when `feature = "regression"`, and it replays fixture-driven hub samples to drive `SystemStatus` snapshots before emitting the OTA safety line.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write deterministic MAX31856 fixture sequences</name>
  <files>
tests/fixtures/max31856_sequences.rs
Cargo.toml
  </files>
  <action>
Create `tests/fixtures/max31856_sequences.rs` containing:
- A small set of SPI transaction sequences that touch bean/env ADC registers, CJ/fault registers, and scaling offsets (e.g., normal read, zero crossing, open fault) encoded as `embedded_hal_mock::spi::Transaction` vectors.
- Expected `SystemStatus` snapshots (bean/env, `pv`, `mv`, integrator/derivative flags, saturation, faults) that the regression harness can assert matches `ArtisanFormatter::format_status_response`.
- A helper function `canonical_fixtures()` returning the sequences so both regression harness and future tests can iterate.
Link the fixture builder to `Cargo.toml` through the `regression` feature so `embedded-hal-mock` only compiles when needed.
  </action>
  <verify>
Run `cargo test --features regression --lib` to ensure the fixture module builds and the helper functions export the expected sequences.
  </verify>
  <done>
The regression fixture catalog compiles under `feature = "regression"` and provides deterministic SPI/Status pairs used by the harness and follow-on tests.
  </done>
</task>

</tasks>

<verification>
Verify `cargo test --features regression --lib` so the gated harness and fixture catalog compile, and the hub-powered replay path is type-checked.
</verification>

<success_criteria>
1. The regression task is gated behind `feature = "regression"` with stubbed no-ops otherwise.
2. Regression runs replay canonical SPI sequences through `SensorConversionHub` and emit the same 16-column STATUS snapshot as the live loop.
3. The fixture catalog under `tests/fixtures/max31856_sequences.rs` compiles and exports data for both harness and future tests.
</success_criteria>

<output>
After completion, create `.planning/phases/72-centralized-conversion-regression-coverage/72-02-SUMMARY.md`
</output>
