---
phase: 68-readme-command-table-for-automation-hooks
plan: 01
type: execute
wave: 1
depends_on:
  - 66-instrumentation-observability
files_modified:
  - README.md
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Automation hooks REG and STATUS/STAT appear in the Supported Artisan Commands table with regression-safety and watchdog/guard telemetry context so instrumentation crews notice them alongside the core commands."
    - "README points automation readers to internalDoc/INSTRUMENTATION_README.MD immediately after the command table so they can interpret the STATUS payload columns and REG regression telemetry expectations."
  artifacts:
    - path: "README.md"
      contains: "REG"
      occurrences: 1
    - path: "README.md"
      contains: "STATUS"
      occurrences: 1
    - path: "README.md"
      contains: "INSTRUMENTATION_README.MD"
      occurrences: 1
  key_links:
    - from: "README.md"
      to: "internalDoc/INSTRUMENTATION_README.MD"
      via: "command table automation hook guidance"
---

<objective>
Update the Supported Artisan Commands table so REG and STATUS/STAT are discoverable with regression/watchdog context, and add a pointer to the instrumentation guide so automation teams know where to decode the extra columns.
Purpose: Fulfill Phase 68's goal of surfacing automation hooks in the README.
Output: README subsection with new command rows and an instrumentation guide link.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-instrumentation-observability/66-01-SUMMARY.md
@internalDoc/INSTRUMENTATION_README.MD
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add REG and STATUS/STAT rows to the command table</name>
  <files>README.md</files>
  <action>
Insert rows for `REG` and `STATUS/STAT` into the Supported Artisan Commands table. Describe `REG` as the regression runner trigger that ramps heater/fan to 100%, feeds the watchdog, and emits SAFETY OT-REGRESSION records so automation can detect an over-temperature regression cycle. Describe `STATUS/STAT` as the automation telemetry snapshot returning ET, BT, Heater, Fan, WatchdogOK, WatchdogFailures, LastWatchdogReason, LEDCGuardTimeouts, RegressionActive (alias `STAT`), and mention it surfaces watchdog guard/regression telemetry without touching `READ`.
  </action>
  <verify>rg -n "\| `REG`" README.md && rg -n "\| `STATUS/STAT`" README.md</verify>
  <done>The command table contains explicit rows for REG and STATUS/STAT, each mentioning its automation telemetry purpose plus the alias for STATUS.</done>
</task>

<task type="auto">
  <name>Task 2: Reference the instrumentation guide near automation hooks</name>
  <files>README.md</files>
  <action>
Add a sentence directly after the Supported Artisan Commands table (or in the automation hooks intro) that directs automation readers to `internalDoc/INSTRUMENTATION_README.MD` for column definitions, STATUS payload expectations, and how REG logs SAFETY OT-REGRESSION so instrumentation can react safely.
  </action>
  <verify>rg -n "INSTRUMENTATION_README.MD" README.md</verify>
  <done>The README now links `internalDoc/INSTRUMENTATION_README.MD` from the automation hook discussion so readers can learn the STATUS/REG expectations.</done>
</task>

</tasks>

<verification>
rg -n "REG" README.md
rg -n "STATUS/STAT" README.md
rg -n "INSTRUMENTATION_README.MD" README.md
</verification>

<success_criteria>
- Supported Artisan Commands table covers REG and STATUS/STAT with automation telemetry narratives.
- README mentions `internalDoc/INSTRUMENTATION_README.MD` next to the automation command discussion.
- Automation engineers can discover REG/STATUS from the README alone.
</success_criteria>

<output>
After completion, create `.planning/phases/68-readme-command-table-for-automation-hooks/68-01-SUMMARY.md`
</output>
