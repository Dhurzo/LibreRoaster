---
phase: 83-rust-modernization-and-unsafe-surface-audit
plan: 03
type: execute
wave: 2
depends_on:
  - 83-01
files_modified:
  - scripts/run-regression-checks.sh
  - quality/modernization/regression-verification.md
autonomous: true
must_haves:
  truths:
    - "Auditors can read a regression-verification plan that lists representative command flows, acceptable telemetry drift thresholds, and the hybrid detection steps (automated mesh plus spot-check manual runs)."
    - "Running `scripts/run-regression-checks.sh` produces logs per command flow with a stored run ID so the behavior comparison can be traced to automation outputs."
  artifacts:
    - path: "quality/modernization/regression-verification.md"
      provides: "Behavior equivalence guidance for Phase 83, including which command flows to rerun, how drift is assessed, and how manual spot-checking integrates with automated suites."
      contains: "command_idempotence"
    - path: "scripts/run-regression-checks.sh"
      provides: "Script that runs the representative regression tests, captures per-test logs under `logs/regression/`, and writes a summary referencing the automation run ID."
      contains: "logs/regression"
  key_links:
    - from: "quality/modernization/regression-verification.md"
      to: "scripts/run-regression-checks.sh"
      via: "Doc references the regression script as the execution mechanism for the listed flows."
      pattern: "scripts/run-regression-checks\\.sh"
    - from: "quality/modernization/regression-verification.md"
      to: "logs/regression/"
      via: "Doc explains that each run writes artifacts into `logs/regression/` with run IDs for traceability."
      pattern: "logs/regression"
---

<objective>
Capture the behavior-equivalence verification recipe so modernization runs keep the same semantics and reviewers can trace regressions to concrete logs.
Purpose: Define which commands/tests prove no regressions occurred, what drift is acceptable, and how to store each regression run for later inspection.
Output: A regression-verification guide plus a script that executes the representative flows, logs their output, and flags their run ID for artifact tracking.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-rust-modernization-and-unsafe-surface-audit/83-CONTEXT.md
@Cargo.toml
@README.md
</context>

<tasks>

<task type="auto">
  <name>Document regression verification flows and thresholds</name>
  <files>quality/modernization/regression-verification.md</files>
  <action>
1. Create `quality/modernization/regression-verification.md` that lists the representative command flows to rerun (e.g., `command_idempotence`, `command_multiplexer_concurrency`, `artisan_integration_test`, `mock_uart_integration`) so every major module is exercised.
2. Define acceptable drift as the telemetry values staying within documented thresholds and note that semantic invariants (command ordering, response sign-off) must not change.
3. Describe the hybrid detection approach: automated `cargo test` runs followed by targeted manual spot-checks of hardware-facing scenarios, and point to the run artifacts under `logs/regression/` for each verification pass.
  </action>
  <verify>
1. `rg -n "command_idempotence" quality/modernization/regression-verification.md` ensures at least one core flow is mentioned.
2. `rg -n "drift" quality/modernization/regression-verification.md` ensures telemetry tolerance is documented.
3. `rg -n "logs/regression" quality/modernization/regression-verification.md` ensures artifact storage is described.
  </verify>
  <done>
Plan documents which tests command flows to rerun, what drift is acceptable, and how automation/manual checks combine, pointing readers to the log directory for each run ID.
  </done>
</task>

<task type="auto">
  <name>Script regression check runner</name>
  <files>scripts/run-regression-checks.sh</files>
  <action>
1. Create `scripts/run-regression-checks.sh` with `set -euo pipefail`, derive `RUN_ID` (`date -u +%Y%m%dT%H%M%SZ` plus `$RANDOM`), and `mkdir -p "logs/regression/$RUN_ID"`.
2. Run representative regression commands/tests (`cargo test --test command_idempotence`, `cargo test --test command_multiplexer_concurrency`, `cargo test --test artisan_integration_test --features regression`, `cargo test --test mock_uart_integration`) sequentially, teeing stdout/stderr into `logs/regression/$RUN_ID/<test>.log` and recording return codes in `logs/regression/$RUN_ID/summary.txt`.
3. After the suite completes, append a summary that includes `run_id`, the list of tests, and any manual spot-check instructions that auditors should perform while reviewing the logs.
  </action>
  <verify>
1. `bash -n scripts/run-regression-checks.sh` succeeds.
2. `rg -n "command_idempotence" scripts/run-regression-checks.sh` confirms the representative test is enumerated.
3. `rg -n "logs/regression" scripts/run-regression-checks.sh` ensures logs are captured into the expected folder.
  </verify>
  <done>
Script runs the defined command flows, stores per-test logs under `logs/regression/$RUN_ID`, and writes a summary referencing the run ID for traceability.
  </done>
</task>

</tasks>

<verification>
1. `quality/modernization/regression-verification.md` documents flows, telemetry drift thresholds, and artifact paths.
2. `scripts/run-regression-checks.sh` builds a run ID, executes the listed tests, and deposits logs into `logs/regression/` plus a summary file.
</verification>

<success_criteria>
- [ ] Regression-verification guide lists the representative flows, acceptable drift, and hybrid detection instructions.
- [ ] Regression script runs the named tests, captures logs per test under `logs/regression/$RUN_ID`, and produces a summary referencing the automation run ID.
</success_criteria>

<output>
After completion, create `.planning/phases/83-rust-modernization-and-unsafe-surface-audit/83-03-SUMMARY.md`
</output>
