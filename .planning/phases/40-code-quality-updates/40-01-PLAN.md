---
phase: 40-code-quality-updates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internalDoc/CODE_QUALITY_ISSUES.md
  - internalDoc/CODE_QUALITY_REMEDIATION.md
autonomous: true
user_setup: []
must_haves:
  truths:
    - "CODE_QUALITY_ISSUES.md reflects v2.2 changes (or confirms no changes)"
    - "CODE_QUALITY_REMEDIATION.md updated if v2.2 resolved existing issues"
    - "Unsafe block count verified (22 blocks from v2.0 baseline)"
    - "New v2.2 structs/enums documented with safe patterns"
  artifacts:
    - path: "internalDoc/CODE_QUALITY_ISSUES.md"
      provides: "Updated issue inventory with v2.2 verification"
      min_lines: 320
    - path: "internalDoc/CODE_QUALITY_REMEDIATION.md"
      provides: "Updated remediation guide if needed"
      min_lines: 338
    - path: ".planning/phases/40-code-quality-updates/geiger-current.md"
      provides: "Fresh cargo-geiger scan for comparison"
    - path: ".planning/phases/40-code-quality-updates/40-01-SUMMARY.md"
      provides: "Execution summary with metrics"
  key_links:
    - from: "internalDoc/CODE_QUALITY_ISSUES.md"
      to: ".planning/phases/31-linting-audit/geiger-report.md"
      via: "baseline comparison"
      pattern: "v2.0.*baseline|22.*unsafe.*blocks"
    - from: "internalDoc/CODE_QUALITY_ISSUES.md"
      to: "src/config/constants.rs"
      via: "TemperatureScale enum documentation"
      pattern: "TemperatureScale|TemperatureSettings"
---

<objective>
Verify and update CODE_QUALITY documentation to reflect v2.2 implementation state.

**Purpose:** Ensure code quality documentation accurately represents the current codebase after v2.2 changes (OT2, READ, UNITS commands).

**Output:**
- Updated CODE_QUALITY_ISSUES.md with v2.2 verification section
- Updated CODE_QUALITY_REMEDIATION.md if any issues were resolved
- Fresh cargo-geiger scan saved for comparison
- Summary documenting verification results
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

# Research and baseline documentation
@.planning/phases/40-code-quality-updates/../40-code-quality-updates-RESEARCH.md
@.planning/phases/31-linting-audit/geiger-report.md
@internalDoc/CODE_QUALITY_ISSUES.md
@internalDoc/CODE_QUALITY_REMEDIATION.md

# v2.2 implementation files to verify
@src/config/constants.rs
@src/input/parser.rs
@src/control/roaster_refactored.rs
</context>

<tasks>

<task type="auto">
  <name>Run cargo-geiger scan for v2.2 verification</name>
  <files>.planning/phases/40-code-quality-updates/geiger-current.md</files>
  <action>
    Run cargo-geiger to get current unsafe block count for comparison with v2.0 baseline:

    1. Create the phase directory:
       mkdir -p .planning/phases/40-code-quality-updates

    2. Run cargo-geiger with --all-targets and --features embedded:
       cargo geiger --all-targets --features embedded > .planning/phases/40-code-quality-updates/geiger-current.md 2>&1

    3. If embedded target fails, try host-only scan:
       cargo geiger --lib --bins --no-default-features > .planning/phases/40-code-quality-updates/geiger-host-only.md 2>&1

    4. Extract the summary from the scan:
       grep -E "(Total|unsafe)" .planning/phases/40-code-quality-updates/geiger-current.md | head -20

    **Note:** If cargo-geiger fails due to embedded compilation issues, use the existing baseline (22 blocks from geiger-report.md) and document that v2.2 additions (TemperatureScale, TemperatureSettings, OT2/UNITS parsing) are safe enum/struct additions that do not introduce new unsafe patterns per Phase 40 Research findings.
  </action>
  <verify>
    File exists: .planning/phases/40-code-quality-updates/geiger-current.md
    Contains: "Total" or "unsafe blocks" indicating scan completed
  </verify>
  <done>
    Cargo-geiger scan completed and saved; current unsafe count extracted for comparison with v2.0 baseline
  </done>
</task>

<task type="auto">
  <name>Review v2.2 files for quality changes</name>
  <files>internalDoc/CODE_QUALITY_ISSUES.md</files>
  <action>
    Review v2.2 files to identify any quality-relevant changes:

    1. Review src/config/constants.rs:
       - Identify TemperatureScale enum (new v2.2 addition)
       - Identify TemperatureSettings struct (new v2.2 addition)
       - Verify no new unsafe blocks introduced
       - Document as safe derive pattern additions

    2. Review src/input/parser.rs:
       - Check OT2 parsing case for new unsafe patterns
       - Check UNITS parsing case for new unsafe patterns
       - Verify parse_ot2_value function uses safe patterns
       - Document any new error handling that affects quality

    3. Review src/control/roaster_refactored.rs:
       - Check command handlers for OT2/UNITS/READ
       - Identify any new error handling patterns
       - Verify panic/expect usage matches existing inventory
       - Document changes that affect quality metrics

    4. Compare findings with geiger-report.md baseline:
       - If cargo-geiger succeeded: compare block counts
       - If cargo-geiger failed: verify no new unsafe patterns manually

    5. Create summary of v2.2 quality impact:
       - No new unsafe blocks (expected per research)
       - New safe enum/struct patterns (TemperatureScale, TemperatureSettings)
       - New error handling patterns in READ response formatting
  </action>
  <verify>
    v2.2 files reviewed; findings documented in CODE_QUALITY_ISSUES.md update section
  </verify>
  <done>
    All v2.2 files reviewed; confirmed no new unsafe patterns introduced
  </done>
</task>

<task type="auto">
  <name>Update CODE_QUALITY_ISSUES.md with v2.2 verification</name>
  <files>internalDoc/CODE_QUALITY_ISSUES.md</files>
  <action>
    Add v2.2 verification section to CODE_QUALITY_ISSUES.md:

    1. Read current CODE_QUALITY_ISSUES.md to find insertion point (after v2.0 baseline)

    2. Add new section at end of file:

    ```markdown
    ## v2.2 Verification (Phase 40)

    **Verified:** 2026-02-08
    **Unsafe Block Count:** [22|CHANGED] (unchanged from v2.0 baseline)

    ### Verification Method
    - cargo-geiger --all-targets --features embedded scan completed
    - Manual review of v2.2 files (src/config/constants.rs, src/input/parser.rs, src/control/roaster_refactored.rs)

    ### v2.2 Changes Reviewed

    | Phase | Changes Made | Quality Impact |
    |-------|--------------|----------------|
    | 35-OT2 | SetFanSpeed enum variant, parse_ot2_value function | No new unsafe - safe enum addition |
    | 36-READ | format_read_response_full with error handling | No new unsafe - panic patterns documented |
    | 37-UNITS | TemperatureScale enum, TemperatureSettings struct | No new unsafe - safe derive patterns |

    ### Unsafe Block Count Verification

    | Metric | v2.0 Baseline | v2.2 Current | Change |
    |--------|---------------|--------------|--------|
    | Total Source Files | 44 | [COUNT] | [+/- COUNT] |
    | Files with Unsafe | 11 | 11 | None |
    | Unsafe Blocks | 22 | [COUNT] | [None|CHANGE] |
    | High Severity | 1 | 1 | None |
    | Medium Severity | 7 | 7 | None |
    | Low Severity | 21 | 21 | None |

    **Status:** [✓ VERIFIED - No changes|⚠ CHANGES NEEDED]

    ### New v2.2 Patterns

    #### TemperatureScale Enum
    **File:** src/config/constants.rs
    **Pattern:** Safe enum with derive macros
    ```rust
    #[derive(Debug, Clone, Copy, PartialEq)]
    pub enum TemperatureScale {
        Celsius,
        Fahrenheit,
    }
    ```
    **Quality:** No unsafe required - standard Rust enum pattern

    #### TemperatureSettings Struct
    **File:** src/config/constants.rs
    **Pattern:** Safe struct with default implementation
    ```rust
    #[derive(Debug, Clone, Copy)]
    pub struct TemperatureSettings {
        pub scale: TemperatureScale,
        // ...
    }
    ```
    **Quality:** No unsafe required - standard Rust struct pattern

    ### Impact on Issue Inventory

    | Issue ID | Status | Notes |
    |----------|--------|-------|
    | ISSUE-001 through ISSUE-031 | Unchanged | v2.2 additions do not affect existing issues |
    | [New issues if any] | [Added] | Document any new quality concerns |
    ```

    3. If cargo-geiger failed, use this text instead:
    ```markdown
    **Status:** ✓ VERIFIED - v2.2 additions use safe patterns

    **Note:** cargo-geiger scan failed due to embedded target compilation.
    Manual review confirms v2.2 additions (TemperatureScale, TemperatureSettings,
    OT2/UNITS parsing) use only safe Rust patterns. No new unsafe blocks introduced.
    ```

    4. Update the file modification timestamp in frontmatter if the file has one
  </action>
  <verify>
    CODE_QUALITY_ISSUES.md has new v2.2 verification section at end
    Section contains: verification date, unsafe count, v2.2 changes table
  </verify>
  <done>
    CODE_QUALITY_ISSUES.md updated with complete v2.2 verification documentation
  </done>
</task>

<task type="auto">
  <name>Update CODE_QUALITY_REMEDIATION.md if needed</name>
  <files>internalDoc/CODE_QUALITY_REMEDIATION.md</files>
  <action>
    Review and update CODE_QUALITY_REMEDIATION.md if v2.2 addressed any issues:

    1. Read current CODE_QUALITY_REMEDIATION.md

    2. Check if any v2.2 changes relate to existing issues:
       - Did UNITS parsing resolve any parsing-related issues?
       - Did error handling improvements address any robustness concerns?
       - Did new structs enable any previously blocked remediations?

    3. If no issues were resolved:
       Add brief note at end of file:
       ```markdown
       ## v2.2 Update (Phase 40)

       **Verified:** 2026-02-08
       **Changes:** No remediation priorities changed

       v2.2 additions (OT2, READ, UNITS commands) do not affect existing
       remediation priorities. The issue inventory and fix patterns remain
       unchanged from the v2.0 baseline.
       ```

    4. If any issues were resolved or priorities changed:
       - Update the relevant issue entries
       - Add new remediation patterns if v2.2 introduced better solutions
       - Update the priority ordering if v2.2 changes risk assessment

    5. If any new issues were identified:
       - Add to the issue inventory following existing format
       - Include severity classification (High/Medium/Low)
       - Provide remediation guidance
  </action>
  <verify>
    CODE_QUALITY_REMEDIATION.md updated with v2.2 notes
    No unresolved issues that v2.2 should have fixed
  </verify>
  <done>
    CODE_QUALITY_REMEDIATION.md reflects current state after v2.2
  </done>
</task>

</tasks>

<verification>
1. cargo-geiger scan saved to .planning/phases/40-code-quality-updates/geiger-current.md
2. CODE_QUALITY_ISSUES.md contains new v2.2 verification section
3. CODE_QUALITY_REMEDIATION.md contains v2.2 update note
4. Unsafe block count documented (22 from baseline, or current if scan succeeded)
5. v2.2 files reviewed: src/config/constants.rs, src/input/parser.rs, src/control/roaster_refactored.rs
6. All new v2.2 patterns (TemperatureScale, TemperatureSettings) documented as safe
</verification>

<success_criteria>
- CODE_QUALITY_ISSUES.md updated with v2.2 verification section
- CODE_QUALITY_REMEDIATION.md updated with v2.2 notes (or confirmed unchanged)
- Unsafe block count verified against v2.0 baseline
- New v2.2 structs/enums documented with safe pattern classification
- Executable summary created at .planning/phases/40-code-quality-updates/40-01-SUMMARY.md
</success_criteria>

<output>
After completion, create `.planning/phases/40-code-quality-updates/40-01-SUMMARY.md` with:
- Verification results summary
- Unsafe count comparison (v2.0 baseline vs v2.2 current)
- v2.2 files reviewed
- Documentation updates made
- Any issues or anomalies noted
</output>
