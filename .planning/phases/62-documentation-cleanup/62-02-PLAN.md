---
phase: 62-documentation-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internalDoc/
autonomous: true

must_haves:
  truths:
    - "User cannot find any outdated Artisan command information in the internal documentation"
    - "Internal documentation links are valid"
    - "Documentation code examples are accurate"
  artifacts:
    - path: "internalDoc/"
      provides: "Internal developer documentation"
  key_links:
    - from: "internalDoc/"
      to: "src/"
      via: "doc links"
---

<objective>
Clean up internalDoc/ directory - fix broken links, remove outdated info, verify code examples.

Purpose: Internal documentation should be accurate and links should work.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/codebase/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Check internalDoc/ for outdated information</name>
  <files>internalDoc/</files>
  <action>
    Review all files in internalDoc/ directory:
    - Search for outdated commands, hardware references
    - Check for version numbers that are stale
    - Identify any contradicting information with current src/
  </action>
  <verify>ls internalDoc/ && grep -r "v1\.\|PID\|deprecated" internalDoc/</verify>
  <done>List of outdated content identified</done>
</task>

<task type="auto">
  <name>Task 2: Verify documentation links</name>
  <files>internalDoc/</files>
  <action>
    Check all markdown links in internalDoc/ files:
    - Verify internal links to other markdown files
    - Verify links to source code are accurate
    - Fix any broken links found
  </action>
  <verify>grep -h "]\(" internalDoc/*.md | head -20</verify>
  <done>All internalDoc/ links are valid</done>
</task>

<task type="auto">
  <name>Task 3: Verify Rustdoc consistency</name>
  <files>src/</files>
  <action>
    Run cargo doc to check for intra-doc link issues:
    cargo doc --no-deps --document-private-items -D warnings
    
    Fix any documentation that contradicts the code
  </action>
  <verify>cargo doc --no-deps --document-private-items -D warnings 2>&1 | grep -i "warning\|error" | head -20</verify>
  <done>No doc warnings about broken intra-doc links</done>
</task>

</tasks>

<verification>
- [ ] internalDoc/ has no outdated Artisan command references
- [ ] All markdown links in internalDoc/ are valid
- [ ] cargo doc --no-deps --document-private-items -D warnings passes with no warnings
</verification>

<success_criteria>
Internal documentation is accurate and links are valid.
</success_criteria>

<output>
After completion, create `.planning/phases/62-documentation-cleanup/62-02-SUMMARY.md`
</output>
