---
phase: 87-wire-modernization-to-quality-policy
plan: 02
type: execute
wave: 2
depends_on:
  - 87-01
files_modified:
  - scripts/run-modernization.sh
  - scripts/run-regression-checks.sh
autonomous: true
must_haves:
  truths:
    - "`run-modernization.sh` includes the full quality baseline check."
    - "`run-regression-checks.sh` includes the full quality baseline check."
  artifacts:
    - path: "scripts/run-modernization.sh"
      provides: "Modernization script with quality checks"
      contains: "scripts/quality-baseline.sh"
    - path: "scripts/run-regression-checks.sh"
      provides: "Regression script with quality checks"
      contains: "scripts/quality-baseline.sh"
  key_links:
    - from: "scripts/run-modernization.sh"
      to: "scripts/quality-baseline.sh"
      via: "shell call"
      pattern: "scripts/quality-baseline.sh"
    - from: "scripts/run-regression-checks.sh"
      to: "scripts/quality-baseline.sh"
      via: "shell call"
      pattern: "scripts/quality-baseline.sh"
---
<objective>
This plan integrates the newly created `quality-baseline.sh` script into the existing `run-modernization.sh` and `run-regression-checks.sh` scripts.

Purpose: To ensure that any automated modernization or regression test runs automatically enforce the defined quality policy, preventing policy bypass as per Requirement QG-01 and the success criteria.
Output: Updated `scripts/run-modernization.sh` and `scripts/run-regression-checks.sh` files.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-wire-modernization-to-quality-policy/RESEARCH.md
@.planning/phases/87-wire-modernization-to-quality-policy/87-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Update run-modernization.sh to include quality baseline check</name>
  <files>scripts/run-modernization.sh</files>
  <action>
    Edit `scripts/run-modernization.sh`. After `cargo fix --workspace --all-features --allow-dirty --allow-staged` and `cargo fmt --all`, insert a call to `scripts/quality-baseline.sh`.
    The insertion point should be after the formatting step and before the final success message or any git operations.

    The relevant section should be modified to include:
    ```bash
    # Ensure fixes and formatting are applied
    cargo fix --workspace --all-features --allow-dirty --allow-staged
    cargo fmt --all

    echo "Running quality baseline checks after modernization..."
    scripts/quality-baseline.sh
    ```
    Ensure the `set -euxo pipefail` at the beginning of the script ensures failure propagation.
  </action>
  <verify>
    Execute `scripts/run-modernization.sh`. It should run the modernization steps and then execute the quality baseline checks. Any failures in the quality baseline should cause `run-modernization.sh` to exit with a non-zero status.
  </verify>
  <done>
    `scripts/run-modernization.sh` successfully calls `scripts/quality-baseline.sh` and exits with non-zero status if quality checks fail.
  </done>
</task>

<task type="auto">
  <name>Update run-regression-checks.sh to include quality baseline check</name>
  <files>scripts/run-regression-checks.sh</files>
  <action>
    Edit `scripts/run-regression-checks.sh`. Insert a call to `scripts/quality-baseline.sh` at the beginning of the script, before any existing regression checks, to ensure the code under test itself adheres to the quality baseline.

    The insertion should be near the top, after `set -euxo pipefail` (if present) and any initial setup, like this:
    ```bash
    #!/bin/bash
    set -euxo pipefail

    echo "Running quality baseline checks before regression tests..."
    scripts/quality-baseline.sh

    # Existing regression checks follow here
    ```
  </action>
  <verify>
    Execute `scripts/run-regression-checks.sh`. It should first execute the quality baseline checks, then proceed with existing regression tests. Any failures in the quality baseline should cause `run-regression-checks.sh` to exit with a non-zero status.
  </verify>
  <done>
    `scripts/run-regression-checks.sh` successfully calls `scripts/quality-baseline.sh` and exits with non-zero status if quality checks fail.
  </done>
</task>

</tasks>

<verification>
Execute both `scripts/run-modernization.sh` and `scripts/run-regression-checks.sh` to confirm that they now correctly integrate and execute the `scripts/quality-baseline.sh` script, and fail if the quality baseline is not met.
</verification>

<success_criteria>
- `run-modernization.sh` successfully incorporates `scripts/quality-baseline.sh` and fails if the baseline is not met.
- `run-regression-checks.sh` successfully incorporates `scripts/quality-baseline.sh` and fails if the baseline is not met.
- All Phase 87 success criteria from ROADMAP.md are met:
    1. `run-modernization.sh` calls `scripts/quality-baseline.sh` for all audit checks.
    2. `run-regression-checks.sh` calls `scripts/quality-baseline.sh`.
    3. Policy ratchets (Tier 1/2) are enforced during automated cleanup runs (by `scripts/quality-baseline.sh` via `.cargo/config.toml`).
</success_criteria>

<output>
After completion, create `.planning/phases/87-wire-modernization-to-quality-policy/87-02-SUMMARY.md`
</output>
