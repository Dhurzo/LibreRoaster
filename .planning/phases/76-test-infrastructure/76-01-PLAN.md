---
phase: 76-test-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tests/common/mod.rs]
autonomous: true

must_haves:
  truths:
    - "All test files can import shared stubs from tests::common"
    - "StubHeater implements control::traits::Heater with call history"
    - "StubFan implements control::traits::Fan with call history"
    - "StubThermometer implements control::traits::Thermometer with configurable returns"
    - "reset_channels() and collect_output() enable test isolation"
  artifacts:
    - path: "tests/common/mod.rs"
      provides: "Shared test stubs and helper functions"
      contains: "StubHeater, StubFan, StubThermometer, reset_channels, collect_output"
  key_links:
    - from: "tests/common/mod.rs"
      to: "src/control/traits.rs"
      via: "use crate::control::traits::{Heater, Fan, Thermometer}"
---

<objective>
Create shared test stubs module to eliminate ~5x duplication in test helpers.

Purpose: Centralize StubHeater, StubFan, StubThermometer implementations that currently duplicate across ~5 test files.
Output: tests/common/mod.rs with shared stubs and helper functions.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

# Reference: Existing stub patterns from test files
@tests/mock_uart_integration.rs (lines 37-91 - StubHeater, StubFan, StubThermometer)
@tests/command_idempotence.rs (lines 12-57 - stub implementations)
@tests/fan_serialization.rs (lines 18-90 - StubFanWithTracking)

# Reference: Traits to implement
@src/control/traits.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/common/mod.rs with shared stubs and helpers</name>
  <files>tests/common/mod.rs</files>
  <action>
Create tests/common/mod.rs module with:

1. **Module setup**: `#[cfg(test)]` module, `use crate::control::traits::{Heater, Fan, Thermometer};`, `use crate::control::RoasterError;`

2. **StubHeater struct**:
   - Fields: `calls: RefCell<Vec<HeaterCall>>`, `status: SsrHardwareStatus`
   - HeaterCall enum: SetPower(f32)
   - Implement Heater trait: set_power records to calls, get_status returns stored status
   - Default returns Ok(()) for set_power, default status = Off

3. **StubFan struct**:
   - Fields: `calls: RefCell<Vec<FanCall>>`, `speed: RefCell<f32>`
   - FanCall enum: SetSpeed(f32)
   - Implement Fan trait: set_speed records and stores, get_speed returns stored
   - Default returns Ok(())

4. **StubThermometer struct**:
   - Fields: `calls: RefCell<Vec<ThermometerCall>>`, `temp: RefCell<f32>`
   - ThermometerCall enum: ReadTemperature
   - Implement Thermometer trait: read_temperature returns configured temp
   - Constructor accepts initial temperature value

5. **Helper functions**:
   - `reset_channels()` - helper for test isolation (clears any global state if needed)
   - `collect_output()` - helper to collect test output (placeholder for future use)
   - Add doc comments explaining each item

6. **Module exports**: pub use all structs and helper functions
  </action>
  <verify>
cargo build --tests 2>&1 | head -30
# Verify tests::common can be imported
grep -r "use tests::common" tests/ || echo "No imports yet - expected"
  </verify>
  <done>
tests/common/mod.rs exists with StubHeater, StubFan, StubThermometer, reset_channels(), collect_output()
All stubs implement their respective traits from control::traits
Module compiles with cargo build --tests
</done>
</task>

</tasks>

<verification>
1. Run `cargo build --tests` - should compile without errors
2. Verify stubs implement correct traits (Heater, Fan, Thermometer)
3. Confirm RefCell is used for interior mutability (per STATE.md decision)
</verification>

<success_criteria>
- tests/common/mod.rs created
- StubHeater implements Heater with call history tracking
- StubFan implements Fan with call history tracking  
- StubThermometer implements Thermometer with configurable temperature
- reset_channels() and collect_output() helper functions exist
- cargo build --tests passes
</success_criteria>

<output>
After completion, create `.planning/phases/76-test-infrastructure/76-01-SUMMARY.md`
</output>
