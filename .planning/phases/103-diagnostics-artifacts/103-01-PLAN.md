---
phase: 103-diagnostics-artifacts
plan: 01
type: execute
wave: 1
  - 102-01
files_modified:
  - scripts/collect_safe_shutdown.py
  - scripts/test_traceability_matrix.py
  - internalDoc/INSTRUMENTATION_README.MD
autonomous: true
must_haves:
  truths:
    - Auditors can run `scripts/collect_safe_shutdown.py` to bundle a safe-shutdown TRACE log, a trace matrix CSV, and metadata about the guard failure into a single artifact.
    - The artifact zip contains the original log, `traceability.csv` produced by `scripts/traceability_matrix.py`, and `metadata.json` with `TraceId`, `watchdog_failure`, `error_category`, and `error_source` for the failure row so auditors can prove the guard event’s diagnostics.
    - `internalDoc/INSTRUMENTATION_README.MD` explains how to run the CLI, inspect the artifact, and replay the matrix so the failure path stays reproducible.
  artifacts:
    - path: "scripts/collect_safe_shutdown.py"
      provides: "CLI that packages a TRACE log, traceability matrix, and metadata into a reusable zip artifact describing the safe-shutdown guard failure."
    - path: "scripts/test_traceability_matrix.py"
      provides: "Regression coverage that runs the CLI on `logs/traceability/sample-safe-shutdown.log` and asserts the artifact contains the guard diagnostics."
    - path: "internalDoc/INSTRUMENTATION_README.MD"
      provides: "Guide describing how to run the CLI, what files the artifact contains, and how auditors can replay the matrix and inspect `metadata.json`."
  key_links:
    - from: "scripts/collect_safe_shutdown.py"
      to: "logs/traceability/sample-safe-shutdown.log"
      via: "CLI argument supplies the log and writes it into the artifact before zipping."
    - from: "scripts/collect_safe_shutdown.py"
      to: "scripts/traceability_matrix.py"
      via: "Reuses the parser helpers to emit `traceability.csv` inside the artifact so the guard row metadata is preserved."
    - from: "internalDoc/INSTRUMENTATION_README.MD"
      to: "scripts/collect_safe_shutdown.py"
      via: "Documentation references the CLI name, arguments, and expected `safe-shutdown-replay.zip` contents so auditors can reproduce the failure path."
---

<objective>
Package safe-shutdown guard TRACE failures into reproducible artifacts, expand regression checks, and document the replay workflow so auditors can rerun the failure trace without hardware.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/103-diagnostics-artifacts/103-CONTEXT.md
@.planning/phases/103-diagnostics-artifacts/103-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bundle the safe-shutdown artifact</name>
  <files>scripts/collect_safe_shutdown.py</files>
  <action>Create `scripts/collect_safe_shutdown.py`, a CLI that accepts `--log` and `--output`. It must parse the given TRACE log with the helpers from `scripts.traceability_matrix`, emit `traceability.csv` with the queue→actuator→telemetry→guard rows, capture the guard row metadata (`TraceId`, `watchdog_failure`, `error_category`, `error_source`) into `metadata.json`, and zip the log + CSV + metadata plus a README snapshot under `--output`. Include helper flags for `--artifact-name`/`--force` so auditors can regenerate the bundle.</action>
  <verify>`python scripts/collect_safe_shutdown.py logs/traceability/sample-safe-shutdown.log --output logs/traceability/safe-shutdown-replay.zip`</verify>
  <done>The CLI packages the sample safe-shutdown trace into `safe-shutdown-replay.zip` containing the raw log, `traceability.csv`, `metadata.json`, and a README entry that documents the guard failure details.</done>
</task>

<task type="auto">
  <name>Task 2: Cover artifact packaging with a regression test</name>
  <files>scripts/test_traceability_matrix.py</files>
  <action>Add `TestTraceabilityMatrix.test_collect_safe_shutdown_artifact` that runs the new CLI in a temp directory, opens the resulting zip, confirms `traceability.csv` exists, and asserts the guard metadata JSON includes `watchdog_failure=init_error_failure` plus the AppError fields found in `logs/traceability/sample-safe-shutdown.log`. Keep the new test lightweight so it can run inside the current `unittest` suite.</action>
  <verify>`PYTHONPATH=. python3 scripts/test_traceability_matrix.py`</verify>
  <done>The regression suite runs the artifact CLI, inspects the zip, and proves the zipped metadata mirrors the guard failure row from the sample log.</done>
</task>

<task type="auto">
  <name>Task 3: Document how to replay the safe-shutdown artifact</name>
  <files>internalDoc/INSTRUMENTATION_README.MD</files>
  <action>Add a "Safe-Shutdown Replay Artifact" section that explains how to run `scripts/collect_safe_shutdown.py` against `logs/traceability/sample-safe-shutdown.log`, what files the zip contains (`traceability.csv`, `metadata.json`, log), and how to rerun `scripts/traceability_matrix.py` on the artifact to regenerate the matrix row. Include commands auditors can copy/paste and mention that the metadata JSON highlights `watchdog_failure=init_error_failure`, `error_category=initialization`, and `error_source=hardware_init_failed`.
  <verify>`python scripts/collect_safe_shutdown.py logs/traceability/sample-safe-shutdown.log --output logs/traceability/safe-shutdown-replay.zip`</verify>
  <done>The instrumentation guide now captures the CLI usage, artifact contents, and replay steps so auditors can reproduce the failure path without running hardware.</done>
</task>

</tasks>

<verification>
- `python scripts/collect_safe_shutdown.py logs/traceability/sample-safe-shutdown.log --output logs/traceability/safe-shutdown-replay.zip`
- `PYTHONPATH=. python3 scripts/test_traceability_matrix.py`
- `python3 scripts/traceability_matrix.py logs/traceability/sample-safe-shutdown.log`
</verification>

<success_criteria>
1. The CLI packages the safe-shutdown log, CSV matrix, and guard metadata into `logs/traceability/safe-shutdown-replay.zip` so auditors have one artifact to ship.
2. Regression coverage verifies the artifact contains the guard failure metadata and the script can be rerun every time the sample log changes.
3. Instrumentation docs explain how to run the CLI, describe the artifact contents, and show auditors how to replay the matrix.
</success_criteria>

<output>
After completion, create `.planning/phases/103-diagnostics-artifacts/103-01-SUMMARY.md`
</output>
