---
phase: 44-protocol-framing-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/output/artisan.rs
autonomous: true

must_haves:
  truths:
    - "READ responses contain exactly four CSV values in ET,BT,HEATER,FAN order with one-decimal precision"
    - "READ responses substitute 0.0 for missing or invalid (NaN/inf) values"
    - "READ responses contain no spaces or extra prefixes/suffixes"
  artifacts:
    - path: "src/output/artisan.rs"
      provides: "READ response formatting and value normalization"
      contains: "format_read_response_full"
  key_links:
    - from: "src/application/tasks.rs"
      to: "ArtisanFormatter::format_read_response_full"
      via: "READ command handling"
      pattern: "format_read_response_full"
---

<objective>
Lock READ CSV formatting to strict 4-value output with placeholder handling.

Purpose: Ensure READ responses always match the framing contract regardless of sensor validity.
Output: Sanitized formatter logic and tests that validate placeholder behavior.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-protocol-framing-contract/44-CONTEXT.md
@.planning/phases/44-protocol-framing-contract/44-RESEARCH.md

@src/output/artisan.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize READ response values to strict CSV</name>
  <files>src/output/artisan.rs</files>
  <action>
Add a helper that normalizes f32 values for READ responses: if value is NaN or infinite, return 0.0; otherwise return the original value. Use the helper for each field in `format_read_response` and `format_read_response_full` (ET, BT, HEATER, FAN) before formatting. Keep the CSV format exact with `{:.1}` precision, no spaces after commas, and no terminators or extra prefixes/suffixes. Do not alter the output boundary or append CRLF in the formatter.
  </action>
  <verify>`cargo test format_read_response`</verify>
  <done>READ responses always format as `a,b,c,d` with one-decimal precision and invalid values map to 0.0.</done>
</task>

<task type="auto">
  <name>Task 2: Add READ response invalid-value regression tests</name>
  <files>src/output/artisan.rs</files>
  <action>
Extend the READ response tests to cover invalid values. Add at least two tests that pass NaN and infinity into `format_read_response_full` and `format_read_response` and assert the output uses `0.0` in those positions while preserving the other fields and 4-value CSV shape. Keep test names specific (for example, `test_format_read_response_full_invalid_values`).
  </action>
  <verify>`cargo test format_read_response_invalid_values`</verify>
  <done>READ response tests prove NaN/inf are clamped to 0.0 with a 4-value CSV output.</done>
</task>

</tasks>

<verification>
`cargo test format_read_response` passes without failures.
</verification>

<success_criteria>
READ responses are emitted as a strict 4-field CSV with one-decimal precision and placeholder handling for invalid values.
</success_criteria>

<output>
After completion, create `.planning/phases/44-protocol-framing-contract/44-01-SUMMARY.md`
</output>
