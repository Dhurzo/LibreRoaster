---
phase: 44-protocol-framing-contract
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/application/tasks.rs
  - src/application/app_builder.rs
  - src/hardware/usb_cdc/tasks.rs
  - src/hardware/uart/tasks.rs
  - src/input/mod.rs
autonomous: true

must_haves:
  truths:
    - "READ responses end with exactly one CRLF over USB CDC"
    - "READ responses end with exactly one CRLF over UART"
    - "Only the dual output boundary appends CRLF for response payloads"
  artifacts:
    - path: "src/application/tasks.rs"
      provides: "Dual output boundary appends CRLF"
      contains: "dual_output_task"
    - path: "src/hardware/usb_cdc/tasks.rs"
      provides: "USB writer does not append CRLF"
      contains: "usb_writer_task"
    - path: "src/hardware/uart/tasks.rs"
      provides: "UART send_response/send_stream route raw payloads"
      contains: "send_response"
    - path: "src/application/app_builder.rs"
      provides: "Only dual_output_task handles response output"
      contains: "start_tasks"
  key_links:
    - from: "src/application/tasks.rs"
      to: "CommChannel::Usb"
      via: "dual_output_task write_bytes"
      pattern: "dual_output_task"
    - from: "src/hardware/uart/tasks.rs"
      to: "ServiceContainer::get_output_channel"
      via: "send_response/send_stream"
      pattern: "get_output_channel"
    - from: "src/application/app_builder.rs"
      to: "dual_output_task"
      via: "spawner.spawn"
      pattern: "dual_output_task"
---

<objective>
Centralize CRLF termination at the dual output boundary.

Purpose: Eliminate double terminators and ensure consistent framing across USB CDC and UART.
Output: Single output boundary that appends CRLF once for all responses.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-protocol-framing-contract/44-CONTEXT.md
@.planning/phases/44-protocol-framing-contract/44-RESEARCH.md

@src/application/tasks.rs
@src/application/app_builder.rs
@src/hardware/usb_cdc/tasks.rs
@src/hardware/uart/tasks.rs
@src/input/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route UART/USB outputs through the single CRLF boundary</name>
  <files>src/hardware/usb_cdc/tasks.rs, src/hardware/uart/tasks.rs</files>
  <action>
Remove CRLF appending from `usb_writer_task` and from `send_response`/`send_stream`. Update `send_response` and `send_stream` to push the raw payload into `ServiceContainer::get_output_channel()` as a `heapless::String<128>` (or compatible size) without any terminator. Keep any logging on the raw payload only. The only CRLF append should remain inside `dual_output_task` in `src/application/tasks.rs`.
  </action>
  <verify>`cargo build`</verify>
  <done>USB/UART helpers no longer append CRLF and responses flow as raw payloads to the output channel.</done>
</task>

<task type="auto">
  <name>Task 2: Ensure only dual_output_task handles outbound responses</name>
  <files>src/application/app_builder.rs, src/input/mod.rs</files>
  <action>
Update task spawning so outbound responses are written only by `dual_output_task`. Remove spawning of `usb_writer_task` and `uart_writer_task` from `Application::start_tasks` in `src/application/app_builder.rs`. If `start_uart_tasks` in `src/input/mod.rs` also spawns `uart_writer_task`, remove it there as well to avoid duplicate output boundaries. Keep `uart_reader_task`, `usb_reader_task`, `dual_output_task`, and `control_loop_task` running.
  </action>
  <verify>`cargo build`</verify>
  <done>`dual_output_task` is the sole writer that appends CRLF for outbound responses.</done>
</task>

</tasks>

<verification>
`cargo build` succeeds and code paths show a single CRLF append in `dual_output_task` only.
</verification>

<success_criteria>
READ responses are terminated exactly once with CRLF across USB CDC and UART, with no duplicate terminators.
</success_criteria>

<output>
After completion, create `.planning/phases/44-protocol-framing-contract/44-02-SUMMARY.md`
</output>
