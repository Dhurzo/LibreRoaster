---
phase: 99-embedded-build-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - 95-build-verification.md
autonomous: true

must_haves:
  truths:
    - "The audited embedded build command is repeatable and produces the validated binaries required for BUILD-01."
    - "95-build-verification.md records the command, environment fingerprint, build output, and artifact locations so auditors trust the claim."
    - "README.md and internalDoc/DEVELOPMENT.md can cite the audit artifact when pointing readers to the verified command."
  artifacts:
    - path: "95-build-verification.md"
      provides: "Audit log proving the embedded release build command executes cleanly and lists the resulting artifacts."
      contains: "cargo build --release --target riscv32imc-unknown-none-elf --features embedded"
    - path: "README.md"
      provides: "Public build instructions referencing the verified command and linking to the audit artifact."
      contains: "cargo build --release --target riscv32imc-unknown-none-elf --features embedded"
    - path: "internalDoc/DEVELOPMENT.md"
      provides: "Internal development guide that mirrors the verified command and artifact references."
      contains: "cargo build --release --target riscv32imc-unknown-none-elf --features embedded"
  key_links:
    - from: "95-build-verification.md"
      to: "target/riscv32imc-unknown-none-elf/release/libreroaster"
      via: "Documented `ls -lh` output so auditors can inspect the ELF binary the command produced."
      pattern: "libreroaster"
    - from: "95-build-verification.md"
      to: "README.md"
      via: "Audit doc points readers back to README whenever BUILD-01 build instructions are referenced."
      pattern: "README\.md"
---

<objective>
Re-run the embedded release build that satisfies BUILD-01, capture its console output and artifacts, and publish the audit-ready `95-build-verification.md` so the command and results are provable.

Purpose: Without a recorded build run we cannot demonstrate that the Phase 95 claims about binary production and the `target/riscv32imc-unknown-none-elf/release` artifact still hold.

Output: A captured build run (with environment metadata) plus an audit artifact detailing the command, any ESP flash image, and the successful artifacts for auditors and future maintainers.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/95-fix-critical-build-blockers/95-01-SUMMARY.md
@README.md
@internalDoc/DEVELOPMENT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run the embedded release build and capture artifacts</name>
  <files>target/riscv32imc-unknown-none-elf/release/libreroaster</files>
  <action>
    Record the environment fingerprint (`rustc --version`, `cargo --version`, `git rev-parse HEAD`), run `cargo build --release --target riscv32imc-unknown-none-elf --features embedded`, and then run `espflash save-image --chip esp32c3 target/riscv32imc-unknown-none-elf/release/libreroaster libreroaster.bin`. Capture the stdout/stderr (either tee to a temp log or copy the relevant excerpts) so the audit doc can quote the command output and binary size. Ensure both `target/riscv32imc-unknown-none-elf/release/libreroaster` and the generated `libreroaster.bin` exist.
  </action>
  <verify>
    test -f target/riscv32imc-unknown-none-elf/release/libreroaster && test -f libreroaster.bin
  </verify>
  <done>The embedded build command succeeded, target release directory contains `libreroaster`, and `libreroaster.bin` exists with size info captured for the audit doc.</done>
</task>

<task type="auto">
  <name>Task 2: Draft `95-build-verification.md` audit artifact</name>
  <files>95-build-verification.md</files>
  <action>
    Create `95-build-verification.md` with sections for: environment fingerprints, the exact `cargo build --release --target riscv32imc-unknown-none-elf --features embedded` command (with time-stamped output summary), the `espflash save-image` invocation that produced `libreroaster.bin`, artifact paths (ELF and .bin), and the BUILD-01 claim this run satisfies. Reference Phase 95's summary so the audit doc explains how this verification proves the earlier binary production claim. Mention where `README.md`/`internalDoc/DEVELOPMENT.md` point readers for the command.
  </action>
  <verify>rg -n "cargo build --release --target riscv32imc-unknown-none-elf --features embedded" 95-build-verification.md</verify>
  <done>`95-build-verification.md` documents the environment, exact commands run, artifact paths (`target/.../libreroaster`, `libreroaster.bin`), and ties the run back to BUILD-01/Phase 95 before pointing to README/DEVELOPMENT for future reproduction.</done>
</task>

</tasks>

<verification>
Confirm the targeted build command produces both the ELF and `.bin` artifacts and that `95-build-verification.md` records the environment, commands, artifact paths, and BUILD-01 tie-in so auditors can rerun the flow.
</verification>

<success_criteria>
- `cargo build --release --target riscv32imc-unknown-none-elf --features embedded` finishes successfully and `espflash save-image` produces `libreroaster.bin`.
- Verified artifacts (`target/riscv32imc-unknown-none-elf/release/libreroaster`, `libreroaster.bin`) are noted for auditors in the audit doc.
- `95-build-verification.md` clearly communicates environment, commands, outputs, and links to README/DEVELOPMENT.
</success_criteria>

<output>
After completion, create `.planning/phases/99-embedded-build-verification/99-01-SUMMARY.md`
</output>
