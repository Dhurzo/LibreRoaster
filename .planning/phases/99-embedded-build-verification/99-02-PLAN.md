---
phase: 99-embedded-build-verification
plan: 02
type: execute
wave: 2
depends_on:
  - 99-01
files_modified:
  - README.md
  - internalDoc/DEVELOPMENT.md
autonomous: true

must_haves:
  truths:
    - "README.md and internalDoc/DEVELOPMENT.md describe the audited `cargo build --release --target riscv32imc-unknown-none-elf --features embedded` command so BUILD-01 remains reproducible."
    - "Both documents point readers to `95-VERIFICATION.md` for the audited proof, including the artifact paths so the claim is traceable."
    - "Future maintainers can follow either document to rerun the build and see the binaries referenced in the audit."
  artifacts:
    - path: "README.md"
      provides: "Public build instructions referencing the verified command and audit artifact."
      contains: "95-VERIFICATION.md"
    - path: "internalDoc/DEVELOPMENT.md"
      provides: "Internal development guide that mirrors the verified command and includes the audit artifact link."
      contains: "95-VERIFICATION.md"
  key_links:
    - from: "README.md"
      to: "95-VERIFICATION.md"
      via: "README now references the audit artifact whenever the embedded build command is mentioned."
      pattern: "95-build-verification\.md"
    - from: "internalDoc/DEVELOPMENT.md"
      to: "95-VERIFICATION.md"
      via: "Development guide links to the audit document to show the verified artifacts and artifact paths."
      pattern: "95-build-verification\.md"
---

<objective>
Align the public and internal documentation with the verified embedded build workflow so BUILD-01 is provably reproducible and operable from both README and DEVELOPMENT.md.

Purpose: The audited command only matters if the docs that people read match the proof we just captured, otherwise narrative and proof diverge.

Output: README and DEVELOPMENT both mention the exact `cargo build --release --target riscv32imc-unknown-none-elf --features embedded` command, link to `95-VERIFICATION.md`, and mention the resulting binary paths (`target/.../libreroaster`, `libreroaster.bin`).
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/95-fix-critical-build-blockers/95-01-SUMMARY.md
@README.md
@internalDoc/DEVELOPMENT.md
@95-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README build instructions with audit link</name>
  <files>README.md</files>
  <action>
    Add or refresh the build section to call out the audited command, the `target/riscv32imc-unknown-none-elf/release/libreroaster` artifact, and the generated `libreroaster.bin`. Note the `95-VERIFICATION.md` proof so readers know where to find the logged console output and artifact sizes. If the command already appears, emphasize the audit link and the binary paths so the doc aligns with the proof.
  </action>
  <verify>rg -n "95-build-verification\.md" README.md && rg -n "cargo build --release --target riscv32imc-unknown-none-elf --features embedded" README.md</verify>
  <done>README.md mentions the audited command, lists the elf/bin artifacts, and points to `95-VERIFICATION.md` for the logged proof.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror the audit reference in DEVELOPMENT.md</name>
  <files>internalDoc/DEVELOPMENT.md</files>
  <action>
    Ensure DEVELOPMENT.md describes the verified `cargo build --release --target riscv32imc-unknown-none-elf --features embedded` workflow, calls out the ELF/`.bin` artifacts, and links to `95-VERIFICATION.md` so internal readers can see the audit log. Update the build section to mention the audit artifact, how to rerun the command, and where to find the binaries produced by `espflash save-image`.
  </action>
  <verify>rg -n "95-build-verification\.md" internalDoc/DEVELOPMENT.md && rg -n "libreroaster" internalDoc/DEVELOPMENT.md</verify>
  <done>DEVELOPMENT.md highlights the audited command, references `95-VERIFICATION.md`, and reminds readers to look for `target/.../libreroaster` and `libreroaster.bin` after running the command.</done>
</task>

</tasks>

<verification>
Confirm both README.md and internalDoc/DEVELOPMENT.md mention the verified build command, the ELF/`.bin` artifacts, and the `95-VERIFICATION.md` proof so BUILD-01 remains traceable and reproducible.
</verification>

<success_criteria>
- README.md lists the verified embedded build command, artifact paths, and `95-VERIFICATION.md` link.
- internalDoc/DEVELOPMENT.md mirrors the command/artefact description and references the audit document.
- Both docs give maintainers runnable commands and point to the new audit artifact.
</success_criteria>

<output>
After completion, create `.planning/phases/99-embedded-build-verification/99-02-SUMMARY.md`
</output>
