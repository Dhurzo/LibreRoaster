---
phase: 70-deterministic-control-pulse
plan: 02
type: execute
wave: 2
depends_on:
  - 70-01
files_modified:
  - src/config/constants.rs
  - src/control/roaster_refactored.rs
  - src/output/artisan.rs
autonomous: true

must_haves:
  truths:
    - "Each STATUS snapshot now appends PV, MV, integrator accumulator, derivative rate, saturation flag, and derivative-availability flag to the deterministic column tail."
    - "`SystemStatus` exposes PV/MV/integrator/derivative/saturation instrumentation fields that `RoasterControl` updates before telemetry is sent."
    - "Integral clamp and derivative-availability flags flip whenever the SSR guard saturates the pre-guard output or the PV delta is finite for the tick, respectively."
  artifacts:
    - path: "src/config/constants.rs"
      provides: "SystemStatus definition extended with the new instrumentation fields plus sensible defaults for the added telemetry columns."
      min_lines: 220
    - path: "src/control/roaster_refactored.rs"
      provides: "Control update routine that computes desired output, tracks derivative deltas, and toggles saturation/integrator flags before telemetry uses the snapshot."
      min_lines: 320
    - path: "src/output/artisan.rs"
      provides: "STATUS formatter that appends the instrumentation tail to the deterministic CSV so automation can parse PV/MV/integrator/derivative/saturation columns in a fixed order."
      min_lines: 260
  key_links:
    - from: "src/output/artisan.rs"
      to: "src/control/roaster_refactored.rs::RoasterControl::update_control"
      via: "Formatter serializes the SystemStatus fields populated right before telemetry emission."
      pattern: "format_status_response"
    - from: "src/control/roaster_refactored.rs"
      to: "src/config/constants.rs::SystemStatus"
      via: "Stores PV/MV/integrator/derivative/saturation instrumentation on each tick."
      pattern: "SystemStatus"
---

<objective>
Extend the Phase 70 telemetry contract so every STATUS response carries PV, MV, integrator accumulator, derivative rate, and saturation/derivative-availability flags while keeping the earlier columns frozen.
Purpose: Let automation consume a deterministic CSV schema that now reflects the same tick’s control state and saturation clues.
Output: An augmented `SystemStatus`, `RoasterControl` instrumentation, and a STATUS formatter that appends the new columns at the tail.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-deterministic-control-pulse/70-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture PV/MV/integrator/derivative/saturation metrics</name>
  <files>src/config/constants.rs, src/control/roaster_refactored.rs</files>
  <action>
1. Add instrumentation fields to `SystemStatus`: `pv`, `mv`, `integrator_value`, `derivative_rate`, `saturation_active`, `integrator_clamped`, and `derivative_available`, plus defaults in the `Default` implementation so STATUS snapshots stay well formed.
2. Give `RoasterControl` a private `Option<f32>` (and optionally a timestamp) that remembers the previous bean temperature so `update_control` can compute a derivative rate (ΔPV/Δt) each tick and set `status.derivative_available` when a finite delta exists.
3. In `update_control`, capture the desired output before calling `apply_guarded_heater`, then assign `status.integrator_value = desired_output`, `status.mv = applied_output`, `status.pv = status.bean_temp`, and `status.derivative_rate` using the new delta logic; set `status.saturation_active`/`status.integrator_clamped` to `true` whenever `apply_guarded_heater` detects a busy window and clear them otherwise.
4. Expose the derivative availability flag only when the previous PV sample exists so automation can tell if derivative filtering is quiet.
  </action>
  <verify>cargo test --lib</verify>
  <done>`SystemStatus` now carries PV/MV/integrator/derivative/saturation fields that `RoasterControl` updates each tick before telemetry snapshots are emitted.</done>
</task>

<task type="auto">
  <name>Task 2: Append the instrumentation tail to STATUS telemetry</name>
  <files>src/output/artisan.rs</files>
  <action>
1. Update `ArtisanFormatter::format_status_response` so after the existing nine columns it appends the new instrumentation values in a fixed order: PV, MV, integrator accumulator, derivative rate, saturation flag (0/1), integral clamp flag (0/1), derivative availability flag (0/1).
2. Update the helper tests (e.g., `test_format_status_response_columns_order`) to assert the expanded column count, confirm the new numeric values, and verify the flags flip when `SystemStatus` fields simulate saturation or missing derivative inputs.
3. Keep the regression/status formatting helpers that expect nine columns untouched apart from the tail so automation parsing earlier columns remains stable.
  </action>
  <verify>cargo test --lib</verify>
  <done>Status formatter continues to produce deterministic CSV lines with the new instrumentation tail and the tests assert the column count and flag behavior.</done>
</task>

</tasks>

<verification>
- `cargo test --lib`
</verification>

<success_criteria>
1. `SystemStatus` exposes the new instrumentation fields and `RoasterControl` sets them before telemetry flows out of the loop.
2. `format_status_response` appends PV/MV/integrator/derivative/saturation columns without touching the existing guarded columns so automation parsing stays deterministic.
3. The saturation/integrator clamp flags follow the guard’s busy state, and the derivative availability flag only flips on ticks with a finite PV delta.
</success_criteria>

<output>
After completion, create `.planning/phases/70-deterministic-control-pulse/70-02-SUMMARY.md`
</output>
