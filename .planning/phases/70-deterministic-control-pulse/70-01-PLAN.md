---
phase: 70-deterministic-control-pulse
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/application/tasks.rs
autonomous: true

must_haves:
  truths:
    - "Every 100 ms control loop iteration marks and logs sensor sampling → PID/LEDC → watchdog feed → telemetry emission before sleeping."
    - "WatchdogFeeder, LedcGuard, and the regression hooks still populate the same SystemStatus fields so automation can map instrumentation to the Phase 69 contracts."
    - "`Timer::after(Duration::from_millis(100))` executes only after telemetry emission and the stage tracker resets, keeping the tick boundary deterministic."
  artifacts:
    - path: "src/application/tasks.rs"
      provides: "The single heartbeat (`control_loop_task`) that drains Artisan commands, samples sensors, runs control updates, feeds watchdogs, and emits STATUS telemetry in one 100 ms tick."
      min_lines: 250
  key_links:
    - from: "src/application/tasks.rs"
      to: "src/safety/watchdog.rs"
      via: "ServiceContainer::with_watchdog(|watchdog| watchdog.feed_async(status.bean_temp))"
      pattern: "watchdog.feed_async"
    - from: "src/application/tasks.rs"
      to: "src/hardware/ledc_guard.rs"
      via: "ledc_guard::total_timeouts()"
      pattern: "ledc_guard::total_timeouts"
---

<objective>
Keep the existing heartbeat loop as the authoritative 100 ms pulse by tagging each stage, reusing the same watchdog/guard instrumentation, and delaying the timer call until telemetry is finished.
Purpose: Guarantee the instrumentation log order (sensor → PID/LEDC → watchdog → telemetry) and prevent any drift of the 100 ms boundary before the loop sleeps.
Output: A staged tracker inside `control_loop_task` that logs each transition and a clear separation between instrumentation and the final `Timer::after` wait.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-deterministic-control-pulse/70-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Log sensor → control → LEDC → watchdog → telemetry stages</name>
  <files>src/application/tasks.rs</files>
  <action>
1. Introduce a lightweight stage tracker (enum + timestamped markers) near the top of `control_loop_task` so each loop clearly names the current phase (sensor read, control update/LEDC write, watchdog feed, telemetry emission).
2. After draining Artisan commands, set the tracker to `SensorRead`, await `ServiceContainer::roaster_async_sensor_read()`, and emit a `debug!` message that includes the `Instant::now()` delta from the tick start so the instrumentation log proves the stage order.
3. After `update_control` and before watchdog instrumentation, mark the `ControlUpdate`/`LedcWrite` stages, log the desired versus applied SSR/ fan values, and capture the pre-guard output so saturation can be detected later.
4. Retain the existing `ServiceContainer::with_watchdog` and `ledc_guard::total_timeouts()` calls to update the status, and mark `WatchdogFeed` once those helpers run so the instrumentation trace remains tied to the same tick.
  </action>
  <verify>cargo test --lib</verify>
  <done>Each tick emits debug-worthy instrumentation that follows sensor → PID/LEDC → watchdog → telemetry so automation can verify the sequencing within the 100 ms boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Close the loop by resetting instrumentation before the timer wait</name>
  <files>src/application/tasks.rs</files>
  <action>
1. After telemetry is emitted (or skipped when continuous mode is off), mark the tracker’s final `TelemetryEmit` stage, log that the tick finished, and clear any stage state so the next iteration starts with a clean slate.
2. Ensure `Timer::after(Duration::from_millis(100)).await` stays at the end of the loop so instrumentation completes before sleeping; if necessary, move the timer invocation below the logging/clear step.
3. Add one last debug line that summarizes whether guard timeouts or watchdog failures occurred during the tick so downstream automation can correlate those failures with the recorded stage order.
  </action>
  <verify>cargo test --lib</verify>
  <done>`Timer::after(Duration::from_millis(100))` runs only after telemetry emission and the stage tracker reset, which keeps the 100 ms tick deterministic.</done>
</task>

</tasks>

<verification>
- `cargo test --lib`
</verification>

<success_criteria>
1. `control_loop_task` logs and timestamps the sensor → control → LEDC → watchdog → telemetry stages inside each tick.
2. The per-tick stage tracker is cleared before `Timer::after(Duration::from_millis(100)).await` so the next iteration always starts from the top of the instrumentation chain.
3. WatchdogFeeder, LedcGuard, and regression instrumentation continue to feed the existing SystemStatus fields without duplication.
</success_criteria>

<output>
After completion, create `.planning/phases/70-deterministic-control-pulse/70-01-SUMMARY.md`
</output>
