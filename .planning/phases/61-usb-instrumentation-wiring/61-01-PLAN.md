---
phase: 61-usb-instrumentation-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/usb_instrumentation_runner.rs
  - internalDoc/INSTRUMENTATION_README.MD
autonomous: true
must_haves:
  truths:
    - "The instrumentation harness actually calls `process_usb_command_data_test` so the exported hook is executed during the documented integration run."
    - "Instrumentation documentation explains where the hook lives and why this run exercises it, leaving no ambiguity for future auditors."
  artifacts:
    - path: "tests/usb_instrumentation_runner.rs"
      provides: "Dedicated instrumentation harness that enqueues a representative USB command via the test-only helper."
    - path: "internalDoc/INSTRUMENTATION_README.MD"
      provides: "Reference describing the wiring, the instrumentation run that exercises it, and its audit rationale."
  key_links:
    - from: "tests/usb_instrumentation_runner.rs"
      to: "src/hardware/usb_cdc/tasks.rs"
      via: "process_usb_command_data_test"
      pattern: "process_usb_command_data_test"
    - from: "internalDoc/INSTRUMENTATION_README.MD"
      to: "tests/usb_instrumentation_runner.rs"
      via: "documentation reference"
      pattern: "usb_instrumentation_runner"
---

<objective>
Push the previously unused `process_usb_command_data_test` helper into a real instrumentation consumer and document the wiring so the milestone’s integration gap is closed.

Purpose: Ensures the hook is executed by a reproducible run and that downstream agents know where to look.
Output: A host/integration runner that invokes the hook plus a short instrumentation README describing the wiring.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-usb-instrumentation-wiring/61-CONTEXT.md
@.planning/phases/61-usb-instrumentation-wiring/61-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Hook instrumentation runner</name>
  <files>tests/usb_instrumentation_runner.rs</files>
  <action>Create a target-specific integration test (e.g., `tests/usb_instrumentation_runner.rs` with `#![cfg(all(test, target_arch = "riscv32"))]`) that resets the `ServiceContainer`, enables the USB multiplexer, and calls `libreroaster::hardware::usb_cdc::tasks::process_usb_command_data_test(b"READ\r")` so the helper enqueues a command and exercises the instrumentation hook. Confirm the test submits the command into the artisan channel and exits cleanly to prove the run actually hits the hook.</action>
  <verify>cargo test --test usb_instrumentation_runner --target riscv32imc-unknown-none-elf</verify>
  <done>The instrumentation harness compiles, runs, and demonstrates a command was emitted via the USB queue after the helper was invoked; the test exits without panics.</done>
</task>

<task type="auto">
  <name>Document the wiring</name>
  <files>internalDoc/INSTRUMENTATION_README.MD</files>
  <action>Write/update `internalDoc/INSTRUMENTATION_README.MD` to name the new instrumentation run (`tests/usb_instrumentation_runner.rs`), describe that it exists solely to call `process_usb_command_data_test`, and explain that the run closes the unused-export gap without rehashing the success criteria or prescribing rerun steps.</action>
  <verify>python3 - <<'PY'
import pathlib
text = pathlib.Path('internalDoc/INSTRUMENTATION_README.MD').read_text()
assert 'process_usb_command_data_test' in text
assert 'usb_instrumentation_runner' in text
PY</verify>
  <done>The internal instrumentation README references the harness, the hooked helper, and why the run exists so future auditors understand the intent.</done>
</task>

</tasks>

<verification>
- `cargo test --test usb_instrumentation_runner --target riscv32imc-unknown-none-elf` confirms the wired hook executes.
- The README contains both `process_usb_command_data_test` and the harness name so the documentation is discoverable.
</verification>

<success_criteria>
- New instrumentation harness exists and enqueues a USB command via the test-only helper.
- Documentation points at the harness and explains the wiring rationale.
- The instrumentation run defined in the README is the one auditors should run to prove the hook is exercised.
</success_criteria>

<output>
After completion, create `.planning/phases/61-usb-instrumentation-wiring/61-01-SUMMARY.md`
</output>
