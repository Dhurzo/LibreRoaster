---
phase: 97-traceability-matrix-tooling
plan: 03
type: execute
wave: 3
depends_on: [97-02]
files_modified:
  - internalDoc/INSTRUMENTATION_README.MD
  - internalDoc/TRACEABILITY_MATRIX.md
autonomous: true

must_haves:
  truths:
    - "The instrumentation guide describes the new `TRACE` stream, how TraceIds connect command → queue → actuator → telemetry → guard, and the parser script that translates it into a matrix."
    - "A dedicated traceability matrix reference document tells auditors how to capture a trace log, run `scripts/traceability_matrix.py`, and interpret the resulting columns."
  artifacts:
    - path: "internalDoc/INSTRUMENTATION_README.MD"
      provides: "Telemetry guide that now includes instructions for the TRACE stream and host parser."
      contains: "TRACE stream", "scripts/traceability_matrix.py"
    - path: "internalDoc/TRACEABILITY_MATRIX.md"
      provides: "Stand‑alone triage checklist describing command capture, matrix columns, and guard/watchdog interpretation."
      contains: "TraceId", "Traceability matrix", "logs/traceability/sample-trace.log"
  key_links:
    - from: "internalDoc/INSTRUMENTATION_README.MD"
      to: "scripts/traceability_matrix.py"
      via: "Guide now references the host parser and its sample log so auditors know how to generate the matrix."
      pattern: "traceability_matrix.py"
---

<objective>
Document the new traceability pipeline so regression triage runs know where to find the TRACE output, how to run the parser, and how to interpret the matrix.

Purpose: Engineers and auditors must be able to recreate the command → queue → actuator → telemetry → guard matrix without reading the code itself. Clear doc pages close the loop for SOLID-03 signoff.

Output: Updated `INSTRUMENTATION_README.MD` with a TRACE section and a new `internalDoc/TRACEABILITY_MATRIX.md` containing a triage checklist and column descriptions.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/97-traceability-matrix-tooling/97-RESEARCH.md
@.planning/phases/97-traceability-matrix-tooling/97-02-PLAN.md
@scripts/traceability_matrix.py
@logs/traceability/sample-trace.log
</context>

<tasks>

<task type="auto">
  <name>Update Instrumentation README with TRACE guidance</name>
  <files>internalDoc/INSTRUMENTATION_README.MD</files>
  <action>
    Add a dedicated section describing the TRACE stream and host parser:

    1. Explain how TraceIds now flow from Artisan commands through the queue into the control loop and how the guard/watchdog values stay tied to that ID.
    2. Show an example `TRACE` line and list the key steps (`command_enqueue`, `queue_dequeue`, `actuator_output`, `telemetry_emit`, `guard_report`).
    3. Reference `scripts/traceability_matrix.py`, describe how to run it against a regression log, and point to the sample trace file in `logs/traceability/sample-trace.log`.
  </action>
  <verify>python scripts/traceability_matrix.py logs/traceability/sample-trace.log</verify>
  <done>The instrumentation guide documents the new TRACE stream, the parser call, and the sample log reference for regression triage.</done>
</task>

<task type="auto">
  <name>Create traceability matrix triage reference</name>
  <files>internalDoc/TRACEABILITY_MATRIX.md</files>
  <action>
    Author a short reference that investigators can copy/paste:

    1. Outline how to capture a trace log during a regression run (e.g., `cargo run --features embedded` plus serial capture) and where to store the artifact (`logs/traceability`).
    2. Provide the parser command, describe the expected columns (command, queue depth, SSR/Fan, ET/BT/PV/MV, guard timeout, watchdog reason), and explain how to spot regressions.
    3. Include a troubleshooting note describing how to interpret guard/watchdog statuses and what to do when the matrix shows a stuck TraceId.
  </action>
  <verify>python scripts/traceability_matrix.py logs/traceability/sample-trace.log</verify>
  <done>The new reference gives auditors a reproducible way to produce and read the traceability matrix, with commands and column explanations.</done>
</task>

</tasks>

<verification>
Run `python scripts/traceability_matrix.py logs/traceability/sample-trace.log` after editing to make sure the referenced script still works and matches the documented sample.
</verification>

<success_criteria>
Documentation surfaces the TRACE stream, parser, and matrix interpretation so auditors can reproduce SOLID-03 evidence without reverse engineering the code.
</success_criteria>

<output>
After completion, create `.planning/phases/97-traceability-matrix-tooling/97-03-SUMMARY.md`
</output>
