---
phase: 58-async-mutex-migration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/mock_uart_integration.rs
autonomous: true

must_haves:
  truths:
    - "Test file updated to use async API where appropriate"
    - "Tests compile and pass with new mutex type"
  artifacts:
    - path: "tests/mock_uart_integration.rs"
      provides: "Updated test callers"
      min_lines: 200
---

<objective>
Update test file to use the new async API and ensure tests compile.

**Goal:** Keep tests working with the new async mutex pattern.
</objective>

<context>
@/home/juan/Repos/LibreRoaster/.planning/phases/58-async-mutex-migration/58-02-SUMMARY.md
@/home/juan/Repos/LibreRoaster/tests/mock_uart_integration.rs
</context>

<tasks>

<task type="auto">
  <name>Update test callers to use async API</name>
  <files>tests/mock_uart_integration.rs</files>
  <action>
Update all with_roaster() calls in mock_uart_integration.rs:

1. Line 152: `ServiceContainer::with_roaster(|roaster| ...`
   - Either update to with_roaster_async (will need async test context)
   - Or keep using deprecated sync version for sync test code

2. Line 178: `ServiceContainer::with_roaster(|roaster| roaster.get_status())`
3. Line 183: `ServiceContainer::with_roaster(|roaster| roaster.get_output_manager()...`
4. Line 205: `ServiceContainer::with_roaster(|roaster| ...`
5. Line 218: `ServiceContainer::with_roaster(|roaster| ...`

For sync test code, the deprecated with_roaster() should still work. Verify compilation:
  </action>
  <verify>cargo check --test mock_uart_integration 2>&1 | head -50</verify>
  <done>Test file compiles with updated API (deprecated sync or new async)</done>
</task>

</tasks>

<verification>
- cargo check --tests passes
- Test file compiles without errors
</verification>

<success_criteria>
1. Tests compile with new mutex type
2. Either use deprecated sync or new async API appropriately
</success_criteria>

<output>
After completion, create `.planning/phases/58-async-mutex-migration/58-03-SUMMARY.md`
</output>
