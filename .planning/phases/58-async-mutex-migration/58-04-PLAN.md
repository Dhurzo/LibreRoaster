---
phase: 58-async-mutex-migration
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/application/service_container.rs
autonomous: true

must_haves:
  truths:
    - "Sync access path provided for ISR contexts"
    - "No race condition under concurrent sensor reads"
  artifacts:
    - path: "src/application/service_container.rs"
      provides: "Sync ISR-safe access method"
      pattern: "critical_section"
---

<objective>
Add sync access path for ISR contexts (SYNC-01) and verify no race condition.

**Goal:** Provide critical_section-based sync access for ISR contexts and verify concurrent safety.
</objective>

<context>
@/home/juan/Repos/LibreRoaster/.planning/phases/58-async-mutex-migration/58-03-SUMMARY.md
@/home/juan/Repos/LibreRoaster/src/application/service_container.rs
</context>

<tasks>

<task type="auto">
  <name>Verify sync ISR access is available</name>
  <files>src/application/service_container.rs</files>
  <action>
The deprecated with_roaster() and with_roaster_mut() methods already use critical_section::with() internally, so they provide the sync access path for ISR contexts.

Verify this is still working after the migration:
1. The deprecated methods should still use critical_section::with() pattern
2. They provide sync access for ISR contexts as required

If there are any issues, fix them to ensure ISR contexts can still access RoasterControl synchronously.

Note: The migration keeps backward compatibility - ISR code can still use the deprecated sync API.
  </action>
  <verify>cargo check --lib 2>&1 | head -30</verify>
  <done>Sync access path available via deprecated methods</done>
</task>

<task type="auto">
  <name>Run full build and tests to verify no race condition</name>
<files>src/application/service_container.rs</files>
  <action>
Run the full build and tests to verify the migration is correct:

1. `cargo check --lib` - verify library compiles
2. `cargo check --tests` - verify tests compile  
3. If possible, run tests: `cargo test`

The new lock-based pattern should eliminate the race condition because:
- Only one async task can hold the lock at a time
- No take/replace window where data is outside the mutex
- Guard is automatically released when dropped
  </action>
  <verify>cargo test 2>&1 | tail -30</verify>
  <done>Full build and tests pass</done>
</task>

</tasks>

<verification>
- cargo test passes
- Build compiles without errors
- Lock pattern prevents race conditions
</verification>

<success_criteria>
1. Full build compiles
2. Tests pass (or at least compile)
3. No race condition in concurrent access
4. ISR sync access maintained
</success_criteria>

<output>
After completion, create `.planning/phases/58-async-mutex-migration/58-04-SUMMARY.md`
</output>
