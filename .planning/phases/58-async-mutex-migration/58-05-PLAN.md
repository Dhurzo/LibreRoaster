---
phase: 58-async-mutex-migration
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/concurrent_sensor_test.rs
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Integration test spawns multiple concurrent tasks calling roaster_async_sensor_read()"
    - "Test verifies no data races or panics under concurrent access"
    - "ASYNC-06 requirement satisfied"
  artifacts:
    - path: "tests/concurrent_sensor_test.rs"
      provides: "Integration test for concurrent sensor reads"
      min_lines: 40
  key_links:
    - from: "tests/concurrent_sensor_test.rs"
      to: "roaster_async_sensor_read"
      via: "async tasks"
      pattern: "spawn.*roaster_async_sensor_read"
---

<objective>
Create integration test to verify no race condition under concurrent sensor reads (ASYNC-06).

**Gap:** Structural verification passed (lock pattern prevents races), but no explicit integration test exists to verify concurrent sensor reads work correctly.

**Goal:** Satisfy ASYNC-06 requirement by adding automated test verification.
</objective>

<context>
@/home/juan/Repos/LibreRoaster/.planning/phases/58-async-mutex-migration/58-CONTEXT.md
@/home/juan/Repos/LibreRoaster/.planning/REQUIREMENTS.md
@/home/juan/Repos/LibreRoaster/.planning/phases/58-async-mutex-migration/58-VERIFICATION.md
@/home/juan/Repos/LibreRoaster/src/application/service_container.rs

# Gap Details from VERIFICATION.md

**Truth:** "No race condition - Integration test verifies concurrent sensor reads work correctly"
**Status:** partial
**Reason:** Structural verification passed - lock pattern prevents race conditions. However, no explicit integration test exists in the test suite to verify concurrent sensor reads.
**Missing:**
- Integration test for concurrent sensor reads (ASYNC-06)
- Test spawning multiple tasks calling roaster_async_sensor_read() simultaneously
- Assertion verifying no data races or panics under concurrent access
</context>

<tasks>

<task type="auto">
  <name>Create integration test for concurrent sensor reads</name>
  <files>tests/concurrent_sensor_test.rs</files>
  <action>
Create integration test that verifies concurrent sensor reads work correctly:

1. Create `tests/concurrent_sensor_test.rs` with:
   - Use embassy_fork to spawn concurrent tasks (or use embassy executor directly)
   - Initialize ServiceContainer with a mock RoasterControl
   - Spawn 3-5 concurrent tasks that each call roaster_async_sensor_read() 
   - Use a timeout to ensure test completes
   - Assert no panics occurred during concurrent access
   - Assert all tasks successfully read values

2. The test should verify:
   - Multiple tasks can call roaster_async_sensor_read() concurrently
   - No data races occur
   - No panics occur under concurrent access
   - All concurrent reads return valid temperature data

3. Reference existing integration tests in tests/ for patterns:
   - tests/mock_uart_integration.rs shows how to set up mock drivers
   - tests/artisan_integration_test.rs shows async test patterns
  </action>
  <verify>cargo test --test concurrent_sensor_test 2>&1 | tail -30</verify>
  <done>Integration test compiles and passes, verifying concurrent sensor reads work without races</done>
</task>

</tasks>

<verification>
- cargo test --test concurrent_sensor_test passes
- Test output shows multiple concurrent tasks completing successfully
- No panics or race condition warnings
</verification>

<success_criteria>
1. Integration test exists in tests/concurrent_sensor_test.rs
2. Test spawns multiple concurrent tasks (3+)
3. All tasks call roaster_async_sensor_read() 
4. Test passes without panics or race conditions
5. ASYNC-06 requirement marked complete in REQUIREMENTS.md
</success_criteria>

<output>
After completion, create `.planning/phases/58-async-mutex-migration/58-05-SUMMARY.md`
</output>
