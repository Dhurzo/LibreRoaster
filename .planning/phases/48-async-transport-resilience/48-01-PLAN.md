---
phase: 48-async-transport-resilience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true

must_haves:
  truths:
    - "UART reads and writes never block the executor"
    - "UART events are buffered in a queue for processing"
    - "Logging output continues during command bursts"
  artifacts:
    - path: "src/hardware/uart/driver.rs"
      provides: "Async UART driver with embassy traits"
      contains: "impl AsyncRead for UartDriver"
    - path: "src/hardware/uart/tasks.rs"
      provides: "Buffered event queue for UART"
      contains: "event_queue: VecDeque"
  key_links:
    - from: "src/hardware/uart/driver.rs"
      to: "embassy_usart"
      via: "AsyncRead/AsyncWrite traits"
---

<objective>
Convert UART from blocking to async with buffered event queues so logging never blocks the executor.

Purpose: UART is dedicated to logging output. This must never block even when SSR/fan control loops are active.
Output: Async UART driver using embassy traits with buffered event queue.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-async-transport-resilience/48-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add embassy_usart dependency</name>
  <files>Cargo.toml</files>
  <action>
Add embassy_usart to Cargo.toml dependencies. Use version compatible with current embassy ecosystem in the project. Check existing embassy dependencies for version alignment.
  </action>
  <verify>grep -q "embassy_usart" Cargo.toml && cargo check compiles</verify>
  <done>embassy_usart crate available for async UART</done>
</task>

<task type="auto">
  <name>Task 2: Create async UART driver with AsyncRead/AsyncWrite</name>
  <files>src/hardware/uart/driver.rs</files>
  <action>
Rewrite UartDriver to implement embassy_usart traits. Convert from esp_hal::Blocking to embassy_usart::Reader/Writer. Use AsyncRead and AsyncWrite traits for non-blocking I/O. Keep the same pin configuration and baud rate (115200).

Key changes:
- Replace `esp_hal::uart::UartTx/Rx<esp_hal::Blocking>` with embassy_usart driver
- Implement `embassy_embedded_hal::AsyncRead<impl Warp>` for the driver
- Implement `embassy_embedded_hal::AsyncWrite<impl Warp>` for the driver
- Keep error types (UartError) but add async variants if needed
  </action>
  <verify>cargo check --target riscv32imc-unknown-none-elf 2>&1 | head -50</verify>
  <done>UartDriver implements AsyncRead and AsyncWrite from embassy_embedded_hal</done>
</task>

<task type="auto">
  <name>Task 3: Add buffered event queue to UART reader task</name>
  <files>src/hardware/uart/tasks.rs</files>
  <action>
Add buffered event queue to uart_reader_task using heapless::VecDeque. Process events from queue in the reader loop rather than directly handling each byte. Queue should be sized appropriately (e.g., 256 bytes) to handle bursts.

Implementation:
- Add `use heapless::VecDeque;` 
- Create `static EVENT_QUEUE: Mutex<VecDeque<u8, 256>>` or similar
- Read from UART async into buffer, push to queue
- Process complete lines (0x0D terminated) from queue separately
- This separates I/O from parsing concerns
  </action>
  <verify>cargo check passes with no new errors</verify>
  <done>UART reader uses buffered event queue, separates I/O from parsing</done>
</task>

</tasks>

<verification>
- UART driver compiles for riscv32 target
- AsyncRead/AsyncWrite traits properly implemented
- Event queue integration compiles without errors
</verification>

<success_criteria>
UART driver compiles in async mode, tasks use buffered queue for event handling
</success_criteria>

<output>
After completion, create `.planning/phases/48-async-transport-resilience/48-01-SUMMARY.md`
</output>
