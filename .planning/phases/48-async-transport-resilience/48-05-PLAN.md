---
phase: 48-async-transport-resilience
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hardware/uart/tasks.rs
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Command queue is consumed by a processor task"
    - "Commands from UART queue flow to artisan_channel"
    - "Commands from USB queue flow to artisan_channel"
  artifacts:
    - path: "src/hardware/uart/tasks.rs"
      provides: "Queue processor task that pops from COMMAND_QUEUE and sends to artisan_channel"
      min_lines: 20
  key_links:
    - from: "COMMAND_QUEUE"
      to: "artisan_channel"
      via: "queue_processor_task"
      pattern: "try_pop.*try_send"
---

<objective>
Wire CommandQueue to artisan_channel so commands are actually processed.

Gap: Commands pushed to COMMAND_QUEUE but never consumed. The queue processor task was never created, so commands accumulate but roaster ignores them.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-async-transport-resilience/48-VERIFICATION.md
@.planning/phases/48-async-transport-resilience/48-03-SUMMARY.md
@.planning/phases/48-async-transport-resilience/48-04-SUMMARY.md

# Gap Summary from VERIFICATION.md:
- Truth 3 "Command queue handles flood" is PARTIAL
- CommandQueue structure is correct, flood tests pass (8/8)
- BUT queue is not wired to processor - commands pushed but never consumed
- control_loop_task reads from artisan_channel, not from COMMAND_QUEUE
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create queue processor task that consumes COMMAND_QUEUE</name>
  <files>src/hardware/uart/tasks.rs</files>
  <action>
Add a new async task `queue_processor_task` that:
1. Loops forever, popping commands from COMMAND_QUEUE
2. Sends each command to artisan_channel via `ServiceContainer::get_artisan_channel().try_send()`
3. Uses `heapless::FifoIndex` to track queue state
4. Small delay between pops to yield to other tasks

Also add USB_COMMAND_QUEUE processor in src/hardware/usb_cdc/tasks.rs if not already wired.

Alternatively, simpler fix: Modify handle_command_data_internal to send directly to artisan_channel instead of pushing to queue.
  </files>
  <verify>
Run `cargo check` to verify compilation
</verify>
  <done>
Commands pushed to COMMAND_QUEUE are consumed and sent to artisan_channel. Verified by checking queue processor task exists and compiles.
</done>
</task>

</tasks>

<verification>
After implementation:
1. `cargo check` passes (embassy async compiles)
2. Queue processor task defined in uart/tasks.rs or usb_cdc/tasks.rs
3. Commands flow: UART reader → COMMAND_QUEUE → queue_processor_task → artisan_channel → control_loop_task
</verification>

<success_criteria>
- CommandQueue is consumed by processor
- Commands from UART and USB routes flow to artisan_channel
- No dead code warnings about unused COMMAND_QUEUE
</success_criteria>

<output>
After completion, create `.planning/phases/48-async-transport-resilience/48-05-SUMMARY.md`
</output>
