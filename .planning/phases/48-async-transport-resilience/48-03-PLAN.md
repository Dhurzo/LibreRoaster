---
phase: 48-async-transport-resilience
plan: 03
type: execute
wave: 2
depends_on: [48-01, 48-02]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "Commands are processed in FIFO order"
    - "Queue rejects new commands when full (no response sent)"
    - "Queue size handles burst commands without rejection"
  artifacts:
    - path: "src/input/mod.rs"
      provides: "FIFO command queue with reject-on-full"
      contains: "struct CommandQueue"
    - path: "src/hardware/uart/tasks.rs"
      provides: "UART command processing uses queue"
      contains: "command_queue.push"
    - path: "src/hardware/usb_cdc/tasks.rs"
      provides: "USB command processing uses queue"
      contains: "command_queue.push"
  key_links:
    - from: "src/hardware/uart/tasks.rs"
      to: "src/input/mod.rs"
      via: "CommandQueue::push"
    - from: "src/hardware/usb_cdc/tasks.rs"
      to: "src/input/mod.rs"
      via: "CommandQueue::push"
---

<objective>
Implement FIFO command queue with reject-on-full behavior. When queue is full, reject new commands (send nothing to Artisan, which times out).

Purpose: Commands from both UART and USB must be queued for ordered processing. Queue must be sized large enough that rejection is rare, but when full, we reject cleanly.
Output: CommandQueue implementation with FIFO ordering and reject-on-full.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-async-transport-resilience/48-CONTEXT.md
@.planning/phases/48-async-transport-resilience/48-01-SUMMARY.md
@.planning/phases/48-async-transport-resilience/48-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CommandQueue struct with FIFO semantics</name>
  <files>src/input/mod.rs</files>
  <action>
Create a CommandQueue struct in src/input/mod.rs that provides:
- FIFO ordering (push to back, pop from front)
- Bounded capacity (use heapless::VecDeque for no_std compatibility)
- TryPush variant that returns error when full (for rejection)
- TryPop variant to retrieve commands

Choose queue size: 32-64 commands should handle bursts. Make it configurable via const.

Implementation:
```rust
use heapless::VecDeque;

pub struct CommandQueue<T, const N: usize> {
    queue: VecDeque<T, N>,
}

impl<T, const N: usize> CommandQueue<T, N> {
    pub fn new() -> Self { ... }
    pub fn push(&mut self, item: T) -> Result<(), QueueError> { ... } // Full -> Error
    pub fn pop(&mut self) -> Option<T> { ... }
    pub fn is_full(&self) -> bool { ... }
    pub fn len(&self) -> usize { ... }
}
```
  </action>
  <verify>cargo check --target riscv32imc-unknown-none-elf passes</verify>
  <done>CommandQueue struct exists with FIFO push/pop and full check</done>
</task>

<task type="auto">
  <name>Task 2: Wire UART task to use CommandQueue</name>
  <files>src/hardware/uart/tasks.rs</files>
  <action>
Update process_command_data in uart/tasks.rs to push to CommandQueue instead of directly to artisan channel. On push failure (queue full), silently drop the command (no response sent - Artisan times out).

Implementation:
- Import CommandQueue from input module
- Add static COMMAND_QUEUE: Mutex<CommandQueue<ArtisanCommand, 32>>
- In process_command_data: parse command, then queue.try_push()
- If try_push fails: log at debug level, silently return (no error sent)
- Reader continues processing next characters
  </action>
  <verify>cargo check --target riscv32imc-unknown-none-elf passes</verify>
  <done>UART commands queued, rejected on queue full</done>
</task>

<task type="auto">
  <name>Task 3: Wire USB task to use CommandQueue</name>
  <files>src/hardware/usb_cdc/tasks.rs</files>
  <action>
Update handle_complete_usb_command in usb_cdc/tasks.rs to push to CommandQueue instead of directly to artisan channel. Same rejection behavior as UART - on queue full, silently drop (no response).

Implementation:
- Import CommandQueue from input module
- Add static USB_COMMAND_QUEUE: Mutex<CommandQueue<ArtisanCommand, 32>>
- In handle_complete_usb_command: after parsing, queue.try_push()
- If try_push fails: log at debug level, silently return (no error sent)
- This is the "reject on full" behavior - Artisan times out waiting
  </action>
  <verify>cargo check --target riscv32imc-unknown-none-elf passes</verify>
  <done>USB commands queued, rejected on queue full</done>
</task>

</tasks>

<verification>
- CommandQueue compiles and provides FIFO semantics
- Both UART and USB routes use CommandQueue
- Rejection behavior: no response sent when queue full
</verification>

<success_criteria>
Commands queued in FIFO order, rejected silently when queue is full
</success_criteria>

<output>
After completion, create `.planning/phases/48-async-transport-resilience/48-03-SUMMARY.md`
</output>
