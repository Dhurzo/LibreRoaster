---
phase: 48-async-transport-resilience
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true

must_haves:
  truths:
    - "USB CDC yields when DMA endpoints are congested"
    - "Formatter stays responsive during back-pressure"
    - "No executor stalls from USB congestion"
  artifacts:
    - path: "src/hardware/usb_cdc/tasks.rs"
      provides: "Back-pressure aware USB tasks"
      contains: "back_pressure handling"
    - path: "src/hardware/usb_cdc/driver.rs"
      provides: "Async USB driver with flow control"
      contains: "impl AsyncWrite for UsbCdcDriver"
  key_links:
    - from: "src/hardware/usb_cdc/tasks.rs"
      to: "embassy_usb"
      via: "AsyncWrite::write with back-pressure"
---

<objective>
Add back-pressure awareness to USB CDC channel so it yields when DMA endpoints report congestion.

Purpose: USB CDC is the primary Artisan command channel. Must remain responsive and never block the executor when Artisan sends burst commands.
Output: Back-pressure aware USB CDC driver and tasks.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-async-transport-resilience/48-CONTEXT.md
@.planning/phases/48-async-transport-resilience/48-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add embassy_usb dependency</name>
  <files>Cargo.toml</files>
  <action>
Add or verify embassy_usb is available in Cargo.toml. This provides the USB device-side async support for embassy. Check for existing USB dependencies and ensure embassy_usb or equivalent async USB stack is present.
  </action>
  <verify>grep -E "embassy_usb|usb" Cargo.toml</verify>
  <done>embassy_usb crate available for async USB</done>
</task>

<task type="auto">
  <name>Task 2: Create async USB CDC driver with AsyncWrite</name>
  <files>src/hardware/usb_cdc/driver.rs</files>
  <action>
Extend UsbCdcDriver to implement embassy_embedded_hal::AsyncWrite and AsyncRead. Add back-pressure detection - check if write buffer is full before writing, yield if congested.

Implementation approach:
- Check if driver can implement embassy_embedded_hal traits
- Add write() that checks endpoint buffer status before writing
- If buffer full: return WouldBlock or yield with Timer::after
- This allows the executor to run other tasks while waiting for USB
- Keep existing error types but handle async WouldBlock scenarios

Note: If embassy_usb integration is complex, focus on making usb_writer_task back-pressure aware by checking write result and yielding appropriately.
  </action>
  <verify>cargo check --target riscv32imc-unknown-none-elf 2>&1 | head -50</verify>
  <done>USB driver has back-pressure handling in async write path</done>
</task>

<task type="auto">
  <name>Task 3: Make USB writer task back-pressure aware</name>
  <files>src/hardware/usb_cdc/tasks.rs</files>
  <action>
Modify usb_writer_task to handle back-pressure. Instead of blindly writing, check write result and yield if congested. This keeps the formatter responsive.

Implementation:
- In usb_writer_task loop, check write_bytes result
- If error indicates buffer full (WouldBlock or similar), yield with Timer::after before retry
- Use exponential backoff or fixed interval (e.g., 1ms, 5ms, 10ms) for yield durations
- This ensures other tasks (SSR/fan control) get CPU time when USB is congested
- Add log warning if prolonged back-pressure detected (for diagnostics)
  </action>
  <verify>cargo check passes with no new errors</verify>
  <done>USB writer yields on back-pressure, executor remains responsive</done>
</task>

</tasks>

<verification>
- USB driver/task compiles for riscv32 target
- Back-pressure handling present in writer task
- cargo check passes without errors
</verification>

<success_criteria>
USB CDC channel yields appropriately when DMA endpoints congested
</success_criteria>

<output>
After completion, create `.planning/phases/48-async-transport-resilience/48-02-SUMMARY.md`
</output>
