---
phase: 81-quality-baseline-and-ratcheting-policy
plan: 02
type: execute
wave: 2
depends_on: ["81-01"]
files_modified:
  - scripts/quality-baseline.sh
  - scripts/quality_baseline.py
autonomous: true
must_haves:
  truths:
    - "User can run one orchestrator command and always execute fmt -> clippy -> test in a deterministic order."
    - "User receives compact pass/fail output that includes module, lint rule, tier, and policy identifier for each finding."
    - "User sees all failures from a run (not first-failure only), and the final verdict is reproducible for unchanged inputs."
  artifacts:
    - path: "scripts/quality-baseline.sh"
      provides: "Single baseline command entrypoint with fixed gate sequencing"
      contains: "cargo fmt"
    - path: "scripts/quality_baseline.py"
      provides: "Policy-aware diagnostics aggregation and compact summary generation"
      contains: "policy_id"
  key_links:
    - from: "scripts/quality-baseline.sh"
      to: ".planning/quality/baseline-policy.toml"
      via: "Runner loads policy before executing gates"
      pattern: "baseline-policy\.toml"
    - from: "scripts/quality-baseline.sh"
      to: "scripts/quality_baseline.py"
      via: "Shell runner pipes gate outputs and statuses into Python policy evaluator"
      pattern: "quality_baseline\.py"
---

<objective>
Implement the baseline orchestrator command that executes deterministic gates and emits actionable, policy-aware compact output without short-circuiting on the first failure.
Purpose: Turn the policy contract into executable behavior so QG-01/QG-02 can be validated from terminal output alone.
Output: A shell entrypoint and Python evaluator that run baseline gates, classify findings by tier, and return deterministic pass/fail verdicts.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-quality-baseline-and-ratcheting-policy/81-CONTEXT.md
@.planning/phases/81-quality-baseline-and-ratcheting-policy/81-RESEARCH.md
@.planning/quality/baseline-policy.toml
@Cargo.toml
@rust-toolchain.toml
</context>

<tasks>

<task type="auto">
  <name>Build deterministic baseline runner entrypoint</name>
  <files>scripts/quality-baseline.sh</files>
  <action>
1. Create `scripts/quality-baseline.sh` as the single orchestrator command; enforce fixed gate order from policy (`fmt`, `clippy`, `test`) and print a compact run header containing policy ID/version plus toolchain context.
2. Run each gate with deterministic flags from policy, including lockfile enforcement and host-safe test scope; do not stop at first gate failure.
3. Capture each gate's exit code and machine-readable output (clippy JSON stream + plain statuses for fmt/test) into temporary files that are passed to the evaluator script before cleanup.
  </action>
  <verify>
1. `bash -n scripts/quality-baseline.sh` passes.
2. `./scripts/quality-baseline.sh` executes all three gates in order and prints exactly one final verdict line.
  </verify>
  <done>
The baseline runner is a single command that always executes deterministic gate order and evaluates the complete run even when earlier gates fail.
  </done>
</task>

<task type="auto">
  <name>Implement policy-aware diagnostics evaluator</name>
  <files>scripts/quality_baseline.py</files>
  <action>
1. Create `scripts/quality_baseline.py` (stdlib-only) to parse `.planning/quality/baseline-policy.toml`, ingest clippy JSON diagnostics, map file paths to configured tiers, and annotate each finding with module, lint rule, tier, and policy rule ID.
2. Implement compact summary output grouped by gate with policy reference format `[{policy_id}@{policy_version}]`, and include inline policy context on each finding (tier + blocking/info mode).
3. Add evaluator inputs for both live runs and fixture mode (`--from-json`, `--fmt-status`, `--test-status`) so later plans can intentionally fail gates without mutating source files.
  </action>
  <verify>
1. `python3 -m py_compile scripts/quality_baseline.py` succeeds.
2. `python3 scripts/quality_baseline.py --help` lists live and fixture-mode arguments.
  </verify>
  <done>
Evaluator output consistently ties findings to module+tier+rule and supports both real baseline runs and deterministic fixture-driven checks.
  </done>
</task>

<task type="auto">
  <name>Enforce verdict semantics and reproducibility messaging</name>
  <files>scripts/quality-baseline.sh; scripts/quality_baseline.py</files>
  <action>
1. Implement final verdict logic: fail on any fmt/test failure and any Tier 1 blocking lint finding; keep Tier 2/3 findings informational-only in this policy version.
2. Ensure final output always includes a deterministic reproducibility line conveying "same input, same verdict" plus the evaluated policy version.
3. Ensure failure output lists all discovered findings across gates before exiting non-zero, and includes the failing gate/module/rule details needed for immediate remediation.
  </action>
  <verify>
1. Running `./scripts/quality-baseline.sh` returns non-zero whenever a blocking condition exists and still prints all recorded failures.
2. `rg -n "same input, same verdict|FINAL VERDICT|QG-POLICY" scripts/quality_baseline.py scripts/quality-baseline.sh` confirms required messaging and policy references.
  </verify>
  <done>
The baseline command returns deterministic policy-driven pass/fail behavior with complete, actionable compact failure reporting.
  </done>
</task>

</tasks>

<verification>
1. Run `bash -n scripts/quality-baseline.sh` and `python3 -m py_compile scripts/quality_baseline.py`.
2. Run `./scripts/quality-baseline.sh` and confirm: fixed gate order, compact summary, all-failures listing, policy-tagged findings, and one final verdict.
</verification>

<success_criteria>
- [ ] `scripts/quality-baseline.sh` is the primary single-command baseline entrypoint and runs fmt -> clippy -> test deterministically.
- [ ] `scripts/quality_baseline.py` emits compact actionable output with module + rule + tier + policy ID/version for every finding.
- [ ] Final verdict logic blocks Tier 1 findings and reports lower tiers informationally without initial blocking.
</success_criteria>

<output>
After completion, create `.planning/phases/81-quality-baseline-and-ratcheting-policy/81-02-SUMMARY.md`
</output>
