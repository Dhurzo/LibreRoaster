---
phase: 81-quality-baseline-and-ratcheting-policy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quality/baseline-policy.toml
  - .planning/quality/ratchet-changelog.md
  - .planning/quality/README.md
autonomous: true
must_haves:
  truths:
    - "User can inspect a versioned quality policy that declares deterministic gate order and policy identifier metadata."
    - "User can see safety/control/protocol modules mapped to a blocking tier while lower-risk modules are explicitly informational-only."
    - "User can trace ratchet updates through a required policy version bump plus a human-readable changelog entry."
  artifacts:
    - path: ".planning/quality/baseline-policy.toml"
      provides: "Policy ID/version, gate ordering, gate commands, and module-to-tier enforcement map"
      contains: "policy_id"
    - path: ".planning/quality/ratchet-changelog.md"
      provides: "Versioned ratchet deltas and cadence guidance"
      contains: "## v1.0.0"
    - path: ".planning/quality/README.md"
      provides: "Operator-facing baseline contract and reproducibility guidance"
      contains: "same input, same verdict"
  key_links:
    - from: ".planning/quality/README.md"
      to: ".planning/quality/baseline-policy.toml"
      via: "README references the authoritative policy ID/version and baseline command contract"
      pattern: "baseline-policy\.toml"
    - from: ".planning/quality/ratchet-changelog.md"
      to: ".planning/quality/baseline-policy.toml"
      via: "Each changelog entry references the policy version it updates"
      pattern: "QG-POLICY"
---

<objective>
Define the baseline quality policy artifacts that make gate behavior explicit, deterministic, and ratchet-ready before any automation logic is implemented.
Purpose: Lock the policy contract (ID/version, gate order, tier mapping, and ratchet governance) so baseline execution and reporting can enforce one unambiguous source of truth.
Output: New `.planning/quality` policy files that describe baseline gates, module tiers, and ratchet versioning rules.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-quality-baseline-and-ratcheting-policy/81-CONTEXT.md
@.planning/phases/81-quality-baseline-and-ratcheting-policy/81-RESEARCH.md
@Cargo.toml
@README.md
</context>

<tasks>

<task type="auto">
  <name>Create versioned baseline policy contract</name>
  <files>.planning/quality/baseline-policy.toml</files>
  <action>
1. Create `.planning/quality/baseline-policy.toml` with `policy_id`, `policy_version`, and a fixed gate sequence in this exact order: `fmt` then `clippy` then `test`.
2. For each gate, define the canonical command (toolchain + lockfile behavior + scope flags) so the baseline can run deterministically on repeated invocations; include host-safe test scope (`--lib --tests`) and avoid `--all-features` for this phase.
3. Add compact-output defaults (`output_mode = "compact"`) and an explicit `collect_all_failures = true` policy setting so the orchestrator is required to report every failing gate/finding.
  </action>
  <verify>
1. `python3 -c "import tomllib, pathlib; tomllib.loads(pathlib.Path('.planning/quality/baseline-policy.toml').read_text())"` succeeds.
2. `rg -n "policy_id|policy_version|gate_order|collect_all_failures" .planning/quality/baseline-policy.toml` returns all required keys.
  </verify>
  <done>
The policy file parses as valid TOML and encodes one deterministic baseline command sequence with explicit all-failures behavior.
  </done>
</task>

<task type="auto">
  <name>Encode criticality tiers and module-to-tier mapping</name>
  <files>.planning/quality/baseline-policy.toml</files>
  <action>
1. Add 3 explicit tiers with enforcement mode per tier: Tier 1 `blocking = true`, Tier 2 `blocking = false`, Tier 3 `blocking = false`.
2. Map Tier 1 to high-criticality modules covering safety/control/protocol first (at minimum include `src/safety/**`, `src/control/**`, `src/input/parser.rs`, `src/output/artisan.rs`, and protocol-critical config paths), then map remaining modules to Tier 2/3.
3. Add a policy rule identifier naming convention for findings (for example `QG-CLIPPY-T1`, `QG-TEST-T1`) so failure output can always cite a stable policy reference.
  </action>
  <verify>
1. `rg -n "blocking = true|blocking = false|src/safety|src/control|src/input/parser.rs|src/output/artisan.rs" .planning/quality/baseline-policy.toml` confirms tier behavior and mapping.
2. `rg -n "QG-.*-T[123]" .planning/quality/baseline-policy.toml` shows rule identifier definitions.
  </verify>
  <done>
The policy explicitly enforces safety/control/protocol modules first and leaves lower-risk modules informational-only with stable rule identifiers.
  </done>
</task>

<task type="auto">
  <name>Publish operator contract and ratchet governance</name>
  <files>.planning/quality/README.md; .planning/quality/ratchet-changelog.md</files>
  <action>
1. Create `.planning/quality/README.md` documenting the single baseline command entrypoint, compact summary expectations, and reproducibility statement using the exact wording concept: "same input, same verdict".
2. Document mandatory rerun behavior after fixes (full baseline rerun, not partial gate reruns) and mention that failed runs report all failures with policy context.
3. Create `.planning/quality/ratchet-changelog.md` with initial `v1.0.0` entry and an update policy that requires every ratchet change to include both a policy version bump and a human-readable delta.
  </action>
  <verify>
1. `rg -n "same input, same verdict|full baseline rerun|compact" .planning/quality/README.md` confirms required messaging.
2. `rg -n "v1.0.0|version bump|delta|ratchet" .planning/quality/ratchet-changelog.md` confirms ratchet governance text.
  </verify>
  <done>
Operators have clear baseline usage docs and ratchet updates are governed by explicit versioned changelog rules.
  </done>
</task>

</tasks>

<verification>
1. Validate policy syntax and required keys with `python3` + `rg` checks from the task verifications.
2. Confirm README and changelog describe one primary command path, compact output, full rerun-after-fix workflow, and versioned ratchet deltas.
</verification>

<success_criteria>
- [ ] `.planning/quality/baseline-policy.toml` defines deterministic gate order, gate commands, and all-failures collection behavior.
- [ ] Tier policy clearly blocks safety/control/protocol modules first while lower-risk tiers are informational-only.
- [ ] `.planning/quality/ratchet-changelog.md` and `.planning/quality/README.md` codify "same input, same verdict" and version-bump ratchet governance.
</success_criteria>

<output>
After completion, create `.planning/phases/81-quality-baseline-and-ratcheting-policy/81-01-SUMMARY.md`
</output>
