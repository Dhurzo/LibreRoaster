---
phase: 81-quality-baseline-and-ratcheting-policy
plan: 03
type: execute
wave: 3
depends_on: ["81-01", "81-02"]
files_modified:
  - tests/quality/fixtures/clippy-tier1-fail.jsonl
  - tests/quality/fixtures/clippy-mixed-failures.jsonl
  - scripts/quality-baseline-selfcheck.sh
  - .planning/quality/failure-drill.md
autonomous: true
must_haves:
  truths:
    - "User can intentionally fail a baseline gate and see actionable output that names module, policy rule, and blocking tier."
    - "User can observe runs reporting all failures in one pass, including mixed blocking and informational findings."
    - "User can rerun after fixes and confirm deterministic behavior remains 'same input, same verdict' under the same policy version."
  artifacts:
    - path: "scripts/quality-baseline-selfcheck.sh"
      provides: "Repeatable intentional-failure drills for policy output validation"
      contains: "quality_baseline.py --from-json"
    - path: "tests/quality/fixtures/clippy-tier1-fail.jsonl"
      provides: "Known blocking diagnostic fixture for top-tier module"
      contains: "src/control/"
    - path: "tests/quality/fixtures/clippy-mixed-failures.jsonl"
      provides: "Mixed Tier 1 and informational diagnostics to validate all-failures reporting"
      contains: "src/hardware/"
    - path: ".planning/quality/failure-drill.md"
      provides: "Documented fail-and-rerun workflow with expected output and reproducibility checks"
      contains: "same input, same verdict"
  key_links:
    - from: "scripts/quality-baseline-selfcheck.sh"
      to: "scripts/quality_baseline.py"
      via: "Selfcheck invokes fixture mode to validate policy evaluation output"
      pattern: "--from-json"
    - from: "scripts/quality-baseline-selfcheck.sh"
      to: "tests/quality/fixtures/clippy-mixed-failures.jsonl"
      via: "Fixture run validates all-findings output and tiered verdict behavior"
      pattern: "mixed"
---

<objective>
Add deterministic failure drills that prove intentional failure visibility, all-failures aggregation, and rerun reproducibility behavior for the baseline policy.
Purpose: Close phase success criteria around intentional failures and rerun confidence without requiring ad-hoc source edits.
Output: Fixture-driven selfcheck scripts and a documented operator drill for fail-and-rerun validation.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/quality/baseline-policy.toml
@.planning/quality/README.md
@scripts/quality-baseline.sh
@scripts/quality_baseline.py
</context>

<tasks>

<task type="auto">
  <name>Create deterministic failure fixtures for policy evaluation</name>
  <files>tests/quality/fixtures/clippy-tier1-fail.jsonl; tests/quality/fixtures/clippy-mixed-failures.jsonl</files>
  <action>
1. Create `tests/quality/fixtures/clippy-tier1-fail.jsonl` containing at least one clippy diagnostic mapped to a Tier 1 module (for example `src/control/...`) with explicit lint rule ID.
2. Create `tests/quality/fixtures/clippy-mixed-failures.jsonl` containing multiple findings across at least two tiers (Tier 1 blocking and Tier 2/3 informational) to validate all-failures listing behavior.
3. Ensure fixture JSON lines match Cargo's `--message-format=json` diagnostic structure required by `scripts/quality_baseline.py`.
  </action>
  <verify>
1. `python3 -c "import json, pathlib; [json.loads(line) for line in pathlib.Path('tests/quality/fixtures/clippy-tier1-fail.jsonl').read_text().splitlines() if line.strip()]"` succeeds.
2. `python3 -c "import json, pathlib; [json.loads(line) for line in pathlib.Path('tests/quality/fixtures/clippy-mixed-failures.jsonl').read_text().splitlines() if line.strip()]"` succeeds.
  </verify>
  <done>
Fixture files provide deterministic blocking and mixed-tier failure inputs for policy evaluator drills.
  </done>
</task>

<task type="auto">
  <name>Automate intentional-failure and rerun checks</name>
  <files>scripts/quality-baseline-selfcheck.sh</files>
  <action>
1. Create `scripts/quality-baseline-selfcheck.sh` to run at least two fixture-mode checks via `scripts/quality_baseline.py --from-json ...` (blocking-only and mixed-failures scenarios).
2. Assert expected exit codes and output substrings (policy ID/version, module path, lint rule, tier, and "blocking"/"info" markers) so failures are actionable and policy-referenced.
3. Add a reproducibility assertion by running the same fixture command twice and verifying the same final verdict text appears both times.
  </action>
  <verify>
1. `bash -n scripts/quality-baseline-selfcheck.sh` passes.
2. `./scripts/quality-baseline-selfcheck.sh` exits 0 only when fixture drills and reproducibility assertions pass.
  </verify>
  <done>
A single selfcheck command proves intentional failure visibility, all-findings output, and deterministic verdict behavior.
  </done>
</task>

<task type="auto">
  <name>Document fail-and-rerun operator drill</name>
  <files>.planning/quality/failure-drill.md</files>
  <action>
1. Create `.planning/quality/failure-drill.md` with exact command steps for: (a) intentional failure drill via selfcheck, (b) normal baseline run, (c) full rerun-after-fix flow.
2. Include expected compact failure headline structure with policy reference (`[policy_id@version]`) and explicit requirement that all failures are listed before final verdict.
3. Document rerun expectation and reproducibility statement using "same input, same verdict", plus where to update policy version/changelog when ratcheting.
  </action>
  <verify>
1. `rg -n "same input, same verdict|full rerun|QG-POLICY|all failures" .planning/quality/failure-drill.md` finds required language.
2. Commands in the drill execute as written on a clean branch and match documented pass/fail outcomes.
  </verify>
  <done>
Operators can intentionally fail, inspect actionable policy-tagged output, and rerun deterministically after fixes.
  </done>
</task>

</tasks>

<verification>
1. Run `./scripts/quality-baseline-selfcheck.sh` to validate intentional failure and reproducibility drill behavior.
2. Run `./scripts/quality-baseline.sh` once after selfcheck to confirm the standard baseline flow remains functional and compact.
</verification>

<success_criteria>
- [ ] Intentional-failure drill produces actionable module+rule+tier+policy output and returns expected non-zero status for blocking cases.
- [ ] Mixed-tier drill proves all findings are listed while only blocking tiers control pass/fail.
- [ ] Documented rerun workflow confirms deterministic "same input, same verdict" behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/81-quality-baseline-and-ratcheting-policy/81-03-SUMMARY.md`
</output>
