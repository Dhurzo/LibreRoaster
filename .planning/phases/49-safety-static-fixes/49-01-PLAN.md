---
phase: 49-safety-static-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main.rs
  - src/hardware/usb_cdc/driver.rs
  - src/hardware/uart/driver.rs
  - src/application/service_container.rs
autonomous: true

must_haves:
  truths:
    - "No unsafe fn make_static in main.rs (replaced with StaticCell)"
    - "driver.rs get_usb_cdc_driver uses StaticCell with documented safety"
    - "driver.rs get_uart_driver uses StaticCell with documented safety"
    - "ServiceContainer::get_instance uses StaticCell pattern"
  artifacts:
    - path: "src/main.rs"
      provides: "make_static removed, StaticCell used"
      contains: "StaticCell"
    - path: "src/hardware/usb_cdc/driver.rs"
      provides: "get_usb_cdc_driver uses StaticCell"
      contains: "StaticCell"
    - path: "src/hardware/uart/driver.rs"
      provides: "get_uart_driver uses StaticCell"
      contains: "StaticCell"
    - path: "src/application/service_container.rs"
      provides: "get_instance uses StaticCell"
      contains: "ConstStaticCell"
  key_links:
    - from: "src/main.rs"
      to: "static_cell crate"
      via: "StaticCell::init() at call sites"
    - from: "usb_cdc/driver.rs"
      to: "static_cell crate"
      via: "StaticCell<Option<UsbCdcDriver>>"
    - from: "uart/driver.rs"
      to: "static_cell crate"
      via: "StaticCell<Option<UartDriver>>"
    - from: "service_container.rs"
      to: "static_cell crate"
      via: "ConstStaticCell::take()"
---

<objective>
Replace all unsafe static/mutable patterns with StaticCell to fix memory safety issues.

Purpose: Eliminate use-after-free bug in make_static and replace unsafe static_mut patterns with safe alternatives.
Output: 4 unsafe patterns refactored to use StaticCell
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase-specific context
@.planning/phases/49-safety-static-fixes/49-CONTEXT.md
@.planning/phases/49-safety-static-fixes/49-RESEARCH.md

# Reference files for current patterns
@src/main.rs (lines 43-49 for make_static, lines 219-220 for usage)
@src/hardware/usb_cdc/driver.rs (lines 136-162)
@src/hardware/uart/driver.rs (lines 57-91)
@src/application/service_container.rs (lines 41-44)
</context>

<tasks>

<task type="auto">
  <name>SAFE-01: Replace make_static in main.rs</name>
  <files>src/main.rs</files>
  <action>
1. Add import: `use static_cell::StaticCell;` at top of file (near other riscv32 imports around line 66)

2. Declare two StaticCells before main():
   - `static SSR_CELL: StaticCell<SsrControlSimple> = StaticCell::new();`
   - `static FAN_CELL: StaticCell<FanController> = StaticCell::new();`

3. Remove the unsafe make_static function (lines 43-49)

4. Replace line 219 (`let static_ssr = unsafe { make_static(real_ssr) };`) with:
   ```rust
   let static_ssr: &'static mut SsrControlSimple = SSR_CELL.init(real_ssr);
   ```

5. Replace line 220 (`let static_fan = unsafe { make_static(fan_controller) };`) with:
   ```rust
   let static_fan: &'static mut FanController = FAN_CELL.init(fan_controller);
   ```

6. Add inline SAFETY comment explaining why this is safe (compile-time memory reservation, single initialization)
  </action>
  <verify>`cargo check --target riscv32imac-unknown-none-elf 2>&1 | head -50`</verify>
  <done>main.rs has no unsafe make_static function, uses StaticCell::init() instead</done>
</task>

<task type="auto">
  <name>SAFE-02: Fix mutable static in usb_cdc/driver.rs</name>
  <files>src/hardware/usb_cdc/driver.rs</files>
  <action>
1. Add import at top: `use static_cell::StaticCell;`

2. Replace line 136 (`static mut USB_CDC_INSTANCE: Option<UsbCdcDriver> = None;`) with:
   ```rust
   static USB_CDC_INSTANCE: StaticCell<Option<UsbCdcDriver>> = StaticCell::new();
   ```

3. Update init_usb_cdc function (line 139-144):
   - Change `critical_section::with(|_| unsafe { USB_CDC_INSTANCE = Some(...); })` 
   - To: `let _ = USB_CDC_INSTANCE.init(Some(UsbCdcDriver::new(usb)));`
   - Note: Only call init once; use a static bool to track initialization if needed, or assume single init

4. Update get_usb_cdc_driver function (lines 152-157):
   - Remove `#[allow(static_mut_refs)]` and `unsafe` block
   - Change to: `USB_CDC_INSTANCE.get().as_mut()`

5. Add inline SAFETY comment explaining: initialized once during early boot, single-threaded context
  </action>
  <verify>`cargo check --target riscv32imac-unknown-none-elf 2>&1 | head -50`</verify>
  <done>driver.rs uses StaticCell pattern, no static_mut_refs warning</done>
</task>

<task type="auto">
  <name>SAFE-03: Fix mutable static in uart/driver.rs</name>
  <files>src/hardware/uart/driver.rs</files>
  <action>
1. Add import at top: `use static_cell::StaticCell;`

2. Replace line 57 (`static mut UART_INSTANCE: Option<UartDriver> = None;`) with:
   ```rust
   static UART_INSTANCE: StaticCell<Option<UartDriver>> = StaticCell::new();
   ```

3. Update init_uart function (line 78-80):
   - Change `critical_section::with(|_| unsafe { UART_INSTANCE = Some(...); })`
   - To: `let _ = UART_INSTANCE.init(Some(UartDriver::new(tx_static, rx_static)));`

4. Update get_uart_driver function (lines 85-91):
   - Remove `#[allow(static_mut_refs)]` and `unsafe` block  
   - Change to: `UART_INSTANCE.get().as_mut()`

5. Add inline SAFETY comment explaining: initialized once during early boot, single-threaded context
  </action>
  <verify>`cargo check --target riscv32imac-unknown-none-elf 2>&1 | head -50`</verify>
  <done>driver.rs uses StaticCell pattern, no static_mut_refs warning</done>
</task>

<task type="auto">
  <name>SAFE-04: Fix ServiceContainer::get_instance()</name>
  <files>src/application/service_container.rs</files>
  <action>
1. Add import at top: `use static_cell::ConstStaticCell;`

2. Replace lines 41-44 (the get_instance function):
   ```rust
   static INSTANCE: ConstStaticCell<ServiceContainer> = ConstStaticCell::new();

   pub fn get_instance() -> &'static mut Self {
       // SAFETY: ConstStaticCell::take() can only be called once.
       // This is called during application initialization before any async tasks start.
       // The returned reference lives for the duration of the program.
       INSTANCE.take()
   }
   ```

3. The new() call (line 42) is no longer needed as ConstStaticCell::new() provides zero-initialized memory.

4. Add inline SAFETY comment explaining: singleton pattern, take() called once during init, reference lives for program duration
  </action>
  <verify>`cargo check --target riscv32imac-unknown-none-elf 2>&1 | head -50`</verify>
  <done>ServiceContainer uses ConstStaticCell pattern</done>
</task>

</tasks>

<verification>
Run full build check after all tasks:
`cargo check --target riscv32imac-unknown-none-elf 2>&1`

If host tests available:
`cargo test --target x86_64-unknown-linux-gnu 2>&1 | tail -30`
</verification>

<success_criteria>
1. No unsafe fn make_static in main.rs (replaced with StaticCell::init())
2. driver.rs get_usb_cdc_driver uses StaticCell with documented safety
3. driver.rs get_uart_driver uses StaticCell with documented safety  
4. ServiceContainer::get_instance uses ConstStaticCell pattern
5. Build succeeds with no warnings about static_mut_refs
</success_criteria>

<output>
After completion, create `.planning/phases/49-safety-static-fixes/49-01-SUMMARY.md`
</output>
