---
phase: 38-architecture-updates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internalDoc/ARCHITECTURE.md
autonomous: true

must_haves:
  truths:
    - "ARCHITECTURE.md documents OT2 command flow from parser through fan control"
    - "ARCHITECTURE.md documents READ telemetry as 4-value CSV format (ET,BT,HEATER,FAN)"
    - "ARCHITECTURE.md documents UNITS command as parse-only with no conversion"
    - "Task descriptions match actual v2.2 code (100ms control_loop, 5ms dual_output with CRLF)"
    - "Command handler chain diagram updated if new commands added"
  artifacts:
    - path: "internalDoc/ARCHITECTURE.md"
      provides: "Updated architecture documentation reflecting v2.2 implementation"
      min_lines: 200
  key_links:
    - from: "parser.rs parse_ot2_value"
      to: "roaster_refactored.rs SetFanSpeed handler"
      via: "ArtisanCommand::SetFanSpeed"
      pattern: "SetFanSpeed.*was_clamped"
    - from: "roaster_refactored.rs ReadStatus handler"
      to: "artisan.rs format_read_response_full"
      via: "ArtisanFormatter::format_read_response_full"
      pattern: "format_read_response_full"
---

<objective>
Update internalDoc/ARCHITECTURE.md to accurately document v2.2 implementation including OT2, READ, and UNITS command flows, and verify task descriptions match actual code.

Purpose: Ensure architecture documentation is accurate and useful for developers and maintainers. The current ARCHITECTURE.md is outdated (references 7-value CSV format, missing OT2/UNITS documentation).

Output: Updated internalDoc/ARCHITECTURE.md with:
- OT2 command flow section (parser → handler → fan control + safety clamping)
- READ telemetry section (4-value CSV format: ET,BT,HEATER,FAN)
- UNITS command section (parse → store, no conversion behavior)
- Verified task timing and descriptions (100ms control_loop, 5ms dual_output)
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@/home/juan/Repos/LibreRoaster/internalDoc/ARCHITECTURE.md
@/home/juan/Repos/LibreRoaster/src/input/parser.rs
@/home/juan/Repos/LibreRoaster/src/control/handlers.rs
@/home/juan/Repos/LibreRoaster/src/control/roaster_refactored.rs
@/home/juan/Repos/LibreRoaster/src/output/artisan.rs
@/home/juan/Repos/LibreRoaster/src/application/tasks.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit current ARCHITECTURE.md against v2.2 implementation</name>
  <files>internalDoc/ARCHITECTURE.md</files>
  <action>
Read the current internalDoc/ARCHITECTURE.md file completely.

Identify all outdated or missing sections that need updating for v2.2:

1. **OT2 Command Flow** (missing):
   - Check if OT2 is mentioned at all
   - Document the flow: parser.rs → roaster_refactored.rs → fan control
   - Note: parse_ot2_value() handles decimal rounding and clamping

2. **READ Telemetry Format** (outdated):
   - Current doc likely says "7-value CSV format"
   - Verify actual code in artisan.rs: format_read_response_full() returns 4 values
   - Format is: ET,BT,HEATER,FAN (not 7 values)

3. **UNITS Command** (missing):
   - Check if UNITS command is documented
   - Document: parser accepts C/F, stores in TemperatureSettings
   - Critical: No actual temperature conversion is applied (stored but not used)

4. **Task Timing** (verify):
   - control_loop_task: Check Timer::after(Duration::from_millis(100))
   - dual_output_task: Check Timer::after(Duration::from_millis(5)) + CRLF addition

Create a list of sections to add/update with line number references from source files.
</action>
  <verify>
Run: grep -n "OT2\|READ\|UNITS\|control_loop\|dual_output" internalDoc/ARCHITECTURE.md
Confirm: Document mentions these topics or gaps are identified
</verify>
  <done>
Audit complete with specific list of outdated sections and required updates
</done>
</task>

<task type="auto">
  <name>Task 2: Update ARCHITECTURE.md with v2.2 command flows</name>
  <files>internalDoc/ARCHITECTURE.md</files>
  <action>
Update internalDoc/ARCHITECTURE.md with the following sections:

## 1. Add "OT2 Command Flow" Section

Insert after the "Command Processing Flow" diagram or in a new "Command Handler Details" section:

```markdown
### OT2 Command Flow (Fan Speed Control)

The OT2 command sets fan speed with decimal value support and safety clamping.

**Flow:**
```
Artisan sends: "OT2 75.5"
    ↓
parse_artisan_command() [parser.rs:78-83]
    ↓
parse_ot2_value() [parser.rs:115-131]
    - Parses f32 decimal value
    - Rounds to nearest integer (0.5 rounds up)
    - Clamps to 0-100 range
    - Returns (clamped_value, was_clamped)
    ↓
ArtisanCommand::SetFanSpeed(value, was_clamped)
    ↓
process_artisan_command() [roaster_refactored.rs:374-385]
    ↓
RoasterCommand::SetFanManual(value)
    ↓
apply_manual_fan() [roaster_refactored.rs:203-228]
    - Sets status.fan_output
    - Calls fan.set_speed() hardware trait
    - Sets status.artisan_control = true
    ↓
Fan PWM output updated
```

**Safety Behavior:**
- If `was_clamped` is true (value was out of 0-100 range):
  - Fan is set to clamped value (0 or 100)
  - Heater is immediately stopped (safety measure)
  - Logged: "OT2 out of range - heater stopped, fan set to X%"

**Parser Details:**
- Accepts: "OT2 75", "OT2 75.5", "ot2 50" (case insensitive)
- Decimal rounding: 50.4 → 50, 50.5 → 51
- Clamping: -5 → 0 (was_clamped=true), 150 → 100 (was_clamped=true)
```

## 2. Update "READ Telemetry Format" Section

Find and update the READ response documentation:

**OLD (incorrect):**
```
READ returns: 7-value CSV (ET,BT,ET2,BT2,ambient,FAN,HEATER)
```

**NEW (correct):**
```markdown
### READ Command Response Format

The READ command returns a 4-value CSV format (changed from 7-value in v2.2):

```
ET,BT,HEATER,FAN
```

**Fields:**
| Position | Field | Source | Description |
|----------|-------|--------|-------------|
| 1 | ET | status.env_temp | Environment temperature (°C) |
| 2 | BT | status.bean_temp | Bean temperature (°C) |
| 3 | HEATER | status.ssr_output | Heater output percentage (0-100) |
| 4 | FAN | status.fan_output | Fan speed percentage (0-100) |

**Implementation:** [artisan.rs:111-119]
```rust
pub fn format_read_response_full(status: &SystemStatus) -> String {
    format!(
        "{:.1},{:.1},{:.1},{:.1}",
        status.env_temp,   // ET
        status.bean_temp,  // BT
        status.ssr_output, // Heater
        status.fan_output  // Fan
    )
}
```

**Note:** ET2, BT2, and ambient temperature fields were removed in v2.2
to match the actual hardware configuration (2 thermocouples only).
```

## 3. Add "UNITS Command State Management" Section

```markdown
### UNITS Command State Management

The UNITS command allows Artisan to specify temperature scale preference.

**Important:** The UNITS command only stores the preference - no actual
temperature conversion is applied. All internal temperatures remain in Celsius.

**Flow:**
```
Artisan sends: "UNITS;C" or "UNITS;F"
    ↓
parse_artisan_command() [parser.rs:46-50]
    - Accepts: C, c, F, f (case insensitive)
    - Returns: ArtisanCommand::Units(is_fahrenheit)
    ↓
process_artisan_command() [roaster_refactored.rs:426-434]
    - Creates TemperatureScale::Celsius or TemperatureScale::Fahrenheit
    - Stores in temp_settings via set_scale()
    - Logs: "Units command received - scale set to X"
```

**Current Behavior:**
- Preference is stored in `RoasterControl.temp_settings`
- No automatic temperature conversion occurs
- All sensor readings and calculations remain in Celsius
- Future enhancement: Apply conversion when formatting output for Artisan

**Parser Details:**
- Syntax: "UNITS;C" or "UNITS;F" (semicolon delimiter)
- Case insensitive: "units;c" and "UNITS;F" both valid
- Error: "UNITS;K" → ParseError::InvalidValue
```

## 4. Verify and Update Task Descriptions

Update the "Async Tasks" section to match actual v2.2 code:

```markdown
### Async Tasks

**Task 1: `control_loop_task`** [tasks.rs:12-101]
- **Period:** 100ms (10 Hz)
- **Responsibilities:**
  - Process Artisan commands from channel
  - Read temperature sensors
  - Update PID control
  - Format and send continuous output when enabled
- **Key Operations:**
  - Calls roaster.process_artisan_command() for each command
  - Calls roaster.read_sensors() every iteration
  - Calls roaster.update_control() for PID/heater updates
  - Formats output via MutableArtisanFormatter when continuous output enabled

**Task 2: `dual_output_task`** [tasks.rs:120-160]
- **Period:** 5ms (200 Hz)
- **Responsibilities:**
  - Route output to active communication channel (USB or UART)
  - Add CRLF line endings to output
- **Implementation:**
  - Reads from output_channel
  - Checks CommandMultiplexer for active channel
  - Appends "\r\n" to each message
  - Writes to USB CDC or UART0 based on active channel
```

## 5. Update Command Handler Chain

If there's a command handler diagram or list, update it to include:
- OT2 → ArtisanCommandHandler (via SetFanSpeed)
- READ → process_artisan_command → format_read_response_full
- UNITS → process_artisan_command → temp_settings
- UP/DOWN → ArtisanCommandHandler (IncreaseHeater/DecreaseHeater)

Add to the handler list in handlers.rs section:
```markdown
**Handler Implementations:**
- `TemperatureCommandHandler`: PID control, temperature setpoints, Start/Stop roast
- `SafetyCommandHandler`: Emergency shutdown, fault conditions
- `ArtisanCommandHandler`: Manual heater/fan override, UP/DOWN adjustments
- `SystemCommandHandler`: Reset functionality

**Direct Handlers (in RoasterControl):**
- `SetFanSpeed` (OT2): Direct fan control with safety clamping
- `ReadStatus` (READ): Telemetry response formatting
- `Units` (UNITS): Temperature scale preference storage
```
</action>
  <verify>
Run: grep -A5 "OT2 Command Flow\|READ Command Response\|UNITS Command" internalDoc/ARCHITECTURE.md
Confirm: All three sections exist with correct technical details
</verify>
  <done>
ARCHITECTURE.md updated with OT2 flow, READ 4-value format, UNITS state management, and verified task descriptions
</done>
</task>

</tasks>

<verification>
1. ARCHITECTURE.md contains new "OT2 Command Flow" section documenting:
   - Parser decimal handling and clamping
   - Handler chain from parser to fan control
   - Safety behavior when clamped

2. ARCHITECTURE.md "READ Command Response Format" section shows:
   - 4-value CSV format (not 7-value)
   - Field order: ET, BT, HEATER, FAN
   - Code reference to format_read_response_full

3. ARCHITECTURE.md contains "UNITS Command State Management" section documenting:
   - Parse-only behavior (no conversion)
   - Storage in TemperatureSettings
   - Clear note that internal temps remain Celsius

4. Task descriptions match code:
   - control_loop_task: 100ms period confirmed
   - dual_output_task: 5ms period with CRLF confirmed

5. No stale references to pre-v2.2 features remain
</verification>

<success_criteria>
- [ ] OT2 command flow documented from parser through fan control output with safety clamping behavior
- [ ] READ telemetry generation documented with 4-value CSV format (ET,BT,HEATER,FAN) - changed from 7-value
- [ ] UNITS command documented showing parse → store behavior with explicit note about no conversion
- [ ] Task descriptions updated to match actual v2.2 implementation (100ms control_loop, 5ms dual_output with CRLF)
- [ ] Document includes code references (file.rs:line numbers) for traceability
- [ ] "Last updated" timestamp added to document header
</success_criteria>

<output>
After completion, create `.planning/phases/38-architecture-updates/38-01-SUMMARY.md`

Summary should include:
- Sections added/updated in ARCHITECTURE.md
- Key findings from code audit (4-value vs 7-value discrepancy)
- Documentation gaps that were filled
- Timestamp of update
</output>
