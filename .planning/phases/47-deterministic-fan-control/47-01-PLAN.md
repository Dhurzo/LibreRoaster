---
phase: 47-deterministic-fan-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/hardware/ledc_bus.rs, src/hardware/fan.rs, src/main.rs]
autonomous: true
must_haves:
  truths:
    - "Fan and SSR updates go through one serialized LEDC path, so the timer never sees concurrent writes."
    - "LedcBus exposes handles for fan and SSR that call `set_duty`/`start_duty_fade` and log when they wait for the guard."
    - "The application wiring instantiates the shared bus once and passes the resulting handles into the hardware layers."
  artifacts:
    - path: "src/hardware/ledc_bus.rs"
      provides: "serial LEDC coordination with guard-aware logging, fade helpers, and typed channel handles for fan and SSR."
    - path: "src/hardware/fan.rs"
      provides: "FanController that now drives the shared bus, rounds to 8-bit duty, and records the applied hardware speed."
    - path: "src/main.rs"
      provides: "Shared bus initialization so the timer, fan channel, and SSR channel all live under one serializer."
  key_links:
    - from: "src/main.rs"
      to: "src/hardware/ledc_bus.rs"
      via: "LedcBus::new(timer, fan_channel, ssr_channel)"
      pattern: "LedcBus"
    - from: "src/hardware/fan.rs"
      to: "src/hardware/ledc_bus.rs"
      via: "FanController::set_speed uses LedcChannelHandle::set_duty or start_duty_fade"
      pattern: "LedcChannelHandle"
    - from: "src/hardware/ssr.rs"
      to: "src/hardware/ledc_bus.rs"
      via: "SsrControlSimple now takes a duty-reader handle from the bus"
      pattern: "LedcChannelHandle"
---

<objective>
Establish a shared LEDC bus so both fan and SSR updates run through one serializer that can optionally fade the fan without audible jumps.

Purpose: The fan needs deterministic writes and guard-aware logging before the SSR loop and telemetry rely on the same hardware timer.
Output: `src/hardware/ledc_bus.rs`, an updated `FanController` that drives the bus, and a boot path in `main.rs` that creates the bus.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/hardware/ssr.rs
@src/hardware/fan.rs
@src/main.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the serialized LEDC bus</name>
  <files>src/hardware/ledc_bus.rs</files>
  <action>Create `LedcBus` that owns the timer/monitor plus the fan and SSR channels behind a `critical_section::Mutex` (or embassy-sync mutex) so only one writer touches the hardware at a time. Provide typed `LedcChannelHandle` wrappers with methods for `set_duty` and `start_duty_fade`, instrument the guard by logging when `Mutex::lock` must wait, and expose the current duty so telemetry can read the value after each write.</action>
  <verify>cargo check --package libreroaster</verify>
  <done>The module compiles, exposes fan/SSR handles, and its API documents how to log waits, set duty, fade, and report the applied duty.</done>
</task>

<task type="auto">
  <name>Task 2: Rework `FanController` to drive the bus</name>
  <files>src/hardware/fan.rs</files>
  <action>Replace the ad-hoc `PWM_CHANNEL_STATE` with a `LedcChannelHandle` field. `FanController::set_speed` should clamp to 0–100%, convert to 8-bit ticks, call either `set_duty` or `start_duty_fade` through the bus (e.g., when the delta exceeds a threshold), update `current_speed` to the duty the bus reports, and keep `get_speed` returning the applied value.</action>
  <verify>cargo check --package libreroaster</verify>
  <done>FanController now depends on the bus handle, uses the bus logging/fade helpers, and `current_speed` mirrors the duty the LEDC actually sees.</done>
</task>

<task type="auto">
  <name>Task 3: Create the bus in `main.rs` for the fan path</name>
  <files>src/main.rs</files>
  <action>After configuring the timer and fan channel, build `LedcBus` with both the fan channel and the SSR channel monitor. Keep a `StaticCell` so the `'static bus can be passed to the fan controller (and later the SSR). Instantiate the updated `FanController` with the fan handle from the bus instead of `SimpleLedcFan`, and keep the rest of the builder logic the same.</action>
  <verify>cargo check --package libreroaster</verify>
  <done>The application boots with a single bus instance, the fan controller now calls `LedcBus`, and the previous `SimpleLedcFan` initialization is removed.</done>
</task>

</tasks>

<verification>
cargo check --package libreroaster
</verification>

<success_criteria>
LedcBus exists, `FanController` drives it, and `main.rs` initializes the bus so future SSR wiring can share the same mutex.
</success_criteria>

<output>
After completion, create `.planning/phases/47-deterministic-fan-control/47-01-SUMMARY.md`
</output>
