---
phase: 47-deterministic-fan-control
plan: 02
type: execute
wave: 2
depends_on: [47-01]
files_modified: [src/hardware/ssr.rs, src/main.rs, src/control/roaster_refactored.rs, tests/fan_serialization.rs]
autonomous: true
must_haves:
  truths:
    - "SSR commands now take the same serialized bus handle as the fan so the shared timer never sees overlapping writes."
    - "SystemStatus.fan_output and the RoasterControl log now reflect the duty that reached the LEDC channel, including how long the bus forced the write to wait."
    - "A host-side `tests/fan_serialization.rs` proves the guard always grants control to one channel at a time and that telemetry reports the applied duty from the bus."
  artifacts:
    - path: "src/hardware/ssr.rs"
      provides: "SSL/SSR control now accepts the bus handle with a duty reader and logs guard contention."
    - path: "src/control/roaster_refactored.rs"
      provides: "Telemetry updates for `SystemStatus::fan_output` that copy values from the bus and emit waits for logging."
    - path: "tests/fan_serialization.rs"
      provides: "Host test targeting the serialized bus to ensure writes execute one after the other and telemetry stashes the actual duty."
  key_links:
    - from: "src/hardware/ssr.rs"
      to: "src/hardware/ledc_bus.rs"
      via: "SsrControlSimple::new now takes a duty-reading handle from the bus"
      pattern: "LedcChannelHandle"
    - from: "src/control/roaster_refactored.rs"
      to: "src/hardware/fan.rs"
      via: "RoasterControl::apply_manual_fan and update_control copy the fan controller's applied speed into `SystemStatus::fan_output`."
      pattern: "fan.set_speed"
    - from: "tests/fan_serialization.rs"
      to: "src/hardware/ledc_bus.rs"
      via: "Stub channels drive the bus to assert serialization and telemetry reporting."
      pattern: "LedcBus"
---

<objective>
Bring SSR and telemetry onto the shared bus so the RoasterControl can report the duty that actually reached the hardware and we can prove serialization with a host test.

Purpose: Hardware telemetry and safety depend on the same serialized writer as the fan, and we need deterministic proof before exposing Servo/SSR concurrency to the executor.
Output: bus-aware SSR wiring, telemetry fixes in `RoasterControl`, and `tests/fan_serialization.rs` that run `cargo test --test fan_serialization`.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/hardware/ledc_bus.rs
@src/hardware/fan.rs
@src/control/roaster_refactored.rs
@src/hardware/ssr.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move SSR onto the LedcBus handle</name>
  <files>src/hardware/ssr.rs</files>
  <action>Change `SsrControlSimple::new`/`SsrControl::new_with_pwm_and_detection` to accept the duty-reader handle produced by `LedcBus`. Keep the duty-reading behavior (monitor + retry) while calling the handle's `set_duty`, log when the guard forced the write to wait, and expose the actual duty so the monitor structs still compile.</action>
  <verify>cargo check --package libreroaster</verify>
  <done>SSR control takes the bus handle, reuses the existing readback helper, and the log shows when the mutex had to wait before writing.</done>
</task>

<task type="auto">
  <name>Task 2: Route SSR initialization through the bus</name>
  <files>src/main.rs</files>
  <action>When building the bus (Plan 01), grab the SSR handle, wrap it with `LedcChannelMonitor`, and call the new SSR constructor that accepts both the handle and the detection pin. Pass the resulting controller to `AppBuilder::with_real_ssr` so the serialized path is the only writer to channel 1.</action>
  <verify>cargo check --package libreroaster</verify>
  <done>Main now fuels SSR via the shared bus handle, and no other code writes to channel 1 directly.</done>
</task>

<task type="auto">
  <name>Task 3: Log applied fan duty in RoasterControl and prove serialization with a test</name>
  <files>src/control/roaster_refactored.rs, tests/fan_serialization.rs</files>
  <action>After `fan.set_speed` returns in `apply_manual_fan` and `update_control`, read the `FanController`'s applied speed (e.g., new getter) and copy it into `SystemStatus::fan_output`. Emit a log entry when the bus wait duration exceeds a guard (use the instrumentation from the bus). Then add `tests/fan_serialization.rs` that drives a mock `LedcBus` with stub channels proving two writers cannot enter the guard simultaneously and that telemetry mirrors the reported duty. Make the host test hit both the fan path and `RoasterControl` to cover `SystemStatus::fan_output` updates.</action>
  <verify>cargo test --test fan_serialization
</verify>
  <done>`SystemStatus::fan_output` equals the duty the bus reports, logs grow when writes wait, and the new host test passes, proving serialization and telemetry alignment.</done>
</task>

</tasks>

<verification>
cargo test --test command_idempotence
cargo test --test fan_serialization
</verification>

<success_criteria>
SSR now runs through the shared bus, RoasterControl mirrors the applied fan duty in telemetry, and the serialization test (`tests/fan_serialization.rs`) passes.
</success_criteria>

<output>
After completion, create `.planning/phases/47-deterministic-fan-control/47-02-SUMMARY.md`
</output>
