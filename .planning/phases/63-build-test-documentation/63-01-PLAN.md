---
phase: 63-build-test-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
user_setup: []

must_haves:
  truths:
    - "User can follow step-by-step instructions to successfully build the firmware"
    - "User can run the test suite and host integration tests using the provided commands"
    - "User can understand and use development flags like async-lock-depth-metrics"
  artifacts:
    - path: "README.md"
      provides: "Build and test documentation"
      min_lines: 300
  key_links:
    - from: "README.md"
      to: "cargo build --release"
      via: "Build commands section"
    - from: "README.md"
      to: "cargo test"
      via: "Test commands section"
    - from: "README.md"
      to: "--features async-lock-depth-metrics"
      via: "Development flags section"
---

<objective>
Add clear, comprehensive build and test documentation to the README.

Purpose: Users can build the firmware, run tests, and use development flags without guessing.

Output: Updated README.md with dedicated Build, Test, and Development Flags sections.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance Build Instructions</name>
  <files>README.md</files>
  <action>
In the "Development" section of README.md, enhance the Build Commands subsection:

1. Add a clear "Prerequisites" subsection listing:
   - Rust toolchain (from rust-toolchain.toml: 1.88)
   - ESP32-C3 target: `rustup target add riscv32imac-unknown-none-elf`
   - espflash tool: `cargo install espflash`

2. Keep existing build commands but organize with clear headers:
   - Debug build: `cargo build`
   - Release build: `cargo build --release`
   - Clean build: `cargo clean`

3. Add a "Building for ESP32-C3" subsection explaining the embedded build:
   - Command: `cargo build --release --target riscv32imac-unknown-none-elf`
   - Output location: `target/riscv32imac-unknown-none-elf/release/libreroaster.bin`
   - Note about embedded target requiring `--target` flag

Existing "Flash Commands" subsection is good - keep it.
  </action>
  <verify>grep -A5 "## Build Commands" README.md && grep "rustup target add" README.md</verify>
  <done>README has Prerequisites section with rustup target and espflash install commands</done>
</task>

<task type="auto">
  <name>Task 2: Enhance Test Suite Instructions</name>
  <files>README.md</files>
  <action>
In the "Development" section of README.md, add a new "Test Commands" subsection after Build Commands:

1. Add basic test running:
   - Run all tests: `cargo test`
   - Run specific test: `cargo test test_name`
   - Run with output: `cargo test -- --nocapture`

2. Add host integration tests section:
   These tests run on the host (x86_64) without embedded hardware:
   
   - Command multiplexer concurrency test:
     ```bash
     cargo test --target x86_64-unknown-linux-gnu --test command_multiplexer_concurrency
     ```
   - Concurrent sensor test (with async lock metrics):
     ```bash
     cargo test --features async-lock-depth-metrics --target x86_64-unknown-linux-gnu --test concurrent_sensor_test
     ```
   - Mock UART integration tests:
     ```bash
     cargo test --target x86_64-unknown-linux-gnu --test mock_uart_integration
     ```
   - Artisan integration tests:
     ```bash
     cargo test --target x86_64-unknown-linux-gnu --test artisan_integration_test
     ```

3. Explain what each test validates (1-2 sentences each)

4. Add note: "Host tests do not require ESP32-C3 hardware"
  </action>
  <verify>grep -A3 "## Test Commands" README.md && grep "x86_64-unknown-linux-gnu" README.md</verify>
  <done>README has Test Commands section with host integration test instructions</done>
</task>

<task type="auto">
  <name>Task 3: Document Development Flags</name>
  <files>README.md</files>
  <action>
In the "Development" section of README.md, add a new "Development Features" subsection:

1. Create table of available Cargo features from Cargo.toml:

| Feature | Purpose | Command Example |
|---------|---------|-----------------|
| `async-lock-depth-metrics` | Enable async mutex depth instrumentation for concurrency testing | `cargo test --features async-lock-depth-metrics ...` |
| `std` | Enable standard library (for host tests) | `cargo test --features std ...` |
| `test` | Enable test helpers | `cargo test --features test ...` |
| `embedded` | Enable embedded binary build | `cargo build --features embedded ...` |

2. Document the async-lock-depth-metrics feature specifically:
   - What it does: Instruments embassy mutex to track lock depth
   - When to use: Running concurrent_sensor_test to verify no race conditions
   - How to interpret results: max_async_lock_depth should never exceed 1

3. Add section on combining features:
   - Example: `cargo test --features "std,test,async-lock-depth-metrics" --target x86_64-unknown-linux-gnu`

Existing "Concurrency Regression Test" and "Concurrent sensor read instrumentation" subsections provide some of this - enhance and formalize them.
  </action>
<verify>grep "async-lock-depth-metrics" README.md | head -3</verify>
<done>README documents async-lock-depth-metrics feature with usage examples</done>
</task>

</tasks>

<verification>
After all tasks:
1. grep "rustup target add" README.md returns prerequisites
2. grep "x86_64-unknown-linux-gnu" README.md shows host test commands
3. grep "async-lock-depth-metrics" README.md shows feature documentation
4. Check all three requirements from ROADMAP.md are addressed
</verification>

<success_criteria>
- BLD-01: Clear step-by-step build instructions with prerequisites exist in README
- BLD-02: Test suite and host integration test commands exist in README  
- BLD-03: Development flags (especially async-lock-depth-metrics) documented in README
</success_criteria>

<output>
After completion, create `.planning/phases/63-build-test-documentation/63-01-SUMMARY.md`
</output>
