---
phase: 77-memory-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/output/artisan.rs
autonomous: true
user_setup: []

must_haves:
  truths:
    - "ArtisanFormatter.bt_history uses heapless::Deque<f32, 5> instead of Vec<f32>"
    - "MutableArtisanFormatter.bt_history uses heapless::Deque<f32, 5> instead of Vec<f32>"
    - "No alloc::format! calls in hot path; uses core::write! with heapless::String"
    - "All existing tests pass without modification"
  artifacts:
    - path: src/output/artisan.rs
      provides: ArtisanFormatter and MutableArtisanFormatter with heapless collections
      contains: "Deque<f32, 5>" (twice)
      contains: "heapless::String"
  key_links:
    - from: ArtisanFormatter::update_bt_history
      to: Deque<f32, 5>
      via: pop_front() then push_back()
      pattern: "pop_front.*push_back"
    - from: ArtisanFormatter::format_time
      to: heapless::String
      via: core::write! macro
      pattern: "write!.*heapless::String"
---

<objective>
Replace heap-allocated collections in ArtisanFormatter with heapless alternatives to eliminate heap allocations in the BT history tracking hot path.

Purpose: Reduce memory fragmentation and allocation overhead in embedded environment by using stack-allocated fixed-capacity collections.
Output: Modified src/output/artisan.rs with heapless::Deque<f32, 5> and heapless::String
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/output/artisan.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Vec with Deque in ArtisanFormatter</name>
  <files>src/output/artisan.rs</files>
  <action>
1. Add imports: `use heapless::{Deque, String};` (replace lines 16-18: `use alloc::format;`, `use alloc::string::String;`, `use alloc::vec::Vec;`)
2. Change line 25: `bt_history: Vec<f32>` → `bt_history: Deque<f32, 5>`
3. Change line 33: `bt_history: Vec::new()` → `bt_history: Deque::<f32, 5>::new()`
4. Change line 40: `self.bt_history.clear()` → reinitialize with `self.bt_history = Deque::<f32, 5>::new();`
5. Modify update_bt_history function (lines 51-56):
   - Replace `history.remove(0)` with `history.pop_front()` then `history.push_back(current_bt)`
   - Handle the Result from push_back: use `let _ = history.push_back(current_bt);`
</action>
  <verify>`cargo check --features artisan 2>&1 | head -50` to verify compilation</verify>
  <done>ArtisanFormatter.bt_history uses heapless::Deque<f32, 5> - verified by grep finding "Deque<f32, 5>" in struct definition</done>
</task>

<task type="auto">
  <name>Task 2: Replace Vec with Deque in MutableArtisanFormatter</name>
  <files>src/output/artisan.rs</files>
  <action>
1. Change line 193: `bt_history: Vec<f32>` → `bt_history: Deque<f32, 5>`
2. Change line 201: `bt_history: Vec::new()` → `bt_history: Deque::<f32, 5>::new()`
3. Change line 208: `self.bt_history.clear()` → `self.bt_history = Deque::<f32, 5>::new();`
4. The function calls to ArtisanFormatter::update_bt_history remain the same (they accept &mut Deque now)
</action>
  <verify>`cargo check --features artisan 2>&1 | head -50` to verify compilation</verify>
  <done>MutableArtisanFormatter.bt_history uses heapless::Deque<f32, 5> - verified by grep finding "Deque<f32, 5>" in struct definition</done>
</task>

<task type="auto">
  <name>Task 3: Replace alloc::format! with heapless::String + core::write!</name>
  <files>src/output/artisan.rs</files>
  <action>
1. Replace format_time function (line 72-74):
   - Change return type from String to heapless::String<8> (max "999.99" = 7 chars + null)
   - Use `let mut buf = heapless::String::<8>::new();`
   - Use `core::write!(&mut buf, "{}.{:02}", elapsed_secs, elapsed_ms / 10).unwrap();`
   - Return buf
2. Replace format_artisan_line function (line 76-78):
   - Change return type to heapless::String<32> (fits: time,ET,BT,ROR,Gas = ~25 chars)
   - Use `let mut buf = heapless::String::<32>::new();`
   - Use `core::write!(&mut buf, "{},{:.1},{:.1},{:.2},{:.1}", time_str, et, bt, ror, gas).unwrap();`
   - Return buf
3. Keep format_read_response, format_read_response_full, format_status_response using alloc::format! (not in hot path - called infrequently)
4. Keep format_chan_ack and format_err using alloc::format! (not in hot path)
</action>
  <verify>`cargo check --features artisan 2>&1 | head -50` to verify compilation</verify>
  <done>Hot path functions use core::write! with heapless::String - verified by grep finding "core::write!" in format_time and format_artisan_line</done>
</task>

</tasks>

<verification>
1. Run `cargo test --features artisan 2>&1` to verify all tests pass
2. Check that no alloc::format! is used in format_time or format_artisan_line (hot path)
3. Verify both formatters use Deque<f32, 5> for bt_history
</verification>

<success_criteria>
1. ArtisanFormatter.bt_history uses heapless::Deque<f32, 5> instead of Vec<f32>
2. MutableArtisanFormatter.bt_history uses heapless::Deque<f32, 5> instead of Vec<f32>
3. No alloc::format! calls in hot path; uses core::write! with heapless::String
4. All existing tests pass without modification (cargo test passes)
</success_criteria>

<output>
After completion, create `.planning/phases/77-memory-optimization/77-01-SUMMARY.md`
</output>
