---
phase: 66-instrumentation-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/constants.rs
  - src/input/parser.rs
  - src/control/roaster_refactored.rs
  - src/output/artisan.rs
  - src/application/tasks.rs
  - internalDoc/INSTRUMENTATION_README.MD
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Automation can poll `STATUS` and parse a CSV that lists ET, BT, heater, fan, watchdog health, consecutive failures, guard timeouts, and regression activity in fixed columns."
    - "Watchdog feed state, failure reason, guard counts, and regression active flags appear in the new payload so instrumentation monitoring can correlate events without touching `READ`."
    - "Internal instrumentation docs explain the `STATUS` payload and parsing expectations for auditors and automation harnesses."
  artifacts:
    - path: "src/input/parser.rs"
      provides: "Artisan command parser extension for `STATUS` plus regression tests."
      contains: "ArtisanCommand::StatusReport, parse_artisan_command branch, parser unit tests."
    - path: "src/output/artisan.rs"
      provides: "Formatter that renders the instrumentation CSV and accompanying unit tests."
      contains: "format_status_response including watchdog/guard/regression fields."
    - path: "src/application/tasks.rs"
      provides: "Control loop wiring that routes STATUS responses through the Artisan output channel."
      contains: "Detects `StatusReport` and sends the CSV string via the shared output channel."
    - path: "internalDoc/INSTRUMENTATION_README.MD"
      provides: "Documentation of the automation-friendly telemetry command."
      contains: "STATUS payload example, field descriptions, automation guidance."
  key_links:
    - from: "src/input/parser.rs"
      to: "src/control/roaster_refactored.rs"
      via: "New `ArtisanCommand::StatusReport` variant handled inside `RoasterControl::process_artisan_command`."
    - from: "src/control/roaster_refactored.rs"
      to: "src/output/artisan.rs"
      via: "Calls to `ArtisanFormatter::format_status_response` when `STATUS` is processed."
    - from: "src/application/tasks.rs"
      to: "src/output/artisan.rs"
      via: "The control loop routes formatted output to the shared Artisan output channel for transmission."
---

<objective>
Expose watchdog, LEDC guard, and regression telemetry via a dedicated `STATUS` Artisan command so automation harnesses can poll instrumentation without breaking the standard `READ` stream.

Purpose: Make the safety state machine observable by machines and auditors, giving them precise values for watchdog health, guard hits, and regression activity alongside the familiar sensor readings.
Output: Parser updates, formatter helpers, control loop wiring, and documentation for the new payload.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/66-instrumentation-observability/66-RESEARCH.md
@internalDoc/INSTRUMENTATION_README.MD
</context>

<tasks>

<task type="auto">
  <name>Task 1: Teach the parser about the STATUS command</name>
  <files>src/config/constants.rs; src/input/parser.rs</files>
  <action>
Add `ArtisanCommand::StatusReport` (or similarly named) to `src/config/constants.rs`, update `parse_artisan_command` to return it for `STATUS` (and optionally `STAT`) with the usual whitespace handling, and extend the parser unit tests so `STATUS` always round-trips. Keep the branch ahead of other commands so `ReadStatus` remains unchanged.
  </action>
  <verify>cargo test input::parser::test_parse_status_command</verify>
  <done>The parser recognizes `STATUS`, returns `ArtisanCommand::StatusReport`, and the new test ensures the variant is returned consistently.</done>
</task>

<task type="auto">
  <name>Task 2: Format the STATUS payload and route it through the control loop</name>
  <files>src/control/roaster_refactored.rs; src/output/artisan.rs; src/application/tasks.rs</files>
  <action>
Add `ArtisanFormatter::format_status_response(status: &SystemStatus)` that emits ET, BT, heater, fan, watchdog feed flag, consecutive failures, last failure reason (falling back to `none`), LEDC guard timeout count, and the regression active flag as comma-separated values. Update `RoasterControl::process_artisan_command` to call this formatter when handling `StatusReport` and have the control loop (in `application/tasks.rs`) send the resulting string through the output channel just like `READ`. Add formatter tests to guard the exact column order.
  </action>
  <verify>cargo test output::artisan::test_format_status_response</verify>
  <done>The control loop publishes a deterministic instrumentation CSV that automation can parse, and unit tests fail if the column order changes.</done>
</task>

<task type="auto">
  <name>Task 3: Document the STATUS payload for automation</name>
  <files>internalDoc/INSTRUMENTATION_README.MD</files>
  <action>
Append a section explaining the new `STATUS` command, include a sample response, outline each column (watchdog flag, failure reason, guard counts, regression active), and explain how automation should interpret the payload versus the existing `READ`. Emphasize that `STATUS` is the automation hook for instrumentation without altering the legacy stream.
  </action>
  <verify>rg -n "STATUS" internalDoc/INSTRUMENTATION_README.MD</verify>
  <done>The instrumentation README explicitly documents the `STATUS` command, its CSV columns, and how automation or auditors should parse the response.</done>
</task>

</tasks>

<verification>
- `cargo fmt -- --check`
- `cargo check`
- `cargo test input::parser::test_parse_status_command`
- `cargo test output::artisan::test_format_status_response`
</verification>

<success_criteria>
- Automation can call `STATUS` to get a CSV telemetry snapshot containing watchdog health, last failure reason, guard timeout count, and regression activity without touching `READ`.
- The CSV formatter is covered by unit tests so the column order stays stable for automation parses.
- Instrumentation docs instruct auditors/harnesses to read the `STATUS` payload and describe how each metric maps to the safety stack.
</success_criteria>

<output>
After completion, create `.planning/phases/66-instrumentation-observability/66-01-SUMMARY.md`
</output>
