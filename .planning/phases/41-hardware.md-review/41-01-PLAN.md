---
phase: 41-hardware.md-review
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: ["internalDoc/hardware.md"]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "hardware.md documents 4-value READ format (ET,BT,HEATER,FAN) matching v2.2"
    - "OT2 fan control command appears in supported commands table"
    - "Communication diagram shows correct 4-value format"
    - "Document has current timestamp (v2.3)"
  artifacts:
    - path: "internalDoc/hardware.md"
      provides: "Updated hardware documentation"
      contains: "ET,BT,HEATER,FAN"
    - path: "internalDoc/hardware.md"
      provides: "OT2 command documented"
      contains: "OT2"
  key_links:
    - from: "internalDoc/hardware.md READ section"
      to: "src/output/artisan.rs format_read_response"
      via: "4-value format consistency"
      pattern: "ET,BT,HEATER,FAN"
---

<objective>
Update hardware.md to accurately reflect v2.2 protocol implementation.

**Purpose:** Fix documentation discrepancies identified during research to ensure hardware.md matches actual v2.2 behavior.

**Output:** Updated internalDoc/hardware.md with corrected READ format, OT2 command documentation, and current timestamp.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/juan/Repos/LibreRoaster/.planning/phases/41-hardware.md-review/41-RESEARCH.md

**Key Issues to Fix:**
1. Line 358: Wrong format `time,ET,BT,ROR,Gas` → should be `ET,BT,HEATER,FAN`
2. Line 365: Caption says `ET,BT,Power,Fan` but needs consistency with 4-value format
3. Line 387: Communication diagram shows wrong format
4. Commands table missing OT2 command
5. No timestamp visible - add "Last Updated: 2026-02-08 (v2.3)"
</context>

<tasks>

<task type="auto">
  <name>Update hardware.md to v2.2 specifications</name>
  <files>internalDoc/hardware.md</files>
  <action>
    Fix four documentation issues in internalDoc/hardware.md:

    **1. Line 358 - UART Format:**
    Change from: `| Formato | `time,ET,BT,ROR,Gas` |`
    Change to: `| Formato | `ET,BT,HEATER,FAN` |`
    (Remove "time," prefix - v2.2 uses 4-value format without timestamp)

    **2. Line 365 - READ Command Response:**
    Change from: `| READ | `READ` | Retorna: `ET,BT,Power,Fan` |`
    Change to: `| READ | `READ` | Retorna: `ET,BT,HEATER,FAN` |`
    (Use uppercase HEATER/FAN for consistency with source code)

    **3. Add OT2 Command to Supported Commands Table (after IO3 row, line 368):**
    Add new row: `| OT2 x | `OT2 <0-100>` | Control ventilador con decimales (%) |`

    **4. Line 387 - Communication Diagram Format:**
    Change from: `"time,ET,BT,ROR,Gas\r\n"`
    Change to: `"ET,BT,HEATER,FAN\r\n"`

    **5. Add Timestamp to Document Header (after line 1):**
    Add: `> Last Updated: 2026-02-08 (v2.3)`
    (Place before first ## heading)
  </action>
  <verify>
    grep -E "ET,BT,HEATER,FAN" internalDoc/hardware.md | wc -l
    # Should find at least 3 occurrences (format, READ command, diagram)
    grep -i "OT2" internalDoc/hardware.md
    # Should show OT2 command in table
    grep -i "Last Updated.*v2.3" internalDoc/hardware.md
    # Should show timestamp
  </verify>
  <done>
    - Line 358 shows `ET,BT,HEATER,FAN` format
    - Line 365 shows correct READ response format
    - OT2 command row exists in supported commands table
    - Line 387 diagram shows 4-value format
    - Timestamp "Last Updated: 2026-02-08 (v2.3)" appears in header
  </done>
</task>

</tasks>

<verification>
**Post-update verification:**

```bash
# Check 4-value format appears in 3+ places
grep -c "ET,BT,HEATER,FAN" internalDoc/hardware.md
# Expected: 3 or more

# Verify OT2 command exists
grep -c "OT2" internalDoc/hardware.md
# Expected: 2+ (command table + description)

# Verify timestamp
grep "Last Updated.*v2.3" internalDoc/hardware.md
# Expected: 1 match

# Confirm no old 7-value format remains
grep "time,ET,BT,ROR,Gas" internalDoc/hardware.md
# Expected: 0 matches (old format should be removed)
```

**Cross-reference with source code:**
```bash
# Verify format_read_response matches documentation
grep -A2 "format_read_response" src/output/artisan.rs
# Expected: 4 comma-separated values
```
</verification>

<success_criteria>
- [x] Line 358 UART format shows `ET,BT,HEATER,FAN` (4-value, no time prefix)
- [x] Line 365 READ command response shows `ET,BT,HEATER,FAN`
- [x] OT2 command appears in supported commands table with decimal fan control
- [x] Line 387 communication diagram shows correct 4-value format
- [x] Timestamp "Last Updated: 2026-02-08 (v2.3)" in document header
- [x] No references to old 7-value format remain
- [x] Document matches v2.2 protocol implementation
</success_criteria>

<output>
After completion, create `.planning/phases/41-hardware.md-review/41-01-SUMMARY.md`
</output>
