---
phase: 82-dead-code-and-dependency-cleanup
plan: 82-02
type: execute
wave: 2
depends_on:
  - 82-01
files_modified:
  - scripts/dead-code-removal.sh
  - quality/dead-code/removal-guidelines.md
autonomous: true
must_haves:
  truths:
    - "Dead code is removed in small batches where each candidate references the inventory entry and the removal run passes the gated test baseline."
    - "Each removal batch records how tests and the quality baseline behaved so reviewers can see the regression verification for DC-02."
  artifacts:
    - path: "scripts/dead-code-removal.sh"
      provides: "Batch runner that applies inventory candidates, runs cargo test + quality baseline, and stores a batch summary."
    - path: "quality/dead-code/removal-guidelines.md"
      provides: "Step-by-step process for forming batches, annotating evidence, and invoking the removal script."
  key_links:
    - from: "scripts/dead-code-removal.sh"
      to: "scripts/quality-baseline.sh"
      via: "Quality baseline invocation after candidate removal to prove DC-02 compliance."
      pattern: "quality-baseline"
    - from: "quality/dead-code/removal-guidelines.md"
      to: "quality/dead-code/README.md"
      via: "Guidance that references the inventory before batching removals."
      pattern: "inventory"
---

<objective>
Create a guided dead-code removal workflow so small batches are verified against the baseline before merging.
Purpose: DC-02 demands regression evidence for each removal decision, so we need tooling and documentation that enforce the gating sequence.
Output: `scripts/dead-code-removal.sh` and `quality/dead-code/removal-guidelines.md` that describe how to form batches, run tests, and record outcomes.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement removal batch runner</name>
  <files>scripts/dead-code-removal.sh</files>
  <action>
    Add a script that:
    - Accepts `BATCH_NAME` and a whitespace-separated `MODULES` list so reviewers know which inventory entries are touched.
    - Copies the list into `quality/dead-code/batches/${BATCH_NAME}.md` before and after removal so diffs are recorded.
    - Runs `cargo test --locked --lib --tests --no-fail-fast` followed by `scripts/quality-baseline.sh` to prove gates still pass.
    - Emits a summary (status, test output path, baseline exit code) into the batch file and prints the summary on stdout.
    - Exits non-zero if any gate fails, leaving the batch file for triage.
  </action>
  <verify>
    Set `BATCH_NAME=demo MODULES=src/control` and execute `scripts/dead-code-removal.sh`, ensuring it runs both `cargo test` and `scripts/quality-baseline.sh`, then inspect `quality/dead-code/batches/demo.md` for the recorded test/baseline statuses.
  </verify>
  <done>
    The batch runner is executable, writes a per-batch summary, and enforces the test + baseline gates before printing a pass/fail status so each change leaves evidence.
  </done>
</task>

<task type="auto">
  <name>Task 2: Document batch removal workflow</name>
  <files>quality/dead-code/removal-guidelines.md</files>
  <action>
    Write guidelines that:
    - Refer back to the inventory (DC-01) when picking modules for a batch.
    - Describe the required actions: capture inventory snapshot, run `scripts/dead-code-removal.sh`, review `quality/dead-code/batches/<name>.md`, and link to the gated baseline report.
    - Call out how to treat failed batches (capture logs, revert, or reassign risk level).
  </action>
  <verify>
    `rg -n "batch" quality/dead-code/removal-guidelines.md` should show sections on forming batches, running the script, and linking to baseline output.
  </verify>
  <done>
    The guidelines make it explicit how to run the removal script, handle failures, and attach the evidence required for DC-02 reviewers.
  </done>
</task>

</tasks>

<verification>
Create a removal batch (e.g., `BATCH_NAME=demo MODULES=src/control`) and confirm the script runs tests + `scripts/quality-baseline.sh`. Ensure `quality/dead-code/batches/demo.md` records the gate results and the guidelines mention this flow.
</verification>

<success_criteria>
Removal workflow tooling exists, runs the gates for each batch, records evidence for reviewers, and the guidelines explain how to operate the script and surface failures.
</success_criteria>

<output>
After completion, create `.planning/phases/82-dead-code-and-dependency-cleanup/82-02-SUMMARY.md`
</output>
