---
phase: 82-dead-code-and-dependency-cleanup
plan: 82-03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/dependency-audit.sh
  - .planning/quality/dependency-allowlist.toml
  - quality/dead-code/dependency-allowlist.md
autonomous: true
must_haves:
  truths:
    - "Users can run a combined `machete` + `cargo +nightly udeps` workflow that respects an allowlist before removing dependencies."
    - "Every audit run references the allowlist and records its findings so reviewers can justify each deletion."
  artifacts:
    - path: "scripts/dependency-audit.sh"
      provides: "Audit runner that executes machete + nightly udeps, records skipped allowlisted entries, and archives a summary."
    - path: ".planning/quality/dependency-allowlist.toml"
      provides: "Declarative allowlist of intentional dependencies with rationale for DC-03 reviewers."
    - path: "quality/dead-code/dependency-allowlist.md"
      provides: "Instructions for updating the allowlist and interpreting the audit outputs."
  key_links:
    - from: "scripts/dependency-audit.sh"
      to: ".planning/quality/dependency-allowlist.toml"
      via: "Reads allowlist entries to skip expected packages and annotate why they remain."
      pattern: "allowlist"
    - from: "quality/dead-code/dependency-allowlist.md"
      to: "scripts/dependency-audit.sh"
      via: "Explains how the script consumes the allowlist and where to find summary files."
      pattern: "machete"
---

<objective>
Automate the dependency audit workflow so DC-03 reviewers can rerun machete/udeps with documented allowlist exceptions.
Purpose: Detect unused crates without banning nightly tooling, and keep allowlisted dependencies justified for the milestone audit.
Output: `scripts/dependency-audit.sh`, `.planning/quality/dependency-allowlist.toml`, and `quality/dead-code/dependency-allowlist.md` describing how to operate the workflow.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dependency audit runner</name>
  <files>scripts/dependency-audit.sh</files>
  <action>
    Develop a script that:
    - Accepts `ALLOWLIST_FILE` (default `.planning/quality/dependency-allowlist.toml`).
    - Calls `cargo machete --all-targets --all-features --output-format text` and captures the output under `quality/dead-code/dependency/audit-<timestamp>-machete.log`.
    - Runs `cargo +nightly udeps --all-targets --all-features --quiet`, writes the list of unused dependencies into `quality/dead-code/dependency/audit-<timestamp>-udeps.log`, and tags entries that appear in the allowlist.
    - Exits non-zero if new unused dependencies outside the allowlist are detected, printing the offending package names for reviewers.
  </action>
  <verify>
    Run `ALLOWLIST_FILE=.planning/quality/dependency-allowlist.toml scripts/dependency-audit.sh` and confirm both machete and udeps logs land in `quality/dead-code/dependency` and the script honors the allowlist entries.
  </verify>
  <done>
    The audit script exists, runs machete + nightly udeps, honors the declared allowlist, and emits summary logs for later review.
  </done>
</task>

<task type="auto">
  <name>Task 2: Document dependency allowlist process</name>
  <files>
    .planning/quality/dependency-allowlist.toml
    quality/dead-code/dependency-allowlist.md
  </files>
  <action>
    - Create a TOML allowlist with placeholder entries (`package`, `reason`, `expires`) for intentional dependencies that `machete`/`udeps` flag but must stay.
    - Write a Markdown companion describing how to update the allowlist, how to interpret the audit logs, and how reviewers should sign off on exceptions.
  </action>
  <verify>
    Confirm the allowlist contains at least one `[allow]` table with `package`, `reason`, `expires`, and `rg -n "machete" quality/dead-code/dependency-allowlist.md` to ensure guidance mentions the toolchain steps.
  </verify>
  <done>
    The allowlist template plus documentation make it easy to justify each dependency flagged during DC-03 audits.
  </done>
</task>

</tasks>

<verification>
Run `scripts/dependency-audit.sh` against the allowlist file and review `quality/dead-code/dependency` logs to ensure machete/udeps output is captured and allowlisted packages stay acknowledged.
</verification>

<success_criteria>
Dependency audit tooling exists, honors the allowlist, and the documentation explains how to keep the exceptions current, satisfying DC-03.
</success_criteria>

<output>
After completion, create `.planning/phases/82-dead-code-and-dependency-cleanup/82-03-SUMMARY.md`
</output>
