---
phase: 78-ssr-deduplication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hardware/ssr.rs
autonomous: true

must_haves:
  truths:
    - "Both SsrControl and SsrControlSimple use detect_heat_source from SsrControlBase"
    - "No duplicate detect_heat_source code exists in the codebase"
    - "All SSR tests pass after refactoring"
  artifacts:
    - path: "src/hardware/ssr.rs"
      provides: "SsrControlBase with detect_heat_source method"
      min_lines: 650
  key_links:
    - from: "SsrControl"
      to: "SsrControlBase::detect_heat_source"
      via: "delegate method call"
    - from: "SsrControlSimple"  
      to: "SsrControlBase::detect_heat_source"
      via: "delegate method call"
---

<objective>
Extract detect_heat_source() method from SsrControl and SsrControlSimple into SsrControlBase, eliminating ~30 lines of duplicate code.

Purpose: SSR-06 requirement - eliminate code duplication while maintaining identical behavior
Output: Refactored ssr.rs with detect_heat_source in SsrControlBase
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@src/hardware/ssr.rs

## Current Code Structure (duplicate):

**SsrControl.detect_heat_source** (lines 213-246):
- Reads detection_pin.is_low()
- Updates base.hardware_status based on detection
- Logs status changes
- Sets base.last_detection_check

**SsrControlSimple.detect_heat_source** (lines 329-362):
- IDENTICAL implementation - reads detection_pin.is_low()
- Updates base.hardware_status based on detection  
- Logs status changes
- Sets base.last_detection_check

## Solution Approach:

1. Add `detect_heat_source` method to `SsrControlBase` that takes a mutable reference to update hardware_status
2. The base method needs access to detection pin state - can be implemented as a generic that accepts a closure for reading the pin
3. Both SsrControl and SsrControlSimple delegate to the new base method
4. Remove duplicate implementations from both structs
</context>

<tasks>

<task type="auto">
  <name>Add detect_heat_source to SsrControlBase</name>
  <files>src/hardware/ssr.rs</files>
  <action>
Add a new `detect_heat_source` method to `SsrControlBase` that:
1. Takes a closure or function that returns `Result&lt;bool, E&gt;` for reading the detection pin
2. Performs the same logic as the current duplicate implementations:
   - Read pin state (is_low = available, not is_low = not detected)
   - Update hardware_status if changed
   - Log status changes (info for Available, warn for NotDetected)
   - Set last_detection_check timestamp
   - Handle error case (pin read failure -> Error status)
3. Returns Result&lt;(), SsrError&gt;

The method signature should work for both SsrControl and SsrControlSimple.
  </action>
  <verify>
Method exists in SsrControlBase impl block and compiles</verify>
  <done>SsrControlBase has detect_heat_source method that handles detection logic</done>
</task>

<task type="auto">
  <name>Refactor SsrControl to use base method</name>
  <files>src/hardware/ssr.rs</files>
  <action>
1. Replace the current detect_heat_source implementation in SsrControl (lines 213-246) with a delegation to SsrControlBase
2. The delegation should pass a closure that reads self.detection_pin.is_low()
3. Update the HeatSourceDetector impl to call the base method instead of the old SsrControl::detect_heat_source
4. Keep the method signature compatible with the HeatSourceDetector trait
  </action>
  <verify>
cargo check passes, no duplicate code in SsrControl</verify>
  <done>SsrControl delegates to SsrControlBase::detect_heat_source</done>
</task>

<task type="auto">
  <name>Refactor SsrControlSimple to use base method</name>
  <files>src/hardware/ssr.rs</files>
  <action>
1. Replace the current detect_heat_source implementation in SsrControlSimple (lines 329-362) with a delegation to SsrControlBase
2. The delegation should pass a closure that reads self.detection_pin.is_low()
3. Update the HeatSourceDetector impl to call the base method
4. Ensure periodic_check still works correctly (it calls detect_heat_source internally)
  </action>
  <verify>
cargo check passes, no duplicate code in SsrControlSimple</verify>
  <done>SsrControlSimple delegates to SsrControlBase::detect_heat_source</done>
</task>

<task type="auto">
  <name>Verify tests pass</name>
  <files>src/hardware/ssr.rs</files>
  <action>
Run cargo test to ensure:
1. All SSR unit tests pass
2. The refactoring did not break existing behavior
3. No regressions in the detection logic
</action>
<verify>cargo test --lib 2>&1 | tail -20</verify>
<cargo test passes for all SSR tests>
</done>
</task>

</tasks>

<verification>
1. Verify detect_heat_source exists only in SsrControlBase (grep for the method)
2. Verify SsrControl and SsrControlSimple no longer have private detect_heat_source methods
3. Run cargo test to confirm all tests pass
</verification>

<success_criteria>
1. detect_heat_source() method exists in SsrControlBase (not duplicated)
2. Both SsrControl and SsrControlSimple delegate to base implementation
3. ~30 lines of duplicate code eliminated
4. All SSR tests pass after refactoring
</success_criteria>

<output>
After completion, create `.planning/phases/78-ssr-deduplication/78-01-SUMMARY.md`
</output>
