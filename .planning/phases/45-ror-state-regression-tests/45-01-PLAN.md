---
phase: 45-ror-state-regression-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/output/artisan.rs
  - src/application/tasks.rs
  - tests/artisan_integration_test.rs
autonomous: true

must_haves:
  truths:
    - "After the second BT sample, ROR is non-zero on the first BT change and stays 0 when BT is unchanged"
    - "ROR tracking resets at roast session start/stop so new sessions begin with ROR at 0"
    - "READ responses remain terminator-free and output framing appends exactly one CRLF"
  artifacts:
    - path: "src/output/artisan.rs"
      provides: "MutableArtisanFormatter ROR timing with reset-aware state"
    - path: "src/application/tasks.rs"
      provides: "Session-bound formatter reset and single-point CRLF framing"
    - path: "tests/artisan_integration_test.rs"
      provides: "Regression coverage for ROR timing/reset and READ framing"
  key_links:
    - from: "src/application/tasks.rs"
      to: "src/output/artisan.rs"
      via: "control_loop_task resets MutableArtisanFormatter on output state change"
      pattern: "formatter\.reset"
    - from: "src/application/tasks.rs"
      to: "\"\\r\\n\""
      via: "dual_output_task appends terminator"
      pattern: "extend_from_slice\(b\"\\r\\n\"\)"
---

<objective>
Correct MutableArtisanFormatter ROR timing and session resets, then lock READ framing + ROR behavior with regression tests.

Purpose: Ensure ROR becomes non-zero only after the second BT sample and resets across roast sessions while preventing framing regressions.
Output: Updated formatter/session wiring plus regression tests for ROR and READ terminators.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-ror-state-regression-tests/45-CONTEXT.md
@.planning/phases/45-ror-state-regression-tests/45-RESEARCH.md
@src/output/artisan.rs
@src/application/tasks.rs
@tests/artisan_integration_test.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align ROR timing with session boundaries</name>
  <files>src/output/artisan.rs, src/application/tasks.rs</files>
  <action>
Update `MutableArtisanFormatter` so ROR stays 0 until the second sample and only becomes non-zero when BT changes.
Implement the logic inside `calculate_ror` to:
- Return 0.0 on the first sample (initialize `last_bt` and history).
- Return 0.0 when the current BT equals the last BT (treat unchanged samples as no change).
- Update `last_bt` every call and compute ROR from history only when a change occurs.
Keep `reset()` clearing `last_bt` and history.

In `control_loop_task`, track continuous output state transitions and call `formatter.reset()` when output switches on or off so ROR restarts on START/STOP sessions.
Use a `was_continuous` boolean to avoid repeated resets on steady state.
  </action>
  <verify>cargo test --features test --lib output::artisan</verify>
  <done>MutableArtisanFormatter returns 0.0 until the second sample with a BT change and resets cleanly on continuous output transitions.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression tests for ROR timing and READ framing</name>
  <files>tests/artisan_integration_test.rs, src/application/tasks.rs</files>
  <action>
Add integration coverage that asserts:
- ROR stays 0 for the first two samples when BT is unchanged, and becomes non-zero on the first BT change after those samples.
- Calling `MutableArtisanFormatter::reset()` returns ROR to 0 for the next sample.

Add a READ framing regression test that verifies `ArtisanFormatter::format_read_response_full()` does not contain `\r` or `\n`.

In `src/application/tasks.rs`, add a small unit test for the CRLF append helper/logic used by `dual_output_task` to ensure exactly one CRLF is appended for a payload without terminators.
Keep the test deterministic and string-based (no async runtime).
  </action>
  <verify>cargo test --features test --test artisan_integration_test</verify>
  <done>Tests assert ROR timing/reset behavior and READ response framing stays terminator-free with a single CRLF boundary.</done>
</task>

</tasks>

<verification>
- `cargo test --features test --test artisan_integration_test`
- `cargo test --features test --lib output::artisan`
</verification>

<success_criteria>
- ROR remains 0 until the second BT sample and becomes non-zero on the first BT change, with resets at session start/stop.
- READ response formatting stays CRLF-free and output framing appends a single terminator.
- Regression tests cover both ROR timing/reset and READ terminator behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/45-ror-state-regression-tests/45-01-SUMMARY.md`
</output>
