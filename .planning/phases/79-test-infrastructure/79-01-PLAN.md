---
phase: 79-test-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib.rs
  - src/common/mod.rs
  - tests/common/mod.rs
autonomous: true

must_haves:
  truths:
    - "StubHeater, StubFan, StubThermometer exist in src/common/mod.rs"
    - "Stubs have pub(crate) visibility for crate-internal access"
    - "Tests can import stubs via crate::common::*"
    - "All tests pass after migration"
  artifacts:
    - path: "src/common/mod.rs"
      provides: "Library-accessible test stubs"
      min_lines: 280
    - path: "src/lib.rs"
      provides: "Module declaration for common"
      contains: "pub mod common"
  key_links:
    - from: "src/lib.rs"
      to: "src/common/mod.rs"
      via: "pub mod common"
      pattern: "pub mod common"
---

<objective>
Migrate test stubs from tests/common/mod.rs to src/common/mod.rs with pub(crate) visibility, enabling library code to use them.
</objective>

<context>
@/home/juan/Repos/LibreRoaster/src/lib.rs
@/home/juan/Repos/LibreRoaster/tests/common/mod.rs

**Current state:**
- Test stubs in tests/common/mod.rs (317 lines)
- Contains StubHeater, StubFan, StubThermometer implementing control traits
- Also has HeaterCall, FanCall, ThermometerCall enums for call tracking
- Helper functions: reset_channels(), collect_output()
- No src/common module exists yet

**Success criteria from roadmap:**
1. StubHeater, StubFan, StubThermometer exist in src/common/mod.rs with pub(crate) visibility
2. Tests can import stubs via crate::common::{StubHeater, StubFan, StubThermometer}
3. Both unit tests and integration tests use the same stubs
4. All tests pass after migration
</context>

<tasks>

<task type="auto">
  <name>Create src/common/mod.rs with migrated stubs</name>
  <files>src/common/mod.rs</files>
  <action>
1. Create directory: src/common/
2. Create file: src/common/mod.rs
3. Copy contents from tests/common/mod.rs but:
   - Remove the #![cfg(all(test, not(target_arch = "riscv32")))] crate attribute
   - Change all `pub` visibility on structs/enums to `pub(crate)`
   - Update the module doc comment to reflect new location
   - Keep re-exports: `pub use crate::control::traits::{Fan, Heater, Thermometer};` and `pub use crate::control::RoasterError;`
4. Update src/lib.rs to add: `pub mod common;` after the other module declarations
  </action>
  <verify>
- src/common/mod.rs exists with content
- src/lib.rs contains "pub mod common;"
- grep -q "pub(crate) struct Stub" src/common/mod.rs
  </verify>
  <done>
src/common/mod.rs exists with StubHeater, StubFan, StubThermometer as pub(crate), src/lib.rs declares the module
</done>
</task>

<task type="auto">
  <name>Update test imports to use crate::common</name>
  <files>tests/common/mod.rs</files>
  <action>
1. Replace the content of tests/common/mod.rs to re-export from the library:
   - Remove all the stub implementations (they're now in src/common/)
   - Keep the module doc comment but update it
   - Add re-exports: `pub use crate::common::{StubHeater, StubFan, StubThermometer, HeaterCall, FanCall, ThermometerCall, reset_channels, collect_output};`
   
The test file should now be a thin shim that re-exports from the library location, maintaining backward compatibility for any existing test code.
  </action>
  <verify>
- tests/common/mod.rs now re-exports from crate::common
- grep -q "pub use crate::common::" tests/common/mod.rs
  </verify>
  <done>
tests/common/mod.rs re-exports stubs from crate::common, existing test imports still work
</done>
</task>

<task type="auto">
  <name>Run tests to verify migration</name>
  <files></files>
  <action>
Run cargo test to verify:
1. All existing tests still compile
2. All tests pass with the new stub location
  </action>
  <verify>cargo test 2>&1 | tail -20</verify>
  <done>
All tests pass - migration successful
</done>
</task>

</tasks>

<verification>
1. src/common/mod.rs contains StubHeater, StubFan, StubThermometer with pub(crate) visibility
2. src/lib.rs declares "pub mod common;"
3. tests/common/mod.rs re-exports from crate::common
4. cargo test passes
</verification>

<success_criteria>
- [ ] StubHeater, StubFan, StubThermometer exist in src/common/mod.rs
- [ ] pub(crate) visibility on structs
- [ ] Tests import via crate::common
- [ ] cargo test passes
</success_criteria>

<output>
After completion, create `.planning/phases/79-test-infrastructure/79-01-SUMMARY.md`
</output>
