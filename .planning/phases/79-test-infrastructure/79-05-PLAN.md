---
phase: 79-test-infrastructure
plan: 05
type: execute
wave: 4
depends_on: ["79-03"]
files_modified:
  - tests/concurrent_sensor_test.rs
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "`concurrent_sensor_reads_verify_async_mutex` runs on the host target without panicking, proving async sensor reads can share `roaster_sync`."
    - "The `roaster_sync` critical section guards the RefCell so multiple threads never borrow it simultaneously when the async sensors run."
    - "`cargo test tests/concurrent_sensor_test.rs` completes, confirming the async lock depth metrics stay within expectations."
  artifacts:
    - path: "tests/concurrent_sensor_test.rs"
      provides: "Benchmark ensuring concurrent sensor reads observe the `ServiceContainer` lock semantics and records async lock depth." 
      contains: "TestCriticalSection" "concurrent_sensor_reads_verify_async_mutex"
  key_links:
    - from: "tests/concurrent_sensor_test.rs"
      to: "ServiceContainer::roaster_sync"
      via: "critical_section::with(|cs| ... roaster_sync.borrow(cs).borrow_mut())"
      pattern: "roaster_sync"
---

<objective>
Guard the `ServiceContainer::roaster_sync` critical section used by the host concurrency test so the `RefCell already borrowed` panic disappears and `cargo test` completes. The verifier currently halts at `concurrent_sensor_reads_verify_async_mutex` because the shared `RefCell` is mutated from multiple futures at once.
Purpose: The test spins up ten async tasks that call `ServiceContainer::roaster_async_sensor_read()`, each entering the `roaster_sync` critical section from separate threads. Without serialization, the underlying `RefCell` panics with `BorrowMutError`. This plan enforces proper mutual exclusion (e.g., via a real `TestCriticalSection` guard or a mutex wrapper) and proves the host suite runs cleanly.
Output: Updated `tests/concurrent_sensor_test.rs` with a deterministic critical-section guard plus a passing `cargo test tests/concurrent_sensor_test.rs` run.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/79-test-infrastructure/79-VERIFICATION.md
@.planning/phases/79-test-infrastructure/79-03-SUMMARY.md
@/home/juan/Repos/LibreRoaster/tests/concurrent_sensor_test.rs
</context>

<tasks>

<task type="auto">
  <name>Implement a robust TestCriticalSection guard</name>
  <files>tests/concurrent_sensor_test.rs</files>
  <action>
1. Replace the current `TestCriticalSection` stub with an implementation that uses a `std::sync::atomic::AtomicBool` (or `Mutex`) to ensure only one thread can `acquire` the critical section at a time, returning `RawRestoreState` to match the API.
2. Ensure `release` clears the flag so the next waiter can enter, and handle contention (spin/wait) so the host test threads never call `borrow_mut()` concurrently.
3. Update the `critical_section::set_impl!` call to register the new guard before any concurrent sensor reads spawn.
  </action>
  <verify>
1. Static analysis (`rg -n "TestCriticalSection" tests/concurrent_sensor_test.rs`) shows the guard holds a synchronization primitive that toggles only once per acquire/release.
2. Unit reasoning: Acquire loops until the atomic flag flips from false to true, preventing simultaneous borrows.
  </verify>
  <done>
The test harness now provides a real critical-section implementation so the `ServiceContainer::roaster_sync` RefCell cannot be borrowed by two threads at once.
  </done>
</task>

<task type="auto">
  <name>Limit the RefCell borrow window in the concurrency test</name>
  <files>tests/concurrent_sensor_test.rs</files>
  <action>
1. Refactor `init_service_container` and the concurrent sensor reads so each `critical_section::with` call borrows `roaster_sync` for the minimum duration (avoid calling `borrow_mut()` inside async loops more than once per task).
2. Where `roaster_sync` is mutated, drop the borrow before spawning additional sensor reads—call helper functions that take the `cs` token, mutate the `RefCell`, and return immediately.
3. Document in comments that the critical section is now safe for simultaneous async readers because the guard serializes access and the borrow does not escape the closure.
  </action>
  <verify>
1. `cargo test tests/concurrent_sensor_test.rs -- --nocapture concurrent_sensor_reads_verify_async_mutex` compiles without `RefCell already borrowed` panics.
2. The borrow lifetime inside `critical_section::with` is limited to a single `borrow_mut()` call per task.
  </verify>
  <done>
Concurrent sensor reads now hit `roaster_sync` sequentially within the guarded critical section, eliminating double-borrow crashes.
  </done>
</task>

<task type="auto">
  <name>Run the concurrent sensor test on x86_64</name>
  <files></files>
  <action>
1. Execute `cargo test tests/concurrent_sensor_test.rs` on the host target.
2. Ensure the test run completes without `RefCell` panics and collects `async_lock_depth_max_for_tests()` results as expected.
  </action>
  <verify>
1. The command exits with status 0 and prints the test name in the summary.
2. No `BorrowMutError` stack traces appear in the log.
  </verify>
  <done>
`cargo test tests/concurrent_sensor_test.rs` now finishes successfully, proving the `roaster_sync` guard keeps concurrent sensor reads safe.
  </done>
</task>

</tasks>

<verification>
1. Run `cargo test tests/concurrent_sensor_test.rs` and confirm it completes on x86_64 without `RefCell already borrowed` errors.
2. `rg -n "TestCriticalSection" tests/concurrent_sensor_test.rs` shows the guard now holds an atomic flag or Mutex.
3. `async_lock_depth_max_for_tests()` remains <= 1 after the test run.
</verification>

<success_criteria>
- [ ] `concurrent_sensor_reads_verify_async_mutex` passes on x86_64.
- [ ] `roaster_sync` access is serialized so `RefCell` never reports `BorrowMutError` in parallel readers.
- [ ] Host `cargo test tests/concurrent_sensor_test.rs` completes successfully, allowing the phase goal to progress.
</success_criteria>

<output>
After completion, create `.planning/phases/79-test-infrastructure/79-05-SUMMARY.md`
</output>
