---
phase: 79-test-infrastructure
plan: 04
type: execute
wave: 4
depends_on: ["79-03"]
files_modified:
  - tests/mock_uart.rs
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Host mock UART read/stream/multiple command tests run on x86_64 and complete without panics, proving the emulator buffer semantics settle."
    - "The `MockUartDriver` drains its RX buffer so `has_data()` flips false after consumption and streaming chunks include every expected byte (including the trailing '0')."
    - "The mock driver never exceeds its allocated buffers while replaying sequenced commands, so heapless-backed helpers cannot overflow during the host suite."
  artifacts:
    - path: "tests/mock_uart.rs"
      provides: "Mock UART driver plus the host suites that exercise Artisan command/response flows."
      contains:
        - "MockUartDriver"
        - "test_mock_uart_read_bytes"
        - "test_mock_uart_streaming"
  key_links:
    - from: "tests/mock_uart.rs"
      to: "MockUartDriver::read_bytes"
      via: "MockUartDriver::add_rx_data / read_bytes"
      pattern: "MockUartDriver::"
    - from: "tests/mock_uart.rs"
      to: "test_mock_uart_streaming"
      via: "AD\r\nOT1 50"
      pattern: "test_mock_uart_streaming"
---

<objective>
Align the host mock UART driver and suites with the emulator’s buffering behavior so `cargo test --test mock_uart` can finish without panic. The verifier currently reports missing final bytes, streaming chunk mismatches, and a heapless overflow during the multiplier command test; fixing those makes the integration shim fully accessible on the host.
Purpose: The `mock_uart` test library still panics before it can exercise the shared shim because the driver never drains the buffer, streaming reads omit the final `0`, and the command sequence overflows an internal Vec. This plan refines the driver/suites so they mirror the host’s UART semantics and keep their buffers within bounds.
Output: `tests/mock_uart.rs` updated with drained buffers, streaming expectations that include the trailing `0`, and heapsafe vector usage plus a successful `cargo test --test mock_uart` run.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/79-test-infrastructure/79-VERIFICATION.md
@.planning/phases/79-test-infrastructure/79-03-SUMMARY.md
@/home/juan/Repos/LibreRoaster/tests/mock_uart.rs
</context>

<tasks>

<task type="auto">
  <name>Drain rx buffer and mark EOF once commands are consumed</name>
  <files>tests/mock_uart.rs</files>
  <action>
1. When `MockUartDriver::read_bytes` consumes the last byte, set `eof = true`, keep `read_index` at `rx_buffer.len()`, and also clear the buffer so the driver drops the processed bytes.
2. Update `has_data`/`remaining_data_len` to honor `eof` so once a command stream is drained they report zero availability instead of staying flagged as data present.
3. Where `add_rx_data` appends streaming data, ensure it leaves room for the final newline/terminator so the subsequent read returns the bytes the tests expect.
  </action>
  <verify>
1. `cargo test --test mock_uart -- --nocapture test_mock_uart_read_bytes` passes and `mock.has_data()` is false after the two reads.
2. The streaming data path (`mock.add_rx_data("\r\nOT1 50")`) followed by `read_bytes` returns every byte so `mock.remaining_data_len()` drops to zero once the chunk is read.
  </verify>
  <done>
MockUartDriver drains RX data when commands finish so host suites no longer see phantom bytes after the reads.
  </done>
</task>

<task type="auto">
  <name>Ensure streaming/multiple command tests match final bytes and keep TX buffers bounded</name>
  <files>tests/mock_uart.rs</files>
  <action>
1. Adjust `test_mock_uart_streaming` to expect the full string `"AD\r\nOT1 50"` and ensure the chunk buffer is sized large enough for the trailing `0`.
2. Guard `MockUartDriver::write_bytes` so `tx_buffer` never grows beyond the test suite limits (e.g., by truncating after verifying output or by clearing the buffer between commands) to avoid heapless Vec overflows during `test_multiple_commands`.
3. Audit `test_multiple_commands` to consume every byte and assert `!mock.has_data()` without triggering heapless overflow warnings.
  </action>
  <verify>
1. `rg -n "AD\r\nOT1 50" tests/mock_uart.rs` shows the updated expectation.
2. Running `cargo test --test mock_uart -- --nocapture test_multiple_commands` finishes without panicking or overflowing `Vec`.
  </verify>
  <done>
The streaming and multiple command suites now expect the final bytes and keep the mock TX buffer within safe bounds, preventing host suite panics.
  </done>
</task>

<task type="auto">
  <name>Prove the host mock UART suite completes</name>
  <files></files>
  <action>
1. Run `cargo test --test mock_uart` on x86_64.
2. Confirm `test_mock_uart_read_bytes`, `test_mock_uart_streaming`, and `test_multiple_commands` finish without `panic` or heapless overflow errors.
  </action>
  <verify>
1. `cargo test --test mock_uart` exits with status 0.
2. Log mentions the three previously failing tests in the pass/fail table and no `panic` stack traces appear.
  </verify>
  <done>
Host mock UART tests now succeed, demonstrating the emulator buffer semantics match the test expectations.
  </done>
</task>

</tasks>

<verification>
1. Run `cargo test --test mock_uart` and ensure it exits successfully on x86_64.
2. `rg -n "test_mock_uart_streaming" tests/mock_uart.rs` shows the streaming expectation includes `AD\r\nOT1 50` and no missing terminator.
3. `cargo test --test mock_uart -- --nocapture test_multiple_commands` runs without heapless Vec overflows.
</verification>

<success_criteria>
- [ ] Host mock UART read/stream/multiple command tests finish without panic.
- [ ] MockUartDriver drains RX data and reports `has_data()` false when the buffer is empty.
- [ ] `cargo test --test mock_uart` passes on x86_64.
</success_criteria>

<output>
After completion, create `.planning/phases/79-test-infrastructure/79-04-SUMMARY.md`
</output>
