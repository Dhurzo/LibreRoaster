---
phase: 79-test-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ["79-01"]
files_modified:
  - tests/common/mod.rs
  - tests/command_idempotence.rs
  - tests/command_multiplexer_concurrency.rs
  - tests/concurrent_sensor_test.rs
  - tests/mock_uart_integration.rs
  - tests/fan_serialization.rs
  - .cargo/config.toml
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Integration suites import the shared stubs via tests_common/crate::common and no longer define StubHeater/Fan/Thermometer locally."
    - "Every RoasterControl::new call in the suites receives a SensorConversionHub so the host signature is satisfied."
    - "cargo test now runs against the x86_64 host target so the std-based dependencies can compile."
  artifacts:
    - path: "tests/common/mod.rs"
      provides: "Shim helpers that re-export the shared stubs and inject SensorConversionHub when building RoasterControl."
      contains: "pub use crate::common::{StubHeater, StubFan, StubThermometer}"
    - path: ".cargo/config.toml"
      provides: "Host-friendly target configuration for cargo test"
      contains: "target ="
  key_links:
    - from: "tests/command_idempotence.rs"
      to: "tests/common/mod.rs"
      via: "use tests_common::{StubHeater, StubFan, StubThermometer}"
      pattern: "use tests_common::"
    - from: "tests/mock_uart_integration.rs"
      to: "libreroaster::hardware::sensors::SensorConversionHub"
      via: "RoasterControl::new(..., SensorConversionHub::new())"
    - from: ".cargo/config.toml"
      to: "cargo test"
      via: "default target setting"
---

<objective>
Close the verification gaps so every integration suite consumes the shared stubs, injects a SensorConversionHub into RoasterControl, and `cargo test` targets the host where std-only dependencies build.
Purpose: The verifier flagged unused shared helpers and failing suites because the host signature changed and the default riscv target pulls in std crates that do not compile. This plan makes the suites rely on the shared module and use a host-friendly configuration so the phase goal is satisfied.
Output: Updated shim, integration tests, and cargo config that allow `cargo test` to run on x86_64 without redefining stubs or dropping the SensorConversionHub.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/79-test-infrastructure/79-VERIFICATION.md
@.planning/phases/79-test-infrastructure/79-01-SUMMARY.md
@/home/juan/Repos/LibreRoaster/.cargo/config.toml
</context>

<tasks>

<task type="auto">
  <name>Extend the tests shim with SensorConversionHub exports and a builder</name>
  <files>tests/common/mod.rs</files>
  <action>
1. Keep the doc comment but mention that the shim now re-exports the shared builder that wires SensorConversionHub.
2. Ensure the existing re-exports still expose `StubHeater`, `StubFan`, `StubThermometer`, and the helper enums/call-tracking data.
3. Add `pub use crate::hardware::sensors::SensorConversionHub;` so the suites can import the hub through the shim.
4. Implement `pub fn build_test_control(heater: Box<dyn Heater + Send>, fan: Box<dyn Fan + Send>) -> RoasterControl` that constructs a `SensorConversionHub::new()` and calls `RoasterControl::new(..., SensorConversionHub::new()).expect("test control should build")`.
  </action>
  <verify>
1. The shim still re-exports the shared stub types plus `SensorConversionHub::new()`.
2. `tests_common::build_test_control` exists and returns `RoasterControl`.
  </verify>
  <done>
Shim consumers can import the shared stubs, SensorConversionHub, and a helper that wires the hub into RoasterControl.
  </done>
</task>

<task type="auto">
  <name>Switch the integration suites to the shared stubs and SensorConversionHub builder</name>
  <files>tests/command_idempotence.rs, tests/command_multiplexer_concurrency.rs, tests/concurrent_sensor_test.rs, tests/mock_uart_integration.rs, tests/fan_serialization.rs</files>
  <action>
1. Remove the local StubHeater/Fan/Thermometer definitions from each module and replace them with imports from `tests_common`.
2. Import `tests_common::build_test_control` (or the helper) plus the stub types so every `build_control()` calls the shim and thereby always passes SensorConversionHub.
3. Where tests customized stub state (e.g., setting `status`), call the shared API (`StubHeater::new()` + `set_status`) instead of touching private fields.
4. Keep the custom tracking fan in `fan_serialization.rs` but reuse `tests_common::StubHeater` and `StubThermometer` so only the fan stub remains local.
  </action>
  <verify>
1. `rg -n "struct StubHeater" tests/*.rs` returns no matches inside the listed integration suites.
2. Every listed file uses `tests_common::build_test_control` (or direct shared helper) when constructing `RoasterControl`.
  </verify>
  <done>
All five integration suites import the shared stubs via the shim and construct RoasterControl through the helper that injects SensorConversionHub.
  </done>
</task>

<task type="auto">
  <name>Let cargo test default to the host target</name>
  <files>.cargo/config.toml</files>
  <action>
1. Remove `target = "riscv32imc-unknown-none-elf"` from the `[build]` section so `cargo test` no longer builds for the riscv target by default.
2. Document the host-default decision with a comment and add an alias such as `flash-riscv = "build --target riscv32imc-unknown-none-elf"` so embedded builds retain a short path.
3. Keep the `[target.riscv32imc-unknown-none-elf]` runner and unstable settings untouched.
  </action>
  <verify>
1. `.cargo/config.toml` no longer pins a default target.
2. Running `cargo test` completes on the host without pulling in riscv/std dependencies.
  </verify>
  <done>
`cargo test` now runs with the x86_64 host target, and there is an alias for the riscv build when needed.
  </done>
</task>

</tasks>

<verification>
1. Run `cargo test` (without `--target`) on x86_64 and confirm it succeeds.
2. `rg -n "struct StubHeater" tests/command_*.rs` shows no local stub definitions.
3. Each `RoasterControl::new` in the listed suites is backed by `tests_common::build_test_control`, ensuring SensorConversionHub is passed.
</verification>

<success_criteria>
- [ ] Integration suites consume the shared stubs via `tests_common`/`crate::common`.
- [ ] Every RoasterControl::new call in those suites receives a SensorConversionHub.
- [ ] `cargo test` compiles on the host without riscv std errors thanks to the new config.
</success_criteria>

<output>
After completion, create `.planning/phases/79-test-infrastructure/79-02-SUMMARY.md`
</output>
