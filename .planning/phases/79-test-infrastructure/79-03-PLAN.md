---
phase: 79-test-infrastructure
plan: 03
type: execute
wave: 3
depends_on: ["79-02"]
files_modified:
  - src/common/mod.rs
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Shared stub helpers (calls, enums, reset/collect helpers) are exported through `libreroaster::common` so integration suites can re-use the same API."
    - "The stub module compiles when the crate is built without the `test` feature because it now relies on `core`/`alloc` instead of `std`."
    - "Host `cargo test` completes so the shim and mock UART suite can run once the re-exported helpers are public."
  artifacts:
    - path: "src/common/mod.rs"
      provides: "Library-exposed stub definitions that tests and shim crates can import through `libreroaster::common`."
      contains:
        - "pub enum HeaterCall"
        - "pub struct StubHeater"
        - "pub fn reset_channels"
    - path: "tests/common/mod.rs"
      provides: "Shim that re-exports the publicly-visible stub helpers for the integration suites."
  key_links:
    - from: "tests/common/mod.rs"
      to: "src/common/mod.rs"
      via: "pub use libreroaster::common::{StubHeater, StubFan, StubThermometer, HeaterCall, FanCall, ThermometerCall, reset_channels, collect_output};"
      pattern: "pub use libreroaster::common::"
    - from: "tests/mock_uart.rs"
      to: "tests_common::build_test_control"
      via: "use tests_common::build_test_control;"
      pattern: "tests_common::"
---

<objective>
Open the shared stub module for host builds so the integration shim can re-export every helper and `cargo test` finishes even when the crate is compiled without the `test` feature.
Purpose: The verifier stopped at `src/common/mod.rs` because the module currently leans on `std` and keeps its helpers at `pub(crate)`, which blocks the shim and host tests. By making the module `core`/`alloc`-friendly and elevating the visibility of the enums/structs/helpers, the shim can re-export them and the host test suite can compile to completion.
Output: `src/common/mod.rs` exposing `HeaterCall`, `StubHeater`, `collect_output`, etc. as public, `core`/`alloc`-based code plus a verification run showing `cargo test` completes on x86_64.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/79-test-infrastructure/79-VERIFICATION.md
@.planning/phases/79-test-infrastructure/79-02-SUMMARY.md
@/home/juan/Repos/LibreRoaster/src/common/mod.rs
@/home/juan/Repos/LibreRoaster/tests/common/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Replace std dependencies with core/alloc helpers</name>
  <files>src/common/mod.rs</files>
  <action>
1. Swap the existing `use std::cell::RefCell;` for `core::cell::RefCell` plus `alloc::{string::String, vec::Vec}` so the module no longer requires `std` when the crate is compiled `no_std` outside the test feature.
2. Update every initialization that currently calls `Vec::new()` or returns `Vec<String>` so it references the `alloc` imports and the helper functions use `String` from `alloc`.
3. Ensure the module documentation reflects that the code now lives in `core`/`alloc` territory so it can be built for the host without opting into `std` explicitly.
  </action>
  <verify>
1. `rg -n "use std::cell::RefCell" src/common/mod.rs` prints nothing.
2. `rg -n "alloc::" src/common/mod.rs` shows the new imports for `Vec` and `String`.
  </verify>
  <done>
The stub module no longer imports `std`, consumes `core`/`alloc` primitives, and is safe to compile under the default `no_std` configuration.
  </done>
</task>

<task type="auto">
  <name>Expose stub enums, structs, and helpers publicly</name>
  <files>src/common/mod.rs</files>
  <action>
1. Change `pub(crate)` on `HeaterCall`, `FanCall`, `ThermometerCall`, `StubHeater`, `StubFan`, `StubThermometer`, `reset_channels`, and `collect_output` into `pub` so they can be re-exported by `tests/common` and used in integration suites.
2. Keep the existing APIs for tracking call history and hardware status so test suites can continue to call `.new()`, `.set_status()`, `.has_call()`, etc., without moving logic into the shim.
3. Verify the new visibility is consistent across the entire file so no stub helpers remain stuck behind `pub(crate)` accidentally.
  </action>
  <verify>
1. `rg -n "pub enum HeaterCall" src/common/mod.rs | grep -q "pub"` succeeds and shows the enum declared as `pub`.
2. `rg -n "pub struct StubHeater" src/common/mod.rs` confirms the struct is public and retains its constructors.
3. `rg -n "pub fn reset_channels" src/common/mod.rs` and `rg -n "pub fn collect_output" src/common/mod.rs` confirm the helpers are now public.
  </verify>
  <done>
Integration suites can now import `HeaterCall`, `StubHeater`, and the reset/collect helpers through `libreroaster::common`, making the shim re-export compile without privacy errors.
  </done>
</task>

</tasks>

<verification>
1. Run `cargo test` (no `--target`) on x86_64 and confirm it completes without `E0603`/`E0433` errors from `src/common/mod.rs`.
2. `rg -n "pub enum HeaterCall" src/common/mod.rs` shows the enum as `pub` so the shim can re-export it.
3. `rg -n "pub struct StubHeater" src/common/mod.rs` shows the struct is public and still has a `new()` constructor the shim can call.
</verification>

<success_criteria>
- [ ] All stub enums, structs, and helpers in `src/common/mod.rs` are `pub` and re-exportable.
- [ ] The module avoids `std` so `cargo test` compiles when the crate is built without the `test` feature.
- [ ] Host `cargo test` finishes successfully, closing the shim/visibility gaps from the verifier report.
</success_criteria>

<output>
After completion, create `.planning/phases/79-test-infrastructure/79-03-SUMMARY.md`
</output>
