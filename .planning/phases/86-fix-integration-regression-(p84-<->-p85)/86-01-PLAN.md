---
phase: 86-fix-integration-regression-p84-p85
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tests/regression_status.rs, tests/fault_injection_scenarios.rs]
autonomous: true

must_haves:
  truths:
    - "Integration tests compile successfully with the 'regression' feature enabled"
    - "STATUS command output is verified to contain exactly 18 columns"
    - "SystemStatus initializers in tests include command_latency_us and max_command_latency_us"
  artifacts:
    - path: "tests/regression_status.rs"
      provides: "Regression snapshot verification for 18-column telemetry"
    - path: "tests/fault_injection_scenarios.rs"
      provides: "Fault injection evidence for 18-column telemetry"
  key_links:
    - from: "tests/regression_status.rs"
      to: "src/output/artisan.rs"
      via: "ArtisanFormatter::format_status_response"
---

<objective>
Restore broken integration tests and fault-injection scenarios after the 18-column STATUS expansion in Phase 85.

Purpose: Ensure the quality gate and regression safety net are functional following the telemetry schema change.
Output: Updated integration test suite compatible with 18-column STATUS output.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/config/constants.rs
@src/output/artisan.rs
@tests/regression_status.rs
@tests/fault_injection_scenarios.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix tests/regression_status.rs</name>
  <files>tests/regression_status.rs</files>
  <action>
    Update tests/regression_status.rs to handle the 18-column telemetry layout:
    1. Update 'status_from_sample' function to initialize 'command_latency_us: 0' and 'max_command_latency_us: 0'.
    2. Update 'expected_warm_status', 'expected_cold_status', and 'expected_fault_status' strings by appending ',0,0' to include the two new columns.
    3. Update all assertions checking parts.len() to expect 18 instead of 16.
    4. Update 'test_column_positions_consistent' to use index 15 for derivative_available (it was already 15, but verify) and add checks for index 16 (command_latency) and 17 (max_command_latency).
    5. Update 'fixture_hub_agreement' test to handle 18 columns when parsing the expected line.
  </action>
  <verify>cargo test --test regression_status --features regression --target x86_64-unknown-linux-gnu</verify>
  <done>tests/regression_status.rs compiles and all 10 tests pass with 18-column assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Fix tests/fault_injection_scenarios.rs</name>
  <files>tests/fault_injection_scenarios.rs</files>
  <action>
    Update tests/fault_injection_scenarios.rs to handle the 18-column telemetry layout:
    1. Update 'status_for_scenario' function to initialize 'command_latency_us: 0' and 'max_command_latency_us: 0'.
    2. Update 'test_all_scenarios_produce_valid_status' assertion from 16 to 18 columns.
    3. Update 'test_status_columns_deterministic_with_faults' assertion from 16 to 18 columns.
  </action>
  <verify>cargo test --test fault_injection_scenarios --features regression --target x86_64-unknown-linux-gnu</verify>
  <done>tests/fault_injection_scenarios.rs compiles and all 6 tests pass with 18-column assertions.</done>
</task>

<task type="auto">
  <name>Task 3: Final Quality Baseline Verification</name>
  <files></files>
  <action>
    Run the project-specific quality baseline orchestrator to ensure no regressions in formatting or standard lints, while also verifying the fixed regression tests.
    
    Commands:
    1. cargo test --features regression (Verify all integration tests together)
    2. ./scripts/quality-baseline.sh (Verify standard gates pass)
  </action>
  <verify>./scripts/quality-baseline.sh</verify>
  <done>All quality gates pass and regression tests are green.</done>
</task>

</tasks>

<verification>
- Run 'cargo test --features regression' to ensure both fixed files pass.
- Run './scripts/quality-baseline.sh' to confirm project-wide quality.
</verification>

<success_criteria>
1. 'tests/regression_status.rs' and 'tests/fault_injection_scenarios.rs' pass all tests with 18-column layout.
2. SystemStatus is initialized with all 18 fields in test fixtures.
3. Quality baseline passes without violations.
</success_criteria>

<output>
After completion, create .planning/phases/86-fix-integration-regression-p84-p85/86-01-SUMMARY.md
</output>
