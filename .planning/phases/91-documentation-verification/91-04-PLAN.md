---
phase: 91-documentation-verification
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [internalDoc/INSTRUMENTATION_README.MD, internalDoc/ARTISAN_CONNECTION.md]
autonomous: false
user_setup: []

must_haves:
  truths:
    - "All telemetry fields in INSTRUMENTATION_README.MD are documented"
    - "Watchdog documentation in INSTRUMENTATION_README.MD is accurate"
    - "LEDC guard documentation in INSTRUMENTATION_README.MD is accurate"
    - "Regression telemetry in INSTRUMENTATION_README.MD is documented"
    - "PID telemetry in INSTRUMENTATION_README.MD is documented"
    - "Artisan connection guide in ARTISAN_CONNECTION.md is complete and accurate"
    - "All cross-references in both files resolve to existing files"
  artifacts:
    - path: "internalDoc/INSTRUMENTATION_README.MD"
      provides: "Telemetry fields, watchdog, LEDC guard, regression, and PID telemetry documentation"
      contains: "Telemetry|Watchdog|Guard|Regression|PID|Instrumentation"
    - path: "internalDoc/ARTISAN_CONNECTION.md"
      provides: "Complete Artisan tool connection guide with setup instructions"
      contains: "Artisan|Connection|USB|UART|Setup"
  key_links:
    - from: "internalDoc/INSTRUMENTATION_README.MD"
      to: "src/output/artisan.rs"
      via: "telemetry field verification"
      pattern: "format_status_response|18"
    - from: "internalDoc/INSTRUMENTATION_README.MD"
      to: "src/application/stage_instrumentation.rs"
      via: "watchdog/guard verification"
      pattern: "watchdog|guard|instrumentation"
    - from: "internalDoc/ARTISAN_CONNECTION.md"
      to: "internalDoc/PROTOCOL.md"
      via: "protocol reference"
      pattern: "PROTOCOL|Commands"
---

<objective>
Verify INSTRUMENTATION_README.MD and ARTISAN_CONNECTION.md accuracy through manual review and grep-assisted verification, ensuring telemetry fields, watchdog/guard documentation, regression/PID telemetry, and Artisan connection guide are complete and accurate.

Purpose: INSTRUMENTATION_README.MD documents all instrumentation telemetry, and ARTISAN_CONNECTION.md provides the complete Artisan connection guide. This verification ensures they accurately reflect the current implementation after v5.0 changes.
Output: Verification report confirming INSTRUMENTATION_README.MD and ARTISAN_CONNECTION.md accuracy with any discrepancies identified and resolved.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-documentation-verification/91-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Verify INSTRUMENTATION_README.MD telemetry fields are documented</name>
  <files>internalDoc/INSTRUMENTATION_README.MD, src/output/artisan.rs</files>
  <action>
    Extract 18 telemetry fields from INSTRUMENTATION_README.MD:
    ```bash
    grep -E "Field|Telemetry|Column|Valor" internalDoc/INSTRUMENTATION_README.MD | head -30
    ```

    Extract STATUS format from artisan.rs:
    ```bash
    grep -A20 "format_status_response" src/output/artisan.rs | grep -E "write|format"
    ```

    Verify all 18 fields are documented:
    1. ET (Environment Temperature)
    2. BT (Bean Temperature)
    3. Heater (heater output 0-100%)
    4. Fan (fan output 0-100%)
    5. WatchdogOK (1 if healthy, 0 if failed)
    6. WatchdogFailures (total failures count)
    7. LastWatchdogReason (reason code for last failure)
    8. LEDCGuardTimeouts (PWM guard timeout count)
    9. RegressionActive (1 if regression mode active)
    10. PV (process variable, BT)
    11. MV (manipulated variable, heater output)
    12. IntegratorValue (PID integrator)
    13. DerivativeValue (PID derivative)
    14. SaturationFlag (1 if output saturated)
    15. IntegratorClampFlag (1 if integrator clamped)
    16. DerivativeAvailableFlag (1 if derivative valid)
    17. CommandLatency (latency in microseconds)
    18. MaxCommandLatency (maximum observed latency)

    Document any missing or incorrectly documented fields.
  </action>
  <verify>All 18 telemetry fields are documented in INSTRUMENTATION_README.MD</verify>
  <done>Telemetry field documentation is complete and accurate</done>
</task>

<task type="auto">
  <name>Verify INSTRUMENTATION_README.MD watchdog and guard documentation</name>
  <files>internalDoc/INSTRUMENTATION_README.MD, src/application/stage_instrumentation.rs</files>
  <action>
    Extract watchdog documentation from INSTRUMENTATION_README.MD:
    ```bash
    grep -A20 "Watchdog|WatchDog|Perro guardián" internalDoc/INSTRUMENTATION_README.MD
    ```

    Extract LEDC guard documentation:
    ```bash
    grep -A20 "Guard|LEDC|PWM guard" internalDoc/INSTRUMENTATION_README.MD
    ```

    Verify implementation in stage_instrumentation.rs:
    ```bash
    grep -E "watchdog|guard|timeout" src/application/stage_instrumentation.rs | head -20
    ```

    Verify documentation covers:
    - Watchdog timeout (100ms loop period)
    - Watchdog failure reasons and codes
    - LEDC guard purpose and timeout behavior
    - Watchdog and guard telemetry fields (5, 6, 7, 8)

    Document any discrepancies between documented and actual watchdog/guard behavior.
  </action>
  <verify>Watchdog and LEDC guard documentation is accurate</verify>
  <done>Watchdog and guard behavior is correctly documented</done>
</task>

<task type="auto">
  <name>Verify INSTRUMENTATION_README.MD regression and PID telemetry</name>
  <files>internalDoc/INSTRUMENTATION_README.MD</files>
  <action>
    Extract regression telemetry documentation:
    ```bash
    grep -A20 "Regression|Regresión" internalDoc/INSTRUMENTATION_README.MD
    ```

    Extract PID telemetry documentation:
    ```bash
    grep -A20 "PID|Control|Integrator|Derivative" internalDoc/INSTRUMENTATION_README.MD
    ```

    Verify documentation covers:
    - Regression mode and RegressionActive field (field 9)
    - REG command usage
    - Over-temperature regression testing
    - PID fields: PV, MV, IntegratorValue, DerivativeValue (fields 10-13)
    - PID flags: SaturationFlag, IntegratorClampFlag, DerivativeAvailableFlag (fields 14-16)

    Check if PID implementation details are accurate (control loop structure, clamping behavior, derivative calculation).
  </action>
  <verify>Regression and PID telemetry are documented accurately</verify>
  <done>Regression and PID telemetry is correctly documented</done>
</task>

<task type="auto">
  <name>Verify ARTISAN_CONNECTION.md is complete and accurate</name>
  <files>internalDoc/ARTISAN_CONNECTION.md, internalDoc/PROTOCOL.md</files>
  <action>
    Extract connection setup from ARTISAN_CONNECTION.md:
    ```bash
    grep -E "USB|UART|Connection|Setup|Connect|Conexión" internalDoc/ARTISAN_CONNECTION.md
    ```

    Verify connection guide covers:
    - USB CDC (native) connection at 115200 baud
    - UART0 connection on GPIO20/21 at 115200 baud
    - Artisan+ device configuration (baud rate, port selection)
    - Command sequence (CHAN, UNITS, FILT handshake)
    - Device enumeration and troubleshooting

    Verify cross-references to PROTOCOL.md:
    ```bash
    grep -oE '\[[^\]]+\]\([^)]+PROTOCOL\.md\)' internalDoc/ARTISAN_CONNECTION.md
    ```

    Check if PROTOCOL.md exists:
    ```bash
    ls -la internalDoc/PROTOCOL.md
    ```

    Document any missing setup steps or inaccurate information.
  </action>
  <verify>Artisan connection guide is complete and accurate</verify>
  <done>Connection guide covers all necessary setup steps</done>
</task>

<task type="auto">
  <name>Verify cross-references in INSTRUMENTATION_README.MD and ARTISAN_CONNECTION.md</name>
  <files>internalDoc/INSTRUMENTATION_README.MD, internalDoc/ARTISAN_CONNECTION.md</files>
  <action>
    Extract all markdown links from both files:
    ```bash
    grep -oE '\[[^\]]+\]\([^)]+\)' internalDoc/INSTRUMENTATION_README.MD internalDoc/ARTISAN_CONNECTION.md
    ```

    Verify each internal link resolves to an existing file:
    ```bash
    for md_file in internalDoc/INSTRUMENTATION_README.MD internalDoc/ARTISAN_CONNECTION.md; do
        echo "Checking $md_file..."
        grep -oE '\[[^\]]+\]\(([^)]+)\)' "$md_file" | sed 's/.*(\([^)]*\).*/\1/' | while read link; do
            if [[ $link == http* ]]; then
                continue  # Skip external links
            fi
            if [[ $link == ../* ]]; then
                target="${link#../}"
            elif [[ $link == internalDoc/* ]]; then
                target="$link"
            else
                target="$(dirname "$md_file")/$link"
            fi
            if [ ! -f "$target" ]; then
                echo "  ❌ BROKEN LINK: $link"
            fi
        done
    done
    ```

    Document any broken links found.
  </action>
  <verify>All internal markdown links resolve to existing files</verify>
  <done>No broken cross-references</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Automated verification of INSTRUMENTATION_README.MD and ARTISAN_CONNECTION.md telemetry fields, watchdog/guard documentation, regression/PID telemetry, connection guide, and cross-references</what-built>
  <how-to-verify>
    Review the automated verification results and manually confirm:
    1. All 18 telemetry fields are documented in INSTRUMENTATION_README.MD
    2. Watchdog documentation is accurate
    3. LEDC guard documentation is accurate
    4. Regression telemetry is documented
    5. PID telemetry is documented
    6. Artisan connection guide is complete and accurate
    7. All cross-references resolve to existing files

    If discrepancies were identified, verify they have been resolved.

    Type "approved" if all verification passed, or describe issues found.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Overall verification checks:
- All 18 telemetry fields are documented
- Watchdog documentation is accurate
- LEDC guard documentation is accurate
- Regression telemetry is documented
- PID telemetry is documented
- Artisan connection guide is complete and accurate
- No broken cross-references
</verification>

<success_criteria>
INSTRUMENTATION_README.MD and ARTISAN_CONNECTION.md verification is complete when:
1. All automated checks (telemetry fields, watchdog/guard, regression/PID, connection guide, cross-refs) pass
2. Human verification confirms accuracy of all sections
3. Any discrepancies identified are documented and resolved
4. Both files are confirmed accurate and synchronized with codebase
</success_criteria>

<output>
After completion, create `.planning/phases/91-documentation-verification/91-04-SUMMARY.md`
</output>
