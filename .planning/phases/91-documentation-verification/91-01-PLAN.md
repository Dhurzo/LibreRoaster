---
phase: 91-documentation-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: false
user_setup: []

must_haves:
  truths:
    - "Pin assignments in README.md pinout table match constants.rs GPIO definitions"
    - "Build/test instructions in README.md work as documented"
    - "Project Status section reflects current v5.0 completion and v5.1 planning"
    - "Hardware specifications in README.md match actual ESP32-C3 setup"
    - "Artisan connection guide in README.md clarifies USB CDC/UART setup"
  artifacts:
    - path: "README.md"
      provides: "Project overview, pinout, build instructions, Artisan connection guide"
      contains: "Pinout|Project Status|Build Instructions|Artisan Connection"
  key_links:
    - from: "README.md"
      to: "src/config/constants.rs"
      via: "pinout table verification"
      pattern: "GPIO [0-9]+"
    - from: "README.md"
      to: "scripts/quality-baseline.sh"
      via: "build/test instructions"
      pattern: "cargo|build|test"
---

<objective>
Verify README.md accuracy through manual review and grep-assisted verification, ensuring pin assignments, build instructions, project status, hardware specs, and Artisan connection guide are all accurate and synchronized with current codebase.

Purpose: README.md is the primary entry point for users and developers. This verification ensures all information is accurate, up-to-date, and reflects the current state of the project after v5.0 completion.
Output: Verification report confirming README.md accuracy with any discrepancies identified and resolved.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-documentation-verification/91-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Verify pin assignments match constants.rs</name>
  <files>README.md, src/config/constants.rs</files>
  <action>
    Extract all GPIO pin assignments from README.md pinout table:
    ```bash
    grep -E "GPIO [0-9]+" README.md | grep -v "^#"
    ```

    Extract all pin definitions from constants.rs:
    ```bash
    grep -E "PIN: u8|const.*GPIO" src/config/constants.rs
    ```

    Compare the two outputs and verify:
    - GPIO 3: THERMOCOUPLE_ET_CS_PIN ✓
    - GPIO 4: THERMOCOUPLE_BT_CS_PIN ✓
    - GPIO 5: SPI_MOSI_PIN ✓
    - GPIO 6: SPI_MISO_PIN ✓
    - GPIO 7: SPI_SCLK_PIN ✓
    - GPIO 9: FAN_PWM_PIN (strapping, documented) ✓
    - GPIO 10: SSR_CONTROL_PIN ✓
    - GPIO 1: HEAT_DETECTION_PIN ✓
    - GPIO 20: UART_TX_PIN ✓
    - GPIO 21: UART_RX_PIN ✓

    Document any discrepancies found. If discrepancies exist, update README.md to match constants.rs.
  </action>
  <verify>Pin assignments in README.md match constants.rs definitions</verify>
  <done>All GPIO pins in pinout table are verified to match code constants</done>
</task>

<task type="auto">
  <name>Verify build/test instructions work as documented</name>
  <files>README.md, scripts/quality-baseline.sh</files>
  <action>
    Extract build and test commands from README.md:
    ```bash
    grep -E "cargo |build|test|flash" README.md | grep -A2 -E "Build|Test|Flash"
    ```

    Verify the documented commands match actual scripts:
    - Check if `scripts/quality-baseline.sh` exists and is referenced
    - Verify `cargo build`, `cargo test`, `cargo fmt`, `cargo clippy` are mentioned
    - Check if ESP-IDF build commands are accurate

    Verify any script references exist:
    ```bash
    ls -la scripts/*.sh
    ```

    Document any discrepancies found. If discrepancies exist, update README.md to reflect actual build/test workflow.
  </action>
  <verify>Build/test instructions in README.md match actual scripts and commands</verify>
  <done>Documented build/test commands work as described</done>
</task>

<task type="auto">
  <name>Verify Project Status reflects current state</name>
  <files>README.md</files>
  <action>
    Check README.md Project Status section:
    - Verify "Current version: v5.0" is accurate
    - Verify milestone description mentions v5.0 complete
    - Verify "Next: v5.1 - Documentation update" is mentioned
    - Check "Recent Changes" section for accuracy

    Compare with ROADMAP.md:
    ```bash
    grep "Phase 90" .planning/ROADMAP.md
    ```

    Verify no stale status information (e.g., "In progress" for completed phases).
  </action>
  <verify>Project Status section accurately reflects v5.0 completion and v5.1 planning</verify>
  <done>Project status is accurate and up-to-date</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Automated verification of README.md pin assignments, build/test instructions, and project status</what-built>
  <how-to-verify>
    Review the automated verification results and manually confirm:
    1. All pin assignments in README.md match constants.rs
    2. Build/test instructions are accurate and work as documented
    3. Project Status reflects current v5.0 completion and v5.1 planning
    4. Hardware specifications match actual ESP32-C3 setup
    5. Artisan connection guide is complete and accurate

    If discrepancies were identified, verify they have been resolved.

    Type "approved" if all verification passed, or describe issues found.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Overall verification checks:
- All GPIO pin assignments in README.md match constants.rs definitions
- Build/test instructions work as documented
- Project Status accurately reflects v5.0 completion and v5.1 planning
- Hardware specifications are correct
- Artisan connection guide is complete and accurate
</verification>

<success_criteria>
README.md verification is complete when:
1. All automated checks (pins, build/test commands, status) pass
2. Human verification confirms accuracy of all sections
3. Any discrepancies identified are documented and resolved
4. README.md is confirmed accurate and synchronized with codebase
</success_criteria>

<output>
After completion, create `.planning/phases/91-documentation-verification/91-01-SUMMARY.md`
</output>
