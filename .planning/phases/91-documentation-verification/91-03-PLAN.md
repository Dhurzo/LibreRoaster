---
phase: 91-documentation-verification
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [internalDoc/HARDWARE.md, internalDoc/DEVELOPMENT.md]
autonomous: false
user_setup: []

must_haves:
  truths:
    - "Pinout table in HARDWARE.md matches constants.rs GPIO definitions"
    - "Hardware specifications in HARDWARE.md match actual ESP32-C3 setup"
    - "Build instructions in DEVELOPMENT.md work as documented"
    - "Flash instructions in DEVELOPMENT.md work as documented"
    - "Test instructions in DEVELOPMENT.md work as documented"
    - "Debugging guide in DEVELOPMENT.md is accurate and helpful"
  artifacts:
    - path: "internalDoc/HARDWARE.md"
      provides: "Pinout, hardware design, ESP32-C3 specifications, thermocouple wiring"
      contains: "GPIO|Pinout|Hardware|ESP32-C3|MAX31856"
    - path: "internalDoc/DEVELOPMENT.md"
      provides: "Build, flash, test, and debugging guides for development workflow"
      contains: "Build|Flash|Test|Debug|Development"
  key_links:
    - from: "internalDoc/HARDWARE.md"
      to: "src/config/constants.rs"
      via: "pinout table verification"
      pattern: "GPIO [0-9]+"
    - from: "internalDoc/DEVELOPMENT.md"
      to: "scripts/quality-baseline.sh"
      via: "build/test instructions"
      pattern: "cargo|build|test|flash"
---

<objective>
Verify HARDWARE.md and DEVELOPMENT.md accuracy through manual review and grep-assisted verification, ensuring pinout, hardware specs, build/flash/test instructions, and debugging guides match the current codebase and workflow.

Purpose: HARDWARE.md contains critical hardware design information, and DEVELOPMENT.md consolidates all development workflow guides. This verification ensures they accurately reflect the current setup after v5.0 changes.
Output: Verification report confirming HARDWARE.md and DEVELOPMENT.md accuracy with any discrepancies identified and resolved.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-documentation-verification/91-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Verify HARDWARE.md pinout matches constants.rs</name>
  <files>internalDoc/HARDWARE.md, src/config/constants.rs</files>
  <action>
    Extract pin assignments from HARDWARE.md (document is in Spanish):
    ```bash
    grep -E "GPIO [0-9]+" internalDoc/HARDWARE.md | grep -v "^#"
    ```

    Extract pin definitions from constants.rs:
    ```bash
    grep -E "PIN: u8" src/config/constants.rs
    ```

    Verify mappings match (HARDWARE.md uses Spanish descriptions):
    - GPIO 3: THERMOCOUPLE_ET_CS_PIN (CS termopar #2) ✓
    - GPIO 4: THERMOCOUPLE_BT_CS_PIN (CS termopar #1) ✓
    - GPIO 5: SPI_MOSI_PIN ✓
    - GPIO 6: SPI_MISO_PIN ✓
    - GPIO 7: SPI_SCLK_PIN ✓
    - GPIO 9: FAN_PWM_PIN (ventilador, strapping) ✓
    - GPIO 10: SSR_CONTROL_PIN ✓
    - GPIO 1: HEAT_DETECTION_PIN (detección de calor) ✓
    - GPIO 20: UART_TX_PIN ✓
    - GPIO 21: UART_RX_PIN ✓

    Check if strapping pin warnings (GPIO2, GPIO8) are documented in HARDWARE.md.
  </action>
  <verify>Pin assignments in HARDWARE.md match constants.rs definitions</verify>
  <done>Pinout table is accurate</done>
</task>

<task type="auto">
  <name>Verify HARDWARE.md specifications match actual setup</name>
  <files>internalDoc/HARDWARE.md</files>
  <action>
    Review hardware specifications in HARDWARE.md:
    ```bash
    grep -E "Especificación|Spec|Componente|Component" internalDoc/HARDWARE.md
    ```

    Verify specifications match actual hardware:
    - ESP32-C3 RISC-V, 32-bit
    - MAX31856 #1 and #2 (dual thermocouple amplifiers)
    - Type-K thermocouples (0-300°C range)
    - SSR on GPIO10
    - Fan control on GPIO9 via LEDC PWM @ 25kHz
    - UART on GPIO20/21 at 115200 baud
    - USB-C for programming and power (500mA minimum)

    Check if MAX31856 SPI wiring (SCLK, MOSI, MISO, CS) matches constants.rs pins.
  </action>
  <verify>Hardware specifications match actual ESP32-C3 setup</verify>
  <done>Hardware specs are accurate</done>
</task>

<task type="auto">
  <name>Verify DEVELOPMENT.md build instructions work</name>
  <files>internalDoc/DEVELOPMENT.md</files>
  <action>
    Extract build commands from DEVELOPMENT.md:
    ```bash
    grep -A5 "Build|Construir|Compilation" internalDoc/DEVELOPMENT.md
    ```

    Verify build commands:
    - Check if `cargo build` is mentioned
    - Check if ESP-IDF build instructions are accurate
    - Verify any required dependencies are documented
    - Check if build environment setup is documented

    Test build commands if possible (within scope):
    ```bash
    cargo build --help
    ```

    Document any discrepancies between documented and actual build workflow.
  </action>
  <verify>Build instructions in DEVELOPMENT.md work as documented</verify>
  <done>Build commands are accurate</done>
</task>

<task type="auto">
  <name>Verify DEVELOPMENT.md flash instructions work</name>
  <files>internalDoc/DEVELOPMENT.md</files>
  <action>
    Extract flash commands from DEVELOPMENT.md:
    ```bash
    grep -A5 "Flash|Flashing|Flashear" internalDoc/DEVELOPMENT.md
    ```

    Verify flash commands:
    - Check if ESP-IDF flash commands are accurate
    - Verify connection instructions (USB-C, etc.)
    - Check if device detection is documented
    - Verify any flash-time parameters (baud rate, etc.)

    Verify FLASH_GUIDE.md symlink points to DEVELOPMENT.md:
    ```bash
    ls -la internalDoc/FLASH_GUIDE.md
    ```
  </action>
  <verify>Flash instructions in DEVELOPMENT.md work as documented</verify>
  <done>Flash commands are accurate</done>
</task>

<task type="auto">
  <name>Verify DEVELOPMENT.md test and debug instructions</name>
  <files>internalDoc/DEVELOPMENT.md, scripts/quality-baseline.sh</files>
  <action>
    Extract test commands from DEVELOPMENT.md:
    ```bash
    grep -A5 "Test|Tests|Pruebas" internalDoc/DEVELOPMENT.md
    ```

    Verify test instructions:
    - Check if `cargo test` is mentioned
    - Check if `scripts/quality-baseline.sh` is referenced
    - Verify test categories (unit tests, integration tests) are documented

    Extract debug commands from DEVELOPMENT.md:
    ```bash
    grep -A5 "Debug|Depurar|Debugging" internalDoc/DEVELOPMENT.md
    ```

    Verify debug guide:
    - Check if debugging methods are documented
    - Verify logging/debug output commands
    - Check if common issues and solutions are provided

    Verify script exists:
    ```bash
    ls -la scripts/quality-baseline.sh
    ```
  </action>
  <verify>Test and debug instructions in DEVELOPMENT.md work as documented</verify>
  <done>Test and debug guides are accurate</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Automated verification of HARDWARE.md and DEVELOPMENT.md pinout, hardware specs, build/flash/test instructions, and debugging guides</what-built>
  <how-to-verify>
    Review the automated verification results and manually confirm:
    1. Pinout table in HARDWARE.md matches constants.rs GPIO definitions
    2. Hardware specifications in HARDWARE.md match actual ESP32-C3 setup
    3. Build instructions in DEVELOPMENT.md work as documented
    4. Flash instructions in DEVELOPMENT.md work as documented
    5. Test instructions in DEVELOPMENT.md work as documented
    6. Debugging guide in DEVELOPMENT.md is accurate and helpful

    If discrepancies were identified, verify they have been resolved.

    Type "approved" if all verification passed, or describe issues found.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Overall verification checks:
- Pinout in HARDWARE.md matches constants.rs
- Hardware specs are accurate
- Build instructions work
- Flash instructions work
- Test instructions work
- Debugging guide is accurate
</verification>

<success_criteria>
HARDWARE.md and DEVELOPMENT.md verification is complete when:
1. All automated checks (pinout, hardware specs, build/flash/test/debug) pass
2. Human verification confirms accuracy of all sections
3. Any discrepancies identified are documented and resolved
4. Both files are confirmed accurate and synchronized with codebase
</success_criteria>

<output>
After completion, create `.planning/phases/91-documentation-verification/91-03-SUMMARY.md`
</output>
