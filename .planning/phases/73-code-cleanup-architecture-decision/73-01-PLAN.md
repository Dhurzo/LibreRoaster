---
phase: 73-code-cleanup-architecture-decision
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hardware/sensors/conversion.rs
  - src/control/roaster_refactored.rs
  - .planning/PROJECT.md
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Build succeeds with no references to sample_sync(), read_bean_sync(), read_env_sync(), or read_sensor_sync() in production code"
    - "All existing tests pass"
    - "PROJECT.md documents the decision to keep log + esp-println over defmt with clear rationale"
  artifacts:
    - path: "src/hardware/sensors/conversion.rs"
      provides: "Sensor conversion logic"
      contains: "No sync methods (sample_sync, read_*_sync)"
    - path: "src/control/roaster_refactored.rs"
      provides: "Roaster control logic"
      contains: "No read_sensors_sync method"
    - path: ".planning/PROJECT.md"
      provides: "Project documentation"
      contains: "Logging architecture decision rationale"
  key_links:
    - from: "src/hardware/sensors/conversion.rs"
      to: "src/logging/"
      via: "log crate + esp_println"
      pattern: "uses log facade, esp_println for output"
---

<objective>
Remove dead sync code from sensors/conversion.rs and document the logging architecture decision in PROJECT.md.

Purpose: Clean up dead code to reduce binary size and document architectural decisions for future maintainers.

Output: Cleaned conversion.rs, updated roaster_refactored.rs, documented logging decision in PROJECT.md.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Source files
@src/hardware/sensors/conversion.rs
@src/control/roaster_refactored.rs
@src/logging/mod.rs
@src/logging/drain_task.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead sync methods from sensors/conversion.rs</name>
  <files>src/hardware/sensors/conversion.rs</files>
  <action>
Remove the following dead sync methods from SensorConversionHub:
- `sample_sync()` (lines 194-209) - removes the entire synchronous sample method
- `read_bean_sync()` (lines 222-224) - removes synchronous bean reader
- `read_env_sync()` (lines 226-229) - removes synchronous env reader  
- `read_sensor_sync()` (lines 244-253) - removes synchronous sensor reader

These are dead code - grep confirms no production references exist.
  </action>
  <verify>
Run: `cargo build 2>&1 | head -50` to verify build succeeds. Then run `grep -r "sample_sync\|read_bean_sync\|read_env_sync\|read_sensor_sync" src/` to confirm no remaining references.
  </verify>
  <done>
Sync methods removed from conversion.rs. Build succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove read_sensors_sync from roaster_refactored.rs</name>
  <files>src/control/roaster_refactored.rs</files>
  <action>
Remove the `read_sensors_sync()` method from RoasterControl. Grep confirms this method is never called anywhere in production code - it was kept for "backwards compatibility" but is now dead code.

Remove both #[cfg(target_arch = "riscv32")] and #[cfg(not(target_arch = "riscv32"))] versions of read_sensors_sync (lines 128-151).
  </action>
  <verify>
Run: `cargo build 2>&1 | head -50` to verify build succeeds. Then run `grep -r "read_sensors_sync" src/` to confirm no remaining references.
  </verify>
  <done>
read_sensors_sync removed from roaster_refactored.rs. Build succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 3: Document logging architecture decision in PROJECT.md</name>
  <files>.planning/PROJECT.md</files>
  <action>
Add a new entry to the "Key Decisions" table in PROJECT.md documenting the decision to keep `log + esp-println` over `defmt`:

| log + esp-println | Embedded logging facade with direct UART0 output via esp_println::println! | Chosen - simpler than defmt-rtt, no drain task needed, reliable output at 115200 baud |

Add rationale:
- esp_println provides direct UART0 output without complex RTT integration
- No buffering or async drain task required  
- Works reliably for debugging/development
- Suitable for the project's needs (not timing-critical logging)

Reference src/logging/mod.rs and src/logging/drain_task.rs which already document this.
  </action>
  <verify>
Read .planning/PROJECT.md and verify the new decision entry exists in the Key Decisions table with rationale.
  </verify>
  <done>
PROJECT.md updated with logging architecture decision.
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify tests pass</name>
  <files>src/</files>
  <action>
Run the test suite to ensure all tests pass after the code removal:

```
cargo test 2>&1 | tail -30
```

If any tests fail due to the removal, investigate and fix. The sync methods should not have been used by tests.
  </action>
  <verify>
All tests pass. Check for: `test result: ok. X passed; 0 failed`
  </verify>
  <done>
All tests pass after code cleanup.
  </done>
</task>

</tasks>

<verification>
- [ ] cargo build succeeds with no errors
- [ ] No references to sample_sync, read_bean_sync, read_env_sync, read_sensor_sync in src/
- [ ] No references to read_sensors_sync in src/
- [ ] cargo test passes
- [ ] PROJECT.md has logging decision entry
</verification>

<success_criteria>
1. Build succeeds with no references to sample_sync(), read_bean_sync(), read_env_sync(), or read_sensor_sync() in production code.
2. All existing tests pass (migrated to async with block_on or gated behind #[cfg(test)]).
3. PROJECT.md documents the decision to keep log + esp-println over defmt with clear rationale.
</success_criteria>

<output>
After completion, create `.planning/phases/73-code-cleanup-architecture-decision/73-01-SUMMARY.md`
</output>
