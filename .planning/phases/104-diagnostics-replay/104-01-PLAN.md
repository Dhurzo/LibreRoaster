---
phase: 104-diagnostics-replay
plan: 01
type: execute
wave: 1
  - 103-01
files_modified:
  - scripts/replay_safe_shutdown.py
  - scripts/test_traceability_matrix.py
  - internalDoc/INSTRUMENTATION_README.MD
autonomous: true
must_haves:
  truths:
    - Auditors can run `scripts/replay_safe_shutdown.py` against a safe-shutdown artifact zip to replay the guard log, regenerate the traceability matrix, and confirm the guard metadata matches the archived `metadata.json`.
    - The replay automation produces human-readable output (TraceId, watchdog_failure, error_category, error_source) and aborts if the metadata drifted away from the stored guard row so artifact integrity is enforced.
    - `internalDoc/INSTRUMENTATION_README.MD` explains how to run the replay automation, inspect the generated matrix, and surface any discrepancies for auditors.
  artifacts:
    - path: "scripts/replay_safe_shutdown.py"
      provides: "CLI that unpacks `safe-shutdown-replay.zip`, regenerates `traceability.csv`, compares the guard row to `metadata.json`, and reports TraceId/watchdog/error metadata for auditors."
    - path: "scripts/test_traceability_matrix.py"
      provides: "Regression coverage that runs the replay CLI against the artifact created by `scripts/collect_safe_shutdown.py` and asserts the guard metadata is unchanged."
    - path: "internalDoc/INSTRUMENTATION_README.MD"
      provides: "Guidance describing how to run `scripts/replay_safe_shutdown.py`, interpret its output, and verify artifact integrity without instrumentation hardware."
  key_links:
    - from: "scripts/replay_safe_shutdown.py"
      to: "logs/traceability/safe-shutdown-replay.zip"
      via: "CLI `--artifact` argument points at the zipped safe-shutdown bundle and decompresses the log/metadata for replay."
    - from: "scripts/replay_safe_shutdown.py"
      to: "scripts/traceability_matrix.py"
      via: "Reuses parser helpers to regenerate the matrix row and compare guard metadata against `metadata.json`."
    - from: "internalDoc/INSTRUMENTATION_README.MD"
      to: "scripts/replay_safe_shutdown.py"
      via: "Documentation references the CLI name, arguments, and expected output so auditors can replay/compare metadata without hardware."
---

<objective>
Automate the replay and verification of safe-shutdown artifacts so auditors can unzip the bundle, regenerate the traceability matrix, and prove the archived guard metadata still matches the live trace.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/103-diagnostics-artifacts/103-CONTEXT.md
@.planning/phases/103-diagnostics-artifacts/103-VERIFICATION.md
@.planning/phases/104-diagnostics-replay/104-CONTEXT.md
@.planning/phases/104-diagnostics-replay/104-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add artifact replay automation</name>
  <files>scripts/replay_safe_shutdown.py</files>
  <action>Create `scripts/replay_safe_shutdown.py` that accepts `--artifact` (default `logs/traceability/safe-shutdown-replay.zip`) and unzips the bundle into a temp directory. Parse the extracted `sample-safe-shutdown.log` with `scripts.traceability_matrix`, write the replayed matrix to `traceability-replay.csv`, read `metadata.json`, and compare the guard row metadata (`TraceId`, `watchdog_failure`, `error_category`, `error_source`). Exit with an error if the metadata differs, otherwise print the metadata values and the path to the regenerated CSV. Support a `--report` flag that emits JSON with the replay results for auditing automation.
  <verify>`python3 scripts/replay_safe_shutdown.py --artifact logs/traceability/safe-shutdown-replay.zip --report replay-report.json`</verify>
  <done>The CLI decompresses the artifact, regenerates the matrix, compares metadata, prints/reports TraceId/watchdog/error details, and fails if the replayed metadata diverges from `metadata.json`.
</task>

<task type="auto">
  <name>Task 2: Cover replay automation with regression test</name>
  <files>scripts/test_traceability_matrix.py</files>
  <action>Add `TestTraceabilityMatrix.test_replay_safe_shutdown_artifact` that runs `scripts/collect_safe_shutdown.py` to build `safe-shutdown-replay.zip` in a temp directory, invokes `scripts/replay_safe_shutdown.py` against that artifact with `--report` to produce JSON, and asserts the guard metadata in the report matches the expected `watchdog_failure=init_error_failure`, `error_category=initialization`, `error_source=hardware_init_failed`. Keep the test fast by cleaning up temp files after assertions.
  <verify>`PYTHONPATH=. python3 scripts/test_traceability_matrix.py`</verify>
  <done>The regression suite runs the replay CLI, inspects the generated report, and confirms the guard metadata for the replayed artifact still matches the canonical failure tokens.
</task>

<task type="auto">
  <name>Task 3: Document replay automation workflow</name>
  <files>internalDoc/INSTRUMENTATION_README.MD</files>
  <action>Add a “Safe-Shutdown Artifact Replay” subsection describing how to run `scripts/replay_safe_shutdown.py`, what the JSON report contains (guard metadata values, report path, CSV path), and how to compare against `metadata.json`/`safe-shutdown-replay.zip` without hardware. Mention the `--report` flag, the expected metadata keys, and how teams can integrate the CLI into CI/audit automation.
  <verify>`python3 scripts/replay_safe_shutdown.py --artifact logs/traceability/safe-shutdown-replay.zip --report replay-report.json`</verify>
  <done>The instrumentation guide now explains how to replay the artifact, examine the CSV/report, and trust that the guard metadata is revalidated during audits.
</task>

</tasks>

<verification>
- `python3 scripts/replay_safe_shutdown.py --artifact logs/traceability/safe-shutdown-replay.zip --report replay-report.json`
- `PYTHONPATH=. python3 scripts/test_traceability_matrix.py`
- `python3 scripts/traceability_matrix.py logs/traceability/sample-safe-shutdown.log`
</verification>

<success_criteria>
1. `scripts/replay_safe_shutdown.py` can read the zipped artifact, regenerate the matrix, compare guard metadata, and emit a JSON report so auditors verify TraceId/watchdog/error values without hardware.
2. Regression coverage runs the replay CLI on the artifact built by `scripts/collect_safe_shutdown.py` and asserts the replay metadata matches `watchdog_failure=init_error_failure` plus the AppError fields.
3. Documentation captures the replay workflow, CLI flags, report format, and audit instructions so teams can revalidate artifacts in automation.
</success_criteria>

<output>
After completion, create `.planning/phases/104-diagnostics-replay/104-01-SUMMARY.md`
</output>
