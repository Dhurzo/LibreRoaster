---
phase: 100-error-taxonomy-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/control/abstractions.rs
  - src/hardware/max31856.rs
  - src/error/app_error.rs
  - src/hardware/init.rs
autonomous: true
must_haves:
  truths:
    - "RoasterError and Max31856Error compile as struct-backed variants with the intended metadata"
    - "AppError::from(RoasterError) handles every new variant without dead arms and hardware init can still report InitError reasons"
  artifacts:
    - path: src/control/abstractions.rs
      provides: Reshaped RoasterError definition with struct payloads, Display/message_token helpers, and variant metadata
    - path: src/error/app_error.rs
      provides: AppError conversions that mirror the new RoasterError shape and keep InitError reason strings
  key_links:
    - from: src/control/abstractions.rs
      to: src/error/app_error.rs
      via: AppError::from implementation preserving variant sources
    - from: src/hardware/max31856.rs
      to: src/control/abstractions.rs
      via: From<Max31856Error> → RoasterError mapping
---

<objective>
Convert the remaining RoasterError/Max31856Error variants to struct-backed payloads so the AppError bridge and downstream modules can build without the dead code paths from the old enum layout.
Purpose: Unlock compilable diagnostics and set the foundation for instrumentation updates in later plans.
Output: Updated enums, conversions, and initialization helpers guaranteeing every error variant carries the right context.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/100-error-taxonomy-completion/100-CONTEXT.md
@src/control/abstractions.rs
@src/hardware/max31856.rs
@src/error/app_error.rs
@src/hardware/init.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reshape RoasterError and Max31856 conversions</name>
  <files>src/control/abstractions.rs, src/hardware/max31856.rs</files>
  <action>Redefine `RoasterError` in `src/control/abstractions.rs` as struct variants that carry metadata where needed, keep `message_token()`/Display in sync, and adjust the `From<Max31856Error>` implementation so each case maps into the new payload fields instead of the previous unit variants.</action>
  <verify>`cargo check --release --features embedded`</verify>
  <done>RoasterError/Max31856Error build with struct payloads, any Display helpers still compile, and no AppError arm is left unreachable.</done>
</task>

<task type="auto">
  <name>Task 2: Update AppError + init errors for the new payloads</name>
  <files>src/error/app_error.rs, src/hardware/init.rs</files>
  <action>Adjust `AppError::from(RoasterError)` so every new struct variant feeds the correct `ControlError`/`TemperatureError` detail, and ensure `src/hardware/init.rs` propagates the metadata down through `InitError` to keep InitError reason strings alive for the safe-shutdown signals.</action>
  <verify>`cargo check --release --features embedded`</verify>
  <done>AppError conversions compile, InitError callers still produce meaningful `what`/`reason`, and `cargo check` succeeds with the new shapes.</done>
</task>

</tasks>

<verification>
Run `cargo check --release --features embedded` after completing the tasks to prove the reshaped enums compile across the workspace.
</verification>

<success_criteria>
1. `cargo check --release --features embedded` passes with the new RoasterError struct variants.
2. `AppError::from(RoasterError)` has no dead match arms and still returns InitError reasons from the hardware init path.
3. The codebase compiles without leftover `AppError` dead paths stemming from unit variants.
</success_criteria>

<output>
After completion, create `.planning/phases/100-error-taxonomy-completion/100-01-SUMMARY.md`
</output>
