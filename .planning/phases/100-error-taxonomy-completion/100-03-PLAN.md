---
phase: 100-error-taxonomy-completion
plan: 03
type: execute
wave: 2
depends_on: [100-01, 100-02]
files_modified:
  - src/main.rs
autonomous: true
must_haves:
  truths:
    - "Safe-shutdown keeps the LED blink heartbeat running while awaiting embassy_time timers and logs the InitError that triggered it"
    - "A telemetry/host event announces each safe-shutdown cycle with the final AppError diagnostics so operators know the exit reason"
  artifacts:
    - path: src/main.rs
      provides: Safe-shutdown loop that maintains LED heartbeat, logs AppError diagnostics, and publishes host-visible events
  key_links:
    - from: src/main.rs
      to: src/error/app_error.rs
      via: `InitError` diagnostics emitted before the infinite blink loop
---

<objective>
Stabilize `enter_safe_shutdown()` so it keeps the heartbeat LED alive while waiting on embassy_time timers, records the triggering `InitError`, and emits a telemetry/TRACE-ready event before the non-returning loop.
Purpose: Make the safe-shutdown flow observable and reliable now that diagnostics compile.
Output: Updated `src/main.rs` safe-shutdown routine with continuous blink, AppError logging, and a host notification each loop.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/100-error-taxonomy-completion/100-CONTEXT.md
@src/main.rs
@src/error/app_error.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Keep the LED heartbeat + AppError logging in safe shutdown</name>
  <files>src/main.rs</files>
  <action>While awaiting embassy_time timers inside `enter_safe_shutdown()`, keep the GPIO8 blink pattern running, log the incoming `InitError` (leveraging `AppError::user_message()`/`source()` as needed), and drop any pending hardware tasks by staying in the loop.</action>
  <verify>`cargo check --release --features embedded`</verify>
  <done>`enter_safe_shutdown()` still compiles, the blink pattern loops indefinitely, and the log line documents the `InitError` metadata before each cycle.</done>
</task>

<task type="auto">
  <name>Task 2: Emit a host-facing event for each shutdown cycle</name>
  <files>src/main.rs</files>
  <action>Before dropping into the infinite blink loop, write a structured string to the Artisan output channel (e.g., `ERR safe_shutdown ...`) that carries the `InitError` category/source so telemetry/TRACE can correlate it—this needs to run once per cycle without blocking the timer schedule.</action>
  <verify>`cargo check --release --features embedded`</verify>
  <done>The safe-shutdown loop still builds and the new output string is emitted once before each blink burst, encoding the `InitError` reason.
</task>

</tasks>

<verification>
Run `cargo check --release --features embedded` to ensure the new safe-shutdown logging/event emission compiles alongside the blink loop.
</verification>

<success_criteria>
1. `enter_safe_shutdown()` continues to blink GPIO8 and logs the triggering `InitError` before each cycle.
2. A host-visible error message hits the Artisan output channel with the AppError diagnostics during each safe-shutdown iteration.
3. The loop still compiles without reintroducing blocking operations or manual retries.
</success_criteria>

<output>
After completion, create `.planning/phases/100-error-taxonomy-completion/100-03-SUMMARY.md`
</output>
