---
phase: 100-error-taxonomy-completion
plan: 02
type: execute
wave: 2
depends_on: [100-01]
files_modified:
  - src/logging/traceability.rs
  - src/application/tasks.rs
autonomous: true
must_haves:
  truths:
    - "TRACE events for telemetry/guard stages embed AppError metadata (category/source or Display) when guard/watchdog failures occur"
    - "Telemetry and guard instrumentation log the same richer diagnostics so hosts can correlate with service-level AppError traces"
  artifacts:
    - path: src/logging/traceability.rs
      provides: Formatted TRACE events carrying AppError diagnostics plus guard/telemetry metrics
    - path: src/application/tasks.rs
      provides: Telemetry/guard stages that capture AppError data from `trace_guard`/`trace_telemetry` calls and emit it along with stage metrics
  key_links:
    - from: src/application/tasks.rs
      to: src/logging/traceability.rs
      via: new AppError-aware `trace_guard`/`trace_telemetry` calls
---

<objective>
Bring AppError diagnostics into TRACE, telemetry, and guard outputs so the live instrumentation reflects the richer categories/sources defined in Phase 100.
Purpose: So operators/guards can tie a guard/telemetry signal back to the exact AppError variant that triggered it.
Output: TRACE formatting helpers and telemetry instrumentation that carry AppError metadata.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/100-error-taxonomy-completion/100-CONTEXT.md
@src/logging/traceability.rs
@src/application/tasks.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend TRACE helpers with AppError metadata</name>
  <files>src/logging/traceability.rs</files>
  <action>Teach `trace_telemetry`/`trace_guard` (and their `format_*` helpers) to accept `Option<&AppError>` (or the derived `category`/`source`) and append those fields to the `TRACE,...` payload while keeping the guard/telemetry metrics intact.</action>
  <verify>`cargo check --release --features embedded`</verify>
  <done>TRACE telemetry/guard events compile with the new AppError parameters and the formatted strings include the category/source when the option is present.</done>
</task>

<task type="auto">
  <name>Task 2: Surface AppError diagnostics in telemetry + guard instrumentation</name>
  <files>src/application/tasks.rs</files>
  <action>When telemetry or guard stages observe handler errors/timeouts, capture the originating `AppError` (calling `source()`/`category()` where available) and pass it to the new trace helpers; ensure info/debug logs and telemetry snapshots output the same diagnostics so hosts can correlate.</action>
  <verify>`cargo check --release --features embedded`</verify>
  <done>Telemetry/guard loops compile, emit richer diagnostics in both log lines and new TRACE payload fields for any observed AppError.</done>
</task>

</tasks>

<verification>
Run `cargo check --release --features embedded` to verify instrumentation still compiles now that TRACE helpers take AppError metadata.
</verification>

<success_criteria>
1. TRACE events from telemetry and guard stages include AppError category/source when available.
2. Telemetry/guard instrumentation passes the AppError information without breaking the existing metrics reporting.
3. Hosts can correlate guard/telemetry logs with the same AppError diagnostics emitted in TRACE.
</success_criteria>

<output>
After completion, create `.planning/phases/100-error-taxonomy-completion/100-02-SUMMARY.md`
</output>
