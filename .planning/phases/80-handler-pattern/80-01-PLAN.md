---
phase: 80-handler-pattern
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/control/roaster_refactored.rs
  - src/control/handlers.rs
autonomous: true
must_haves:
  truths:
    - "process_artisan_command forwards SetHeater, SetFan, SetFanSpeed, UP, and DOWN through process_command while start/stop/status/units remain local."
    - "ArtisanCommandHandler remains the single source of truth for manual_heater/manual_fan and status updates so manual commands never diverge from the handler chain."
    - "SetFanSpeed still stops the heater whenever `was_clamped` is true before or after delegating the fan update to the handler."
  artifacts:
    - path: "src/control/roaster_refactored.rs"
      provides: "Central dispatch for Artisan commands plus heater/fan actuation"
      contains: "process_artisan_command"
    - path: "src/control/handlers.rs"
      provides: "ArtisanCommandHandler storing manual setpoints and status updates"
      contains: "ArtisanCommandHandler"
  key_links:
    - from: "src/control/roaster_refactored.rs"
      to: "src/control/handlers.rs"
      via: "process_command loops over RoasterCommandHandler implementations"
      pattern: "process_command\\("
    - from: "src/control/roaster_refactored.rs"
      to: "src/control/traits.rs"
      via: "SetFanSpeed clamps call heater.set_power(0.0) when OT2 input is out of range"
      pattern: "heater\\.set_power\\(0\.0\\)"
---

<objective>
Refactor the Artisan command pipeline so manual heater/fan actions flow through the existing RoasterCommandHandler chain instead of repeating their logic inside `process_artisan_command`.
Purpose: Keep manual state synchronized with the handler responsible for PID/manual modes and remove the 125-line match so future manual-command adjustments live in one place.
Output: Updated dispatch logic in `src/control/roaster_refactored.rs`, supporting adjustments in `src/control/handlers.rs`, and validation via targeted integration tests.
</objective>

<execution_context>
@/home/juan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/juan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@/home/juan/Repos/LibreRoaster/src/control/roaster_refactored.rs
@/home/juan/Repos/LibreRoaster/src/control/handlers.rs
</context>

<tasks>

<task type="auto">
  <name>Delegate Artisan manual commands to process_command</name>
  <files>src/control/roaster_refactored.rs</files>
  <action>
1. In `process_artisan_command`, convert the manual commands (`SetHeater`, `SetFan`, `IncreaseHeater`, `DecreaseHeater`) into their matching `RoasterCommand` variants and forward them to `self.process_command`, reusing the cached `current_time` for heater adjustments.
2. Keep the `StartRoast`, `EmergencyStop`, `StatusReport`, `ReadStatus`, `Units`, `Chan`, `RunRegression`, and `Filt` branches local to `process_artisan_command` so they continue to bypass the handler chain.
3. For `SetFanSpeed`, delegate the fan update through `process_command` but leave the `was_clamped` safety block nearby so the clamp logic still stops the heater, captures metrics, and logs the OT2 warning.
  </action>
  <verify>
1. `cargo check --lib` completes, demonstrating the new imports and call sites compile.
2. `rg -n "process_command" src/control/roaster_refactored.rs` shows the manual commands now reference `process_command` rather than inline handling.
  </verify>
  <done>
Manual Artisan commands are no longer implemented inside the large match in `process_artisan_command`; they now pass through `process_command` with OT2 safety logic intact.
  </done>
</task>

<task type="auto">
  <name>Let ArtisanCommandHandler drive manual state while still actuating hardware</name>
  <files>src/control/roaster_refactored.rs; src/control/handlers.rs</files>
  <action>
1. Remove the early `if let` intercepts for `RoasterCommand::SetHeaterManual` and `SetFanManual` inside `process_command`, letting the handler array observe these commands.
2. After the handler loop reports success, detect when the command affected manual heater or fan output and call `apply_manual_heater`/`apply_manual_fan` (or a refactored helper) so the hardware actuation keeps pace with the handler's manual setpoints, consulting `self.artisan_handler.get_manual_heater()` and `get_manual_fan()` as needed.
3. If new helper methods are required on `ArtisanCommandHandler` to expose the manual outputs or to avoid duplicating clamp math, add them to `src/control/handlers.rs` next to the existing getters so the handler remains authoritative over manual state.
  </action>
  <verify>
1. `cargo test --lib artisan_command_handler_tests` (or equivalent smallest suite) passes, confirming the handler still satisfies its contract.
2. A quick `rg -n "apply_manual_(heater|fan)" src/control/roaster_refactored.rs` confirms the hardware helpers remain wired after manual commands.
  </verify>
  <done>
Manual heater/fan commands flow through `ArtisanCommandHandler`, the handler still owns the manual setpoints, and the hardware helpers run to drive the SSR/fan outputs.
  </done>
</task>

<task type="auto">
  <name>Verify Artisan manual commands against the integration suite</name>
  <files>tests/fan_serialization.rs; tests/mock_uart_integration.rs</files>
  <action>
1. Run `cargo test tests/fan_serialization.rs tests/mock_uart_integration.rs` to exercise the manual command paths that rely on `process_artisan_command`.
2. Confirm the OT2 scenario (`SetFanSpeed` with `was_clamped`) still stops the heater and leaves the fan handler in manual mode by observing the test output or ensuring the commands do not panic.
  </action>
  <verify>
1. The test command exits with status 0 and prints both test names in the summary.
2. No `panic!`, `RefCell`, or `InvalidState` errors appear in the logs during the manual command phases.
  </verify>
  <done>
Integration tests covering manual fan/heater commands and OT2 clamp behavior pass, proving the handler-based dispatch matches previous behavior.
  </done>
</task>

</tasks>

<verification>
1. Run `cargo check --lib` to ensure the new dispatch code compiles.
2. Run `cargo test tests/fan_serialization.rs tests/mock_uart_integration.rs` to validate manual commands and the OT2 clamp path.
</verification>

<success_criteria>
- [ ] `process_artisan_command` delegates the manual Artisan commands through `process_command`, eliminating the ~125-line match.
- [ ] `ArtisanCommandHandler` remains the authoritative source of manual heater/fan state while hardware helpers still actuate the SSR/fan outputs.
- [ ] OT2 `SetFanSpeed` keeps stopping the heater when `was_clamped` and logs the warning, and the integration tests succeed.
</success_criteria>

<output>
After completion, create `.planning/phases/80-handler-pattern/80-01-SUMMARY.md`
</output>
