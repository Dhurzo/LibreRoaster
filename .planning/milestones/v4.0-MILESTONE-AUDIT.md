---
milestone: v4.0
audited: 2026-02-20T13:45:00Z
status: passed
scores:
  requirements: 7/7
  phases: 3/3
  integration: 1/1
  flows: 3/3
tech_debt:
  - phase: 58-async-mutex-migration
    items:
      - "Deprecated `with_roaster()`/`with_roaster_mut()` wrappers still exist purely for ISR compatibility; plan to retire them once async callers have settled and hardware ISR paths adopt the new API."
      - "`tests/mock_uart_integration.rs` continues to rely on the sync `roaster_sync` helper; revisit this when the async mutex test harness is stable so the legacy path can be pruned."
---

# Milestone Audit – v4.0 Async Sensor Race Condition Fix

## Requirements Coverage
| Requirement | Status | Phase | Notes |
|-------------|--------|-------|-------|
| ASYNC-01: Replace `ServiceContainer.roaster` with `embassy_sync::Mutex` | satisfied | 58 | Verified via `service_container.rs` fields and lock usage; build succeeds. |
| ASYNC-02: Remove take/replace from `roaster_async_sensor_read()` | satisfied | 58 | Method now waits on `self.roaster.lock().await` (no take/replace window) and compiles cleanly. |
| ASYNC-03: `roaster_async_sensor_read()` uses async lock (`lock().await`) | satisfied | 58 | Lock pattern confirmed in `service_container.rs` lines 120-140, listing asynchronous guard. |
| ASYNC-04: `with_roaster()` replaced with async version | satisfied | 58 | New `with_roaster_async()` future is awaited throughout `tasks.rs`; sync helper marked `#[deprecated]`. |
| ASYNC-05: Update all callers of `with_roaster()` | satisfied | 58 | `tasks.rs` and `hardware/usb_cdc/tasks.rs` now await the async helper; compiler warnings removed. |
| ASYNC-06: Integration test proving concurrent sensor reads | satisfied | 60 | `tests/concurrent_sensor_test.rs` with `async-lock-depth-metrics` runs batches of sensor reads and verifies no `ContainerError`. |
| SYNC-01: Provide critical-section access for ISRs | satisfied | 58 | `roaster_sync` field plus deprecated sync wrappers maintain ISR access paths without touching the async mutex. |

## Phases
| Phase | Status | Score | Critical Gaps |
|-------|--------|-------|---------------|
| 58-async-mutex-migration | passed | 6/6 truths verified | None |
| 60-concurrent-sensor-test | passed | 3/3 truths verified | None |
| 61-usb-instrumentation-wiring | passed | 3/3 truths verified | None |

## Integration & Wiring
| Area | Status | Notes |
|------|--------|-------|
| Wiring completeness | complete | `process_usb_command_data_test` is consumed by `tests/usb_instrumentation_runner.rs`, which resets the queue, drains artisan commands, and documents the wiring.
| Flow coverage | complete | The async sensor-read flow, concurrent sensor harness, and USB instrumentation runner trace through `ServiceContainer`, the embassy mutex, and USB command queue instrumentation.

**Recommended checks:**
- `cargo test --test concurrent_sensor_test --target x86_64-unknown-linux-gnu --features async-lock-depth-metrics`
- `cargo test --test usb_instrumentation_runner --target riscv32imc-unknown-none-elf` (run on riscv32 hardware/emulator that provides `std`)

## Tech Debt
- **Phase 58-async-mutex-migration**
  - Deprecated sync helpers (`with_roaster()`/`with_roaster_mut()`) remain for ISR compatibility; plan a cleanup once async contexts and ISR callers have migrated fully.
  - `tests/mock_uart_integration.rs` still relies on `roaster_sync`; revisit once the async harness covers the same scenarios so the legacy path can be pruned.
