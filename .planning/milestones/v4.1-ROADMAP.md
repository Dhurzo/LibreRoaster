# Milestone v4.1: Documentation Update

**Status:** ✅ SHIPPED 2026-02-23
**Phases:** 62-69
**Total Plans:** 11

## Overview

Cleared the documentation backlog, tightened instrumentation visibility for automation, and aligned the safety/regression surface so auditors can follow every step of the DOC→STATUS→regression story.

## Phases

### Phase 62: Documentation Cleanup
**Goal:** Remove outdated information and align the documentation bundle with the live codebase state.
**Depends on:** None
**Plans:** 3 plans

**Plans:**
- [x] 62-01-PLAN.md — Clean up README.md so the async/safety narrative, command table, and hardware notes reflect current behavior.
- [x] 62-02-PLAN.md — Tidy internalDoc/ so it mirrors the README, fixes broken links, and highlights the refreshed instrumentation guidance.
- [x] 62-03-PLAN.md — Rebuild FLASH_GUIDE.md and ARTISAN_CONNECTION.md so every broken link disappears.

**Details:**
- Closed CLN-01…CLN-03 by removing stale Artisan commands, pinouts, and async/safety stories; every clue now points at the live code.
- Cross-links now surface the instrumentation guide and the new STATUS/REG guidance without sending readers to deleted files.

### Phase 63: Build and Test Documentation
**Goal:** Provide clear instructions for building, testing, and running the firmware.
**Depends on:** Phase 62
**Plans:** 1 plan

**Plans:**
- [x] 63-01-PLAN.md — Add comprehensive build, test, and development flags documentation.

**Details:**
- Fulfilled BLD-01…BLD-03 with step-by-step build docs, host/CI test scripts, and `async-lock-depth-metrics` guidance so contributors can reproduce instrumentation.

### Phase 64: Documentation Consistency Fixes
**Goal:** Fix the binary path, target name, and macOS port inconsistencies reported by the audit.
**Depends on:** Phase 63
**Plans:** 1 plan

**Plans:**
- [x] 64-01-PLAN.md — Fix documentation inconsistencies (binary paths, target name, macOS ports).

**Details:**
- Swapped every `target/release/libreroaster.bin` reference for `target/riscv32imc-unknown-none-elf/release/libreroaster.bin`, standardized the target triple to `riscv32imc-unknown-none-elf`, and added the macOS `/dev/cu.usbmodem-*` hint.
- This block closes the integration gaps that prevented the audit from certifying Phase 64.

### Phase 65: Watchdog Timer Safety
**Goal:** Ship a fed Task Watchdog, LEDC guard, and over-temperature regression runner that all emit SAFETY telemetry.
**Depends on:** Phase 64
**Plans:** 2 plans

**Plans:**
- [x] 65-01-PLAN.md — Install WatchdogFeeder, capture failure reasons in `SystemStatus`, and feed the watchdog before each control loop sleep.
- [x] 65-02-PLAN.md — Add an LEDC guard timeout, log guard events, and launch a regression runner that reports `SAFETY OT-REGRESSION` while keeping the watchdog healthy.

**Details:**
- Achieved WDT-01…WDT-03: the watchdog feed is recorded, LEDC guard timeouts release hardware before a watchdog reset, and the regression runner emits SAFETY telemetry while shutting off heater/fan safely.

### Phase 66: Instrumentation Observability
**Goal:** Make the watchdog/guard/regression state observable through a deterministic instrumentation snapshot.
**Depends on:** Phase 65
**Plans:** 1 plan

**Plans:**
- [x] 66-01-PLAN.md — Implement the STATUS command, formatter, and documentation for automation-friendly telemetry.

**Details:**
- Delivered OBS-01…OBS-03: `STATUS` emits the fixed CSV columns documenting watchdog health, guard counts, and regression activity while leaving `READ` unchanged, and the instrumentation guide explains the payload.

### Phase 67: Documentation Consistency Verification
**Goal:** Close the Phase 64 audit gap with a verification report that proves the binary path, target triple, and macOS port claims.
**Depends on:** Phase 64
**Plans:** 1 plan

**Plans:**
- [x] 67-01-PLAN.md — Capture grep evidence for FLASH_GUIDE and README and publish the verification summary for Phase 64.

**Details:**
- `.planning/phases/64-documentation-consistency-fixes/64-VERIFICATION.md` now records the grep command outputs with line numbers plus a summary that explicitly tells auditors the gap is closed.

### Phase 68: README Command Table for Automation Hooks
**Goal:** Surface REG and STATUS/STAT in the Supported Artisan Commands table and link readers to the instrumentation guide.
**Depends on:** Phase 66
**Plans:** 1 plan

**Plans:**
- [x] 68-01-PLAN.md — Update README so REG and STATUS/STAT appear alongside the existing command list and link to `internalDoc/INSTRUMENTATION_README.MD`.

**Details:**
- Completed the automation hook documentation so readers can find REG/STATUS/STAT without searching internalDoc; the links now point automation engineers directly at the payload spec.

### Phase 69: Regression Helper API Alignment
**Goal:** Reconcile the public `safety::regression` API so only the necessary entry points remain exported.
**Depends on:** Phase 65
**Plans:** 1 plan

**Plans:**
- [x] 69-01-PLAN.md — Hide `run_overtemp_regression` while keeping `regression_task` and `request_regression` public.

**Details:**
- Privatized the helper and limited the `pub use target_impl` exports to `regression_task` and `request_regression`, aligning the module surface with the actual callers.

---

## Milestone Summary

**Decimal Phases:**

- None

**Key Decisions:**

- Locked the STATUS CSV layout and instrumentation formatter so automation parsing can rely on a deterministic column order.
- Linked the Supported Artisan Commands table to `internalDoc/INSTRUMENTATION_README.MD` so automation readers can find REG/STATUS/STAT without chasing internal docs.
- Privatized `run_overtemp_regression` while keeping `regression_task`/`request_regression` public so the API surface matches actual consumers.

**Issues Resolved:**

- The documentation binary path and target-triple mismatches are now fully aligned with the Rust toolchain and FLASH_GUIDE outputs.
- Automation engineers can discover REG/STATUS/STAT from the README and see the instrumentation payload in the linked guide.
- The regression helper no longer appears in the public API, removing the dead export from the milestone surface.

**Issues Deferred:**

- None; the next milestone will define fresh requirements instead of carrying forward additional gaps.

**Technical Debt Incurred:**

- None beyond the existing backlog. The new instrumentation snapshot keeps the controls lean and observable.

---

_For current project status, see .planning/ROADMAP.md_

---
