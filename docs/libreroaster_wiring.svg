<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1200 820" width="1200" height="820">
  <defs>
    <style>
      .title { font-family: 'Segoe UI', Arial, sans-serif; font-size: 22px; font-weight: bold; fill: #1e293b; }
      .subtitle { font-family: 'Segoe UI', Arial, sans-serif; font-size: 12px; fill: #64748b; }
      .box { fill: #ffffff; stroke: #334155; stroke-width: 2; rx: 8; }
      .box-ac { fill: #fff5f5; stroke: #dc2626; stroke-width: 2; rx: 8; }
      .box-power { fill: #fff7ed; stroke: #f97316; stroke-width: 2; rx: 8; }
      .box-optional { fill: #f8fafc; stroke: #94a3b8; stroke-width: 1.5; rx: 8; stroke-dasharray: 5,3; }
      .box-label { font-family: 'Segoe UI', Arial, sans-serif; font-size: 13px; font-weight: bold; fill: #1e293b; }
      .box-sublabel { font-family: 'Segoe UI', Arial, sans-serif; font-size: 10px; fill: #64748b; }
      .pin { font-family: 'Consolas', 'Courier New', monospace; font-size: 10px; font-weight: bold; fill: #4f46e5; }
      .pin-func { font-family: 'Segoe UI', Arial, sans-serif; font-size: 10px; fill: #475569; }
      .pin-warn { font-family: 'Segoe UI', Arial, sans-serif; font-size: 10px; fill: #dc2626; font-style: italic; }
      .wire-label { font-family: 'Segoe UI', Arial, sans-serif; font-size: 9px; font-weight: bold; fill: #475569; }
      .warn-badge { font-family: 'Segoe UI', Arial, sans-serif; font-size: 9px; fill: #92400e; font-weight: bold; }
      .critical-badge { font-family: 'Segoe UI', Arial, sans-serif; font-size: 9px; fill: #dc2626; font-weight: bold; }
      .note { font-family: 'Segoe UI', Arial, sans-serif; font-size: 10px; fill: #475569; }
      .legend-title { font-family: 'Segoe UI', Arial, sans-serif; font-size: 12px; font-weight: bold; fill: #1e293b; }
      .legend-text { font-family: 'Segoe UI', Arial, sans-serif; font-size: 10px; fill: #334155; }
      .bom-text { font-family: 'Segoe UI', Arial, sans-serif; font-size: 9px; fill: #64748b; }
      /* Wires */
      .w-spi { stroke: #2196F3; stroke-width: 2.5; fill: none; }
      .w-pwr33 { stroke: #E53935; stroke-width: 2; fill: none; }
      .w-pwr12 { stroke: #FF5722; stroke-width: 2.5; fill: none; }
      .w-gnd { stroke: #212121; stroke-width: 2; fill: none; stroke-dasharray: 4,2; }
      .w-ac-l { stroke: #D32F2F; stroke-width: 3; fill: none; }
      .w-ac-n { stroke: #1565C0; stroke-width: 2.5; fill: none; }
      .w-pe   { stroke: #4CAF50; stroke-width: 2; fill: none; stroke-dasharray: 3,2; }
      .w-pwm { stroke: #7B1FA2; stroke-width: 2.5; fill: none; }
      .w-uart { stroke: #059669; stroke-width: 2; fill: none; }
      .w-signal { stroke: #64748b; stroke-width: 1.5; fill: none; }
      .w-opt { stroke: #94a3b8; stroke-width: 1.5; fill: none; stroke-dasharray: 4,4; }
    </style>
    <!-- Arrow marker for data direction -->
    <marker id="arrow-blue" viewBox="0 0 10 10" refX="10" refY="5" markerWidth="8" markerHeight="8" orient="auto">
      <path d="M 0 0 L 10 5 L 0 10 z" fill="#2196F3"/>
    </marker>
    <marker id="arrow-green" viewBox="0 0 10 10" refX="10" refY="5" markerWidth="8" markerHeight="8" orient="auto">
      <path d="M 0 0 L 10 5 L 0 10 z" fill="#059669"/>
    </marker>
    <marker id="arrow-orange" viewBox="0 0 10 10" refX="10" refY="5" markerWidth="8" markerHeight="8" orient="auto">
      <path d="M 0 0 L 10 5 L 0 10 z" fill="#FF5722"/>
    </marker>
  </defs>

  <!-- Background -->
  <rect width="1200" height="820" fill="#f8fafc"/>

  <!-- ════ TITLE ════ -->
  <text x="20" y="28" class="title">LibreRoaster v5.3 — Realistic Wiring Diagram</text>
  <text x="20" y="48" class="subtitle">ESP32-C3 (RISC-V 160MHz) | 2x MAX31856 Type-K | SSR Heater | PWM Fan 25kHz | TC4 Protocol via USB CDC + UART</text>

  <!-- ════════════════════════════════════════════════════════════════════════════════════════ -->
  <!-- ESP32-C3 MICROCONTROLLER (Center) -->
  <!-- ════════════════════════════════════════════════════════════════════════════════════════ -->
  <g transform="translate(230, 80)">
    <rect x="0" y="0" width="180" height="620" class="box"/>
    <text x="90" y="22" text-anchor="middle" class="box-label">ESP32-C3</text>
    <text x="90" y="36" text-anchor="middle" class="box-sublabel">RISC-V 160MHz · 400KB SRAM</text>
    <line x1="10" y1="46" x2="170" y2="46" stroke="#e2e8f0"/>

    <!-- Power -->
    <rect x="10" y="50" width="160" height="18" rx="3" fill="#fee2e2"/>
    <text x="15" y="63" class="pin">5V</text>
    <text x="40" y="63" class="pin-func">USB power (500mA min)</text>

    <rect x="10" y="72" width="160" height="18" rx="3" fill="#fef3c7"/>
    <text x="15" y="85" class="pin">3.3V</text>
    <text x="45" y="85" class="pin-func">Regulated out → MAX31856 ×2, SSR, Mic</text>

    <rect x="10" y="94" width="160" height="18" rx="3" fill="#e5e7eb"/>
    <text x="15" y="107" class="pin">GND</text>
    <text x="45" y="107" class="pin-func">Common ground — ALL devices</text>

    <line x1="10" y1="120" x2="170" y2="120" stroke="#e2e8f0"/>

    <!-- SPI Bus (shared) -->
    <rect x="10" y="125" width="160" height="16" rx="3" fill="#eff6ff"/>
    <text x="15" y="137" class="pin">GPIO7</text>
    <text x="60" y="137" class="pin-func">SPI SCLK — shared bus</text>

    <rect x="10" y="145" width="160" height="16" rx="3" fill="#eff6ff"/>
    <text x="15" y="157" class="pin">GPIO5</text>
    <text x="60" y="157" class="pin-func">SPI MOSI — shared bus</text>

    <rect x="10" y="165" width="160" height="16" rx="3" fill="#eff6ff"/>
    <text x="15" y="177" class="pin">GPIO6</text>
    <text x="60" y="177" class="pin-func">SPI MISO — shared bus</text>

    <!-- Chip Selects -->
    <rect x="10" y="190" width="160" height="16" rx="3" fill="#e0e7ff"/>
    <text x="15" y="202" class="pin">GPIO4</text>
    <text x="60" y="202" class="pin-func">BT CS → MAX31856 #1</text>

    <rect x="10" y="210" width="160" height="16" rx="3" fill="#e0e7ff"/>
    <text x="15" y="222" class="pin">GPIO3</text>
    <text x="60" y="222" class="pin-func">ET CS → MAX31856 #2</text>

    <line x1="10" y1="238" x2="170" y2="238" stroke="#e2e8f0"/>

    <!-- Heater Control -->
    <rect x="10" y="243" width="160" height="16" rx="3" fill="#fce7f3"/>
    <text x="15" y="255" class="pin">GPIO10</text>
    <text x="60" y="255" class="pin-func">SSR PWM 1Hz (zero-cross)</text>
    <text x="15" y="267" class="pin-warn">✅ SAFE — no-strapping pin</text>

    <rect x="10" y="275" width="160" height="16" rx="3" fill="#fce7f3"/>
    <text x="15" y="287" class="pin">GPIO1</text>
    <text x="60" y="287" class="pin-func">HEAT DETECT (SSR feedback)</text>
    <text x="15" y="299" class="pin-func">Pull-up enabled</text>

    <line x1="10" y1="310" x2="170" y2="310" stroke="#e2e8f0"/>

    <!-- Fan Control -->
    <rect x="10" y="315" width="160" height="16" rx="3" fill="#f3e5f5"/>
    <text x="15" y="327" class="pin">GPIO9</text>
    <text x="60" y="327" class="pin-func">FAN PWM 25kHz</text>
    <text x="15" y="339" class="pin-warn">⚠️ STRAPPING — external 10kΩ pull-down!</text>

    <line x1="10" y1="350" x2="170" y2="350" stroke="#e2e8f0"/>

    <!-- UART -->
    <rect x="10" y="355" width="160" height="16" rx="3" fill="#ecfdf5"/>
    <text x="15" y="367" class="pin">GPIO20</text>
    <text x="60" y="367" class="pin-func">UART TX → to Artisan</text>

    <rect x="10" y="375" width="160" height="16" rx="3" fill="#ecfdf5"/>
    <text x="15" y="387" class="pin">GPIO21</text>
    <text x="60" y="387" class="pin-func">UART RX ← from Artisan</text>

    <line x1="10" y1="400" x2="170" y2="400" stroke="#e2e8f0"/>

    <!-- Optional peripherals -->
    <rect x="10" y="405" width="160" height="16" rx="3" fill="#fefce8"/>
    <text x="15" y="417" class="pin">GPIO2</text>
    <text x="60" y="417" class="pin-func">MIC ADC (acoustic crack detect)</text>
    <text x="15" y="429" class="pin-warn">⚠️ STRAPPING — high-Z input only</text>
    <text x="15" y="441" class="pin-func">(optional, Welford RMS algorithm)</text>

    <rect x="10" y="448" width="160" height="16" rx="3" fill="#fefce8"/>
    <text x="15" y="460" class="pin">GPIO8</text>
    <text x="60" y="460" class="pin-func">Status LED (error blink)</text>
    <text x="15" y="472" class="pin-warn">⚠️ STRAPPING — safe as output</text>
    <text x="15" y="484" class="pin-func">(optional, 3 shorts→pause pattern)</text>

    <line x1="10" y1="495" x2="170" y2="495" stroke="#e2e8f0"/>

    <!-- USB CDC -->
    <rect x="10" y="500" width="160" height="40" rx="3" fill="#e0e7ff"/>
    <text x="15" y="515" class="pin">USB-C</text>
    <text x="60" y="515" class="pin-func">USB CDC (native) — PRIMARY</text>
    <text x="15" y="530" class="pin-func">to Artisan (no adapter needed)</text>

    <!-- Annotations -->
    <text x="90" y="560" text-anchor="middle" class="note" font-weight="bold">Key:</text>
    <text x="90" y="576" text-anchor="middle" class="note">🔵 GPIO3-7 = SPI bus (shared)</text>
    <text x="90" y="592" text-anchor="middle" class="note">🟣 GPIO9-10 = LEDC PWM</text>
    <text x="90" y="608" text-anchor="middle" class="note">🟢 GPIO20-21 = UART</text>
  </g>

  <!-- ════════════════════════════════════════════════════════════════════════════════════════ -->
  <!-- MAX31856 #1 — BEAN TEMPERATURE (Top-Left) -->
  <!-- ════════════════════════════════════════════════════════════════════════════════════════ -->
  <g transform="translate(30, 80)">
    <rect x="0" y="0" width="170" height="220" class="box"/>
    <text x="85" y="20" text-anchor="middle" class="box-label">MAX31856 #1</text>
    <text x="85" y="34" text-anchor="middle" class="box-sublabel">Bean Temperature (BT)</text>
    <line x1="10" y1="42" x2="160" y2="42" stroke="#e2e8f0"/>

    <rect x="10" y="48" width="150" height="16" rx="3" fill="#e0e7ff"/>
    <text x="15" y="61" class="pin">CS → GPIO4</text>

    <rect x="10" y="68" width="150" height="16" rx="3" fill="#eff6ff"/>
    <text x="15" y="81" class="pin">MOSI → GPIO5</text>

    <rect x="10" y="88" width="150" height="16" rx="3" fill="#eff6ff"/>
    <text x="15" y="101" class="pin">MISO → GPIO6</text>

    <rect x="10" y="108" width="150" height="16" rx="3" fill="#eff6ff"/>
    <text x="15" y="121" class="pin">SCLK → GPIO7</text>

    <rect x="10" y="130" width="150" height="16" rx="3" fill="#fee2e2"/>
    <text x="15" y="143" class="pin">VCC → 3.3V</text>

    <rect x="10" y="150" width="150" height="16" rx="3" fill="#e5e7eb"/>
    <text x="15" y="163" class="pin">GND</text>

    <line x1="10" y1="175" x2="160" y2="175" stroke="#e2e8f0"/>
    <rect x="10" y="180" width="150" height="30" rx="3" fill="#fff7ed"/>
    <text x="15" y="195" class="pin-func">T+ ← Type-K (yellow) → Bean probe</text>
    <text x="15" y="207" class="pin-func">T- ← Type-K (red)</text>
  </g>

  <!-- ════════════════════════════════════════════════════════════════════════════════════════ -->
  <!-- MAX31856 #2 — ENVIRONMENT TEMPERATURE (Mid-Left) -->
  <!-- ════════════════════════════════════════════════════════════════════════════════════════ -->
  <g transform="translate(30, 330)">
    <rect x="0" y="0" width="170" height="220" class="box"/>
    <text x="85" y="20" text-anchor="middle" class="box-label">MAX31856 #2</text>
    <text x="85" y="34" text-anchor="middle" class="box-sublabel">Environment Temp (ET)</text>
    <line x1="10" y1="42" x2="160" y2="42" stroke="#e2e8f0"/>

    <rect x="10" y="48" width="150" height="16" rx="3" fill="#e0e7ff"/>
    <text x="15" y="61" class="pin">CS → GPIO3</text>

    <rect x="10" y="68" width="150" height="16" rx="3" fill="#eff6ff"/>
    <text x="15" y="81" class="pin">MOSI → GPIO5</text>

    <rect x="10" y="88" width="150" height="16" rx="3" fill="#eff6ff"/>
    <text x="15" y="101" class="pin">MISO → GPIO6</text>

    <rect x="10" y="108" width="150" height="16" rx="3" fill="#eff6ff"/>
    <text x="15" y="121" class="pin">SCLK → GPIO7</text>

    <rect x="10" y="130" width="150" height="16" rx="3" fill="#fee2e2"/>
    <text x="15" y="143" class="pin">VCC → 3.3V</text>

    <rect x="10" y="150" width="150" height="16" rx="3" fill="#e5e7eb"/>
    <text x="15" y="163" class="pin">GND</text>

    <line x1="10" y1="175" x2="160" y2="175" stroke="#e2e8f0"/>
    <rect x="10" y="180" width="150" height="30" rx="3" fill="#fff7ed"/>
    <text x="15" y="195" class="pin-func">T+ ← Type-K (yellow) → Exhaust</text>
    <text x="15" y="207" class="pin-func">T- ← Type-K (red)</text>
  </g>

  <!-- ════════════════════════════════════════════════════════════════════════════════════════ -->
  <!-- OPTIONAL: ACOUSTIC MICROPHONE (Bottom-Left) -->
  <!-- ════════════════════════════════════════════════════════════════════════════════════════ -->
  <g transform="translate(30, 580)">
    <rect x="0" y="0" width="170" height="110" class="box-optional"/>
    <text x="85" y="18" text-anchor="middle" class="box-label">Microphone (optional)</text>
    <text x="85" y="30" text-anchor="middle" class="box-sublabel">MAX4466 Electret + Amplifier</text>
    <line x1="10" y1="38" x2="160" y2="38" stroke="#e2e8f0"/>

    <rect x="10" y="44" width="150" height="16" rx="3" fill="#fefce8"/>
    <text x="15" y="57" class="pin">OUT → GPIO2 (ADC)</text>

    <rect x="10" y="64" width="150" height="16" rx="3" fill="#fee2e2"/>
    <text x="15" y="77" class="pin">VCC → 3.3V</text>

    <rect x="10" y="84" width="150" height="16" rx="3" fill="#e5e7eb"/>
    <text x="15" y="97" class="pin">GND</text>
  </g>

  <!-- ════════════════════════════════════════════════════════════════════════════════════════ -->
  <!-- SSR — SOLID STATE RELAY (Top-Right) -->
  <!-- ════════════════════════════════════════════════════════════════════════════════════════ -->
  <g transform="translate(460, 80)">
    <rect x="0" y="0" width="200" height="190" class="box"/>
    <text x="100" y="20" text-anchor="middle" class="box-label">SSR-25DA</text>
    <text x="100" y="34" text-anchor="middle" class="box-sublabel">25A Zero-Cross Solid State Relay</text>
    <line x1="10" y1="42" x2="190" y2="42" stroke="#e2e8f0"/>

    <rect x="10" y="48" width="180" height="16" rx="3" fill="#fce7f3"/>
    <text x="15" y="61" class="pin">DC+ (control) ← GPIO10</text>

    <rect x="10" y="68" width="180" height="16" rx="3" fill="#e5e7eb"/>
    <text x="15" y="81" class="pin">DC- (control GND)</text>

    <rect x="10" y="90" width="180" height="16" rx="3" fill="#fee2e2"/>
    <text x="15" y="103" class="pin">AC-IN ← AC Live (via terminal block)</text>
    <text x="15" y="115" class="pin-warn">⚠️ HIGH VOLTAGE — 110-240V AC</text>

    <rect x="10" y="122" width="180" height="16" rx="3" fill="#fee2e2"/>
    <text x="15" y="135" class="pin">AC-OUT → Heating Element</text>
    <text x="15" y="147" class="pin-warn">⚠️ Switched Live — check wiring rating</text>

    <rect x="10" y="154" width="180" height="16" rx="3" fill="#fce7f3"/>
    <text x="15" y="167" class="pin">COM (feedback) → GPIO1</text>
  </g>

  <!-- ════════════════════════════════════════════════════════════════════════════════════════ -->
  <!-- HEATING ELEMENT (Far-Right Top) -->
  <!-- ════════════════════════════════════════════════════════════════════════════════════════ -->
  <g transform="translate(700, 80)">
    <rect x="0" y="0" width="180" height="120" class="box-ac"/>
    <text x="90" y="20" text-anchor="middle" class="box-label">Heating Element</text>
    <text x="90" y="34" text-anchor="middle" class="box-sublabel">800-2000W</text>
    <line x1="10" y1="42" x2="170" y2="42" stroke="#e2e8f0"/>

    <rect x="10" y="48" width="160" height="18" rx="3" fill="#fee2e2"/>
    <text x="15" y="62" class="pin">L ← AC-OUT from SSR</text>

    <rect x="10" y="70" width="160" height="18" rx="3" fill="#e0f2fe"/>
    <text x="15" y="84" class="pin">N ← AC Neutral (direct)</text>

    <rect x="10" y="92" width="160" height="18" rx="3" fill="#dcfce7"/>
    <text x="15" y="106" class="pin">PE → Earth Ground ⚠️ MANDATORY</text>
  </g>

  <!-- ════════════════════════════════════════════════════════════════════════════════════════ -->
  <!-- AC MAINS (Far-Right) -->
  <!-- ════════════════════════════════════════════════════════════════════════════════════════ -->
  <g transform="translate(700, 230)">
    <rect x="0" y="0" width="180" height="120" class="box-ac"/>
    <text x="90" y="20" text-anchor="middle" class="box-label">AC Mains</text>
    <text x="90" y="34" text-anchor="middle" class="box-sublabel">110-240V / 50-60Hz</text>
    <line x1="10" y1="42" x2="170" y2="42" stroke="#e2e8f0"/>

    <rect x="10" y="48" width="160" height="18" rx="3" fill="#fee2e2"/>
    <text x="15" y="62" class="pin">L (Live) → SSR AC-IN</text>

    <rect x="10" y="70" width="160" height="18" rx="3" fill="#e0f2fe"/>
    <text x="15" y="84" class="pin">N (Neutral) → Heater</text>

    <rect x="10" y="92" width="160" height="18" rx="3" fill="#dcfce7"/>
    <text x="15" y="106" class="pin">PE (Earth) → Heater</text>
  </g>

  <!-- ════════════════════════════════════════════════════════════════════════════════════════ -->
  <!-- MOSFET DRIVER + DC FAN (Middle-Right) -->
  <!-- ════════════════════════════════════════════════════════════════════════════════════════ -->
  <g transform="translate(460, 300)">
    <rect x="0" y="0" width="200" height="140" class="box"/>
    <text x="100" y="20" text-anchor="middle" class="box-label">MOSFET Driver</text>
    <text x="100" y="34" text-anchor="middle" class="box-sublabel">IRF520 / IRLZ44N</text>
    <line x1="10" y1="42" x2="190" y2="42" stroke="#e2e8f0"/>

    <rect x="10" y="48" width="180" height="16" rx="3" fill="#f3e5f5"/>
    <text x="15" y="61" class="pin">GATE ← GPIO9 (+ 100Ω series)</text>

    <rect x="10" y="68" width="180" height="16" rx="3" fill="#e5e7eb"/>
    <text x="15" y="81" class="pin">10kΩ GATE-GND pull-down ⚠️</text>
    <text x="15" y="93" class="pin-warn">(prevents floating during boot)</text>

    <rect x="10" y="100" width="180" height="16" rx="3" fill="#f3e5f5"/>
    <text x="15" y="113" class="pin">DRAIN → Fan (–)</text>

    <rect x="10" y="120" width="180" height="16" rx="3" fill="#e5e7eb"/>
    <text x="15" y="133" class="pin">SOURCE → GND</text>
  </g>

  <g transform="translate(700, 380)">
    <rect x="0" y="0" width="180" height="100" class="box-power"/>
    <text x="90" y="20" text-anchor="middle" class="box-label">DC Fan</text>
    <text x="90" y="34" text-anchor="middle" class="box-sublabel">12V/24V · 80-120mm · PWM</text>
    <line x1="10" y1="42" x2="170" y2="42" stroke="#e2e8f0"/>

    <rect x="10" y="48" width="160" height="16" rx="3" fill="#fff7ed"/>
    <text x="15" y="61" class="pin">+12V ← Power Supply</text>

    <rect x="10" y="68" width="160" height="16" rx="3" fill="#f3e5f5"/>
    <text x="15" y="81" class="pin">– (GND) → MOSFET DRAIN</text>
  </g>

  <!-- ════════════════════════════════════════════════════════════════════════════════════════ -->
  <!-- 12V POWER SUPPLY (Bottom-Right) -->
  <!-- ════════════════════════════════════════════════════════════════════════════════════════ -->
  <g transform="translate(700, 510)">
    <rect x="0" y="0" width="180" height="100" class="box-power"/>
    <text x="90" y="20" text-anchor="middle" class="box-label">12V Power Supply</text>
    <text x="90" y="34" text-anchor="middle" class="box-sublabel">5A minimum</text>
    <line x1="10" y1="42" x2="170" y2="42" stroke="#e2e8f0"/>

    <rect x="10" y="48" width="160" height="16" rx="3" fill="#fff7ed"/>
    <text x="15" y="61" class="pin">+12V → Fan +</text>

    <rect x="10" y="68" width="160" height="16" rx="3" fill="#e5e7eb"/>
    <text x="15" y="81" class="pin">GND → Common ground</text>
  </g>

  <!-- ════════════════════════════════════════════════════════════════════════════════════════ -->
  <!-- UART-TO-USB ADAPTER (Bottom) -->
  <!-- ════════════════════════════════════════════════════════════════════════════════════════ -->
  <g transform="translate(460, 480)">
    <rect x="0" y="0" width="200" height="130" class="box"/>
    <text x="100" y="20" text-anchor="middle" class="box-label">USB-UART Adapter</text>
    <text x="100" y="34" text-anchor="middle" class="box-sublabel">FTDI / CH340 / CP2102</text>
    <line x1="10" y1="42" x2="190" y2="42" stroke="#e2e8f0"/>

    <rect x="10" y="48" width="180" height="16" rx="3" fill="#ecfdf5"/>
    <text x="15" y="61" class="pin">RX ← ESP32 GPIO20 (UART TX)</text>

    <rect x="10" y="68" width="180" height="16" rx="3" fill="#ecfdf5"/>
    <text x="15" y="81" class="pin">TX → ESP32 GPIO21 (UART RX)</text>

    <rect x="10" y="88" width="180" height="16" rx="3" fill="#fee2e2"/>
    <text x="15" y="101" class="pin">VCC → 3.3V ⚠️ CRITICAL</text>
    <text x="15" y="113" class="pin-warn">MUST be 3.3V — 5V will DAMAGE ESP32-C3!</text>
  </g>

  <!-- ════════════════════════════════════════════════════════════════════════════════════════ -->
  <!-- WIRES / CONNECTIONS -->
  <!-- ════════════════════════════════════════════════════════════════════════════════════════ -->

  <!-- === SPI BUS (shared — MAX31856 #1 to ESP32-C3) === -->
  <!-- SCLK -->   <line class="w-spi" x1="200" y1="188" x2="230" y2="202"/>
  <!-- MOSI -->   <line class="w-spi" x1="200" y1="149" x2="230" y2="222"/>
  <!-- MISO -->   <line class="w-spi" x1="200" y1="169" x2="230" y2="242"/>
  <!-- BT CS -->  <line class="w-spi" x1="200" y1="129" x2="230" y2="270"/>
  <!-- ET CS -->  <line class="w-spi" x1="200" y1="379" x2="230" y2="290"/>

  <!-- === POWER 3.3V (ESP32-C3 to both MAX31856) === -->
  <line class="w-pwr33" x1="230" y1="157" x2="200" y2="211"/>
  <line class="w-pwr33" x1="230" y1="157" x2="200" y2="461"/>

  <!-- === GND (ESP32-C3 to ALL) === -->
  <line class="w-gnd" x1="230" y1="179" x2="200" y2="240"/>
  <line class="w-gnd" x1="230" y1="179" x2="200" y2="490"/>
  <line class="w-gnd" x1="410" y1="179" x2="460" y2="160"/>
  <line class="w-gnd" x1="410" y1="179" x2="460" y2="400"/>

  <!-- === SSR CONTROL (GPIO10 to SSR) === -->
  <line class="w-pwm" x1="410" y1="323" x2="460" y2="129"/>
  <text x="420" y="315" class="wire-label">GPIO10 SSR PWM 1Hz</text>

  <!-- === SSR COMPONENT INTERNAL (feedback) === -->
  <line class="w-signal" x1="460" y1="235" x2="410" y2="355"/>
  <text x="420" y="245" class="wire-label">GPIO1 Heat Detect</text>

  <!-- === FAN PWM (GPIO9 to MOSFET) === -->
  <line class="w-pwm" x1="410" y1="395" x2="460" y2="349"/>
  <text x="420" y="370" class="wire-label">GPIO9 FAN PWM 25kHz</text>

  <!-- === UART (ESP32 to Adapter) === -->
  <line class="w-uart" x1="410" y1="436" x2="460" y2="529"/>
  <text x="420" y="440" class="wire-label">GPIO20 TX → Adapter RX</text>
  <line class="w-uart" x1="410" y1="456" x2="460" y2="549"/>
  <text x="420" y="460" class="wire-label">GPIO21 RX ← Adapter TX</text>

  <!-- === AC MAINS to SSR === -->
  <line class="w-ac-l" x1="700" y1="279" x2="660" y2="171"/>
  <text x="665" y="270" class="wire-label">AC Live In</text>

  <!-- === SSR to HEATER === -->
  <line class="w-ac-l" x1="660" y1="171" x2="700" y2="129"/>
  <text x="665" y="155" class="wire-label">Switched Live</text>

  <!-- === AC NEUTRAL (Mains to Heater, bypassing SSR) === -->
  <line class="w-ac-n" x1="790" y1="280" x2="790" y2="200"/>
  <text x="795" y="245" class="wire-label">N (direct)</text>

  <!-- === PROTECTIVE EARTH === -->
  <line class="w-pe" x1="790" y1="320" x2="790" y2="200"/>
  <text x="795" y="315" class="wire-label">PE (Earth) ⚠️</text>

  <!-- === 12V POWER === -->
  <line class="w-pwr12" x1="700" y1="560" x2="700" y2="480"/>
  <text x="705" y="525" class="wire-label">+12V</text>

  <!-- === MOSFET to FAN === -->
  <line class="w-pwm" x1="660" y1="440" x2="700" y2="449"/>
  <text x="665" y="430" class="wire-label">DRAIN → Fan –</text>

  <!-- === OPTIONAL MIC === -->
  <line class="w-opt" x1="200" y1="641" x2="230" y2="486"/>
  <text x="210" y="655" class="wire-label">GPIO2 Mic ADC (optional)</text>

  <!-- ════════════════════════════════════════════════════════════════════════════════════════ -->
  <!-- SAFETY ANNOTATIONS -->
  <!-- ════════════════════════════════════════════════════════════════════════════════════════ -->
  <g transform="translate(930, 80)">
    <rect x="0" y="0" width="250" height="480" rx="6" fill="#fff" stroke="#cbd5e1" stroke-width="1.5"/>
    <rect x="0" y="0" width="250" height="28" rx="6" fill="#f1f5f9"/>
    <text x="125" y="19" text-anchor="middle" class="legend-title">⚠️ Critical Notes</text>

    <rect x="10" y="36" width="230" height="2" fill="#e2e8f0"/>

    <text x="15" y="56" class="critical-badge">⛔ UART MUST be 3.3V logic!</text>
    <text x="15" y="70" class="note">5V TTL on GPIO20/21 destroys ESP32-C3.</text>

    <text x="15" y="90" class="critical-badge">⛔ Earth ground is MANDATORY</text>
    <text x="15" y="104" class="note">Heater chassis PE must be connected to</text>
    <text x="15" y="118" class="note">mains earth ground. Safety requirement.</text>

    <text x="15" y="143" class="warn-badge">⚠️ GPIO9 is a strapping pin</text>
    <text x="15" y="157" class="note">Sampled at boot. 10kΩ pull-down on FET</text>
    <text x="15" y="171" class="note">gate to prevent floating HIGH during ESP</text>
    <text x="15" y="185" class="note">reset. See ESP32-C3 datasheet §Strapping.</text>

    <text x="15" y="210" class="warn-badge">⚠️ GPIO2 is a strapping pin</text>
    <text x="15" y="224" class="note">Safe for analog mic (high-impedance).</text>
    <text x="15" y="238" class="note">Do NOT drive GPIO2 LOW at boot.</text>

    <text x="15" y="263" class="warn-badge">⚠️ GPIO8 is a strapping pin</text>
    <text x="15" y="277" class="note">Safe as output-only LED indicator.</text>

    <text x="15" y="302" class="warn-badge">⚠️ Never leave unattended!</text>
    <text x="15" y="316" class="note">This is a high-power heating device.</text>
    <text x="15" y="330" class="note">Keep fire extinguisher nearby.</text>

    <text x="15" y="355" class="warn-badge">⚠️ SSR heat sink required</text>
    <text x="15" y="369" class="note">25A SSR generates significant heat at</text>
    <text x="15" y="383" class="note">high duty. Mount on proper heat sink.</text>

    <text x="15" y="408" class="warn-badge">⚠️ Probe placement matters</text>
    <text x="15" y="422" class="note">BT probe must contact bean mass.</text>
    <text x="15" y="436" class="note">ET probe in exhaust airflow, not near</text>
    <text x="15" y="450" class="note">heater direct radiation.</text>
  </g>

  <!-- ════════════════════════════════════════════════════════════════════════════════════════ -->
  <!-- LEGEND -->
  <!-- ════════════════════════════════════════════════════════════════════════════════════════ -->
  <g transform="translate(930, 580)">
    <rect x="0" y="0" width="250" height="230" rx="6" fill="#fff" stroke="#cbd5e1" stroke-width="1.5"/>
    <rect x="0" y="0" width="250" height="28" rx="6" fill="#f1f5f9"/>
    <text x="125" y="19" text-anchor="middle" class="legend-title">Wire Legend</text>
    <rect x="10" y="36" width="230" height="2" fill="#e2e8f0"/>

    <line x1="15" y1="55" x2="45" y2="55" class="w-spi"/>
    <text x="55" y="59" class="legend-text">SPI bus (SCLK/MOSI/MISO)</text>

    <line x1="15" y1="77" x2="45" y2="77" class="w-pwr33"/>
    <text x="55" y="81" class="legend-text">3.3V power rail</text>

    <line x1="15" y1="99" x2="45" y2="99" class="w-pwr12"/>
    <text x="55" y="103" class="legend-text">12V power (fan supply)</text>

    <line x1="15" y1="121" x2="45" y2="121" class="w-gnd"/>
    <text x="55" y="125" class="legend-text">GND (common ground)</text>

    <line x1="15" y1="143" x2="45" y2="143" class="w-ac-l"/>
    <text x="55" y="147" class="legend-text">⚠️ AC Live (HIGH VOLTAGE)</text>

    <line x1="15" y1="165" x2="45" y2="165" class="w-ac-n"/>
    <text x="55" y="169" class="legend-text">AC Neutral</text>

    <line x1="15" y1="187" x2="45" y2="187" class="w-pe"/>
    <text x="55" y="191" class="legend-text">PE Protective Earth</text>

    <line x1="15" y1="209" x2="45" y2="209" class="w-pwm"/>
    <text x="55" y="213" class="legend-text">PWM/Signal control</text>
  </g>

  <!-- ════════════════════════════════════════════════════════════════════════════════════════ -->
  <!-- BOM CORNER -->
  <!-- ════════════════════════════════════════════════════════════════════════════════════════ -->
  <g transform="translate(20, 720)">
    <text x="0" y="10" class="legend-title" fill="#64748b">
      BOM (est.): ESP32-C3 $3-8 + 2×MAX31856 $20 + SSR $5-10 + FHN $5-10 + Fan $5-15 + Heater $10-30 + PSU $10-20 + adapter $3-8 = ~$65-105
    </text>
  </g>

</svg>
